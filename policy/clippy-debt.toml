schema_version = "1.0"

# clippy-debt.toml records crate- or path-scoped technical debt that
# blocks promoting a Clippy lint to `deny`. Each entry must have an
# owner, a reason, an `expires` date, and a `status`. PR 08 introduces
# this file in advisory mode; the Clippy strict flip in PR 12
# requires every active debt entry to either be receipted or removed.

# Initial entries are placeholders documenting the staging strategy.
# Add real debt as crates fail Clippy promotion.

# [[debt]]
# lint = "clippy::arithmetic_side_effects"
# path = "crates/bitnet-kernels/**"
# owner = "numeric-kernels"
# reason = "Kernel arithmetic needs explicit checked/wrapping/saturating policy before promotion."
# expires = "2026-07-01"
# status = "active"

[[debt]]
lint = "clippy::manual_checked_ops"
path = "crates/bitnet-kernels/**"
owner = "numeric-kernels"
reason = "PR5 measurement found 25 kernel findings; cleanup belongs with the numeric/kernel lint pass so arithmetic invariants are preserved."
expires = "2026-09-15"
status = "active"

[[debt]]
lint = "clippy::manual_checked_ops"
path = "crates/bitnet-models/**"
owner = "model-contracts"
reason = "PR5 measurement found model config and serde findings through the SRP Clippy surface; convert with the numeric/config invariants in the cleanup PR."
expires = "2026-09-15"
status = "active"

[[debt]]
lint = "clippy::manual_pop_if"
path = "workspace"
owner = "clippy-policy"
reason = "The installed Rust 1.95 Clippy reports this lint as unknown; keep it staged until the toolchain exposes the lint."
expires = "2026-09-15"
status = "active"

[[debt]]
lint = "clippy::duration_suboptimal_units"
path = "crates/bitnet-kernels/**"
owner = "kernels"
reason = "PR5 measurement found OpenCL session timeout findings; defer broad duration cleanup until all workspace hits can be measured together."
expires = "2026-09-15"
status = "active"

[[debt]]
lint = "clippy::duration_suboptimal_units"
path = "crates/bitnet-models/**"
owner = "model-contracts"
reason = "PR5 measurement found model security timeout findings; defer with the broader duration-unit cleanup."
expires = "2026-09-15"
status = "active"

[[debt]]
lint = "clippy::duration_suboptimal_units"
path = "crates/bitnet-tokenizers/**"
owner = "tokenizers"
reason = "PR5 measurement found tokenizer download timeout findings; defer with the broader duration-unit cleanup."
expires = "2026-09-15"
status = "active"

[[debt]]
lint = "clippy::unnecessary_trailing_comma"
path = "crates/bitnet-kernels/**"
owner = "kernels"
reason = "PR5 measurement found 50 kernel formatting findings; keep staged until the mechanical cleanup can run without mixing with lint activation."
expires = "2026-09-15"
status = "active"

[[debt]]
lint = "clippy::disallowed_fields"
path = "workspace"
owner = "clippy-policy"
reason = "Protected field seams are not configured in clippy.toml yet; activation waits for real protected fields instead of a decorative lint."
expires = "2026-09-15"
status = "active"
