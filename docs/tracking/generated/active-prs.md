<!-- GENERATED: do not edit by hand. Run cargo run -p xtask --no-default-features -- campaign generate. -->
# Active Campaign PRs

| Campaign | Item | PR | Branch | Notes |
|---|---|---:|---|---|
| slm-cpu | SLM-CPU-241 | #1131 | `codex/slm-cpu/slm-cpu-241-apply-linear-callsite-descriptor` | Consume the SLM-CPU-240 prompt-bound per-callsite dispatch descriptor blocker and add or precisely block the FeedForward::apply_linear prompt-bound no-bias descriptor propagation point needed before a valid candidate-on receipt can execute. A valid implementation must carry the per-callsite emitter identity, validated SLM-CPU-235 receipt-pair identity, prompt/generated/text digests, model SHA, tokenizer source and strictness, runtime_api=cpu, selected_backend=cpu-rust, selected and candidate path/kernel identity, bias_present=false, fallback=false, prompt IDs, generated IDs, and decoded text to the exact Qwen3/Qwen2.5 Q8_0 feed_forward.down_proj callsite without mutating model-load hook state across prompt/session boundaries. Candidate execution may only occur under the explicit no-bias gate and must remain disabled by default; when the gate is absent the default path must remain eager_f32_candle. A valid blocker must name the exact missing session descriptor construction, callsite argument, generated/text digest lifetime, dispatch branch, command surface, or runtime ownership boundary. This item must not claim timing improvement, allocation reduction, speedup, sustained throughput, Q4/Q5 support, server/accelerator execution, Qwen3.5 support, or BitNet QK256 changes. |
