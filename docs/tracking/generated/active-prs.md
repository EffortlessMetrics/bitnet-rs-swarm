<!-- GENERATED: do not edit by hand. Run cargo run -p xtask --no-default-features -- campaign generate. -->
# Active Campaign PRs

| Campaign | Item | PR | Branch | Notes |
|---|---|---:|---|---|
| slm-cpu | SLM-CPU-246 | #1560 | `codex/slm-cpu/slm-cpu-246-no-bias-role-expansion-policy` | Consume the SLM-CPU-245 Qwen3/Qwen2.5 Q8_0 no-bias timing/allocation envelope and define or precisely block the next receipt-bound no-bias role expansion policy beyond the proven feed_forward.down_proj path. A valid implementation must preserve explicit gate opt-in, preserve default eager_f32_candle when the gate is absent, keep feed_forward.down_proj as the only proven executable no-bias role, name the required per-role receipt ladder before any new role executes, and order expansion as feed_forward.up_proj, feed_forward.gate_proj, attention.o_proj, then q/k/v only where bias policy and tensor/callsite identity are exact. It must require same prompt IDs, generated IDs, decoded text, model SHA, tokenizer source and strictness, runtime_api=cpu, selected_backend=cpu-rust, fallback=false, selected/candidate path and kernel identity, and allocation/timing envelope evidence before any role can claim parity or performance. A valid blocker must name the exact missing role identity, bias policy, selector gate, receipt emitter, candidate-off/on receipt, timing/allocation envelope, or runtime ownership boundary. This item must not enable a broad no-bias runtime, expand candidate execution to additional roles, claim speedup, allocation reduction, sustained throughput, default-runtime promotion, Q4/Q5 support, server/accelerator execution, Qwen3.5 support, or BitNet QK256 changes without matching per-role receipts. |
