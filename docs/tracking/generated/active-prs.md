<!-- GENERATED: do not edit by hand. Run cargo run -p xtask --no-default-features -- campaign generate. -->
# Active Campaign PRs

| Campaign | Item | PR | Branch | Notes |
|---|---|---:|---|---|
| slm-cpu | SLM-CPU-095 | #577 | `codex/slm-cpu-095-packed-q8-matvec-scratch` | Implement or precisely classify the packed Q8_0 sidecar matvec output-scratch boundary queued by SLM-CPU-094. A valid slice may add behavior-preserving inner matvec helpers that fill caller-provided output slices for the opt-in exact-tensor packed Q8_0 sidecar path, and must record whether the full runtime can reuse caller-owned output storage through Candle Tensor construction. Any runtime-adjacent change must preserve the established Qwen3 Q8_0 appliance oracle before claiming even bounded allocation improvement: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, and fallback_used=false. The slice must not promote packed_q8_sidecar to the default runtime, claim speedup without before/after repeated receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths. |
