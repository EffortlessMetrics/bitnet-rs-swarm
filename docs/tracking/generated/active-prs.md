<!-- GENERATED: do not edit by hand. Run cargo run -p xtask --no-default-features -- campaign generate. -->
# Active Campaign PRs

| Campaign | Item | PR | Branch | Notes |
|---|---|---:|---|---|
| slm-cpu | SLM-CPU-139 | #800 | `codex/slm-cpu-139-source-order-qproj-exact-model-receipts` | Restore or precisely block the exact Qwen2.5 Q8_0 model cache, then capture Qwen3 Q8_0 and Qwen2.5 Q8_0 before/after strict CPU receipts on current main with the SLM-CPU-137 source-order q_proj candidate identity fields. A valid slice must preserve identical model SHA, GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, source-order q_proj candidate path/kernel/dimensions/receipt identity, q_proj numeric evidence, and fallback_used=false. If capture is unavailable, emit a machine-checkable blocker naming the exact missing model cache, receipt field, trace identity field, q_proj numeric evidence, or runtime input binding. It must not claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior. |
