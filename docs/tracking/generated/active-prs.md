<!-- GENERATED: do not edit by hand. Run cargo run -p xtask --no-default-features -- campaign generate. -->
# Active Campaign PRs

| Campaign | Item | PR | Branch | Notes |
|---|---|---:|---|---|
| slm-cpu | SLM-CPU-074 | #219 | `codex/slm-cpu-074-packed-q8-sidecar-instrumentation` | Add the packed Q8_0 sidecar matvec instrumentation and scratch-elimination gate after SLM-CPU-073. The slice must keep eager_f32_candle as the default runtime, keep packed sidecar execution opt-in and exact-tensor scoped to `layers.0.attention.q_proj.weight`, measure or eliminate input flatten/to_vec, output Vec allocation, and bias extraction costs before any broader packed-Q8 runtime selection, add per-call or aggregate timing counters around sidecar input materialization, packed matvec compute, output tensor construction, and selector dispatch, and preserve identical model SHA, tokenizer source/strictness, prompt IDs, generated IDs, decoded text, selected CPU backend identity, dense hook identity, and fallback_used=false. It must not claim speedup unless a separate bounded before/after timing artifact proves it, enable packed Q8_0 by default, broaden beyond the exact tensor, start Q4/Q5 runtime support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths. |
