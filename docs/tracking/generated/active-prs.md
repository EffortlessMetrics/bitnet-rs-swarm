<!-- GENERATED: do not edit by hand. Run cargo run -p xtask --no-default-features -- campaign generate. -->
# Active Campaign PRs

| Campaign | Item | PR | Branch | Notes |
|---|---|---:|---|---|
| slm-cpu | SLM-CPU-244 | #1550 | `codex/slm-cpu/slm-cpu-244-no-bias-dispatch-candidate-receipts` | Consume the SLM-CPU-243 production warm-session prompt/session descriptor path and add or precisely block the fail-closed no-bias apply_linear dispatch branch for the exact Qwen3/Qwen2.5 Q8_0 feed_forward.down_proj callsite. A valid implementation must execute dense_linear_no_bias_candidate_forward only when the explicit gate is enabled, the prompt/session descriptor is present, the tensor and callsite identity match exactly, the model family is Qwen3 or Qwen2.5 with quant_format=Q8_0, bias_present=false, runtime_api=cpu, selected_backend=cpu-rust, fallback=false, and the default eager_f32_candle path remains selected when the gate is absent. If candidate-on execution is wired, it must capture fresh candidate-off/candidate-on receipts proving same prompt IDs, generated IDs, decoded text, model SHA, tokenizer source and strictness, selected backend/kernel identity, selected/candidate path identity, and fallback=false for both Qwen3 and Qwen2.5. A valid blocker must name the exact missing dispatch condition, descriptor validation, tensor/callsite identity, post-decode receipt emitter, command surface, or runtime ownership boundary. This item must not claim timing improvement, allocation reduction, speedup, sustained throughput, Q4/Q5 support, server/accelerator execution, Qwen3.5 support, or BitNet QK256 changes. |
