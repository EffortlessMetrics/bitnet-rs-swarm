id = "nvidia-5070ti"
title = "NVIDIA RTX 5070 Ti validation"
status = "active"

objective = "Maintain the completed RTX 5070 Ti CUDA BitNet proof lane while qualifying strict BitNet CUDA performance with selected-device receipts and no CPU, OpenCL, WGPU, dense CUDA, or generic GPU conflation."

end_state = [
  "RTX 5070 Ti CUDA backend identity is distinct from generic CUDA and WGPU.",
  "CUDA and NVML probe facts are recorded before kernel execution claims.",
  "CUDA smoke, parity, receipts, and benchmarks are sequenced after identity.",
  "Strict BitNet CUDA proof receipts name the official GGUF, explicit tokenizer, W1.58A8 layout, selected RTX 5070 Ti CUDA backend, 9950X3D CPU reference, fallback status, QK256 CUDA invocation counts, upload-once weight residency, and measured timing.",
  "Repeated strict ask benchmark receipts preserve same-model CPU AVX-512 and RTX 5070 Ti CUDA evidence before any speedup claim is accepted.",
  "CUDA-DENSE-001 remains an optional dense regular-LLM reference lane and is not part of completed BitNet packed-kernel proof.",
]

hard_constraints = [
  "CUDA visibility is not kernel execution.",
  "WGPU smoke is not CUDA proof.",
  "CPU fallback cannot count as CUDA execution.",
  "Performance claims require driver, CUDA, VRAM, power, and thermal context.",
  "speedup_claim must remain false unless a same-model fallback-free benchmark receipt explicitly upgrades it.",
  "Dense regular-LLM CUDA receipts cannot satisfy BitNet packed I2S or QK256 proof acceptance.",
]

[[work_item]]
id = "RTX5070TI-003"
status = "merged"
branch = "codex/rtx5070ti-003-backend-identity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Preserve RTX 5070 Ti requested and selected CUDA backend identity without adding kernels or inference claims."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-common --no-default-features --features cpu",
  "cargo test --locked -p bitnet-device-config-core --no-default-features --features cpu",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-common/src/backend_selection.rs",
  "crates/bitnet-common/src/kernel_registry.rs",
  "crates/bitnet-device-config-core/Cargo.toml",
  "crates/bitnet-device-config-core/src/lib.rs",
  "crates/bitnet-cli-config-core/src/lib.rs",
  "crates/bitnet-cli/src/main.rs",
  "docs/tracking/bitnet-alignment/status.md",
  "docs/tracking/bitnet-alignment/workstream-ledger.yaml",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "xtask/src/campaign.rs",
  "xtask/src/main.rs",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "RTX 5070 Ti CUDA backend identity is preserved through config and logging surfaces.",
]
must_not_claim = [
  "CUDA execution works.",
  "WGPU smoke proves CUDA.",
  "BitNet inference works on RTX 5070 Ti.",
]

[[work_item]]
id = "RTX5070TI-004"
status = "merged"
branch = "codex/nvidia-5070ti/RTX5070TI-004-cuda-nvml-probe"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["RTX5070TI-003"]
acceptance = "Add RTX 5070 Ti CUDA and NVML runtime probe without claiming kernel execution or BitNet inference."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-device-probe --no-default-features --features cuda",
]
allowed_paths = [
  "crates/bitnet-device-probe/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/cuda/**",
]
may_claim = [
  "RTX 5070 Ti CUDA runtime visibility is recorded after probe receipts exist.",
]
must_not_claim = [
  "CUDA kernel execution works.",
  "BitNet inference works on RTX 5070 Ti.",
]

[[work_item]]
id = "RTX5070TI-005"
status = "merged"
branch = "codex/nvidia-5070ti/RTX5070TI-005-smoke-receipt"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["RTX5070TI-004"]
acceptance = "Compile and run a tiny CUDA kernel on the selected RTX 5070 Ti with a fallback-free smoke receipt and no BitNet inference or speedup claim."
commands = [
  "cargo fmt --all -- --check",
  "Manual RTX 5070 Ti CUDA smoke command with receipt artifact.",
]
allowed_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-kernels/tests/**",
  "crates/bitnet-cli/src/main.rs",
  "ci/hardware/_templates/cuda-smoke-receipt.json",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "RTX 5070 Ti CUDA executes a tiny smoke kernel fallback-free after a smoke receipt exists.",
]
must_not_claim = [
  "BitNet inference works on RTX 5070 Ti.",
  "CUDA is faster than CPU.",
  "QK256 CUDA works.",
]

[[work_item]]
id = "RTX5070TI-006"
status = "merged"
branch = "codex/nvidia-5070ti/RTX5070TI-006-cuda-cpu-parity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["RTX5070TI-005"]
acceptance = "Run one deterministic fixture through 9950X3D CPU reference and RTX 5070 Ti CUDA target, record error metrics and fallback-free selected-device identity, and emit mismatch debug artifacts without full inference claims."
commands = [
  "cargo fmt --all -- --check",
  "Manual RTX 5070 Ti CPU/CUDA parity command with receipt artifact.",
]
allowed_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "tests/**",
  "ci/hardware/_templates/cuda-parity-receipt.json",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-06/cuda-parity.json",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
]
may_claim = [
  "RTX 5070 Ti CUDA matches the 9950X3D CPU reference for the named parity fixture after cuda-parity.json exists.",
]
must_not_claim = [
  "Full BitNet CUDA inference works.",
  "QK256 CUDA works.",
  "CUDA is faster than CPU.",
]

[[work_item]]
id = "RTX5070TI-007"
status = "merged"
branch = "codex/nvidia-5070ti/RTX5070TI-007-cuda-receipts-counters"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["RTX5070TI-006"]
acceptance = "Record CUDA runtime identity and kernel invocation counters in smoke and parity receipts, including fallback-free strict validation, without adding benchmarks or BitNet inference claims."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-receipts --no-default-features",
  "CUDA smoke and parity receipt validation commands.",
]
allowed_paths = [
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-bench-receipts/**",
  "crates/bitnet-kernels/**",
  "ci/hardware/_templates/cuda-smoke-receipt.json",
  "ci/hardware/_templates/cuda-parity-receipt.json",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
]
may_claim = [
  "RTX 5070 Ti CUDA smoke and parity evidence is backed by validated receipts and kernel counters after receipt validation exists.",
]
must_not_claim = [
  "Full BitNet CUDA inference works.",
  "QK256 CUDA works.",
  "CUDA is faster than CPU.",
]

[[work_item]]
id = "RTX5070TI-008"
status = "merged"
branch = "codex/nvidia-5070ti/RTX5070TI-008-benchmark-baseline"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["RTX5070TI-006", "RTX5070TI-007"]
acceptance = "Benchmark parity-tested RTX 5070 Ti CUDA kernels/subgraphs against the 9950X3D CPU reference with driver/runtime/VRAM/power/thermal context and no full inference or unproven speedup claim."
commands = [
  "cargo fmt --all -- --check",
  "Manual RTX 5070 Ti benchmark command with receipt artifact.",
]
allowed_paths = [
  "crates/bitnet-kernels/Cargo.toml",
  "crates/bitnet-kernels/benches/**",
  "crates/bitnet-bench-receipts/**",
  "ci/hardware/_templates/cuda-benchmark-receipt.json",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-06/cuda-benchmark.json",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
]
may_claim = [
  "RTX 5070 Ti CUDA benchmark data exists for the named parity-tested kernel or subgraph after cuda-benchmark.json exists.",
]
must_not_claim = [
  "Full BitNet CUDA inference works.",
  "QK256 CUDA works.",
  "CUDA speedup exists without a same-model benchmark artifact.",
]

[[work_item]]
id = "CUDA-BITNET-001"
status = "merged"
branch = "codex/cuda-bitnet-001-context-handles"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["RTX5070TI-007"]
acceptance = "Add persistent CUDA BitNet context, stream lifetime, reusable workspace, and weight handles without claiming full BitNet inference."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-kernels --no-default-features --features cpu",
  "cargo test --locked -p bitnet-kernels --no-default-features --features cuda",
]
allowed_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-kernels/src/lib.rs",
  "crates/bitnet-inference/**",
  "crates/bitnet-common/src/backend_selection.rs",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
]
may_claim = [
  "CUDA BitNet context, stream, workspace, and weight-handle infrastructure exists after CUDA-BITNET-001 lands.",
]
must_not_claim = [
  "Full BitNet CUDA inference works.",
  "CUDA speedup exists.",
]

[[work_item]]
id = "CUDA-BITNET-002"
status = "merged"
branch = "codex/cuda-bitnet-002-i2s-linear"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-BITNET-001"]
acceptance = "Add reusable CUDA I2S linear primitive with CPU/CUDA parity, tails and padding support, and kernel stats without full inference claims."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-kernels --no-default-features --features cuda",
  "Manual RTX 5070 Ti I2S parity command with receipt artifact.",
]
allowed_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-kernels/tests/**",
  "crates/bitnet-quantization/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
]
may_claim = [
  "CUDA I2S primitive parity exists for named fixtures after CUDA-BITNET-002 lands.",
]
must_not_claim = [
  "QK256 CUDA works.",
  "Full BitNet CUDA inference works.",
]

[[work_item]]
id = "CUDA-BITNET-003"
status = "merged"
branch = "codex/cuda-bitnet-003-qk256-gemv"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-BITNET-001"]
acceptance = "Replace scaffold-only QK256 launch with a compiled packed fused dequant GEMV CUDA kernel that passes CPU scalar parity."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-kernels --no-default-features --features cuda",
  "Manual RTX 5070 Ti QK256 parity command with receipt artifact.",
]
allowed_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-kernels/tests/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-dispatch/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
]
may_claim = [
  "QK256 CUDA GEMV works for receipt-backed parity fixtures after CUDA-BITNET-003 lands.",
]
must_not_claim = [
  "Full BitNet CUDA inference works.",
  "CUDA speedup exists.",
]

[[work_item]]
id = "CUDA-BITNET-004"
status = "merged"
branch = "codex/cuda-bitnet-004-upload-once-weights"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-BITNET-002", "CUDA-BITNET-003"]
acceptance = "Prepack and upload BitNet weights once for CUDA with per-layer handles and receipt fields for packed_at_load and uploaded_once."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-models --no-default-features --features cpu",
  "cargo test --locked -p bitnet-inference --no-default-features --features cpu,cuda",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
]
may_claim = [
  "CUDA BitNet weight handles can be packed and uploaded once after CUDA-BITNET-004 lands.",
]
must_not_claim = [
  "Full BitNet CUDA inference works.",
  "The routed transformer proof is upload-once until a receipt proves it.",
]

[[work_item]]
id = "CUDA-BITNET-005"
status = "merged"
branch = "codex/cuda-bitnet-005-route-linear"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-BITNET-004"]
acceptance = "Route actual BitNetLinear transformer dispatch through the selected CUDA backend with coverage counters and strict CPU fallback rejection."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu,cuda",
  "cargo test --locked -p bitnet-inference --no-default-features --features cpu,cuda",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-common/src/backend_selection.rs",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
]
may_claim = [
  "Actual BitNetLinear dispatch can contribute CUDA work after CUDA-BITNET-005 lands.",
]
must_not_claim = [
  "Full BitNet CUDA inference works until the strict one-token proof lands.",
  "Dense CUDA fallback counts as BitNet packed-kernel proof.",
]

[[work_item]]
id = "CUDA-BITNET-006"
status = "merged"
branch = "codex/cuda-bitnet-006-one-token-proof"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-BITNET-005"]
acceptance = "Add strict one-token BitNet CUDA proof with official GGUF, real tokenizer, CUDA kernel invocations greater than zero, zero CPU fallback, CPU/CUDA greedy or top-1 agreement, fallback_used=false, and speedup_claim=false."
commands = [
  "cargo fmt --all -- --check",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo test --locked -p bitnet-cli --bin bitnet --no-default-features --features cpu,full-cli",
  "cargo clippy --locked -p bitnet-cli --bin bitnet --no-default-features --features cpu,full-cli -- -D warnings",
  "Manual RTX 5070 Ti strict one-token BitNet CUDA proof with receipt artifact.",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-06/strict-bitnet-cpu-reference.json",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-06/strict-bitnet-cuda-proof.json",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
]
may_claim = [
  "Strict one-token BitNet CUDA inference ran fallback-free on the RTX 5070 Ti after the proof receipt exists.",
]
must_not_claim = [
  "CUDA speedup exists.",
  "Short decode works.",
  "The routed proof path uploads weights once unless the receipt proves it.",
]

[[work_item]]
id = "CUDA-BITNET-007"
status = "merged"
branch = "codex/cuda-bitnet-007-short-decode-proof"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-BITNET-006"]
acceptance = "Add short greedy BitNet CUDA decode proof with timing, kernel invocation growth, memory high-water mark, and fallback_used=false."
commands = [
  "cargo fmt --all -- --check",
  "Manual RTX 5070 Ti short decode BitNet CUDA proof with receipt artifact.",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-06/strict-bitnet-cuda-short-decode.json",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
]
may_claim = [
  "Strict BitNet CUDA short decode works after strict-bitnet-cuda-short-decode.json exists.",
]
must_not_claim = [
  "CUDA speedup exists.",
  "Full benchmark performance is proven.",
]

[[work_item]]
id = "CUDA-BITNET-008"
status = "merged"
branch = "codex/cuda-bitnet-008-benchmark-baseline"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-BITNET-007"]
acceptance = "Add RTX 5070 Ti full BitNet CUDA benchmark baseline comparing 9950X3D CPU scalar, AVX2, AVX-512, and CUDA with matching model, tokenizer, prompt profile, strict loader mode, fallback_used=false, and runtime context."
commands = [
  "cargo fmt --all -- --check",
  "Manual RTX 5070 Ti BitNet CUDA benchmark with receipt artifact.",
]
allowed_paths = [
  "crates/bitnet-kernels/benches/**",
  "crates/bitnet-bench-receipts/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-06/strict-bitnet-cuda-benchmark.json",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
]
may_claim = [
  "Strict BitNet CUDA same-model benchmark baseline evidence exists after strict-bitnet-cuda-benchmark.json validates, with speedup_claim=false.",
]
must_not_claim = [
  "CUDA speedup exists before same-model benchmark receipt proves it.",
]

[[work_item]]
id = "CUDA-BITNET-009"
status = "merged"
branch = "codex/cuda-bitnet-009-upload-once-routed-proof"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-BITNET-008"]
acceptance = "Route the strict RTX 5070 Ti CUDA inference proof through persistent CUDA BitNet weight handles so receipts prove weights_uploaded_once=true, per_token_weight_upload=false, qk256_gemv_cuda invocations greater than zero, and zero CPU fallback."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-kernels --no-default-features --features cuda",
  "cargo test --locked -p bitnet-qk256-dispatch --no-default-features --features cuda",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "Manual RTX 5070 Ti strict one-token and short-decode BitNet CUDA proofs with upload-once receipt fields.",
]
allowed_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-bench-receipts/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-06/strict-bitnet-cuda-proof.json",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-06/strict-bitnet-cuda-short-decode.json",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-06/strict-bitnet-cuda-benchmark.json",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
]
may_claim = [
  "Strict RTX 5070 Ti BitNet CUDA inference uses upload-once CUDA weight handles only after strict proof receipts record weights_uploaded_once=true and per_token_weight_upload=false.",
]
must_not_claim = [
  "CUDA speedup exists before a refreshed same-model benchmark receipt proves it.",
]

[[work_item]]
id = "CUDA-ANSWER-010"
status = "merged"
branch = "codex/cuda-answer/CUDA-ANSWER-010-qk256-i2s-parity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-BITNET-009"]
acceptance = "Align RTX 5070 Ti QK256 I2_S CUDA GEMV with the BitNet.cpp/GGML grouped bitplane layout so strict CUDA ask answers the constrained math prompt and the committed deterministic answer corpus passes with selected_backend=nvidia-rtx-5070-ti-cuda, runtime_api=cuda, fallback_used=false, selected_kernel=qk256_gemv_cuda, and speedup_claim=false."
commands = [
  "RUSTFLAGS=-D warnings cargo check --locked --workspace --no-default-features --features cpu",
  "RUSTFLAGS=-D warnings cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo test --locked -p bitnet-kernels --no-default-features --features cuda --lib qk256 -- --nocapture",
  "cargo clippy --locked --workspace --no-default-features --features cpu -- -D warnings",
  "cargo run --locked --release -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- ask --device nvidia-rtx-5070-ti-cuda --model models/microsoft-bitnet-b1.58-2B-4T-gguf/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --question \"What is 2+2? Answer with only the number.\" --max-new-tokens 8 --temperature 0 --strict-cuda --receipt-out target/bitnet/receipts/cuda-answer-readiness/strict-cuda-ask-math-closeout.json",
  "cargo run --locked --release -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- answer-corpus --device nvidia-rtx-5070-ti-cuda --model models/microsoft-bitnet-b1.58-2B-4T-gguf/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --corpus ci/quality/bitnet-answer-corpus.yaml --per-prompt-timeout-seconds 300 --dump-logit-steps 1 --logits-topk 20 --fail-on-quality --json-out target/bitnet/receipts/cuda-answer-readiness/cuda-answer-corpus-closeout.json",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/src/model_cache.rs",
  "crates/bitnet-ffi/src/config.rs",
  "xtask/src/main.rs",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/strict-cuda-ask-math.json",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/cuda-answer-corpus.json",
  "docs/reports/CUDA_ANSWER_010_STRICT_5070TI_CORPUS.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
  "crates/bitnet-qk256-dispatch/**",
  "dense CUDA reference lanes",
  "WGPU, Vulkan, D3D12, OpenCL, Metal, NPU, or SLM runtime work",
]
may_claim = [
  "The official Microsoft I2_S artifact passes the committed deterministic answer corpus through the strict RTX 5070 Ti CUDA answer path after CUDA-ANSWER-010.",
  "The strict CUDA answer path records selected_backend=nvidia-rtx-5070-ti-cuda, runtime_api=cuda, fallback_used=false, selected_kernel=qk256_gemv_cuda, prompt prefill, upload-once weights, and answer quality pass for the committed corpus.",
]
must_not_claim = [
  "Broad chat quality is proven beyond the committed deterministic answer corpus.",
  "CUDA speedup exists before a same-model fallback-free benchmark receipt upgrades speedup_claim.",
  "Dense CUDA, generic GPU, WGPU, OpenCL, Metal, NPU, or SLM proof satisfies RTX 5070 Ti BitNet packed I2_S CUDA answer readiness.",
]

[[work_item]]
id = "CUDA-ANSWER-011"
status = "merged"
branch = "codex/cuda-answer/CUDA-ANSWER-011-cpu-cuda-parity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-ANSWER-010"]
acceptance = "Record same-box 9950X3D AVX-512 CPU and RTX 5070 Ti CUDA answer-corpus receipts for the official Microsoft I2_S artifact, compare them with the generic answer parity tool, and preserve the first divergence evidence without claiming exact CPU/CUDA parity when generated tokens or top-k logits differ."
commands = [
  "cargo run --locked --release -p bitnet-cli --no-default-features --features cpu,full-cli -- answer-corpus --device cpu --cpu-kernel avx512 --model models/microsoft-bitnet-b1.58-2B-4T-gguf/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --corpus ci/quality/bitnet-answer-corpus.yaml --per-prompt-timeout-seconds 300 --dump-logit-steps 1 --logits-topk 20 --fail-on-quality --json-out target/bitnet/receipts/cuda-answer-readiness/cpu-avx512-answer-corpus-closeout.json",
  "cargo run --locked --release -p bitnet-cli --no-default-features --features cpu,full-cli -- answer-parity --left target/bitnet/receipts/cuda-answer-readiness/cpu-avx512-answer-corpus-closeout.json --right ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/cuda-answer-corpus.json --left-label 9950x3d-avx512 --right-label rtx5070ti-cuda --machine windows-9950x3d-rtx5070ti --json-out target/bitnet/receipts/cuda-answer-readiness/cpu-avx512-vs-cuda-answer-parity-closeout.json",
  "Parse committed CPU and parity JSON receipts with Python.",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/cpu-avx512-answer-corpus.json",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/cpu-avx512-vs-cuda-answer-parity.json",
  "docs/reports/CUDA_ANSWER_011_CPU_CUDA_PARITY.md",
  "docs/specs/rtx5070ti-cuda-answer-readiness.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "dense CUDA reference lanes",
  "WGPU, Vulkan, D3D12, OpenCL, Metal, NPU, or SLM runtime work",
]
may_claim = [
  "Both same-box 9950X3D AVX-512 CPU and RTX 5070 Ti CUDA answer-corpus receipts pass the committed deterministic corpus for the official Microsoft I2_S artifact.",
  "The original CPU/CUDA parity receipt recorded first divergence as logits_or_kernel/top-k evidence and preserved the yes_no_water generated-answer divergence.",
]
must_not_claim = [
  "Exact CPU/CUDA generated-token parity is proven.",
  "Exact CPU/CUDA top-k logit parity is proven.",
  "CUDA speedup exists before a same-model fallback-free benchmark receipt upgrades speedup_claim.",
  "Broad chat quality is proven beyond the committed deterministic answer corpus.",
]

[[work_item]]
id = "CUDA-ANSWER-012"
status = "merged"
branch = "codex/cuda-answer/CUDA-ANSWER-012-logit-divergence"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-ANSWER-011"]
acceptance = "Align the RTX 5070 Ti CUDA QK256 inline-scale path with BitNet.cpp I2_S x I8_S activation semantics so same-box CPU AVX-512 and RTX 5070 Ti CUDA generated token IDs and decoded text match for the five committed deterministic corpus cases while preserving remaining top-k-only divergence evidence without speed or broad-chat claims."
commands = [
  "cargo fmt -p bitnet-kernels -p bitnet-qk256-dispatch -- --check",
  "cargo check --locked -p bitnet-qk256-dispatch --no-default-features --features cuda",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "BITNET_RUN_CUDA_QK256_GEMV=1 cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda test_cuda_qk256_gemv_live_parity_opt_in -- --nocapture",
  "BITNET_RUN_CUDA_QK256_GEMV=1 cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda test_cuda_qk256_gemv_live_bitnet_i8s_scaled_parity_opt_in -- --nocapture",
  "cargo run --locked --release -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- answer-corpus --device nvidia-rtx-5070-ti-cuda --model models/microsoft-bitnet-b1.58-2B-4T-gguf/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --corpus ci/quality/bitnet-answer-corpus.yaml --per-prompt-timeout-seconds 300 --dump-logit-steps 1 --logits-topk 20 --fail-on-quality --json-out target/bitnet/receipts/cuda-answer-readiness/cuda-answer-corpus-i8s-scale-fix.json",
  "cargo run --locked --release -p bitnet-cli --no-default-features --features cpu,full-cli -- answer-parity --left ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/cpu-avx512-answer-corpus.json --right target/bitnet/receipts/cuda-answer-readiness/cuda-answer-corpus-i8s-scale-fix.json --left-label 9950x3d-avx512 --right-label rtx5070ti-cuda --machine windows-9950x3d-rtx5070ti --json-out target/bitnet/receipts/cuda-answer-readiness/cpu-avx512-vs-cuda-answer-parity-i8s-scale-fix.json",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-kernels/src/cuda/bitnet_context.rs",
  "crates/bitnet-kernels/tests/snapshots/snapshot_wave19__qk256_gemv_config_default_debug.snap",
  "crates/bitnet-qk256-dispatch/src/lib.rs",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/cuda-answer-corpus.json",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/cpu-avx512-vs-cuda-answer-parity.json",
  "docs/reports/CUDA_ANSWER_011_CPU_CUDA_PARITY.md",
  "docs/reports/CUDA_ANSWER_012_BITNET_I8S_CUDA_PARITY.md",
  "docs/specs/rtx5070ti-cuda-answer-readiness.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "crates/bitnet-tokenizers/**",
  "model loader behavior",
  "dense CUDA reference lanes",
  "WGPU, Vulkan, D3D12, OpenCL, Metal, NPU, or SLM runtime work",
]
may_claim = [
  "The strict RTX 5070 Ti CUDA answer corpus still passes all five committed deterministic cases for the official Microsoft I2_S artifact.",
  "Same-box 9950X3D AVX-512 CPU and RTX 5070 Ti CUDA generated token IDs and decoded text match for all five committed deterministic corpus cases after the CUDA QK256 BitNet I8_S activation fix.",
  "The yes_no_water generated-answer divergence from CUDA-ANSWER-011 is closed.",
]
must_not_claim = [
  "Exact CPU/CUDA top-k logit parity is proven.",
  "CUDA speedup exists before a same-model fallback-free benchmark receipt upgrades speedup_claim.",
  "Broad chat quality is proven beyond the committed deterministic answer corpus.",
  "Full CUDA residency for every transformer operation is proven.",
]

[[work_item]]
id = "CUDA-PROD-001"
status = "merged"
branch = "codex/cuda-prod/CUDA-PROD-001-strict-ask-receipt-default"
pr = 4054
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-ANSWER-012"]
acceptance = "Make strict `bitnet ask` validate backend, fallback, and answer quality even when the user does not pass --receipt-out by writing a default answer receipt for strict CPU/CUDA ask runs, preserving no speed claim and no broad chat claim."
commands = [
  "cargo fmt -p bitnet-cli -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli strict_ask -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/main.rs",
  "docs/reports/CUDA_PROD_001_STRICT_ASK_RECEIPT_DEFAULT.md",
  "docs/specs/rtx5070ti-cuda-answer-readiness.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "model loader behavior",
  "dense CUDA reference lanes",
  "WGPU, Vulkan, D3D12, OpenCL, Metal, NPU, or SLM runtime work",
]
may_claim = [
  "Strict `bitnet ask` writes and validates an answer receipt by default for strict CPU/CUDA modes when --receipt-out is omitted.",
  "The default strict CUDA ask receipt preserves selected backend, runtime API, fallback status, QK256 kernel identity, answer quality, and speedup_claim=false.",
]
must_not_claim = [
  "Broad chat quality is proven beyond the committed deterministic answer corpus.",
  "CUDA speedup exists before a same-model fallback-free benchmark receipt upgrades speedup_claim.",
  "Full CUDA residency for every transformer operation is proven.",
  "The model fetch/verify/install UX is complete.",
]

[[work_item]]
id = "CUDA-PROD-002"
status = "merged"
branch = "codex/cuda-prod/CUDA-PROD-002-warm-session-receipts"
pr = 4059
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-PROD-001"]
acceptance = "Add a strict CUDA warm-session ask/chat path that loads the model once, initializes the RTX 5070 Ti CUDA context once, uploads BitNet weights once, serves multiple deterministic turns, and emits per-turn or session-summary receipts without broad chat, speed, server, or full-residency claims."
commands = [
  "cargo fmt -p bitnet-cli -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli session -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "docs/reports/**",
  "docs/specs/rtx5070ti-cuda-answer-readiness.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "dense CUDA reference lanes",
  "WGPU, Vulkan, D3D12, OpenCL, Metal, NPU, or SLM runtime work",
]
may_claim = [
  "The strict RTX 5070 Ti CUDA session path reuses model load, CUDA context, and upload-once BitNet weights across multiple deterministic turns.",
  "Session receipts preserve selected backend, runtime API, fallback status, per-turn quality, and speedup_claim=false.",
]
must_not_claim = [
  "CUDA speedup exists before a same-model fallback-free benchmark receipt upgrades speedup_claim.",
  "Broad chat quality is proven beyond committed deterministic prompts.",
  "Full CUDA residency for every transformer operation is proven.",
  "Server inference readiness is complete.",
]

[[work_item]]
id = "CUDA-PROD-003"
status = "merged"
branch = "codex/cuda-prod/CUDA-PROD-003-residency-coverage"
pr = 4073
merge_sha = "bbc7d00b41afa8f0c3ab7a3882fc032e8d07e68d"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-PROD-002"]
acceptance = "Add CUDA execution-residency coverage receipts for the strict RTX 5070 Ti answer path so QK256 linears, upload-once weights, KV cache, norms, RoPE, attention/softmax, LM head, sampling, host/device transfer accounting, and non-resident phases are visible without claiming speedup or full residency before coverage proves it."
commands = [
  "cargo fmt -p bitnet-cli -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli cuda -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "docs/reports/**",
  "docs/specs/rtx5070ti-cuda-answer-readiness.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "dense CUDA reference lanes",
  "WGPU, Vulkan, D3D12, OpenCL, Metal, NPU, or SLM runtime work",
]
may_claim = [
  "Strict RTX 5070 Ti CUDA receipts expose which decode phases are CUDA-resident, CPU-resident, transfer-accounted, or not yet claimed.",
  "QK256 BitNet linear CUDA coverage and upload-once weight residency remain visible in the normal answer path.",
]
must_not_claim = [
  "CUDA speedup exists before a same-model fallback-free benchmark receipt upgrades speedup_claim.",
  "Broad chat quality is proven beyond committed deterministic prompts.",
  "Full CUDA residency for every transformer operation is proven unless every phase is counted as resident.",
  "Server inference readiness is complete.",
]

[[work_item]]
id = "CUDA-PROD-004"
status = "merged"
branch = "codex/cuda-prod/CUDA-PROD-004-benchmark-baseline"
pr = 4093
merge_sha = "85aba1312b9e21523b459d90010bf67f5594830c"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-PROD-003"]
acceptance = "Add strict RTX 5070 Ti answer-path benchmark receipts after residency coverage, measuring same-model CPU AVX-512 and CUDA runs across deterministic profiles with model load, tokenizer load, CUDA context init, weight upload, prompt render/tokenize, prefill, first token, steady decode, kernel time, host/device transfer accounting, VRAM, power/temperature where available, and speedup_claim=false until the benchmark evidence is explicitly accepted."
commands = [
  "cargo fmt -p bitnet-cli -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli bench cuda -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-bench-receipts/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/specs/rtx5070ti-cuda-answer-readiness.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "dense CUDA reference lanes",
  "WGPU, Vulkan, D3D12, OpenCL, Metal, NPU, or SLM runtime work",
]
may_claim = [
  "Strict RTX 5070 Ti CUDA answer-path timing is measured with same-model CPU/CUDA receipt context and fallback status.",
  "Benchmark receipts expose timing splits, residency boundaries, and speedup_claim=false.",
]
must_not_claim = [
  "CUDA speedup exists before a same-model fallback-free benchmark receipt is reviewed and accepted.",
  "Broad chat quality is proven beyond committed deterministic prompts.",
  "Full CUDA residency for every transformer operation is proven unless every phase is counted as resident.",
  "Server inference readiness is complete.",
]

[[work_item]]
id = "CUDA-BITNET-PERF-002"
status = "merged"
branch = "codex/cuda-bitnet-perf-002-repeated-strict-ask"
pr = 4112
head_sha = "66894fb8de7bc7f3c8bea6147883c7e22615b1f3"
merge_sha = "699b1204bba9443c59c243c82a2f6cc0acccdcea"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-PROD-004"]
acceptance = "Add repeated strict ask benchmark receipts for the official Microsoft I2_S model, comparing same-model 9950X3D CPU AVX-512 and RTX 5070 Ti CUDA runs with deterministic policy, fallback_used=false, measured QK256 timing/transfer counters, and speedup_claim=false pending explicit benchmark review."
commands = [
  "cargo fmt -p bitnet-bench-receipts -- --check",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-bench-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "dense CUDA reference lanes",
  "WGPU, Vulkan, D3D12, OpenCL, Metal, NPU, or SLM runtime work",
]
may_claim = [
  "Repeated strict ask benchmark evidence exists for the exact committed strict_ask_math_8 profile on 9950X3D CPU AVX-512 and RTX 5070 Ti CUDA.",
  "The repeated receipt records measured QK256 CUDA kernel time and transfer byte counters with fallback_used=false.",
]
must_not_claim = [
  "CUDA speedup is accepted before a later benchmark qualification review upgrades the exact profile.",
  "Broad chat quality is proven beyond committed deterministic prompts.",
  "Full CUDA residency for every transformer operation is proven.",
  "Dense regular-LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
  "Server inference readiness is complete.",
]

[[work_item]]
id = "CUDA-BITNET-PERF-003"
status = "merged"
branch = "codex/cuda-bitnet-perf-003-warm-session-benchmark"
pr = 4117
head_sha = "c15690ccfc9633e73c37372255c143b0e00ea602"
merge_sha = "1351ab04b2d49dbb7402982984116bc3e799665e"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-BITNET-PERF-002", "CUDA-PROD-002", "CUDA-PROD-003"]
acceptance = "Add repeated strict CUDA warm-session benchmark receipts for the official Microsoft I2_S model with two deterministic turns per run, model/tokenizer/context loaded once, upload-once QK256 weight handles, fallback_used=false, measured QK256 timing/transfer counters, and speedup_claim=false pending explicit benchmark review."
commands = [
  "cargo fmt -p bitnet-bench-receipts -- --check",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-bench-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "dense CUDA reference lanes",
  "WGPU, Vulkan, D3D12, OpenCL, Metal, NPU, or SLM runtime work",
]
may_claim = [
  "Repeated strict CUDA warm-session benchmark evidence exists for the exact strict_cuda_warm_session_2_turns profile on RTX 5070 Ti CUDA.",
  "The repeated warm-session receipt records model/tokenizer/context loaded once, upload-once QK256 weight handles, fallback_used=false, and measured QK256 CUDA kernel time plus transfer byte counters.",
]
must_not_claim = [
  "CUDA speedup is accepted before a later benchmark qualification review upgrades the exact profile.",
  "Broad chat quality is proven beyond committed deterministic prompts.",
  "Full CUDA residency for every transformer operation is proven.",
  "Dense regular-LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
  "Server inference readiness is complete.",
]

[[work_item]]
id = "CUDA-BITNET-PERF-004"
status = "merged"
branch = "codex/cuda-bitnet-perf-004-qualification"
pr = 4124
head_sha = "9628ca403d431ceb44535fd694b5ee833adf26f2"
merge_sha = "4a314ba9e8ddf530d8916d10cceaf0d858a5593e"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-BITNET-PERF-002", "CUDA-BITNET-PERF-003"]
acceptance = "Add a governed benchmark qualification review for the repeated strict ask and warm-session RTX 5070 Ti BitNet CUDA receipts, explicitly keeping speedup_claim=false and recording the missing evidence before any profile-specific speedup claim can be accepted."
commands = [
  "cargo fmt -p bitnet-bench-receipts -- --check",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-bench-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "dense CUDA reference lanes",
  "WGPU, Vulkan, D3D12, OpenCL, Metal, NPU, or SLM runtime work",
]
may_claim = [
  "Benchmark qualification review evidence exists for the current repeated strict ask and warm-session RTX 5070 Ti BitNet CUDA receipts.",
  "The review records why speedup_claim and benchmark_qualified_speedup remain false for the reviewed profiles.",
]
must_not_claim = [
  "CUDA speedup is accepted for any profile.",
  "Broad chat quality is proven beyond committed deterministic prompts.",
  "Full CUDA residency for every transformer operation is proven.",
  "Dense regular-LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
  "Server inference readiness is complete.",
]

[[work_item]]
id = "CUDA-DENSE-001"
status = "merged"
branch = "codex/cuda-dense-001-receipt-boundary"
pr = 4104
head_sha = "fb306c7becdb11ca7fb4b21e122f6ba287331cb9"
merge_sha = "44c6eac5d27107a83dc94e97b0c75ec8308932a9"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["RTX5070TI-007"]
acceptance = "Add the regular LLM CUDA dense-lane receipt boundary with dense_regular_llm_cuda labels, speedup/full-residency claims false, and a validator proving dense receipts cannot satisfy BitNet packed I2S or QK256 proof acceptance."
commands = [
  "cargo fmt -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
]
may_claim = [
  "Dense regular-LLM CUDA receipts have a separate dense_regular_llm_cuda label and claim boundary.",
  "Dense regular-LLM CUDA receipts are rejected as BitNet packed I2S/QK256 proof.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA matmul parity exists.",
  "Dense regular LLM CUDA speedup exists.",
  "Full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-002"
status = "merged"
branch = "codex/cuda-dense-002-gemm-parity"
pr = 4106
head_sha = "5f6cd1ff56d0c0b659debc9c89a0f5f73aec4653"
merge_sha = "0de3a32c5ba973515bf9f4eeba981ec396d8ed9b"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-001"]
acceptance = "Add the first dense CUDA FP16/BF16 or cuBLAS-backed GEMM smoke/parity fixture with dense_regular_llm_cuda receipts, fallback_used=false, CPU reference comparison, and no BitNet packed I2S/QK256 or speedup claim."
commands = [
  "cargo fmt -p bitnet-kernels -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda dense_f16 -- --nocapture",
  "BITNET_RUN_RTX5070TI_DENSE_CUDA_GEMM=1 BITNET_RTX5070TI_DENSE_CUDA_GEMM_RECEIPT=ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/dense-f16-gemm-parity.json cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda live_rtx5070ti_dense_f16_cuda_gemm_matches_cpu_reference_when_enabled -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation -- --nocapture",
  "cargo test --locked -p bitnet-receipts-core --lib -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "docs/reports/**",
  "docs/tracking/bitnet-alignment/backend-status.yaml",
  "docs/tracking/bitnet-alignment/status.md",
  "docs/tracking/bitnet-alignment/workstream-ledger.yaml",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
]
may_claim = [
  "A dense regular-LLM CUDA GEMM/parity fixture exists for the exact recorded receipt.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
]

[[work_item]]
id = "CUDA-DENSE-003"
status = "merged"
branch = "codex/cuda-dense-003-tensor-residency"
pr = 4119
head_sha = "f920790a957b7968ea4db896595c0041d935ce62"
merge_sha = "11e125b4b1ba13a575e782b2e5369ea9ccd96bfa"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-002"]
acceptance = "Add fixture-level dense regular-LLM CUDA tensor residency evidence for the RTX 5070 Ti FP16 GEMM path, proving input/output tensors are CUDA device buffers for the launch, transfer byte accounting matches kernel stats, fallback_used=false, and dense receipts still cannot satisfy BitNet packed I2S/QK256, speedup, dense GGUF inference, persistent session residency, or full CUDA residency claims."
commands = [
  "cargo fmt -p bitnet-kernels -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda dense_f16 -- --nocapture",
  "BITNET_RUN_RTX5070TI_DENSE_CUDA_GEMM=1 BITNET_RTX5070TI_DENSE_CUDA_GEMM_RECEIPT=ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/dense-f16-gemm-residency.json BITNET_RTX5070TI_DENSE_CUDA_GEMM_ARTIFACT_PATH=ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/dense-f16-gemm-residency.json cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda live_rtx5070ti_dense_f16_cuda_gemm_matches_cpu_reference_when_enabled -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation -- --nocapture",
  "cargo test --locked -p bitnet-receipts-core --lib -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-kernels/src/lib.rs",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense GGUF inference implementation",
]
may_claim = [
  "Dense regular-LLM CUDA tensor residency is recorded for the deterministic FP16 GEMM fixture.",
  "The fixture proves fallback-free RTX 5070 Ti CUDA device-buffer residency for the inputs and output of the dense FP16 GEMM launch.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Persistent dense model/session residency exists.",
  "Full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-004"
status = "merged"
branch = "codex/cuda-dense-004-persistent-gemm"
pr = 4121
head_sha = "0fd3642e1dc3493dc8cb7ee16203cdbbcf817d5e"
merge_sha = "4c67f3e096ea7eeb1fceb4ff1dcaf69c3461eb3c"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-003"]
acceptance = "Add persistent fixture-session residency for the dense regular-LLM CUDA FP16 GEMM path, proving one CUDA context/module, upload-once input device buffers, one output device buffer, repeated launches, per-run host-to-device bytes equal zero, fallback_used=false, and no BitNet packed I2S/QK256, speedup, dense GGUF inference, or full CUDA residency claim."
commands = [
  "cargo fmt -p bitnet-kernels -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda dense_f16 -- --nocapture",
  "BITNET_RUN_RTX5070TI_DENSE_CUDA_GEMM_SESSION=1 BITNET_RTX5070TI_DENSE_CUDA_GEMM_SESSION_RECEIPT=ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/dense-f16-gemm-persistent.json BITNET_RTX5070TI_DENSE_CUDA_GEMM_SESSION_ARTIFACT_PATH=ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/dense-f16-gemm-persistent.json BITNET_RTX5070TI_DENSE_CUDA_GEMM_SESSION_RUNS=3 cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda live_rtx5070ti_dense_f16_cuda_gemm_persistent_session_when_enabled -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation -- --nocapture",
  "cargo test --locked -p bitnet-receipts-core --lib -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense GGUF inference implementation",
]
may_claim = [
  "Dense regular-LLM CUDA persistent fixture-session residency is recorded for the deterministic FP16 GEMM fixture.",
  "The fixture proves fallback-free repeated RTX 5070 Ti CUDA launches with one context/module and upload-once input device buffers.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Persistent dense model/session inference exists.",
  "Full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-PLANNER-001"
status = "merged"
branch = "codex/cuda-planner-001-model-aware"
pr = 4127
head_sha = "4d2ec6815fec2ff1bf68da6f9e17e5bbf1c53291"
merge_sha = "191ad376baa39fa04dc8ce9e5113e35c9b045dd6"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Add a model-aware dispatch planner contract that keeps BitNet QK256 CUDA and dense regular-LLM CUDA routes separate, rejects unsupported strict CUDA fallback explicitly, and does not change kernels, model math, tokenizer, loader, transformer, or server behavior."
commands = [
  "cargo fmt -p bitnet-kernels -- --check",
  "cargo test --locked -p bitnet-kernels --lib --no-default-features dispatch_planner -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/dispatch_planner.rs",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF inference implementation",
]
may_claim = [
  "The planner can distinguish BitNet QK256 CUDA routes from dense regular-LLM CUDA routes.",
  "Strict CUDA planning records unsupported operations instead of silently selecting CPU fallback.",
]
must_not_claim = [
  "Any new CUDA kernel exists.",
  "BitNet QK256 CUDA math changed.",
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "General dense GGUF CUDA inference is complete.",
  "CUDA speedup is accepted.",
  "Full CUDA residency for every transformer operation is proven.",
]

[[work_item]]
id = "CUDA-PLANNER-002"
status = "merged"
branch = "codex/cuda-planner-002-capability-spec"
pr = 4129
head_sha = "3bb28760dd407da5f8633434d4f69e12caa53878"
merge_sha = "9e19a9a6ff04620200d862861f0a383132d3ea54"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Add conservative model-family and quantization metadata label mapping into the model-aware CUDA dispatch spec so BitNet I2_S/QK256 and dense regular-LLM FP16/BF16 routes can be selected without treating unknown or mismatched metadata as CUDA-supported."
commands = [
  "cargo fmt -p bitnet-kernels -- --check",
  "cargo test --locked -p bitnet-kernels --lib --no-default-features dispatch_planner -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/dispatch_planner.rs",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF inference implementation",
]
may_claim = [
  "The planner can derive a conservative model-aware dispatch spec from model-family and quantization labels.",
  "Recognized BitNet I2_S/QK256 labels and dense regular-LLM FP16/BF16 labels route to separate CUDA planner paths.",
  "Unknown or mismatched labels remain unsupported under strict CUDA.",
]
must_not_claim = [
  "Any new CUDA kernel exists.",
  "Runtime model loading or tokenizer behavior changed.",
  "General dense GGUF CUDA inference is complete.",
  "Arbitrary GGUF quantizations are CUDA-supported.",
  "CUDA speedup is accepted.",
  "Full CUDA residency for every transformer operation is proven.",
]

[[work_item]]
id = "CUDA-PLANNER-003"
status = "merged"
branch = "codex/cuda-planner-003-receipts"
pr = 4140
head_sha = "f7dee3f86534d322bd4fb8e92f19d4bc089fab49"
merge_sha = "e2b261c9cb53d0bcbed0abe00ed2018bda1d67aa"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-PLANNER-002"]
acceptance = "Add receipt-ready model-aware planner summaries with separate BitNet QK256 CUDA op counts, dense regular-LLM CUDA op counts, CPU fallback op counts, unsupported op counts, fallback_used, selected route labeling, and strict_cuda_ready without emitting real ask/session/benchmark receipts yet."
commands = [
  "cargo fmt -p bitnet-kernels -- --check",
  "cargo test --locked -p bitnet-kernels --lib --no-default-features dispatch_planner -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/dispatch_planner.rs",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF inference implementation",
  "ask, answer-corpus, warm-session, or benchmark receipt emission changes",
]
may_claim = [
  "The planner can summarize BitNet QK256 CUDA, dense regular-LLM CUDA, CPU fallback, and unsupported op counts for future receipts.",
  "Mixed BitNet and dense CUDA routes are visible and do not produce an unambiguous selected-route claim.",
  "Strict CUDA readiness is true only for non-empty, unambiguous, all-CUDA, fallback-free, supported plans.",
]
must_not_claim = [
  "Real ask, answer-corpus, warm-session, or benchmark receipts emit execution_plan yet.",
  "Any new CUDA kernel exists.",
  "Runtime model loading or tokenizer behavior changed.",
  "General dense GGUF CUDA inference is complete.",
  "CUDA speedup is accepted.",
  "Full CUDA residency for every transformer operation is proven.",
]

[[work_item]]
id = "CUDA-PLANNER-004"
status = "merged"
branch = "codex/cuda-planner-004-execution-plan-receipts"
pr = 4146
head_sha = "75215f07536b2cb0af390469cda735a2666cfa29"
merge_sha = "52165f2dbd67ab7dcc5caf4e7f496b57290253a7"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-PLANNER-003"]
acceptance = "Emit model-aware execution_plan fields in strict ask/run, answer-corpus, warm-session, and benchmark receipts so BitNet QK256 CUDA and dense regular-LLM CUDA routes remain claim-separated in real artifacts."
commands = [
  "cargo fmt -p bitnet-cli -p bitnet-bench-receipts -- --check",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,full-cli planner_receipts -- --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet --no-default-features --features cpu,full-cli strict_cuda_ask_receipt -- --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet --no-default-features --features cpu,full-cli answer_corpus -- --nocapture",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/src/lib.rs",
  "crates/bitnet-cli/src/planner_receipts.rs",
  "crates/bitnet-cli/src/commands/answer_corpus.rs",
  "crates/bitnet-bench-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF inference implementation",
  "server runtime behavior",
]
may_claim = [
  "Strict CUDA ask/run receipts emit execution_plan for the BitNet I2_S/QK256 route.",
  "Answer-corpus, warm-session, and benchmark receipts preserve the BitNet QK256 CUDA route and reject dense regular-LLM CUDA as BitNet proof.",
  "Committed RTX 5070 Ti benchmark receipts carry execution_plan metadata without changing their speedup or residency claim boundaries.",
]
must_not_claim = [
  "Any new CUDA kernel exists.",
  "BitNet QK256 math changed.",
  "Runtime model loading, tokenizer behavior, prompt-template behavior, transformer behavior, or server behavior changed.",
  "General dense GGUF CUDA inference is complete.",
  "CUDA speedup is accepted.",
  "Full CUDA residency for every transformer operation is proven.",
]

[[work_item]]
id = "CUDA-DENSE-005"
status = "merged"
branch = "codex/cuda-dense-005-model-family-planner-bridge"
pr = 4150
head_sha = "0b805c9931aba0d80eef03c5fc3da8aa85a07e26"
merge_sha = "bacc3d753bd6ac385f08b160cf3f253f5deb43ce"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-004", "CUDA-PLANNER-004"]
acceptance = "Bridge dense regular-LLM CUDA fixture receipts to the model-aware execution_plan contract so Qwen-family FP16 dense metadata routes to dense_regular_llm_cuda while remaining rejected as BitNet packed I2S/QK256 proof, with no dense GGUF inference, kernel, speedup, server, tokenizer, loader, or transformer claim."
commands = [
  "cargo fmt -p bitnet-kernels -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda dense_f16_gemm_receipt_contract -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation dense_regular_llm_cuda -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "crates/bitnet-kernels/src/cuda/dense_gemm.rs",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF inference implementation",
]
may_claim = [
  "Dense regular-LLM CUDA fixture receipts now carry a model-aware execution_plan route for qwen/dense_fp16 metadata.",
  "The dense execution_plan route remains claim-separated from BitNet packed I2S/QK256 proof.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Any new CUDA kernel exists.",
  "Runtime model loading, tokenizer behavior, prompt-template behavior, transformer behavior, or server behavior changed.",
  "Full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-006"
status = "merged"
branch = "codex/cuda-dense-006-gguf-descriptors"
pr = 4152
head_sha = "122fddbfd4cd3aba1fbab4f9e3e7d4c029b239e9"
merge_sha = "1afc9a7cbdfb8164333e4829ccb6815556c27094"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-005"]
acceptance = "Add dense GGUF tensor descriptor inspection through the existing GGUF reader for Qwen-family dense tensors, recording required tensor-role coverage and a descriptor-only receipt while rejecting BitNet packed markers and making no dense GGUF inference, CUDA execution, speedup, server, tokenizer, loader, transformer, or full-residency claim."
commands = [
  "cargo fmt -p bitnet-models -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-models --lib --no-default-features dense_gguf_descriptor -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation dense_gguf_descriptor -- --nocapture",
  "cargo test --locked -p bitnet-receipts-core --lib --no-default-features dense_gguf_descriptor -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "crates/bitnet-models/src/dense_gguf_descriptors.rs",
  "crates/bitnet-models/src/lib.rs",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF inference implementation",
  "server runtime behavior",
]
may_claim = [
  "Dense GGUF tensor descriptor inspection can classify Qwen-family tensor roles from GGUF reader metadata.",
  "Descriptor-only Q8_0 dense GGUF evidence records that a future quant bridge is required before strict dense CUDA routing can be claimed.",
  "Descriptor receipts remain rejected as BitNet packed I2S/QK256 proof and as dense CUDA execution proof.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Any new CUDA kernel exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, or server behavior changed.",
  "Dense GGUF CUDA execution ran.",
  "Full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-007"
status = "merged"
branch = "codex/cuda-dense-007-linear-fixture"
pr = 4156
head_sha = "5e5d21f254682a85ef7db1cb8f6e72229d3143e3"
merge_sha = "3462965c543b27fd517daf32c6b65b6ed5a23cf6"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-006"]
acceptance = "Add descriptor-driven dense GGUF linear fixture extraction for Qwen-family dense tensors, materializing one F32/F16/Q8_0 linear tensor as F32, computing a deterministic CPU reference matvec, and validating a fixture-extraction receipt without dense GGUF inference, CUDA execution, speedup, server, tokenizer, transformer, or BitNet packed-kernel proof claims."
commands = [
  "cargo fmt -p bitnet-models -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-models --lib --no-default-features dense_gguf_linear -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation dense_gguf_linear -- --nocapture",
  "cargo test --locked -p bitnet-receipts-core --lib --no-default-features dense_gguf_linear -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "crates/bitnet-models/src/dense_gguf_descriptors.rs",
  "crates/bitnet-models/src/dense_gguf_linear_fixture.rs",
  "crates/bitnet-models/src/lib.rs",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF inference implementation",
  "server runtime behavior",
]
may_claim = [
  "Dense GGUF linear fixture extraction can select a Qwen-family dense linear tensor from GGUF reader data.",
  "F32, F16, and Q8_0 dense linear tensors can be materialized as F32 for deterministic CPU-reference fixture work.",
  "Linear fixture receipts remain rejected as BitNet packed I2S/QK256 proof and as dense CUDA execution proof.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Any new CUDA kernel exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, or server behavior changed.",
  "Dense GGUF CUDA execution ran.",
  "CPU/CUDA dense GGUF parity is proven.",
  "Full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-008"
status = "merged"
branch = "codex/cuda-dense-008-real-linear-parity"
pr = 4168
head_sha = "c4dcb9a6e332f0b369416e541d1ecd9323929791"
merge_sha = "d03c107ecb5fdfda4fce5682ab2737517399cfba"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-007"]
acceptance = "Add a dense GGUF single-linear CPU/CUDA parity bridge that converts descriptor-extracted row-major dense linear fixture data into the existing strict FP16 GEMM CUDA layout, validates a dense GGUF linear CUDA parity receipt contract, and keeps dense GGUF inference, speedup, server, tokenizer, transformer, and BitNet packed-kernel proof claims false."
commands = [
  "cargo fmt -p bitnet-kernels -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda dense_gguf_linear -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation dense_gguf_linear_cuda -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation -- --nocapture",
  "cargo test --locked -p bitnet-receipts-core --lib --no-default-features dense_gguf_linear -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/src/formats/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense GGUF full-model inference implementation",
  "server runtime behavior",
]
may_claim = [
  "Descriptor-extracted dense GGUF single-linear fixture data can be converted into the existing FP16 GEMM CUDA layout.",
  "A dense GGUF linear CUDA parity receipt contract can validate fallback-free single-linear dense CUDA execution against the FP16 CPU bridge.",
  "Dense GGUF linear CUDA parity receipts remain rejected as BitNet packed I2S/QK256 proof.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, or server behavior changed.",
  "A full Qwen token, layer, decode, or chat path works on CUDA.",
  "Full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-STATUS-001"
status = "merged"
branch = "docs/cuda-capability-matrix"
pr = 5158
head_sha = "9f3cc37206c10f9a6db866edf84a78d4551f5417"
merge_sha = "25357a82dcff5f1e7797d84b22a8e0306b7970ea"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Add a user-facing CUDA capability matrix status page for the 9950X3D + RTX 5070 Ti lane, sourced from the model coverage matrix and NVIDIA campaign proof ledger. The page must distinguish official BitNet I2_S/QK256, dense Qwen2.5, Qwen3, SmolLM2, and later dense candidates; preserve speedup_claim=false and server_ready=false where the model coverage row says false; and avoid creating any new model, runtime, CUDA, answer, speed, server, full-residency, or publication claim."
commands = [
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/status/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/hardware/**",
  "ci/model-artifacts/**",
  ".github/workflows/**",
  "README.md",
]
may_claim = [
  "Users have a CUDA capability status page that summarizes current model tiers, routes, speed, server, and claim boundaries from existing proof sources.",
]
must_not_claim = [
  "The status page creates new model, CUDA, answer, speed, server, or residency proof.",
  "Qwen3 product CLI readiness is promoted.",
  "SmolLM2 CPU answer readiness or CUDA planning is unblocked.",
  "Dense regular LLM CUDA proof satisfies BitNet packed I2_S/QK256 proof.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-UX-001"
status = "merged"
branch = "codex/cuda-ux-receipts-explain"
pr = 4185
head_sha = "804ffb6df4728ec3e90196f92129e3f6a07942d0"
merge_sha = "3a421ec7d58e60acf6f08d4020ffacfc42f8edc5"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-PLANNER-004", "CUDA-DENSE-009"]
acceptance = "Add a user-facing `bitnet receipts explain` command that reads existing BitNet and dense CUDA JSON receipts, supports `--latest` under target/bitnet/receipts, and prints or emits a compact proof summary covering artifact kind, claim, model, backend, execution plan route, kernels, quality, timing, residency, and claim limits without changing inference, tokenizer, loader, kernel, transformer, benchmark, or server behavior."
commands = [
  "cargo fmt -p bitnet-cli -- --check",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,full-cli receipts -- --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet --no-default-features --features cpu,full-cli receipts -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/cuda-bitnet-perf-003-warm-session-benchmark.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/dense-f16-gemm-residency.json --json",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "`bitnet receipts explain` can summarize existing BitNet CUDA and dense regular-LLM CUDA receipt proof fields.",
  "`bitnet receipts explain --latest` can locate the newest JSON receipt under the default local receipt directory.",
  "Receipt explanations preserve planner route, fallback, timing, residency, and claim-limit visibility for operator UX.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, or server behavior changed.",
  "A full Qwen token, layer, decode, or chat path works on CUDA.",
  "Full dense regular LLM CUDA residency exists.",
  "Receipt explanation validates every schema-specific receipt rule.",
]

[[work_item]]
id = "CUDA-DENSE-009"
status = "merged"
branch = "codex/cuda-dense-009-extracted-linear-parity"
pr = 4174
head_sha = "07199a18d3d09c884e2443e5b7dfb67ae154e7b8"
merge_sha = "d061e6004ec741abb89b9aa2ff078e4e6f8fb8f7"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-008"]
acceptance = "Add a CLI/operator harness that extracts one dense GGUF single-linear fixture through bitnet-models, converts it to the CUDA-DENSE-008 FP16 GEMM CUDA bridge, emits and validates a dense_gguf_linear_cuda_parity receipt, and keeps dense GGUF inference, Qwen one-token/decode/chat, speedup, server, tokenizer, transformer, BitNet packed proof, and full-residency claims false."
commands = [
  "cargo fmt -p bitnet-cli -- --check",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_linear_parity -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/Cargo.toml",
  "crates/bitnet-cli/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "The CLI can extract a dense GGUF single-linear fixture and route it through the existing dense FP16 GEMM CUDA parity bridge.",
  "The emitted dense_gguf_linear_cuda_parity receipt validates fallback-free single-linear dense CUDA parity while preserving the dense route execution_plan.",
  "Dense GGUF linear CUDA parity receipts remain rejected as BitNet packed I2S/QK256 proof.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, or server behavior changed.",
  "A full Qwen token, layer, decode, or chat path works on CUDA.",
  "Full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-010"
status = "merged"
branch = "codex/cuda-dense-010-live-linear-receipt"
pr = 4207
head_sha = "87c006e483e0b17e6e4d8187b7827933d4d402c9"
merge_sha = "fc38faecfe5ba637da09decfe472160efb51e95b"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-009"]
acceptance = "Record a live RTX 5070 Ti dense GGUF single-linear CUDA parity receipt for the verified Qwen2.5 0.5B Q8_0 artifact using the existing dense-gguf-linear-parity harness, preserving dense_regular_llm_cuda routing, fallback_used=false, BitNet packed QK256 proof false, dense GGUF inference false, speedup_claim=false, and full_cuda_residency_claimed=false."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- model fetch qwen2.5-0.5b-instruct-q8_0",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-linear-parity --model <verified-qwen2.5-q8-gguf> --role attention_q --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-linear-cuda-parity-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-linear-cuda-parity-qwen25-q8.json",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/src/commands/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "A verified Qwen2.5 0.5B Q8_0 dense GGUF attention_q tensor was extracted and run through the existing dense FP16 CUDA bridge on the RTX 5070 Ti.",
  "The live dense_gguf_linear_cuda_parity receipt records dense_regular_llm_cuda routing, fallback_used=false, transfer accounting, and parity pass.",
  "The live dense GGUF linear CUDA parity receipt remains rejected as BitNet packed I2S/QK256 proof.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, or server behavior changed.",
  "A full Qwen token, layer, decode, or chat path works on CUDA.",
  "Full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-011"
status = "merged"
branch = "codex/cuda-dense-011-layer0-linear-sweep"
pr = 4209
head_sha = "1fd8c499805edef444eba732100839401067dddd"
merge_sha = "8516c04df9dc91e8db57eabc9de4452eb4ee2293"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-010"]
acceptance = "Record live RTX 5070 Ti dense GGUF linear role-sweep receipts for the verified Qwen2.5 0.5B Q8_0 artifact using the existing dense-gguf-linear-parity harness, covering attention_q, attention_k, attention_v, attention_output, mlp_gate, mlp_up, mlp_down, and output while preserving dense_regular_llm_cuda routing, fallback_used=false, BitNet packed QK256 proof false, dense GGUF inference false, speedup_claim=false, and full_cuda_residency_claimed=false."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-linear-parity --model <verified-qwen2.5-q8-gguf> --role <role> --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-linear-role-sweep-qwen25-q8\\<role>.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-linear-role-sweep-qwen25-q8\\attention_q.json",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_linear_parity -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/src/commands/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "Verified Qwen2.5 0.5B Q8_0 dense GGUF attention, MLP, and output linear tensors were extracted and run through the existing dense FP16 CUDA bridge on the RTX 5070 Ti.",
  "The role-sweep receipts record dense_regular_llm_cuda routing, fallback_used=false, transfer accounting, and parity pass for every covered role.",
  "The live dense GGUF role-sweep CUDA parity receipts remain rejected as BitNet packed I2S/QK256 proof.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, or server behavior changed.",
  "A full Qwen token, layer, decode, or chat path works on CUDA.",
  "Full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-012"
status = "merged"
pr = 4212
head_sha = "2c1ae45e00d3d3eb966e94d02dd806af901e6ce0"
merge_sha = "27f387608ce983a902a24ee99f1ed6306e281c9a"
branch = "codex/cuda-dense-012-linear-sweep-command"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-011"]
acceptance = "Add an aggregate dense GGUF linear role-sweep command and receipt validator that routes multiple verified Qwen2.5 0.5B Q8_0 dense GGUF linear roles through the existing dense FP16 CUDA bridge in one receipt, recording planner counts, per-role kernel stats, aggregate transfer accounting, fallback_used=false, BitNet packed QK256 proof false, dense GGUF inference false, speedup_claim=false, and full_cuda_residency_claimed=false."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-linear-role-sweep --model <verified-qwen2.5-q8-gguf> --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-linear-role-sweep-aggregate-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-linear-role-sweep-aggregate-qwen25-q8.json",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_linear -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features dense_gguf_linear_role_sweep -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "Verified Qwen2.5 0.5B Q8_0 dense GGUF attention, MLP, and output linear tensors can be extracted and routed through the existing dense FP16 CUDA bridge by one aggregate command.",
  "The aggregate role-sweep receipt records dense_regular_llm_cuda routing, fallback_used=false, transfer accounting, and parity pass for every covered role.",
  "The aggregate dense GGUF role-sweep CUDA parity receipt remains rejected as BitNet packed I2S/QK256 proof.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, or server behavior changed.",
  "A full Qwen token, layer, decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-013"
status = "merged"
branch = "codex/cuda-dense-013-one-layer-plan"
pr = 4214
head_sha = "db0c0aebc5c94556c2ad56c06ad4a09b5466f631"
merge_sha = "42636fc86ad26d96bf15bd4bcc0017c4c9851c72"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-012", "CUDA-PLANNER-004"]
acceptance = "Add a dense GGUF one-layer execution-plan gap receipt for the verified Qwen2.5 0.5B Q8_0 artifact, proving the planner routes layer-0 dense linear ops to dense_regular_llm_cuda while strict CUDA rejects unsupported non-linear layer ops with no CPU fallback, no dense GGUF inference, no Qwen token/decode/chat, no speedup, no full-residency, and no BitNet packed QK256 proof claim."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-one-layer-plan --model <verified-qwen2.5-q8-gguf> --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_one_layer -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features dense_gguf_one_layer -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "A verified Qwen2.5 0.5B Q8_0 dense GGUF first-layer execution plan records dense linear ops as dense_regular_llm_cuda-routable.",
  "The same receipt records strict CUDA unsupported gaps for non-linear layer ops with fallback_used=false and cpu_fallback_ops=0.",
  "The dense one-layer planner gap receipt remains rejected as BitNet packed I2S/QK256 proof.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-051"
status = "merged"
branch = "cuda/dense-qwen25-one-token-proof"
pr = 4645
head_sha = "647dde90da229ab3391cbea91d92f7aa0df51d12"
merge_sha = "77227fbee7dba09457444a7d1ade740d98aa33c5"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-050"]
acceptance = "Refresh or add the real RTX 5070 Ti Qwen2.5 0.5B Q8_0 one-token strict CUDA proof so the receipt records dense_regular_llm_cuda routing, selected RTX 5070 Ti CUDA backend identity, fallback_used=false, CPU/CUDA selected token IDs, kernel and transfer stats, quality gate result, speedup_claim=false, and bitnet_packed_i2s_qk256_proof=false."
commands = [
  "cargo run --locked --release -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-qwen-one-token-strict-cuda --model <qwen2.5-0.5b-q8_0.gguf> --prompt \"What is 2+2? Answer with only the number.\" --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-13\\dense-qwen25-q8-one-token-cuda.json",
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-13\\dense-qwen25-q8-one-token-cuda.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-device-probe/Cargo.toml",
  "crates/bitnet-kernels/Cargo.toml",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
  "ci/model-artifacts/**",
  "README.md",
]
may_claim = [
  "The exact Qwen2.5 0.5B Q8_0 artifact has refreshed one-token strict RTX 5070 Ti CUDA proof after the receipt exists.",
  "The receipt preserves dense_regular_llm_cuda routing, selected RTX 5070 Ti CUDA backend identity, and fallback_used=false.",
  "BitNet CUDA dependencies use target-aware cudarc link modes so Windows CUDA proof builds align with Candle dynamic-linking while non-Windows advisory lanes use fallback dynamic loading and do not require CUDA link libraries.",
]
must_not_claim = [
  "Dense Qwen proof is BitNet packed I2S/QK256 proof.",
  "Dense Qwen speedup is accepted.",
  "Short decode, warm-session chat, or server readiness is proven.",
  "General dense GGUF CUDA inference is complete.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-DENSE-052"
status = "merged"
branch = "cuda/dense-qwen25-short-decode-unblock"
pr = 4695
head_sha = "e13c4d483db9ce797086f34e4e4dbe3011da1799"
merge_sha = "928c72621598be6486bf7614d800aa0268a3962f"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Add an 8-32 token deterministic Qwen2.5 0.5B Q8_0 short-decode strict CUDA receipt with fallback_used=false, stable greedy token sequence, valid UTF-8 answer, no raw special-token garbage, and recorded prefill, KV, logits, sampler, kernel, and transfer evidence. The current-source rerun supersedes the stale-binary diagnostic blocker and records decoded text `The answer is 4. What is` with CPU/CUDA generated-token equality."
commands = [
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-14\\dense-qwen25-q8-short-decode-diagnostic.json",
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-14\\dense-qwen25-q8-short-decode-current-source.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
  "ci/model-artifacts/**",
  "README.md",
]
may_claim = [
  "A current-source Qwen2.5 0.5B Q8_0 short-decode run proves fallback-free RTX 5070 Ti CUDA token parity for the recorded bounded prompt.",
  "CUDA-DENSE-052 is promoted only for the recorded short-decode proof scope and exact Qwen2.5 0.5B Q8_0 artifact.",
]
must_not_claim = [
  "Dense Qwen proof is BitNet packed I2S/QK256 proof.",
  "Broad chat quality, server readiness, or speedup is accepted.",
  "General dense GGUF CUDA inference is complete.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-DENSE-053"
status = "merged"
branch = "cuda/dense-qwen25-warm-session-proof"
pr = 4713
head_sha = "2210a2767797f5e7e3c55e3afe2f7e6c0ff633a8"
merge_sha = "27aab4a5a30187ccaadbe6959dcbd7b90c75a7f6"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Add a Qwen2.5 0.5B Q8_0 warm-session strict CUDA proof showing model_loaded_once=true, tokenizer_loaded_once=true, CUDA context initialized once, intended buffers reused, per-turn receipts plus session summary, fallback_used=false, and full_cuda_residency_claimed=false unless every phase is proven device-resident. The current-source receipt records three turns, 24 total generated tokens, generated-token equality, runtime buffer reuse, upload-once weights, and speedup/full-residency/BitNet proof claims false."
commands = [
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-14\\dense-qwen25-q8-warm-session-current-source.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
  "ci/model-artifacts/**",
  "README.md",
]
may_claim = [
  "A current-source Qwen2.5 0.5B Q8_0 warm-session run proves fallback-free RTX 5070 Ti CUDA token parity for the recorded bounded three-turn session.",
  "The exact Qwen2.5 0.5B Q8_0 artifact has warm-session strict RTX 5070 Ti CUDA proof for the recorded receipt scope.",
]
must_not_claim = [
  "Dense Qwen proof is BitNet packed I2S/QK256 proof.",
  "Full CUDA residency or speedup is accepted without receipt evidence.",
  "Server readiness is proven.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-DENSE-054"
status = "merged"
branch = "cuda/dense-qwen25-benchmark-qualification"
pr = 4720
head_sha = "53c3c475327d6a3eda808afe6a0ddd5e74a93c06"
merge_sha = "a20cfbb78645f4acd0e6b8fd20bc280356daced6"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Add a Qwen2.5 0.5B Q8_0 benchmark qualification review that consumes one-token, short-decode, and warm-session receipts, accepts or rejects speedup by exact profile with reasons, and keeps bitnet_packed_i2s_qk256_proof=false."
commands = [
  "cargo test --locked -p bitnet-bench-receipts --no-default-features",
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\<date>\\dense-qwen25-q8-benchmark-qualification.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-bench-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
  "ci/model-artifacts/**",
  "README.md",
]
may_claim = [
  "Qwen2.5 0.5B Q8_0 dense CUDA speed decisions are profile-specific after the qualification receipt exists.",
]
must_not_claim = [
  "Any global dense CUDA speedup claim.",
  "Dense Qwen proof is BitNet packed I2S/QK256 proof.",
  "Server readiness is proven.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-MODEL-001"
status = "merged"
branch = "model/cuda-qwen3-0_6b-artifact-contract"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-UX-010"]
acceptance = "Add the Qwen3 0.6B artifact contract with source, file identity, SHA256, bytes, GGUF type, architecture, quantization, tokenizer, chat template, context length, license, storage envelope, and VRAM estimate without promoting CPU, CUDA, BitNet, speed, or server claims."
commands = [
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "git diff --check",
]
allowed_paths = [
  "ci/model-artifacts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  ".github/workflows/**",
  "ci/hardware/**",
  "README.md",
]
may_claim = [
  "Qwen3 0.6B has an artifact contract after the model artifact entry and report land.",
]
must_not_claim = [
  "Qwen3 CPU answer readiness, CUDA proof, speedup, or server readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-MODEL-002"
status = "merged"
branch = "model/cuda-qwen3-0_6b-cpu-sanity"
pr = 4866
head_sha = "6aef1aea515437f818541f1b3db8d64c99cbf038"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-001"]
acceptance = "Add Qwen3 0.6B CPU answer sanity proof for math, capital, short summary, basic code, and yes/no prompts using the exact artifact contract, without CUDA or speed claims."
commands = [
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\<date>\\qwen3-0_6b-cpu-answer-corpus.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "git diff --check",
]
allowed_paths = [
  "ci/model-artifacts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "crates/bitnet-cli/src/model_cache.rs",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
  "README.md",
]
may_claim = [
  "Qwen3 0.6B has CPU answer sanity evidence after the exact corpus receipt exists.",
]
must_not_claim = [
  "Qwen3 CUDA proof, speedup, or server readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-MODEL-003"
status = "merged"
branch = "model/cuda-qwen3-0_6b-all-layer-plan"
pr = 4903
head_sha = "30b8929ebefccdfc7274573aa30af11fcabbb2f4"
merge_sha = "23bdbb8af760a2653c153006499724c73722b9ed"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-002"]
acceptance = "Add the Qwen3 0.6B CUDA all-layer plan with routed ops, unsupported ops, model-boundary gaps, and claim boundaries before any one-token CUDA proof."
commands = [
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\<date>\\qwen3-0_6b-cuda-all-layer-plan.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
  "README.md",
]
may_claim = [
  "Qwen3 0.6B has a CUDA all-layer plan after the plan receipt exists.",
]
must_not_claim = [
  "Qwen3 one-token CUDA execution, answer quality, speedup, or server readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-MODEL-004"
status = "merged"
branch = "model/cuda-qwen3-0_6b-one-token-proof"
pr = 4940
head_sha = "8379ab58c9a81288a702790043f8e8139cb155e5"
merge_sha = "098626112588f8877d0f0c53d8a2c5d88ddacb89"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-003"]
acceptance = "Add Qwen3 0.6B one-token strict CUDA proof with verified artifact, tokenizer and prompt authority, CPU/CUDA selected token evidence, fallback_used=false, and no speed claim."
commands = [
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\<date>\\qwen3-0_6b-one-token-cuda.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
  "README.md",
]
may_claim = [
  "Qwen3 0.6B has one-token strict RTX 5070 Ti CUDA proof after the receipt exists.",
]
must_not_claim = [
  "Qwen3 short-decode, warm-session, speedup, or server readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-MODEL-005"
status = "merged"
pr = 4971
head_sha = "b03a3e45b58589b4f2bac677baee51833f9e1e1d"
merge_sha = "605df0c2e44210a6dc6098efe18728e203686592"
branch = "model/cuda-qwen3-0_6b-short-decode"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-004"]
acceptance = "Add Qwen3 0.6B short-decode strict CUDA proof with fallback_used=false, receipt-backed quality boundaries, valid UTF-8 decoded text, CPU/CUDA generated-token equality or explicit first-divergence evidence, and no speed/server/BitNet QK256 claim."
commands = [
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\<date>\\qwen3-0_6b-short-decode-cuda.json",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features qwen3_06b_model_identity",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features qwen_short_decode",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
  "README.md",
]
may_claim = [
  "Qwen3 0.6B has short-decode strict RTX 5070 Ti CUDA proof after the receipt exists.",
]
must_not_claim = [
  "Qwen3 warm-session, speedup, or server readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-MODEL-006"
status = "merged"
branch = "model/cuda-qwen3-0_6b-warm-session"
pr = 4985
head_sha = "2c776bfd3285077348e1df5d3ca5505425cefb17"
merge_sha = "22108dbf9593db391cd4526f826c4514e3b65036"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-005"]
acceptance = "Add Qwen3 0.6B warm-session strict CUDA proof with model/tokenizer/CUDA context loaded once, runtime buffers reused where intended, weights_uploaded_once=true, per_turn_weight_upload=false, fallback_used=false for every turn, and no speed/server/full-residency/BitNet QK256 claim."
commands = [
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\<date>\\qwen3-0_6b-warm-session-cuda.json",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features qwen_warm_session",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
  "README.md",
]
may_claim = [
  "Qwen3 0.6B has bounded warm-session strict RTX 5070 Ti CUDA proof after the receipt exists.",
]
must_not_claim = [
  "Qwen3 speedup or server readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-MODEL-007"
status = "merged"
branch = "model/cuda-qwen3-benchmark-qualification"
pr = 4996
head_sha = "52c8d0b1c9bbe99e0f3700a0eb003bb239829e8b"
merge_sha = "7a4f184ba02766be229104732c525c7ef5d67ffa"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-006"]
acceptance = "Add a governed Qwen3 0.6B benchmark qualification review for one-token, short-decode, and warm-session strict CUDA proof receipts, preserving fallback_used=false evidence while rejecting speedup until repeated same-artifact comparator and profile-threshold evidence exists."
commands = [
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-15\\qwen3-0_6b-benchmark-qualification.json",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features dense_gguf_qwen_benchmark_qualification",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-bench-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "ci/model-artifacts/**",
  ".github/workflows/**",
  "README.md",
]
may_claim = [
  "Qwen3 0.6B has a benchmark qualification review for the current one-token, short-decode, and warm-session strict RTX 5070 Ti CUDA proof receipts.",
  "The Qwen3 benchmark qualification review rejected speedup for every reviewed profile.",
]
must_not_claim = [
  "Qwen3 speedup exists.",
  "Qwen3 benchmark-qualified speed exists.",
  "Qwen3 server readiness exists.",
  "Qwen3 full CUDA residency exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Dense regular LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
]

[[work_item]]
id = "CUDA-MODEL-008"
status = "merged"
branch = "codex/cuda-qwen3-status-sync"
pr = 5007
head_sha = "1185556444aa569016361ca73d8b16710ddc1fab"
merge_sha = "c093870e1ec429615f8a02e783f699fa4ababff7"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-007"]
acceptance = "Sync the Qwen3 0.6B model coverage row and CUDA model-status dashboard to the exact earned accelerator-ready candidate tier after one-token, short-decode, warm-session, and benchmark review receipts landed, while keeping product CLI, speedup, server, full-residency, broad dense GGUF, and BitNet QK256 claims false."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli model_status",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- model status --device nvidia-rtx-5070-ti-cuda",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- model status --device nvidia-rtx-5070-ti-cuda --format json",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/model_cache.rs",
  "ci/model-artifacts/model-coverage-matrix.toml",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "ci/hardware/**",
  ".github/workflows/**",
  "README.md",
]
may_claim = [
  "Qwen3 0.6B is visible in model status as an accelerator-ready dense SLM candidate for the exact RTX 5070 Ti dense_regular_llm_cuda receipt ladder after CUDA-MODEL-007.",
  "The Qwen3 status row records one-token, short-decode, warm-session, and benchmark review evidence while preserving speedup_claim=false.",
]
must_not_claim = [
  "Qwen3 product CLI readiness exists.",
  "Qwen3 speedup exists.",
  "Qwen3 server readiness exists.",
  "Qwen3 full CUDA residency exists.",
  "Qwen3 broad dense GGUF readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Dense regular LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
]

[[work_item]]
id = "CUDA-MODEL-009"
status = "merged"
branch = "cuda-model-009-qwen3-product-ux-audit"
pr = 5507
head_sha = "3411a38c3b21b62fa04f7c435bb5ae382d020da5"
merge_sha = "2f77739bb75e6364f5bbc25e4218a77e7b4814cf"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-008"]
acceptance = "Audit Qwen3 0.6B product UX readiness across model verify, normal ask, chat/warm path, receipt explain, model status, fallback rejection, quality gate, benchmark review, and claim booleans without promoting product CLI readiness."
commands = [
  "git diff --check",
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
]
allowed_paths = [
  "docs/reports/CUDA_MODEL_009_QWEN3_PRODUCT_UX_AUDIT.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "ci/hardware/**",
  ".github/workflows/**",
]
may_claim = [
  "Qwen3 product UX gaps are audited before user-path ask/chat evidence or product CLI review.",
]
must_not_claim = [
  "Qwen3 product CLI readiness exists.",
  "Qwen3 speedup exists.",
  "Qwen3 server readiness exists.",
  "Qwen3 full CUDA residency exists.",
]

[[work_item]]
id = "CUDA-MODEL-010"
status = "merged"
branch = "cuda-model-010-qwen3-ask-user-path-clean"
pr = 5529
head_sha = "0d710cdb5eb600db089158d3738cd8ea957dbabe"
merge_sha = "bbd48fec17afed8d1a1962f9ed3a36bb0413311f"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-009"]
acceptance = "Record Qwen3 0.6B Q8_0 through the normal bitnet ask user path with selected_backend=nvidia-rtx-5070-ti-cuda, selected_route=dense_regular_llm_cuda, fallback_used=false, valid decoded text, quality gate evidence, and no product, server, speedup, full-residency, or BitNet QK256 promotion."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli qwen3_user_path_uses_qwen3_prerequisite_receipts",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli dense_qwen_ask_resolves_qwen3_explicit_model_file",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli receipts_explain_links_qwen3_dense_receipt_to_candidate_coverage",
  "cargo test --locked -p bitnet-receipts-core --no-default-features qwen",
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/src/commands/dense_gguf_linear_parity/**",
  "crates/bitnet-cli/src/commands/receipts.rs",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-18/qwen3-0_6b-ask-user-path-cuda*.json",
  "docs/reports/CUDA_MODEL_010_QWEN3_ASK_USER_PATH_RECEIPT.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
]
may_claim = [
  "Qwen3 0.6B ran through the normal bitnet ask user path on the RTX 5070 Ti dense_regular_llm_cuda route after the user-path receipt exists.",
  "The Qwen3 ask receipt rejects hidden fallback and remains explainable against the Qwen3 candidate coverage row.",
]
must_not_claim = [
  "Qwen3 product CLI readiness exists.",
  "Qwen3 chat readiness exists.",
  "Qwen3 speedup exists.",
  "Qwen3 server readiness exists.",
  "Qwen3 full CUDA residency exists.",
  "Qwen3 broad dense GGUF readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Dense regular LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
]

[[work_item]]
id = "CUDA-MODEL-011"
status = "merged"
branch = "codex/cuda-model-011-qwen3-chat-user-path"
pr = 5645
head_sha = "dcba77e42b0ba670ffb9c5b8b194a57130de33e1"
merge_sha = "5e98c71c14cdf59c6119283d26f11ca03eb13562"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-010"]
acceptance = "Record Qwen3 0.6B Q8_0 through the normal bitnet chat user path with model/tokenizer/CUDA context/weights loaded once across multiple prompts, selected_backend=nvidia-rtx-5070-ti-cuda, selected_route=dense_regular_llm_cuda, fallback_used=false, quality gate evidence, and no product, server, speedup, full-residency, or BitNet QK256 promotion."
commands = [
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features committed_qwen3_chat_user_path_receipts_validate",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli model_status_dashboard_lists_qwen3_as_accelerator_ready_candidate",
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-receipts/tests/cuda_receipt_validation.rs",
  "crates/bitnet-cli/src/model_cache.rs",
  "ci/model-artifacts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-18/qwen3-0_6b-chat-user-path-*",
  "docs/model-artifacts/**",
  "docs/reports/CUDA_MODEL_011_QWEN3_CHAT_USER_PATH_RECEIPT.md",
  "plans/native-rust-inference/dense-qwen3.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
]
may_claim = [
  "Qwen3 0.6B ran through the normal bitnet chat user path on the RTX 5070 Ti dense_regular_llm_cuda route after the user-path receipt exists.",
  "The Qwen3 chat receipt records model_loaded_once=true, tokenizer_loaded_once=true, cuda_context_initialized_once=true, weights_uploaded_once=true, and fallback_used=false.",
]
must_not_claim = [
  "Qwen3 product CLI readiness exists.",
  "Qwen3 speedup exists.",
  "Qwen3 server readiness exists.",
  "Qwen3 full CUDA residency exists.",
  "Qwen3 broad dense GGUF readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Dense regular LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
]

[[work_item]]
id = "CUDA-MODEL-012"
status = "merged"
branch = "cuda-model-012-qwen3-product-cli-review"
pr = 5660
head_sha = "8601a87e20d7842da0064e59638695eb7869987a"
merge_sha = "609a20d79e391ee17b8ee8f8ce36f7e63904c147"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
acceptance = "Review Qwen3 0.6B Q8_0 product CLI promotion after normal ask and chat user-path receipts. If accepted, promote only current_tier/product_cli_ready while keeping server_ready=false, speedup_claim=false, benchmark_qualified=false, full_residency_claim=false, bitnet_packed_i2s_qk256_proof=false, and no Qwen2.5 proof inheritance."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli model_status_dashboard_lists_qwen3_as_product_cli_ready",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli receipts_explain_links_qwen3_dense_receipt_to_product_cli_coverage",
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "git diff --check",
]
allowed_paths = [
  "ci/model-artifacts/**",
  "crates/bitnet-cli/src/model_cache.rs",
  "crates/bitnet-cli/src/commands/receipts.rs",
  "docs/model-artifacts/**",
  "docs/reports/CUDA_MODEL_012_QWEN3_PRODUCT_CLI_PROMOTION_REVIEW.md",
  "plans/native-rust-inference/dense-qwen3.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
]
may_claim = [
  "Qwen3 0.6B is product CLI-ready for bounded normal ask/chat user paths on the RTX 5070 Ti dense_regular_llm_cuda route.",
  "Qwen3 keeps dense_regular_llm_cuda_proof=true and bitnet_packed_i2s_qk256_proof=false.",
]
must_not_claim = [
  "Qwen3 server readiness exists.",
  "Qwen3 speedup exists.",
  "Qwen3 benchmark-qualified speed exists.",
  "Qwen3 full CUDA residency exists.",
  "Qwen3 broad dense GGUF readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Dense regular LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
]

[[work_item]]
id = "CUDA-MODEL-013"
status = "merged"
branch = "codex/cuda-model-013-qwen3-server-smoke-receipt"
pr = 5896
head_sha = "57dd69b55bb076ac80c532cee12ba4f1c7ea83f5"
merge_sha = "996171bf192e10d77bd7dfb84aa1bb37254bc158"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-012"]
acceptance = "Record Qwen3 0.6B Q8_0 through the shared-engine non-streaming /v1/chat/completions server path with selected_backend=nvidia-rtx-5070-ti-cuda, runtime_api=cuda, selected_route=dense_regular_llm_cuda, fallback_used=false, non-empty UTF-8 quality gate evidence, server_smoke_response_claimed=true, server_ready_claimed=false, and no speedup, full-residency, broad dense GGUF, Qwen2.5-inheritance, or BitNet QK256 promotion."
commands = [
  "cargo build --release --locked -p bitnet-server --no-default-features --features cpu,cuda",
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-19\\server-strict-dense-qwen3-q8-smoke.json",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features qwen3_server_shared_engine",
  "cargo test --locked -p bitnet-server --no-default-features --features cpu qwen3",
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-19/server-strict-dense-qwen3-q8-smoke.json",
  "ci/model-artifacts/model-coverage-matrix.toml",
  "docs/model-artifacts/**",
  "docs/reports/CUDA_MODEL_013_QWEN3_SERVER_SMOKE_RECEIPT.md",
  "docs/reports/CUDA_MODEL_014_QWEN3_EXACT_PROFILE_SERVER_READINESS_REVIEW.md",
  "docs/status/**",
  "plans/native-rust-inference/dense-qwen3.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
]
may_claim = [
  "Qwen3 0.6B has bounded non-streaming shared-engine server-smoke evidence for the exact RTX 5070 Ti dense_regular_llm_cuda route after the receipt exists.",
  "The Qwen3 server-smoke receipt rejects hidden fallback and preserves exact model SHA, endpoint profile, generation policy, quality gate, and claim booleans.",
]
must_not_claim = [
  "Qwen3 server readiness exists.",
  "Qwen3 speedup exists.",
  "Qwen3 benchmark-qualified speed exists.",
  "Qwen3 full CUDA residency exists.",
  "Qwen3 broad dense GGUF readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Dense regular LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
]

[[work_item]]
id = "CUDA-MODEL-014"
status = "merged"
branch = "codex/cuda-model-014-qwen3-server-ready-review"
pr = 5866
head_sha = "dd0e5d35555620091939d705004c504d6b87ad09"
merge_sha = "ecae13be5aecdbb2e865dc26e1beec7a8deac019"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Review Qwen3 0.6B Q8_0 exact-profile server readiness and preserve server_ready=false unless a current-source non-streaming /v1/chat/completions RTX 5070 Ti shared-engine receipt exists and is accepted. The merged review rejected readiness because the receipt was missing at review time."
commands = [
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "git diff --check",
]
allowed_paths = [
  "ci/model-artifacts/model-coverage-matrix.toml",
  "docs/model-artifacts/**",
  "docs/reports/CUDA_MODEL_014_QWEN3_EXACT_PROFILE_SERVER_READINESS_REVIEW.md",
  "docs/status/**",
  "plans/native-rust-inference/dense-qwen3.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
]
may_claim = [
  "CUDA-MODEL-014 preserved Qwen3 server_ready=false because no committed server-smoke receipt existed at review time.",
]
must_not_claim = [
  "Qwen3 server readiness exists.",
  "Qwen3 speedup exists.",
  "Qwen3 benchmark-qualified speed exists.",
  "Qwen3 full CUDA residency exists.",
  "Qwen3 broad dense GGUF readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Dense regular LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
]

[[work_item]]
id = "CUDA-MODEL-014B"
status = "merged"
branch = "codex/cuda-model-014-qwen3-server-ready-accept"
pr = 5918
head_sha = "369c8e933ba52d3a576afc7dac89b55261f152e2"
merge_sha = "e525ef3a364b8592bf0de9314139b9629aac7318"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-013"]
acceptance = "Accept Qwen3 0.6B Q8_0 exact-profile server readiness only for the committed current-source non-streaming /v1/chat/completions RTX 5070 Ti shared-engine receipt. Promote server_ready=true in the model coverage row only for that exact profile while preserving speedup_claim=false, benchmark_qualified=false, full_residency_claim=false, broad dense GGUF readiness false, Qwen2.5 inheritance false, and BitNet QK256 proof false."
commands = [
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli model_status_dashboard_lists_qwen3_as_product_cli_ready",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli receipts_explain_links_qwen3_dense_receipt_to_product_cli_coverage",
  "git diff --check",
]
allowed_paths = [
  "ci/model-artifacts/model-coverage-matrix.toml",
  "crates/bitnet-cli/src/model_cache.rs",
  "crates/bitnet-cli/src/commands/receipts.rs",
  "xtask/src/model_coverage.rs",
  "docs/model-artifacts/**",
  "docs/reports/CUDA_MODEL_014_QWEN3_EXACT_PROFILE_SERVER_READINESS_REVIEW.md",
  "docs/reports/CUDA_MODEL_014B_QWEN3_EXACT_PROFILE_SERVER_READY.md",
  "docs/status/**",
  "plans/native-rust-inference/dense-qwen3.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
]
may_claim = [
  "Qwen3 has exact-profile server readiness only for the current-source RTX 5070 Ti non-streaming shared-engine /v1/chat/completions dense_regular_llm_cuda receipt.",
  "The runtime server receipt still records server_ready_claimed=false; model coverage owns the exact-profile product claim.",
]
must_not_claim = [
  "Qwen3 speedup exists.",
  "Qwen3 benchmark-qualified speed exists.",
  "Qwen3 full CUDA residency exists.",
  "Qwen3 broad dense GGUF readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Dense regular LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
]

[[work_item]]
id = "CUDA-MODEL-015"
status = "merged"
branch = "codex/cuda-model-015-qwen3-repeated-comparator-plan"
pr = 5931
head_sha = "ded4f8d55fd3f5e1163f6dd9585c77ac886156a2"
merge_sha = "f0221874619a6bf63f5c213a4fd4bc142dc63e52"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-014B"]
acceptance = "Define and queue the Qwen3 repeated same-artifact CPU/CUDA comparator baseline for one_token, short_decode_8, short_decode_32, warm_session_3_turns, and decode_128_from_warm_context on Windows 9950X3D + RTX 5070 Ti. Record that the existing 2026-05-15 Qwen3 benchmark review is insufficient for speed or benchmark qualification because it has runs_per_backend=1 and repeated_evidence=false. Preserve speedup_claim=false, benchmark_qualified=false, full_residency_claim=false, exact-profile-only server readiness, broad dense GGUF false, Qwen2.5 inheritance false, and BitNet QK256 proof false."
commands = [
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features qwen3",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "plans/native-rust-inference/dense-qwen3.md",
  "docs/reports/CUDA_MODEL_015_QWEN3_REPEATED_COMPARATOR_BASELINE.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "ci/model-artifacts/model-coverage-matrix.toml",
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "crates/bitnet-tokenizers/**",
  ".github/workflows/**",
]
may_claim = [
  "Qwen3 requires repeated same-artifact CPU/CUDA comparator evidence before benchmark-qualified or speedup review.",
  "The committed 2026-05-15 Qwen3 benchmark qualification review remains proof evidence only because runs_per_backend=1 and repeated_evidence=false.",
  "The next Qwen3 performance baseline must preserve fallback_used=false, selected RTX 5070 Ti CUDA backend identity, and dense_regular_llm_cuda route identity.",
]
must_not_claim = [
  "Qwen3 speedup exists.",
  "Qwen3 benchmark-qualified speed exists.",
  "Qwen3 full CUDA residency exists.",
  "Qwen3 broad dense GGUF readiness exists.",
  "Qwen3 server readiness is broader than the exact non-streaming shared-engine profile accepted by CUDA-MODEL-014B.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Dense regular LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
  "Runtime math, tokenizer, loader, kernel, or server behavior changed.",
]

[[work_item]]
id = "CUDA-MODEL-016"
status = "merged"
branch = "codex/cuda-model-016-qwen3-repeated-comparator-receipt"
pr = 5941
head_sha = "b725aecde350a33e40235b81adec2bb9eec78fb8"
merge_sha = "e602f3280b5d3b1109632568299e53ed2cd74407"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-015"]
acceptance = "Add a Qwen3 repeated comparator receipt validator and generator for one_token, short_decode_8, short_decode_32, warm_session_3_turns, and decode_128_from_warm_context on Windows 9950X3D + RTX 5070 Ti. Require at least three same-artifact CPU/CUDA comparator runs per profile, selected nvidia-rtx-5070-ti-cuda backend, dense_regular_llm_cuda route, fallback_used=false, quality/parity pass fields, runtime performance phase fields, transfer byte/timing fields or explicit source labeling, VRAM high-water, and power/thermal context. Preserve speedup_claim=false, benchmark_qualified_speedup=false, full_cuda_residency_claimed=false, broad dense GGUF false, Qwen2.5 inheritance false, and BitNet QK256 proof false."
commands = [
  "cargo fmt -p bitnet-bench-receipts -- --check",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features qwen3",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-bench-receipts/**",
  "docs/reports/CUDA_MODEL_016_QWEN3_REPEATED_COMPARATOR_RECEIPT_CONTRACT.md",
  "plans/native-rust-inference/dense-qwen3.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "ci/model-artifacts/model-coverage-matrix.toml",
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "crates/bitnet-tokenizers/**",
  ".github/workflows/**",
]
may_claim = [
  "The Qwen3 repeated comparator receipt contract and generator exist for the five CUDA-MODEL-015 profiles.",
  "The contract requires repeated fallback-free same-artifact CPU/CUDA comparator evidence before any Qwen3 benchmark-qualified or speedup review.",
  "The generated receipt preserves selected RTX 5070 Ti CUDA backend identity and dense_regular_llm_cuda route identity.",
]
must_not_claim = [
  "Qwen3 speedup exists.",
  "Qwen3 benchmark-qualified speed exists.",
  "Qwen3 full CUDA residency exists.",
  "Qwen3 broad dense GGUF readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Dense regular LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
  "Runtime math, tokenizer, loader, kernel, or server behavior changed.",
]

[[work_item]]
id = "CUDA-MODEL-017A"
status = "merged"
pr = 228
branch = "codex/nvidia-5070ti/CUDA-MODEL-017A-qwen3-capture-tooling"
head_sha = "a84fc6825c16acbbfeaaa452b8108f9aba66736b"
merge_sha = "e41917185cda8d524065c6566a174dc5f02c16e0"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-016"]
acceptance = "Add or update governed Qwen3 source-capture tooling so current source can produce every CUDA-MODEL-017 source receipt profile: one_token, short_decode_8, short_decode_32, warm_session_3_turns, and decode_128_from_warm_context. The tooling must keep product ask/chat max-token bounds unchanged unless a separate product review changes them, must preserve exact Qwen3 0.6B Q8_0 artifact identity, selected nvidia-rtx-5070-ti-cuda backend, dense_regular_llm_cuda route, fallback_used=false, quality/parity fields, timing/transfer/VRAM/power/thermal fields, and must fail closed before emitting receipts that would be ambiguous about warm-context reuse."
commands = [
  "cargo fmt -p bitnet-cli -p bitnet-receipts-core -p bitnet-bench-receipts -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli qwen",
  "cargo test --locked -p bitnet-receipts-core --no-default-features qwen",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features qwen3",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-bench-receipts/**",
  "docs/reports/CUDA_MODEL_017A_QWEN3_CAPTURE_TOOLING_GAP.md",
  "plans/native-rust-inference/dense-qwen3.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "ci/model-artifacts/model-coverage-matrix.toml",
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  ".github/workflows/**",
]
may_claim = [
  "CUDA-MODEL-017A adds the source-capture tooling prerequisite required before CUDA-MODEL-017 hardware receipts can be collected.",
  "Current source can emit and validate unambiguous Qwen3 short_decode_32 and decode_128_from_warm_context source receipts without changing product ask/chat token bounds.",
  "The capture-tooling prerequisite preserves Qwen3-specific dense_regular_llm_cuda proof boundaries.",
]
must_not_claim = [
  "Qwen3 repeated comparator hardware receipts exist.",
  "Qwen3 speedup exists.",
  "Qwen3 benchmark-qualified speed exists.",
  "Qwen3 full CUDA residency exists.",
  "Qwen3 broad dense GGUF readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Dense regular LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
  "Product ask/chat token bounds changed.",
]

[[work_item]]
id = "CUDA-MODEL-017"
status = "merged"
branch = "codex/nvidia-5070ti/CUDA-MODEL-017AA-qwen3-decode128-run03"
pr = 506
head_sha = "73785553f1f0b61963ee522c8a53a287e47cac1e"
merge_sha = "53b908867874a71209f9c54d421a46322af33e2f"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Collect the Qwen3 repeated same-artifact CPU/CUDA hardware comparator source receipts for one_token, short_decode_8, short_decode_32, warm_session_3_turns, and decode_128_from_warm_context on Windows 9950X3D + RTX 5070 Ti, then generate and validate the qwen3_cuda_repeated_comparator aggregate. Each profile must have at least three source receipts for the exact Qwen3 0.6B Q8_0 artifact, selected nvidia-rtx-5070-ti-cuda backend, dense_regular_llm_cuda route, fallback_used=false, quality/parity pass fields, runtime performance phase fields, transfer byte/timing fields or explicit source labeling, VRAM high-water, and power/thermal context. Preserve speedup_claim=false, benchmark_qualified_speedup=false, full_cuda_residency_claimed=false, broad dense GGUF false, Qwen2.5 inheritance false, and BitNet QK256 proof false."
commands = [
  "cargo run --locked -p bitnet-bench-receipts --no-default-features --bin qwen3_cuda_repeated_comparator_receipt -- --print-manifest",
  "cargo run --locked -p bitnet-bench-receipts --no-default-features --bin qwen3_cuda_repeated_comparator_receipt -- --one-token-run <PATH> --short-decode-8-run <PATH> --short-decode-32-run <PATH> --warm-session-3-run <PATH> --decode-128-from-warm-context-run <PATH> --receipt-out ci/hardware/windows-9950x3d-rtx5070ti/<run-date>/qwen3-0_6b-repeated-comparator.json",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features qwen3",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "crates/bitnet-cli/src/commands/dense_gguf_linear_parity/mod.rs",
  "docs/reports/**",
  "plans/native-rust-inference/dense-qwen3.md",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "ci/model-artifacts/model-coverage-matrix.toml",
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  ".github/workflows/**",
]
may_claim = [
  "CUDA-MODEL-017 has the current-source Qwen3 one_token strict CUDA source receipt set complete under ci/hardware/windows-9950x3d-rtx5070ti/2026-05-23/qwen3-perf-017/run-01/ through run-03/.",
  "CUDA-MODEL-017 has the current-source Qwen3 short_decode_8 strict CUDA source receipt set complete under ci/hardware/windows-9950x3d-rtx5070ti/2026-05-23/qwen3-perf-017/run-01/ through run-03/ with full-logits D2H sampling explicitly recorded.",
  "CUDA-MODEL-017 has the current-source Qwen3 short_decode_32 strict CUDA source receipt set complete under ci/hardware/windows-9950x3d-rtx5070ti/2026-05-23/qwen3-perf-017/run-01/ through run-03/ with full-logits D2H sampling explicitly recorded.",
  "CUDA-MODEL-017 has the current-source Qwen3 warm_session_3_turns strict CUDA source receipt set complete under ci/hardware/windows-9950x3d-rtx5070ti/2026-05-23/qwen3-perf-017/run-01/ through run-03/ with full-logits D2H sampling explicitly recorded.",
  "CUDA-MODEL-017 has the current-source Qwen3 decode_128_from_warm_context strict CUDA source receipt set complete under ci/hardware/windows-9950x3d-rtx5070ti/2026-05-23/qwen3-perf-017/run-01/ through run-03/ with full-logits D2H sampling explicitly recorded.",
  "CUDA-MODEL-017 has a validated qwen3_cuda_repeated_comparator aggregate at ci/hardware/windows-9950x3d-rtx5070ti/2026-05-23/qwen3-perf-017/qwen3-0_6b-repeated-comparator.json.",
  "CUDA-MODEL-017A merged the source-capture profile tooling prerequisite.",
  "The qwen3_cuda_repeated_comparator aggregate proves repeated Qwen3 same-artifact CPU/CUDA comparator evidence only, with speedup_claim=false and benchmark_qualified_speedup=false.",
]
must_not_claim = [
  "Qwen3 speedup exists.",
  "Qwen3 benchmark-qualified speed exists.",
  "Qwen3 full CUDA residency exists.",
  "Qwen3 broad dense GGUF readiness exists.",
  "Qwen3 logits transfer reduction exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Dense regular LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
  "Runtime math, tokenizer, loader, kernel, or server behavior changed.",
  "A manifest preflight proves hardware execution.",
]

[[work_item]]
id = "CUDA-MODEL-018"
status = "merged"
branch = "codex/nvidia-5070ti/CUDA-MODEL-018-qwen3-benchmark-review"
pr = 511
head_sha = "9dc8424b3f29bd2dafe7b37c32f2aa13cfb7433f"
merge_sha = "fdcb3643ff9aabd97616cb6e9c76e7206aa14b6a"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-017"]
acceptance = "Review the validated qwen3_cuda_repeated_comparator aggregate from CUDA-MODEL-017 and accept or reject exact-profile Qwen3 benchmark-qualified speed, speedup, and residency claims. The review must name each profile, source aggregate receipt, CPU comparator profile, CUDA profile, sample count, route/backend identity, fallback status, and claim decision. Broad dense GGUF readiness, Qwen2.5 proof inheritance, and BitNet QK256 proof must remain false unless separately proven by their own receipts."
commands = [
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\<run-date>\\qwen3-0_6b-repeated-comparator.json",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features qwen3",
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/reports/**",
  "docs/status/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
  "ci/model-artifacts/model-coverage-matrix.toml",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  ".github/workflows/**",
]
may_claim = [
  "PR #511 reviews the validated qwen3_cuda_repeated_comparator aggregate at ci/hardware/windows-9950x3d-rtx5070ti/2026-05-23/qwen3-perf-017/qwen3-0_6b-repeated-comparator.json.",
  "PR #511 rejects Qwen3 speedup, benchmark-qualified speed, and full-residency promotion for every reviewed exact profile.",
  "Future Qwen3 benchmark or speed decisions remain exact-profile only and must cite a fresh repeated comparator after runtime optimization or sharper transfer/residency measurement.",
]
must_not_claim = [
  "Qwen3 speedup exists.",
  "Qwen3 benchmark-qualified speed exists.",
  "Qwen3 full CUDA residency exists.",
  "Qwen3 broad dense GGUF readiness exists.",
  "Qwen3 proof inherits Qwen2.5 evidence.",
  "Dense regular LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
  "Runtime math, tokenizer, loader, kernel, or server behavior changed.",
  "A blocked review item proves hardware execution.",
]

[[work_item]]
id = "CUDA-MODEL-SMOLLM2-001"
status = "merged"
branch = "model/cuda-smollm2-360m-artifact-contract"
pr = 5029
head_sha = "43eadc606965180e95108b65aa558f9c79431c74"
merge_sha = "2f59e5548c59c5efe9d725c13b1b6ce9980e6d2e"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-MODEL-008"]
acceptance = "Add an exact SmolLM2 360M artifact contract with source, file identity, SHA256, byte size, GGUF metadata, quantization, tokenizer, chat template, context length, license, storage envelope, and VRAM estimate where available, while keeping CPU answer, CUDA, product CLI, speedup, server, full-residency, broad dense GGUF, and BitNet QK256 claims false."
commands = [
  "git diff --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
]
allowed_paths = [
  "ci/model-artifacts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/hardware/**",
  ".github/workflows/**",
  "README.md",
]
may_claim = [
  "SmolLM2 360M artifact identity is recorded after the exact artifact contract lands.",
  "SmolLM2 360M remains a candidate until later CPU, CUDA, benchmark, and product UX proof lands.",
]
must_not_claim = [
  "SmolLM2 CPU answer readiness exists.",
  "SmolLM2 CUDA execution exists.",
  "SmolLM2 product CLI readiness exists.",
  "SmolLM2 speedup exists.",
  "SmolLM2 server readiness exists.",
  "SmolLM2 full CUDA residency exists.",
  "SmolLM2 proof inherits Qwen2.5 or Qwen3 evidence.",
  "Dense regular LLM CUDA proof satisfies BitNet packed I2S/QK256 proof.",
]

[[work_item]]
id = "CUDA-UX-008"
status = "merged"
branch = "cli/cuda-model-support-dashboard"
pr = 4724
head_sha = "200303d764d95d4b85673b8292cba109698f9106"
merge_sha = "2c9daac4d90acb821f81d24e190f9f3a1452aa65"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Add `bitnet model status --device nvidia-rtx-5070-ti-cuda` backed by the model coverage matrix so users can see CPU answer, CUDA answer, benchmark, and server proof states without promoting unsupported claims."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli model_status",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- model status --device nvidia-rtx-5070-ti-cuda",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- model status --device nvidia-rtx-5070-ti-cuda --format json",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/model-artifacts/model-coverage-matrix.toml",
  "docs/status/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
  "README.md",
]
may_claim = [
  "The CLI can summarize current RTX 5070 Ti model support states from the coverage matrix after the command lands.",
]
must_not_claim = [
  "Status output creates new model, CUDA, answer, speed, or server proof.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-UX-009"
status = "merged"
branch = "docs/rtx5070ti-bitnet-cuda-guide"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-PROD-010"]
acceptance = "Add the strict RTX 5070 Ti BitNet CUDA user guide covering prerequisites, model verification, strict ask, warm session, receipts explain, benchmark reporting, and claim boundaries without changing runtime behavior or product claims."
commands = [
  "git diff --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
]
allowed_paths = [
  "docs/tutorials/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  ".github/workflows/**",
  "ci/hardware/**",
  "ci/model-artifacts/**",
  "README.md",
]
may_claim = [
  "Users have a strict RTX 5070 Ti BitNet CUDA guide after the tutorial lands.",
]
must_not_claim = [
  "Broad chat quality, server readiness, global speedup, or dense CUDA proof changed.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-UX-010"
status = "merged"
branch = "docs/9950x3d-5070ti-cuda-quickstart"
pr = 4768
head_sha = "ab36634a5069de04d920c06c1b5f492dea987c40"
merge_sha = "2a5b8dcf020500c58b06eca0fa9fde298f81d5df"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-UX-008", "CUDA-UX-009"]
acceptance = "Add the 9950X3D + RTX 5070 Ti CUDA quickstart with model verify, ask, chat, bench, and receipts explain commands, where each command states what it proves and what it does not prove."
commands = [
  "git diff --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
]
allowed_paths = [
  "docs/tutorials/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  ".github/workflows/**",
  "ci/hardware/**",
  "ci/model-artifacts/**",
  "README.md",
]
may_claim = [
  "Users have a 9950X3D + RTX 5070 Ti CUDA quickstart after the tutorial lands.",
]
must_not_claim = [
  "The quickstart creates new model, CUDA, answer, speed, or server proof.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-SERVER-001"
status = "merged"
branch = "server/strict-cuda-smoke"
pr = 4820
head_sha = "3fd3f392c979ad2128635daac82bd8df1ff3152f"
merge_sha = "599b5595fcb09d2fcb5e082380bc4778a72d47a7"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-UX-008", "CUDA-DENSE-054"]
acceptance = "Classify exact strict RTX 5070 Ti dense Qwen server response receipts with route, model coverage, backend, runtime, quality-gate, and claim booleans while preserving model coverage server=false until a committed hardware server-smoke receipt lands."
commands = [
  "cargo test --locked -p bitnet-server --no-default-features --features cpu server_shared_engine_receipt",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli receipts_explain_links_server_smoke_receipt_to_dense_qwen_coverage",
  "cargo fmt -p bitnet-server -p bitnet-cli -- --check",
  "cargo check --locked -p bitnet-server --no-default-features --features cpu,cuda",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-server/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "ci/model-artifacts/model-coverage-matrix.toml",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  ".github/workflows/**",
  "README.md",
]
may_claim = [
  "Exact strict RTX 5070 Ti dense Qwen server response receipts are classified as dense_regular_llm_cuda and linked to the dense_qwen25_05b_q8_cuda coverage row when generated by the shared engine path.",
]
must_not_claim = [
  "A committed hardware server-smoke receipt exists.",
  "Production server readiness exists.",
  "Model coverage server_ready is promoted.",
  "Server proof creates a global CUDA speedup or answer-quality claim.",
  "Dense Qwen proof is BitNet packed I2S/QK256 proof.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-SERVER-002"
status = "merged"
branch = "server/dense-qwen-strict-smoke-receipt"
pr = 4854
head_sha = "749a648fa9eb17258c4d7350900870b1cab2ec0e"
merge_sha = "ce1773bf63db67b73cc48d82f9f748601a507e18"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-SERVER-001"]
acceptance = "Commit a bounded strict RTX 5070 Ti dense Qwen server-smoke receipt for the exact Qwen2.5 0.5B Q8_0 profile, with selected backend nvidia-rtx-5070-ti-cuda, route dense_regular_llm_cuda, fallback_used=false, non-empty UTF-8 response, quality gate passed, speedup_claim=false, and model coverage server_ready promoted only if the receipt justifies that exact profile."
commands = [
  "cargo test --locked -p bitnet-server --no-default-features --features cpu,cuda",
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\<date>\\server-strict-dense-qwen25-q8-smoke.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\<date>\\server-strict-dense-qwen25-q8-smoke.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-server/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "ci/model-artifacts/model-coverage-matrix.toml",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  ".github/workflows/**",
  "README.md",
]
may_claim = [
  "A bounded dense Qwen strict CUDA server-smoke receipt exists for the exact Qwen2.5 0.5B Q8_0 RTX 5070 Ti profile after the receipt validates.",
]
must_not_claim = [
  "Production server readiness exists.",
  "Global dense GGUF server readiness exists.",
  "Server proof creates a global CUDA speedup or answer-quality claim.",
  "Dense Qwen proof is BitNet packed I2S/QK256 proof.",
  "Official BitNet QK256 server smoke exists.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-SERVER-003"
status = "merged"
branch = "docs/dense-qwen-server-readiness"
pr = 5190
head_sha = "9aa2c0c2cdb1e928d5565a2970f6de04f21e2a8d"
merge_sha = "2d53cec49ff1741e2cf9c390e121aa0b365c5670"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-SERVER-002"]
acceptance = "Audit the bounded dense Qwen strict RTX 5070 Ti server-smoke receipt against BITNET-SPEC-0010 and record that server_ready remains false because the receipt lacks artifact checksum identity, endpoint or request-profile scope, and generation-policy fields."
commands = [
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-15\\server-strict-dense-qwen25-q8-smoke.json",
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- check-file-policy --report-dir target/bitnet/reports --fail-on-error",
  "git diff --check",
]
allowed_paths = [
  "ci/model-artifacts/model-coverage-matrix.toml",
  "docs/model-artifacts/MODEL_COVERAGE_MATRIX.md",
  "docs/reports/CUDA_SERVER_003_DENSE_QWEN_READINESS_AUDIT.md",
  "docs/status/CUDA_CAPABILITY_MATRIX.md",
  "plans/cuda-5070ti-productization/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  ".github/workflows/**",
  "ci/hardware/**",
  "README.md",
]
may_claim = [
  "The bounded dense Qwen server-smoke receipt has been audited against BITNET-SPEC-0010.",
  "The current receipt is useful bounded evidence but is not sufficient for exact-profile server_ready promotion.",
]
must_not_claim = [
  "Production server readiness exists.",
  "Model coverage server_ready is promoted.",
  "A refreshed server receipt with artifact checksum, endpoint/profile, and generation policy exists.",
  "Server proof creates a global CUDA speedup or full-residency claim.",
  "Dense Qwen proof is BitNet packed I2S/QK256 proof.",
]

[[work_item]]
id = "CUDA-SERVER-004"
status = "merged"
branch = "server/dense-qwen-receipt-fields"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-SERVER-003"]
acceptance = "Harden shared-engine dense Qwen server receipts so future strict RTX 5070 Ti server-smoke receipts include artifact checksum identity, endpoint/request-profile scope, generation-policy scope, and claim booleans required by BITNET-SPEC-0010, while leaving server_ready, speedup, full residency, and BitNet QK256 proof false."
commands = [
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features server_shared_engine_chat_completion",
  "cargo test --locked -p bitnet-server --no-default-features --features cpu server_shared_engine_receipt",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,full-cli receipts_explain",
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-server/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/model-artifacts/model-coverage-matrix.toml",
  "docs/specs/BITNET-SPEC-0010-server-readiness-proof-boundary.md",
  "plans/cuda-5070ti-productization/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "ci/hardware/**",
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  ".github/workflows/**",
  "README.md",
]
may_claim = [
  "Future shared-engine server receipts can carry exact artifact checksum, endpoint/request-profile, and generation-policy fields required before dense Qwen server readiness promotion.",
  "Stale dense Qwen server-smoke receipts warn as missing the new exact-profile fields.",
]
must_not_claim = [
  "A refreshed hardware server-smoke receipt has been committed.",
  "Production server readiness exists.",
  "Model coverage server_ready is promoted.",
  "Server proof creates a global CUDA speedup, full residency, or broad chat-quality claim.",
  "Dense Qwen proof is BitNet packed I2S/QK256 proof.",
  "Official BitNet QK256 server smoke exists.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-SERVER-005"
status = "merged"
pr = 5410
head_sha = "746f148ec34fbd90d7e3ca9f7efa51cd93bdd97c"
merge_sha = "7923c9fce06ab14bf5d0da6796f003b38c085755"
branch = "docs/dense-qwen-exact-server-readiness"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-SERVER-004"]
acceptance = "Promote dense Qwen2.5 0.5B Q8_0 server_ready only for the exact bounded RTX 5070 Ti profile after a refreshed or supplemental shared-engine server receipt carries artifact checksum identity, endpoint/request-profile scope, generation policy, route and claim booleans required by BITNET-SPEC-0010, passes the server_shared_engine_chat_completion validator, and keeps speedup, full residency, global dense server readiness, and BitNet QK256 proof false."
commands = [
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-17\\server-strict-dense-qwen25-q8-smoke.json",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features server_shared_engine_chat_completion",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-17\\server-strict-dense-qwen25-q8-smoke.json",
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- check-file-policy --report-dir target/bitnet/reports --fail-on-error",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "ci/model-artifacts/model-coverage-matrix.toml",
  "crates/bitnet-cli/src/model_cache.rs",
  "crates/bitnet-receipts/tests/cuda_receipt_validation.rs",
  "docs/model-artifacts/**",
  "docs/reports/**",
  "docs/specs/BITNET-SPEC-0010-server-readiness-proof-boundary.md",
  "docs/status/**",
  "plans/cuda-5070ti-productization/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
  "docs/tutorials/rtx5070ti-dense-qwen-cuda.md",
  "xtask/src/model_coverage.rs",
]
forbidden_paths = [
  "crates/bitnet-server/src/**",
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  ".github/workflows/**",
  "README.md",
]
may_claim = [
  "Dense Qwen2.5 0.5B Q8_0 server_ready can be true only for the exact validated RTX 5070 Ti shared-engine server profile.",
  "The refreshed or supplemental server receipt satisfies BITNET-SPEC-0010 exact-profile fields and the server_shared_engine_chat_completion validator.",
]
must_not_claim = [
  "Global dense GGUF server readiness exists.",
  "Official BitNet QK256 server readiness exists.",
  "Dense Qwen proof is BitNet packed I2S/QK256 proof.",
  "A global CUDA speedup or full-residency claim exists.",
  "Production deployment, concurrency, or broad chat-quality readiness exists.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-SERVER-006"
status = "merged"
branch = "server/bitnet-qk256-strict-server-smoke"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-SERVER-004"]
acceptance = "Add or promote official BitNet I2_S/QK256 strict RTX 5070 Ti server-smoke evidence separately from dense Qwen, using route bitnet_qk256_cuda, preserving QK256 invocation evidence and zero BitNet linear CPU fallback, keeping dense regular-LLM proof false, and leaving speedup, full residency, and broad production server readiness false unless separately qualified."
commands = [
  "cargo test --locked -p bitnet-server --no-default-features --features cpu,cuda bitnet_qk256_server_smoke",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features bitnet_qk256",
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\<date>\\server-strict-bitnet-qk256-smoke.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\<date>\\server-strict-bitnet-qk256-smoke.json",
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-server/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "ci/model-artifacts/model-coverage-matrix.toml",
  "docs/model-artifacts/**",
  "docs/reports/**",
  "docs/specs/**",
  "docs/status/**",
  "plans/cuda-5070ti-productization/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/qk256_gemv.rs",
  "crates/bitnet-qk256-dispatch/**",
  ".github/workflows/**",
  "README.md",
]
may_claim = [
  "Official BitNet I2_S/QK256 strict server-smoke evidence exists only for the exact validated RTX 5070 Ti profile if the receipt validates.",
  "The BitNet server-smoke receipt is separate from dense regular-LLM CUDA proof.",
]
must_not_claim = [
  "Dense Qwen proof validates official BitNet QK256 server readiness.",
  "Official BitNet QK256 server smoke creates dense regular-LLM CUDA proof.",
  "A global CUDA speedup or full-residency claim exists.",
  "Production deployment, concurrency, or broad chat-quality readiness exists.",
  "Crates.io or docs.rs badges are promoted from pending before publish.",
]

[[work_item]]
id = "CUDA-PROD-008"
status = "merged"
branch = "docs/cuda-5070ti-proof-reconciliation"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Reconcile the 5070 Ti BitNet and dense proof state before runtime changes, including current state, last real receipt, next missing proof, and claim boundaries."
commands = [
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/tracking/campaigns/nvidia-5070ti/CAMPAIGN.md",
  "docs/tracking/campaigns/nvidia-5070ti/active.toml",
  "plans/cuda-5070ti-productization/**",
  "ci/model-artifacts/model-coverage-matrix.toml",
]
forbidden_paths = [
  "crates/**",
  ".github/workflows/**",
  "ci/hardware/**",
  "ci/model-artifacts/artifact-manifest.toml",
  "ci/model-artifacts/tokenizer-authority.toml",
  "docs/tracking/generated/**",
]
may_claim = [
  "The CUDA 5070 Ti productization current-state ledger is reconciled against existing source-of-truth docs.",
  "The next BitNet proof is profile-specific benchmark qualification.",
  "The next dense Qwen proof is a receipt-source audit before further promotion.",
]
must_not_claim = [
  "Runtime behavior changed.",
  "New CUDA execution proof exists.",
  "New model answer readiness exists.",
  "Dense Qwen proof is BitNet proof.",
  "BitNet QK256 proof is dense SLM proof.",
  "CUDA speedup is accepted.",
  "Generated dashboards were hand-edited.",
]

[[work_item]]
id = "CUDA-DENSE-050"
status = "merged"
branch = "docs/cuda-dense-qwen25-product-audit"
pr = 4589
head_sha = "63279b8e8ab60d081970b207d694674c99a3d062"
merge_sha = "77e01cbed226f5b7fec9bc9414b5f0723a64bf63"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-PROD-008"]
acceptance = "Audit Qwen2.5 0.5B Q8_0 dense CUDA receipts to distinguish real hardware/user-path evidence from validators and contracts before new dense runtime work."
commands = [
  "python -m json.tool <each committed dense qwen receipt>",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "git diff --check",
]
allowed_paths = [
  "docs/reports/CUDA_DENSE_QWEN25_Q8_PRODUCT_AUDIT.md",
  "docs/tracking/campaigns/nvidia-5070ti/CAMPAIGN.md",
  "docs/tracking/campaigns/nvidia-5070ti/active.toml",
  "plans/cuda-5070ti-productization/**",
]
forbidden_paths = [
  "crates/**",
  ".github/workflows/**",
  "ci/hardware/**",
  "ci/model-artifacts/**",
  "docs/tracking/generated/**",
]
may_claim = [
  "The dense Qwen current proof state has been audited.",
  "Committed dense Qwen receipts are classified as hardware/user-path evidence, validator evidence, contract evidence, or synthetic fixtures.",
]
must_not_claim = [
  "Runtime behavior changed.",
  "New dense CUDA proof exists.",
  "Dense Qwen proof is BitNet proof.",
  "Dense Qwen speedup is accepted.",
  "Server readiness exists.",
  "Generated dashboards were hand-edited.",
]

[[work_item]]
id = "CUDA-PROD-009"
status = "merged"
branch = "cuda/bitnet-strict-preflight"
pr = 4604
head_sha = "c83cd67e7f8f03a2aef9acfb785f0ef7f420b126"
merge_sha = "2bb701f867771eb31c4333606bfad4157932831c"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-PROD-008"]
acceptance = "Harden strict BitNet CUDA user preflight with `bitnet cuda doctor`, fail-closed strict backend/tokenizer checks before generation, default strict receipt path visibility, and preserved speedup_claim=false."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli cuda_doctor",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli ask_strict",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/tests/snapshots/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  ".github/workflows/**",
  "ci/hardware/**",
  "ci/model-artifacts/**",
]
may_claim = [
  "`bitnet cuda doctor` exists as a strict RTX 5070 Ti CUDA preflight surface.",
  "Strict BitNet CUDA ask rejects generic `cuda` before generation.",
  "Strict BitNet CUDA ask resolves tokenizer authority before generation.",
  "The strict CUDA preflight preserves speedup_claim=false.",
]
must_not_claim = [
  "New CUDA generation proof exists.",
  "QK256 kernels executed in this PR.",
  "BitNet answer quality improved.",
  "Dense SLM CUDA proof changed.",
  "Server readiness exists.",
  "CUDA speedup is accepted.",
]

[[work_item]]
id = "CUDA-PROD-010"
status = "merged"
branch = "cuda/bitnet-i2s-benchmark-qualification"
pr = 4616
merge_sha = "20b0997ea984b65628b77c2d9aeb541a28b7a3de"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-BITNET-PERF-004", "CUDA-UX-006", "CUDA-UX-007"]
acceptance = "Add a governed BitNet I2_S/QK256 CUDA product benchmark qualification receipt and report for the five target user profiles, wire report-only bench output for text/json/csv against that receipt, preserve receipts explain coverage, and keep every profile speedup-unqualified until fresh profile-specific benchmark evidence exists."
commands = [
  "cargo run --locked -p bitnet-bench-receipts --bin strict_cuda_benchmark_qualification_receipt --no-default-features -- --receipt-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-13\\cuda-prod-010-benchmark-qualification.json",
  "python -m json.tool ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-13\\cuda-prod-010-benchmark-qualification.json",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features",
  "cargo test --locked -p bitnet-cli --no-default-features --features full-cli --test cli_arg_tests bench_",
  "cargo test --locked -p bitnet-cli --no-default-features --features full-cli report_only_cuda_benchmark_receipt_skips_startup_backend_selection",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo fmt -p bitnet-cli -p bitnet-bench-receipts -- --check",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- bench --device cuda --cuda-benchmark-receipt ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-13\\cuda-prod-010-benchmark-qualification.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-13\\cuda-prod-010-benchmark-qualification.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-bench-receipts/**",
  "crates/bitnet-cli/src/commands/benchmark.rs",
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/tests/cli_arg_tests.rs",
  "crates/bitnet-device-probe/Cargo.toml",
  "crates/bitnet-kernels/Cargo.toml",
  "ci/hardware/windows-9950x3d-rtx5070ti/2026-05-13/cuda-prod-010-benchmark-qualification.json",
  "docs/reports/CUDA_PROD_010_BENCHMARK_QUALIFICATION.md",
  "docs/tracking/campaigns/nvidia-5070ti/CAMPAIGN.md",
  "docs/tracking/campaigns/nvidia-5070ti/active.toml",
  "docs/tracking/campaigns/nvidia-5070ti/events/2026-05-13T145025Z-CUDA-PROD-010-in-progress.toml",
  "docs/tracking/campaigns/nvidia-5070ti/events/2026-05-13T145726Z-CUDA-PROD-010-pr-open.toml",
]
forbidden_paths = [
  "crates/bitnet-server/**",
  "crates/bitnet-kernels/src/**",
  "crates/bitnet-device-probe/src/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "ci/model-artifacts/**",
  "docs/tracking/generated/**",
  "docs/tracking/campaigns/nvidia-5070ti/generated/**",
  "dense CUDA runtime proof changes",
  "server runtime behavior",
]
may_claim = [
  "A governed BitNet I2_S/QK256 CUDA benchmark qualification review exists for the five target product profiles.",
  "`bitnet bench --device cuda --cuda-benchmark-receipt <path>` can report the committed qualification receipt in text, JSON, and CSV without running a fresh benchmark.",
  "`bitnet receipts explain` can summarize the committed BitNet CUDA benchmark qualification receipt.",
  "No benchmark profile is accepted for a speedup claim.",
]
must_not_claim = [
  "A fresh CUDA benchmark was executed by the report-only bench path.",
  "CUDA speedup is accepted for any profile.",
  "Dense regular LLM CUDA proof changed or proves BitNet packed I2_S/QK256 behavior.",
  "Server readiness exists.",
  "Full CUDA feature validation is green in the local Windows environment.",
]

[[work_item]]
id = "CUDA-DENSE-PERF-001"
status = "merged"
branch = "codex/cuda-dense-perf-001-baseline"
pr = 4437
head_sha = "d11d06c93cba256905694e942715c71ef9d0948e"
merge_sha = "f0874e65c29b6ab22996870848acba303661abac"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-046", "CUDA-UX-003", "CUDA-UX-004"]
acceptance = "Normalize the existing RTX 5070 Ti dense Qwen CUDA one-token, short-decode, and warm-session receipts into a governed benchmark baseline receipt with prerequisite receipt SHA256 hashes, profile timing, kernel, transfer, residency, route, fallback, and claim-boundary fields while preserving speedup_claim=false, benchmark_qualified_speedup=false, full_cuda_residency_claimed=false, server_ready_claimed=false, dense_gguf_inference_claimed=false, and bitnet_packed_i2s_qk256_proof=false."
commands = [
  "cargo run --locked -p bitnet-bench-receipts --bin dense_qwen_cuda_benchmark_baseline_receipt --no-default-features",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features dense_gguf_qwen_cuda_benchmark_baseline -- --nocapture",
  "cargo fmt -p bitnet-bench-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "Cargo.lock",
  "crates/bitnet-bench-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "speedup or full CUDA residency claim upgrades",
]
may_claim = [
  "A dense Qwen CUDA benchmark baseline receipt now aggregates the existing one-token, short-decode, and warm-session RTX 5070 Ti receipts.",
  "The baseline records selected_route=dense_regular_llm_cuda, selected_backend=nvidia-rtx-5070-ti-cuda, fallback_used=false, profile timing, kernel invocation, and transfer byte evidence.",
  "The baseline records prerequisite receipt SHA256 hashes and keeps profile-specific speedup qualification blocked for a later repeated CPU/CUDA comparator.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "benchmark_qualified_speedup is accepted.",
  "Full dense CUDA residency is proven.",
  "Server readiness exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-DENSE-PERF-002"
status = "merged"
branch = "codex/cuda-dense-perf-002-repeated-comparator"
pr = 4440
head_sha = "8d783813b4649eca84794601c62b5534d2848bb2"
merge_sha = "2779dad4468c60451fe03c048f5172992f981994"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-PERF-001"]
acceptance = "Add repeated same-artifact dense Qwen CPU/CUDA comparator receipts after the CUDA-DENSE-PERF-001 baseline, covering one_token, short_decode_8, and warm_session_3_turns profiles with at least three runs per backend where practical, the same verified qwen2.5-0.5b-instruct-q8_0 GGUF SHA, tokenizer authority, prompt template, deterministic generation policy, generated-token comparison or first-divergence report, fallback_used=false for both CPU and CUDA profiles, CPU and CUDA timing splits, CUDA kernel timing, transfer byte evidence and transfer-timing status, VRAM/power/thermal context where available, and speedup_claim=false plus benchmark_qualified_speedup=false pending a later profile-specific qualification review."
commands = [
  "cargo run --locked -p bitnet-bench-receipts --bin dense_qwen_cuda_repeated_comparator_receipt --no-default-features",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features dense_gguf_qwen_repeated_comparator -- --nocapture",
  "cargo fmt -p bitnet-bench-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "Cargo.lock",
  "crates/bitnet-bench-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "speedup or full CUDA residency claim upgrades",
]
may_claim = [
  "Repeated dense Qwen CPU/CUDA comparator evidence exists for the exact profiles recorded by CUDA-DENSE-PERF-001.",
  "The comparator preserves same-artifact, same-tokenizer, same-prompt, deterministic generation, fallback-free CPU and CUDA route evidence.",
  "The comparator keeps dense Qwen speedup qualification blocked for a later profile-specific review.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "benchmark_qualified_speedup is accepted.",
  "Full dense CUDA residency is proven.",
  "Server readiness exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-DENSE-PERF-003"
status = "merged"
branch = "codex/cuda-dense-perf-003-transfer-timing"
pr = 4443
head_sha = "36e32c0ab026d4f8186ee86d82c783a8b8d2bdf3"
merge_sha = "e584b154a5a42f8fe6691cc41d8186c0eb6a5542"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-PERF-002"]
acceptance = "Record measured device-to-host logits download timing in the dense Qwen one-token, short-decode, and warm-session strict CUDA runtime receipts using the verified qwen2.5-0.5b-instruct-q8_0 GGUF artifact on RTX 5070 Ti; keep host-to-device timing explicitly unmeasured with source fields, preserve fallback_used=false, speedup_claim=false, benchmark_qualified_speedup=false, full_cuda_residency_claimed=false, server_ready_claimed=false, dense ask/chat claim boundaries, and bitnet_packed_i2s_qk256_proof=false; add validator coverage rejecting missing transfer timing source fields."
commands = [
  "cargo fmt -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features qwen_ -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-qwen-one-token-strict-cuda --model C:\\Users\\steven\\AppData\\Local\\bitnet-rs\\models\\qwen2.5-0.5b-instruct-q8_0\\qwen2.5-0.5b-instruct-q8_0.gguf --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-10\\dense-qwen-perf-003-transfer-timing\\dense-gguf-qwen-one-token-strict-cuda-qwen25-q8.json",
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-qwen-short-decode-strict-cuda --model C:\\Users\\steven\\AppData\\Local\\bitnet-rs\\models\\qwen2.5-0.5b-instruct-q8_0\\qwen2.5-0.5b-instruct-q8_0.gguf --one-token-proof ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-10\\dense-qwen-perf-003-transfer-timing\\dense-gguf-qwen-one-token-strict-cuda-qwen25-q8.json --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-10\\dense-qwen-perf-003-transfer-timing\\dense-gguf-qwen-short-decode-strict-cuda-qwen25-q8.json",
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-qwen-warm-session-strict-cuda --model C:\\Users\\steven\\AppData\\Local\\bitnet-rs\\models\\qwen2.5-0.5b-instruct-q8_0\\qwen2.5-0.5b-instruct-q8_0.gguf --one-token-proof ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-10\\dense-qwen-perf-003-transfer-timing\\dense-gguf-qwen-one-token-strict-cuda-qwen25-q8.json --short-decode-proof ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-10\\dense-qwen-perf-003-transfer-timing\\dense-gguf-qwen-short-decode-strict-cuda-qwen25-q8.json --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-10\\dense-qwen-perf-003-transfer-timing\\dense-gguf-qwen-warm-session-strict-cuda-qwen25-q8.json",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "speedup or full CUDA residency claim upgrades",
]
may_claim = [
  "Dense Qwen strict CUDA runtime receipts now record measured device-to-host logits download timing.",
  "The one-token, short-decode, and warm-session receipts keep host-to-device timing explicitly unmeasured with source fields.",
  "The receipts preserve same-artifact RTX 5070 Ti dense_regular_llm_cuda routing, fallback_used=false, and blocked speed/full-residency/server/BitNet-proof claims.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "benchmark_qualified_speedup is accepted.",
  "Full dense CUDA residency is proven.",
  "Server readiness exists.",
  "Host-to-device transfer timing is measured.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-DENSE-PERF-004"
status = "merged"
branch = "codex/cuda-dense-perf-004-qualification-review"
pr = 4445
head_sha = "170756479ae24aca517f7d5d5ce9b777de89f367"
merge_sha = "f1818820e3991641f6302534f810a774c98a5035"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-PERF-003"]
acceptance = "Add a governed dense Qwen benchmark qualification review after CUDA-DENSE-PERF-001/002/003, consuming the baseline, repeated CPU/CUDA comparator, and D2H transfer-timing receipts; explicitly keep speedup_claim=false and benchmark_qualified_speedup=false because reviewed CUDA mean total times are slower than CPU means and host-to-device timing remains unmeasured; preserve fallback_used=false, full_cuda_residency_claimed=false, server_ready_claimed=false, dense ask/chat claim boundaries, and bitnet_packed_i2s_qk256_proof=false; add validator coverage rejecting speedup/profile acceptance and missing transfer timing source fields."
commands = [
  "cargo run --locked -p bitnet-bench-receipts --bin dense_qwen_cuda_benchmark_qualification_receipt --no-default-features",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features dense_gguf_qwen_benchmark_qualification -- --nocapture",
  "cargo fmt -p bitnet-bench-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-bench-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "speedup or full CUDA residency claim upgrades",
]
may_claim = [
  "A dense Qwen benchmark qualification review exists for the current baseline, repeated comparator, and D2H transfer-timing receipts.",
  "The reviewed dense Qwen profiles remain same-artifact, deterministic, fallback-free, and generated-token matched.",
  "No dense Qwen profile is speedup-qualified because CUDA means are slower than CPU means and H2D timing remains unmeasured.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "benchmark_qualified_speedup is accepted.",
  "Full dense CUDA residency is proven.",
  "Server readiness exists.",
  "Host-to-device transfer timing is measured.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-DENSE-PERF-005"
status = "merged"
branch = "codex/cuda-dense-perf-005-h2d-envelope"
pr = 4450
head_sha = "03f57197fd0556be5cb9885ecae56a9a101e17cd"
merge_sha = "da1cf17767e7ab6cbf19514ed82d9963daf3a679"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-PERF-004"]
acceptance = "Record a measured host-to-device model-load wall-clock envelope in the dense Qwen one-token, short-decode, and warm-session strict CUDA runtime receipts using the verified qwen2.5-0.5b-instruct-q8_0 GGUF artifact on RTX 5070 Ti; preserve measured device-to-host logits download timing, fallback_used=false, speedup_claim=false, benchmark_qualified_speedup=false, full_cuda_residency_claimed=false, server_ready_claimed=false, dense ask/chat claim boundaries, and bitnet_packed_i2s_qk256_proof=false; clearly label the H2D value as a model-load envelope that includes non-transfer overhead rather than pure CUDA event copy timing; add validator coverage rejecting missing H2D envelope fields and timing/accounting mismatches."
commands = [
  "cargo fmt -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features qwen_ -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-qwen-one-token-strict-cuda --model C:\\Users\\steven\\AppData\\Local\\bitnet-rs\\models\\qwen2.5-0.5b-instruct-q8_0\\qwen2.5-0.5b-instruct-q8_0.gguf --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-10\\dense-qwen-perf-005-h2d-transfer-envelope\\dense-gguf-qwen-one-token-strict-cuda-qwen25-q8.json",
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-qwen-short-decode-strict-cuda --model C:\\Users\\steven\\AppData\\Local\\bitnet-rs\\models\\qwen2.5-0.5b-instruct-q8_0\\qwen2.5-0.5b-instruct-q8_0.gguf --one-token-proof ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-10\\dense-qwen-perf-005-h2d-transfer-envelope\\dense-gguf-qwen-one-token-strict-cuda-qwen25-q8.json --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-10\\dense-qwen-perf-005-h2d-transfer-envelope\\dense-gguf-qwen-short-decode-strict-cuda-qwen25-q8.json",
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-qwen-warm-session-strict-cuda --model C:\\Users\\steven\\AppData\\Local\\bitnet-rs\\models\\qwen2.5-0.5b-instruct-q8_0\\qwen2.5-0.5b-instruct-q8_0.gguf --one-token-proof ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-10\\dense-qwen-perf-005-h2d-transfer-envelope\\dense-gguf-qwen-one-token-strict-cuda-qwen25-q8.json --short-decode-proof ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-10\\dense-qwen-perf-005-h2d-transfer-envelope\\dense-gguf-qwen-short-decode-strict-cuda-qwen25-q8.json --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-10\\dense-qwen-perf-005-h2d-transfer-envelope\\dense-gguf-qwen-warm-session-strict-cuda-qwen25-q8.json",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "speedup or full CUDA residency claim upgrades",
]
may_claim = [
  "Dense Qwen strict CUDA runtime receipts now record a measured host-to-device model-load wall-clock envelope with explicit source, scope, and non-transfer-overhead fields.",
  "The one-token, short-decode, and warm-session receipts preserve measured device-to-host logits download timing.",
  "The receipts preserve same-artifact RTX 5070 Ti dense_regular_llm_cuda routing, fallback_used=false, and blocked speed/full-residency/server/BitNet-proof claims.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "benchmark_qualified_speedup is accepted.",
  "Full dense CUDA residency is proven.",
  "Server readiness exists.",
  "Host-to-device timing is pure CUDA event copy timing.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-DENSE-PERF-006"
status = "merged"
branch = "codex/cuda-dense-perf-006-h2d-envelope-qualification"
pr = 4452
head_sha = "89f441a0385f9ecc9267bb8943c3b405b407e605"
merge_sha = "7ad21d6078f3c8b2c4b30055125ac16142e88c9a"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-PERF-005"]
acceptance = "Refresh dense Qwen benchmark qualification tooling to consume the CUDA-DENSE-PERF-005 H2D model-load envelope receipts for one-token, short-decode, and warm-session profiles; record the envelope timing, source, scope, and non-transfer-overhead flag in profile reviews and evidence summaries; keep pure host-to-device CUDA event copy timing blocked, keep speedup_claim=false and benchmark_qualified_speedup=false, and preserve full-residency/server/BitNet-proof claim boundaries."
commands = [
  "cargo fmt -p bitnet-bench-receipts -- --check",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features dense_gguf_qwen_benchmark_qualification -- --nocapture",
  "cargo run --locked -p bitnet-bench-receipts --bin dense_qwen_cuda_benchmark_qualification_receipt --no-default-features",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-bench-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "speedup or full CUDA residency claim upgrades",
]
may_claim = [
  "Dense Qwen benchmark qualification tooling consumes H2D model-load envelope receipts.",
  "The refreshed qualification receipt records H2D envelope timing with explicit source, scope, and non-transfer-overhead fields.",
  "Pure H2D CUDA event copy timing remains blocked and speedup claims remain false.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "benchmark_qualified_speedup is accepted.",
  "Full dense CUDA residency is proven.",
  "Server readiness exists.",
  "Host-to-device timing is pure CUDA event copy timing.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-DENSE-029"
status = "merged"
branch = "codex/cuda-dense-029-mlp-activation-fixture"
pr = 4303
head_sha = "c685c198160fbad853cf58ac20ba30311627bfce"
merge_sha = "04a1bb3e8b7eaf532b2ac2f7f195b2f3e7fb6d2a"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-028"]
acceptance = "Extract a dense GGUF MLP activation CPU-reference fixture after CUDA-DENSE-028 made attention_v_mix CUDA-routable, record activation hashes, dependency authority for mlp_gate and mlp_up, and the missing CUDA MLP activation kernel gap, while keeping dense GGUF inference, Qwen token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, server, and CUDA kernel math claims false."
commands = [
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli mlp_activation -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features mlp_activation -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-mlp-activation-fixture --model <verified-qwen2.5-q8-gguf> --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-mlp-activation-fixture-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-mlp-activation-fixture-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo fmt -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "A dense GGUF MLP activation CPU-reference fixture exists for the verified Qwen2.5 0.5B Q8_0 artifact.",
  "The fixture records deterministic SiLU(gate) times up activation hashes and source dependency authority for verified MLP gate/up inputs.",
  "The next governed dense CUDA proof is MLP activation kernel parity.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "The mlp_activation gap is CUDA-routable.",
]

[[work_item]]
id = "CUDA-DENSE-030"
status = "merged"
branch = "codex/cuda-dense-030-mlp-activation-parity"
pr = 4310
head_sha = "f845485f394b222a9f183cf3b10138ea172677a6"
merge_sha = "6b8a13cddd47afa844cb7c11ebd3df00fb0263f4"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-029"]
acceptance = "Run the dense GGUF MLP activation fixture through a strict RTX 5070 Ti CUDA F32 SiLU(gate) times up kernel, compare against CPU-reference activation values, record kernel stats, transfer bytes, and tensor residency, and keep dense GGUF inference, Qwen token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, server, and route-promotion claims false."
commands = [
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda mlp_activation -- --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet --no-default-features --features cpu,cuda,full-cli mlp_activation -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features mlp_activation -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-mlp-activation-cuda-parity --model <verified-qwen2.5-q8-gguf> --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-mlp-activation-cuda-parity-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-mlp-activation-cuda-parity-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo fmt -p bitnet-kernels -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/cuda/activations.rs",
  "crates/bitnet-kernels/src/cuda/mod.rs",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "A dense GGUF MLP activation CUDA parity receipt exists for the verified Qwen2.5 0.5B Q8_0 artifact.",
  "The receipt records strict RTX 5070 Ti CUDA execution of SiLU(gate) times up for the extracted activation fixture.",
  "The next governed dense CUDA proof is one-layer route promotion for verified mlp_activation.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "The one-layer planner routes mlp_activation to dense_regular_llm_cuda.",
]

[[work_item]]
id = "CUDA-DENSE-031"
status = "merged"
branch = "codex/cuda-dense-031-mlp-activation-route"
pr = 4314
head_sha = "7330c50cceced8f20a95780cd8ad3b3f7730e813"
merge_sha = "2c634308992ff901458bdb35c24069e8beaa5bc1"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-030"]
acceptance = "Promote verified dense GGUF mlp_activation to the one-layer planner route after CUDA-DENSE-030, refresh the one-layer execution-plan receipt to 14 dense_regular_llm_cuda ops, unsupported_ops=0, strict_cuda_ready=true, and keep dense GGUF inference, Qwen token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, server, and CUDA kernel math claims false."
commands = [
  "cargo test --locked -p bitnet-kernels --lib --no-default-features model_aware_dense_fp16_routes_mlp_activation_to_dense_cuda -- --nocapture",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_one_layer -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features one_layer -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-one-layer-plan --model <verified-qwen2.5-q8-gguf> --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo fmt -p bitnet-kernels -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/dispatch_planner.rs",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "The dense GGUF one-layer planner routes verified mlp_activation to dense_regular_llm_cuda.",
  "The one-layer execution-plan receipt records 14 dense CUDA-routable ops, zero unsupported strict CUDA ops, and strict_cuda_ready=true.",
  "The next governed dense CUDA proof is a full one-layer CPU reference harness.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "A CUDA kernel changed.",
]

[[work_item]]
id = "CUDA-DENSE-032"
status = "merged"
branch = "codex/cuda-dense-032-reference-harness-tracker"
pr = 4316
head_sha = "771c1409d8d414a27a9bb78fb8901cb68ac1e896"
merge_sha = "4aadda0c74cc1fbc13b33ed79608dea2bb77c1a8"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-031"]
acceptance = "Define the full dense GGUF one-layer CPU reference harness contract after CUDA-DENSE-031 made every one-layer op CUDA-routable, covering deterministic Qwen2.5 0.5B Q8_0 input fixture sources, CPU reference op ordering and numeric tolerances, receipt fields, and next CUDA comparison boundaries while keeping CUDA execution, dense GGUF inference, Qwen token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, server, and CUDA kernel math claims false."
commands = [
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/hardware/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
  "new hardware receipt evidence",
]
may_claim = [
  "CUDA-DENSE-032 defines the governed full one-layer CPU reference harness contract for the verified Qwen2.5 0.5B Q8_0 dense GGUF artifact.",
  "The next governed dense CUDA proof can compare the CUDA-routable one-layer plan against that CPU reference harness.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, loader behavior, QK256 math, BitNet CUDA behavior, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "A CPU reference harness implementation or CUDA comparison receipt exists.",
]

[[work_item]]
id = "CUDA-DENSE-033"
status = "merged"
branch = "codex/cuda-dense-032-cpu-reference-harness"
pr = 4318
head_sha = "1f720d90abd7222108929fba3828a4e43a48d626"
merge_sha = "2744cc9b201ca2817a8ba2757b92b95381167064"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-032"]
acceptance = "Implement the full dense GGUF layer-0 CPU reference harness defined by CUDA-DENSE-032, composing the verified Qwen2.5 0.5B Q8_0 dense GGUF layer phases into a deterministic CPU-only pass with per-phase hashes, final layer output hash, and receipt validation while keeping CUDA execution, dense GGUF inference, Qwen token/decode/chat, speedup, persistent/full residency, BitNet packed proof, tokenizer, loader, transformer, QK256, server, and CUDA kernel math claims false."
commands = [
  "cargo fmt -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,full-cli one_layer_cpu_reference -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features one_layer_cpu_reference -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "ci/hardware/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
  "new hardware receipt evidence",
]
may_claim = [
  "CUDA-DENSE-033 implements a governed full layer-0 CPU reference harness for dense GGUF Qwen-family artifacts.",
  "The dense_gguf_one_layer_cpu_reference receipt records deterministic input, per-phase hashes, and final layer output hash for the CPU-only layer pass.",
  "The next governed dense CUDA proof can compare the CUDA-routable one-layer plan against this CPU reference harness.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "CUDA integrated one-layer execution or CUDA comparison receipt exists.",
]

[[work_item]]
id = "CUDA-DENSE-034"
status = "merged"
branch = "codex/cuda-dense-034-integrated-parity-tracker"
pr = 4324
head_sha = "7e8178f75ebc88f645105a8ce078907d40af6dbe"
merge_sha = "bcc57cd0d44d6a65afffd4b1b16afa8dd79beca6"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-033"]
acceptance = "Define the governed integrated dense GGUF one-layer CUDA parity contract after CUDA-DENSE-033, requiring the future implementation to run the full layer-0 CUDA-routable plan against the same Qwen2.5 0.5B Q8_0 CPU reference harness, compare per-phase and final output parity, record per-op kernel stats and aggregate H2D/D2H transfer accounting, and keep dense GGUF inference, Qwen token/decode/chat, speedup, persistent/full residency, BitNet packed proof, tokenizer, loader, transformer, QK256, server, and CUDA kernel math claims false."
commands = [
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/hardware/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
  "new hardware receipt evidence",
]
may_claim = [
  "CUDA-DENSE-034 defines the governed integrated one-layer CUDA parity contract for dense GGUF Qwen-family artifacts.",
  "The future CUDA-DENSE-034 implementation must compare a full CUDA-routable layer pass against the CUDA-DENSE-033 CPU reference harness.",
  "The next implementation is allowed to claim one-layer CUDA parity only after receipt validation proves fallback_used=false and layer output parity.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, or server behavior changed.",
  "A full Qwen token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "CUDA integrated one-layer execution or CUDA comparison receipt exists in this tracker-only PR.",
]

[[work_item]]
id = "CUDA-DENSE-035"
status = "merged"
branch = "codex/cuda-dense-035-one-layer-cuda-parity"
pr = 4328
head_sha = "1a13be90c4b88c5f3f67089f421427d7fcb9e6cd"
merge_sha = "f49bd81e0e230746b783862c0d965bf98d0f569f"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-034"]
acceptance = "Implement the governed integrated dense GGUF one-layer CUDA parity harness defined by CUDA-DENSE-034, composing the verified layer-0 CPU reference phases through the CUDA-routable dense_regular_llm_cuda plan, comparing per-phase and final output parity, recording per-op kernel stats and aggregate H2D/D2H transfer accounting, and keeping dense GGUF inference, Qwen token/decode/chat, speedup, persistent/full residency, BitNet packed proof, tokenizer, loader, transformer, QK256, server, and CUDA kernel math claims false."
commands = [
  "cargo fmt -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_one_layer -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features one_layer -- --nocapture",
  "cargo test --locked -p bitnet-cli --test cli_smoke --no-default-features --features cpu,cuda,full-cli -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-one-layer-cuda-parity --model <verified-qwen2.5-q8-gguf> --layer-index 0 --seq-len 4 --position-offset 1 --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-cuda-integrated-parity-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-cuda-integrated-parity-qwen25-q8.json",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
]
may_claim = [
  "CUDA-DENSE-035 implements a strict integrated dense GGUF one-layer CUDA parity diagnostic harness for the existing governed Qwen-family layer-0 contract.",
  "The diagnostic command emits dense_gguf_one_layer_cuda_integrated_parity receipts and compares CUDA-routed phase outputs against the CUDA-DENSE-033 CPU reference harness.",
  "The receipt validator requires 14 dense_regular_llm_cuda governed ops, explicit host glue for residual phases, fallback_used=false, transfer accounting, per-op kernel stats, and narrow one-layer claim boundaries.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Qwen one-token, short decode, or chat works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
  "A hardware receipt exists unless the real verified Qwen artifact command has been run and committed.",
]

[[work_item]]
id = "CUDA-DENSE-036"
status = "merged"
branch = "codex/cuda-dense-036-all-layer-plan"
pr = 4331
head_sha = "0c909b4b02e7da417258875ffd143859b54a1e6b"
merge_sha = "515502b87ccb47f98011470dc68304c8c874f940"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-035"]
acceptance = "Define the governed dense GGUF all-layer execution-plan receipt contract after CUDA-DENSE-035 proved integrated layer-0 CUDA parity, requiring the future implementation to inspect every Qwen-family transformer layer, report per-layer routed op counts and any graph differences, preserve dense_regular_llm_cuda route separation, explicitly list model-boundary gaps such as token embeddings, final norm, LM head/logits, KV cache, and sampling, and keep dense GGUF inference, Qwen one-token/decode/chat, speedup, persistent/full residency, BitNet packed proof, tokenizer, loader, transformer runtime, QK256, server, and CUDA kernel math claims false."
commands = [
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/hardware/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "dense GGUF model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "new hardware receipt evidence",
]
may_claim = [
  "CUDA-DENSE-036 defines the governed all-layer execution-plan receipt contract for dense GGUF Qwen-family artifacts after integrated layer-0 parity.",
  "The future CUDA-DENSE-036 implementation must prove whether every transformer layer has the same CUDA-routable block plan or list layer-specific differences.",
  "The contract requires model-boundary gaps to be explicit before any Qwen one-token proof can be claimed.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Qwen one-token, short decode, or chat works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
  "All-layer execution-plan receipt implementation or hardware evidence exists in this tracker-only PR.",
]

[[work_item]]
id = "CUDA-DENSE-037"
status = "merged"
branch = "codex/cuda-dense-037-all-layer-plan-impl"
pr = 4333
head_sha = "98bb29445b465631b7a461a1a1291b0cd1fd6b2f"
merge_sha = "c01c82517f32c15a01674d3e453d54d99fe34273"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-036"]
acceptance = "Implement the governed dense GGUF all-layer execution-plan receipt command after CUDA-DENSE-036, inspecting every Qwen-family transformer layer from descriptors, reporting per-layer routed op counts and graph differences, preserving dense_regular_llm_cuda route separation, explicitly listing token embedding, final norm, LM head/logits, KV cache, and sampling model-boundary gaps, and keeping dense GGUF inference, Qwen one-token/decode/chat, speedup, persistent/full residency, BitNet packed proof, tokenizer, loader, transformer runtime, QK256, server, and CUDA kernel math claims false."
commands = [
  "cargo fmt -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_all_layer -- --nocapture",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_one_layer -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features all_layer -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-all-layer-plan --model <verified-qwen2.5-q8-gguf> --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-all-layer-plan-qwen25-q8.json",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "dense GGUF model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "new hardware receipt evidence unless the real verified Qwen artifact command has been run and committed",
]
may_claim = [
  "CUDA-DENSE-037 implements a governed dense GGUF all-layer execution-plan receipt command for Qwen-family transformer blocks.",
  "The dense_gguf_all_layer_execution_plan receipt records per-layer 14-op dense_regular_llm_cuda routing, layer graph matching, explicit model-boundary gaps, and strict transformer-block CUDA readiness.",
  "The validator rejects dense inference, Qwen token/decode/chat, speedup, full residency, and BitNet packed I2S/QK256 proof claims.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Qwen one-token, short decode, or chat works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
  "A hardware receipt exists unless the real verified Qwen artifact command has been run and committed.",
]

[[work_item]]
id = "CUDA-DENSE-038"
status = "merged"
branch = "codex/cuda-dense-038-model-boundary-fixtures"
pr = 4348
head_sha = "8e34d1cf144988fe796aa4b9c2e27d5b1fde0e81"
merge_sha = "74c621586cf430ca0b4686d3e972cc10310d2c2f"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-037"]
acceptance = "Implement governed dense GGUF model-boundary fixture receipts after CUDA-DENSE-037, covering token embedding lookup, final model norm, LM head/output projection, logits shape/hash/top-k diagnostics, and the selected dense_regular_llm_cuda route boundary, while keeping KV cache policy, sampling integration, Qwen one-token/short decode/chat, speedup, persistent/full residency, server readiness, BitNet packed proof, tokenizer behavior, loader behavior, transformer runtime behavior, QK256, BitNet CUDA, and CUDA kernel math claims false."
commands = [
  "cargo fmt -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_model_boundary -- --nocapture",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_all_layer -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features model_boundary -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-model-boundary-fixtures --model <verified-qwen2.5-q8-gguf> --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-model-boundary-fixtures-qwen25-q8.json",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "dense GGUF model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "KV cache policy implementation",
  "sampling implementation",
  "tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "new hardware receipt evidence unless the real verified Qwen artifact command has been run and committed",
]
may_claim = [
  "CUDA-DENSE-038 defines the next governed dense GGUF model-boundary fixture implementation after all transformer blocks have a strict CUDA route plan.",
  "The future dense_gguf_model_boundary_fixtures receipt must cover token embedding lookup, final norm, LM head/output projection, logits shape/hash/top-k diagnostics, and explicit remaining KV/sampling gaps.",
  "The CUDA-DENSE-038 implementation may unblock a later Qwen one-token proof only after receipt validation keeps fallback, inference, speedup, full residency, and BitNet packed proof claims false.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Qwen one-token, short decode, or chat works on CUDA.",
  "KV cache policy or sampling integration is implemented.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
  "A hardware receipt exists unless the real verified Qwen artifact command has been run and committed.",
]

[[work_item]]
id = "CUDA-DENSE-039"
status = "merged"
branch = "codex/cuda-dense-039-kv-policy"
pr = 4357
head_sha = "0f0457e96e508d741ce4b021dddbf68c1c42a2f5"
merge_sha = "aa025b6fc02f46b12a1008f2ec7e554066c716a8"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-038"]
acceptance = "Implement governed dense GGUF KV-cache policy receipts after CUDA-DENSE-038, recording prefill KV write policy, decode KV read/write policy, estimated bytes per token/layer/all layers, planned strict CUDA residency, remaining sampling gap, and claim-boundary rejection of runtime KV allocation, dense GGUF inference, Qwen one-token/short decode/chat, speedup, persistent/full residency, server readiness, BitNet packed proof, tokenizer behavior, loader behavior, transformer runtime behavior, QK256, BitNet CUDA, and CUDA kernel math claims."
commands = [
  "cargo fmt -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_kv_cache -- --nocapture",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_model_boundary -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features kv_cache -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-kv-cache-policy --model <verified-qwen2.5-q8-gguf> --device-index 0 --seq-len 4 --decode-steps 1 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-kv-cache-policy-qwen25-q8.json",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "dense GGUF model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "runtime KV cache allocation or residency implementation",
  "sampling implementation",
  "tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
]
may_claim = [
  "CUDA-DENSE-039 records the governed dense GGUF KV-cache policy needed after model-boundary fixtures and before sampling policy.",
  "The dense_gguf_kv_cache_policy receipt records estimated KV bytes for prefill, decode reads/writes, and max context from the SHA-verified Qwen2.5 0.5B Q8_0 artifact.",
  "KV policy is now explicit, but runtime KV allocation, measured transfer timing, sampling integration, and Qwen one-token proof remain future gates.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Qwen one-token, short decode, or chat works on CUDA.",
  "Runtime KV cache allocation or CUDA residency is implemented.",
  "Sampling integration is implemented.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-DENSE-040"
status = "merged"
branch = "codex/cuda-dense-040-sampling-policy"
pr = 4370
head_sha = "07fcc58d362e3e3109eaaf985b9616b81e362b02"
merge_sha = "5cf420a3e0e3b3abf3df17196fc5b58c5060acde"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-039"]
acceptance = "Implement governed dense GGUF logits-transfer and sampling-policy receipts after CUDA-DENSE-039, recording LM-head logits source/hash/length/top-k evidence, deterministic greedy CPU sampler policy, estimated D2H logits bytes per decode step, remaining model-boundary policy gaps, and claim-boundary rejection of runtime sampling integration, dense GGUF inference, Qwen one-token/short decode/chat, speedup, persistent/full residency, server readiness, BitNet packed proof, tokenizer behavior, loader behavior, transformer runtime behavior, QK256, BitNet CUDA, and CUDA kernel math claims."
commands = [
  "cargo fmt -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_sampling -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features sampling_policy -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-sampling-policy --model <verified-qwen2.5-q8-gguf> --device-index 0 --seq-len 4 --top-k 5 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-sampling-policy-qwen25-q8.json",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "dense GGUF model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "runtime sampling integration",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
]
may_claim = [
  "CUDA-DENSE-040 records the governed dense GGUF logits-transfer and deterministic sampling policy needed after KV cache policy and before Qwen one-token proof.",
  "The dense_gguf_sampling_policy receipt records LM-head fixture logits hash, top-k evidence, greedy CPU sampler settings, and estimated logits D2H bytes from the SHA-verified Qwen2.5 0.5B Q8_0 artifact.",
  "KV and sampling policies are now explicit model-boundary gates, but runtime sampling integration and Qwen one-token proof remain future work.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Qwen one-token, short decode, or chat works on CUDA.",
  "Runtime sampling integration is implemented.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-DENSE-041"
status = "merged"
branch = "codex/cuda-dense-041-one-token"
pr = 4381
head_sha = "1dbeab07d803eabe530336e093147d11963f043e"
merge_sha = "e56fa451bfc955da2fe419c090a796e9cd0aed2c"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-040"]
acceptance = "Define the governed Qwen one-token strict CUDA proof contract after CUDA-DENSE-040, requiring the future implementation to consume the verified Qwen2.5 0.5B Q8_0 artifact, all-layer CUDA route receipt, model-boundary fixtures, KV-cache policy, and sampling-policy receipt; execute exactly one deterministic greedy token through the dense_regular_llm_cuda route with fallback_used=false; compare CPU/CUDA selected token and logits/top-k evidence; record tokenizer and prompt authority, selected backend, kernel/residency/timing evidence, and preserve speedup, short-decode, chat, server, full-residency, BitNet packed proof, QK256, tokenizer behavior, loader behavior, transformer runtime behavior, and CUDA kernel math non-claims."
commands = [
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "runtime dense GGUF inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
]
may_claim = [
  "CUDA-DENSE-041 defines the governed Qwen one-token strict CUDA proof contract after all model-boundary policy gates are explicit.",
  "The future CUDA-DENSE-041 implementation must consume the verified Qwen artifact, all-layer route, model-boundary fixtures, KV-cache policy, and sampling policy before claiming one-token CUDA proof.",
  "The one-token proof is the next dense CUDA gate before short decode, warm session, ask/chat UX, benchmark qualification, or server reuse.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Qwen one-token, short decode, or chat works on CUDA.",
  "Runtime sampling integration, token generation, persistent-session residency, or full dense regular LLM CUDA residency exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
  "A hardware Qwen one-token strict CUDA receipt exists in this tracker-only PR.",
]

[[work_item]]
id = "CUDA-DENSE-042"
status = "merged"
branch = "codex/cuda-dense-042-one-token-impl"
pr = 4392
head_sha = "dfd26a74481023860abea923e28f2ec933b163c0"
merge_sha = "7e9e4d3faf9729befd40c190c98eddbe325970ac"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-041"]
acceptance = "Add the strict dense Qwen one-token CUDA proof receipt validator and synthetic rejection tests required by CUDA-DENSE-041, requiring future receipts to prove one deterministic greedy token through dense_regular_llm_cuda with fallback_used=false, CPU/CUDA selected-token agreement, prerequisite all-layer/model-boundary/KV/sampling receipt hashes, tokenizer/prompt authority, kernel/residency/timing evidence, and claim-boundary rejection of short-decode, chat, speedup, full-residency, server, BitNet packed proof, QK256, tokenizer behavior, loader behavior, transformer runtime behavior, CUDA kernel math, and dense GGUF broad-inference claims."
commands = [
  "cargo fmt -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features qwen_one_token -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features sampling_policy -- --nocapture",
  "cargo check --locked -p bitnet-receipts-core --no-default-features",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "runtime dense GGUF inference implementation",
  "Qwen one-token command or hardware receipt",
  "Qwen short decode or chat implementation",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
]
may_claim = [
  "CUDA-DENSE-042 adds a fail-closed validator for future dense Qwen one-token strict CUDA proof receipts.",
  "The validator requires prerequisite receipt hashes, tokenizer/prompt authority, selected dense CUDA route, CPU/CUDA selected-token agreement, kernel/residency/timing evidence, and fallback_used=false.",
  "Synthetic tests prove sampling-policy-only receipts and forbidden claim upgrades cannot satisfy the one-token proof gate.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "A hardware Qwen one-token strict CUDA receipt exists.",
  "Qwen one-token, short decode, or chat works on CUDA.",
  "Runtime sampling integration, token generation, persistent-session residency, or full dense regular LLM CUDA residency exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-DENSE-043"
status = "merged"
branch = "codex/cuda-dense-043-one-token-runtime"
pr = 4407
head_sha = "b6bf0b652d7e2f80b27cd88f1c482032b206b432"
merge_sha = "fde79efa3608ccaddc4a3a175b942fd918e1175a"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-042"]
acceptance = "Define the governed CUDA-DENSE-044 runtime proof gate after CUDA-DENSE-042, documenting that the future implementation must consume the SHA-verified qwen2.5-0.5b-instruct-q8_0 artifact plus all-layer plan, model-boundary fixture, KV-cache policy, and sampling-policy receipts; execute exactly one deterministic greedy token through dense_regular_llm_cuda on the RTX 5070 Ti with fallback_used=false; compare CPU/CUDA selected token and logits/top-k evidence; emit and validate a dense_gguf_qwen_one_token_strict_cuda_proof hardware receipt; and preserve short-decode, chat, speedup, persistent/full-residency, server, BitNet packed proof, QK256, tokenizer behavior, loader behavior, transformer runtime behavior, and CUDA kernel math non-claims."
commands = [
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "Qwen short decode or chat implementation",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
]
may_claim = [
  "CUDA-DENSE-043 defines the governed runtime gate for the first dense Qwen one-token strict CUDA proof.",
  "The future CUDA-DENSE-044 implementation must prove one deterministic greedy token through dense_regular_llm_cuda on the RTX 5070 Ti with fallback_used=false.",
  "The future one-token proof must link the all-layer plan, model-boundary fixtures, KV-cache policy, and sampling-policy prerequisite receipts.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "A hardware Qwen one-token strict CUDA receipt exists.",
  "Qwen short decode or chat works on CUDA.",
  "Qwen one-token works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "Server readiness exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-DENSE-044"
status = "merged"
branch = "codex/cuda-dense-044-one-token-runtime-impl"
pr = 4418
head_sha = "bd26e1d502c3f6a129fbdae2315646dabcef8397"
merge_sha = "42a3bdc3a1ccc1ea531eb8717d7b26ee0174b6ce"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-043"]
acceptance = "Implement the governed dense Qwen one-token strict CUDA runtime proof defined by CUDA-DENSE-043, consuming the SHA-verified qwen2.5-0.5b-instruct-q8_0 artifact plus all-layer plan, model-boundary fixture, KV-cache policy, and sampling-policy receipts; execute exactly one deterministic greedy token through dense_regular_llm_cuda on the RTX 5070 Ti with fallback_used=false; compare CPU/CUDA selected token and logits/top-k evidence; emit and validate a dense_gguf_qwen_one_token_strict_cuda_proof hardware receipt; and preserve short-decode, chat, speedup, persistent/full-residency, server, BitNet packed proof, QK256, tokenizer behavior, loader behavior, transformer runtime behavior, and CUDA kernel math non-claims."
commands = [
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- model verify qwen2.5-0.5b-instruct-q8_0 --json",
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-qwen-one-token-strict-cuda --model <verified-qwen-gguf> --device-index 0 --json-out ci/hardware/windows-9950x3d-rtx5070ti/2026-05-09/dense-gguf-qwen-one-token-strict-cuda-qwen25-q8.json",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features qwen_one_token -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "Qwen short decode or chat implementation",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
]
may_claim = [
  "CUDA-DENSE-044 records the first governed dense Qwen one-token strict CUDA proof if the hardware receipt validates.",
  "The accepted receipt proves one deterministic greedy token through dense_regular_llm_cuda on the RTX 5070 Ti with fallback_used=false.",
  "The one-token proof links the all-layer plan, model-boundary fixtures, KV-cache policy, and sampling-policy prerequisite receipts.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Qwen short decode or chat works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "Server readiness exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-DENSE-045"
status = "merged"
branch = "codex/cuda-dense-045-short-decode-proof"
pr = 4420
head_sha = "c692a16828d465b9159c4bd20654a6f7b7b55472"
merge_sha = "6fc2fec58642c9da69dde6762a1b0b530810b51a"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-044"]
acceptance = "Extend the governed dense Qwen strict CUDA runtime from the CUDA-DENSE-044 one-token proof to a bounded deterministic short-decode proof, generating 5-16 tokens through dense_regular_llm_cuda on the RTX 5070 Ti with fallback_used=false, recording prompt/tokenizer authority, generated token IDs, CPU/CUDA comparison or first divergence evidence, kernel/residency/timing/transfer summaries, prerequisite receipt hashes, and a validated dense_gguf_qwen_short_decode_strict_cuda_proof receipt while preserving chat, speedup, persistent/full-residency, server, BitNet packed proof, QK256, tokenizer behavior, loader behavior, transformer runtime behavior, and CUDA kernel math non-claims."
commands = [
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- model verify qwen2.5-0.5b-instruct-q8_0 --json",
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-qwen-short-decode-strict-cuda --model <verified-qwen-gguf> --device-index 0 --max-new-tokens 8 --json-out ci/hardware/windows-9950x3d-rtx5070ti/2026-05-09/dense-gguf-qwen-short-decode-strict-cuda-qwen25-q8.json",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features qwen_short_decode -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "Qwen chat implementation",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
]
may_claim = [
  "CUDA-DENSE-045 may record a bounded dense Qwen short-decode strict CUDA proof if the hardware receipt validates.",
  "The accepted receipt may prove 5-16 deterministic greedy tokens through dense_regular_llm_cuda on the RTX 5070 Ti with fallback_used=false.",
  "The short-decode proof remains claim-separated from chat, speedup, full-residency, server readiness, and BitNet packed I2S/QK256 proof.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete beyond the bounded short-decode gate.",
  "Qwen chat works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "Server readiness exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-DENSE-046"
status = "merged"
branch = "codex/cuda-dense-046-warm-session"
pr = 4422
head_sha = "4f0b37c3920940e85ba25df6cc4c3986801e7beb"
merge_sha = "8131952fd82c5dc7e51c3f8d4138078e2f428d53"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-045"]
acceptance = "Extend the governed dense Qwen strict CUDA runtime from the CUDA-DENSE-045 short-decode proof to a warm-session proof, loading the SHA-verified qwen2.5-0.5b-instruct-q8_0 artifact and tokenizer once, initializing CUDA once, reusing dense CUDA weights and governed runtime buffers across multiple deterministic turns, emitting per-turn and session-summary dense_gguf_qwen_warm_session_strict_cuda receipts with fallback_used=false, generated-token evidence, kernel/residency/timing/transfer summaries, prerequisite receipt hashes, and preserving ask/chat UX, speedup, persistent/full-residency beyond the warm-session scope, server, BitNet packed proof, QK256, tokenizer behavior, loader behavior, transformer runtime behavior, and CUDA kernel math non-claims."
commands = [
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- model verify qwen2.5-0.5b-instruct-q8_0 --json",
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-qwen-warm-session-strict-cuda --model <verified-qwen-gguf> --device-index 0 --turns 3 --max-new-tokens 8 --json-out ci/hardware/windows-9950x3d-rtx5070ti/2026-05-09/dense-gguf-qwen-warm-session-strict-cuda-qwen25-q8.json",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features qwen_warm_session -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "Qwen ask/chat UX implementation",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
]
may_claim = [
  "CUDA-DENSE-046 may record a bounded dense Qwen warm-session strict CUDA proof if the hardware receipt validates.",
  "The future receipt may prove multiple deterministic turns through dense_regular_llm_cuda on the RTX 5070 Ti with model/tokenizer load, CUDA initialization, and weight upload reused within the session.",
  "The warm-session proof remains claim-separated from ask/chat UX, speedup, full-residency, server readiness, and BitNet packed I2S/QK256 proof.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete beyond the bounded warm-session gate.",
  "Qwen ask/chat UX works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists beyond the scoped warm-session receipt.",
  "Server readiness exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-DENSE-047"
status = "merged"
pr = 4492
head_sha = "58f1fdedd6018efdc27a7fa854650cecf6c9c25e"
merge_sha = "4b7f1a1baf25df156c164f7d2c961cc1e37a0c0a"
branch = "codex/nvidia-5070ti/CUDA-DENSE-047-qwen15-all-layer-plan"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-046"]
acceptance = "Record the first RTX 5070 Ti strict CUDA route audit for the verified Qwen2.5 1.5B Q4_K_M dense GGUF artifact after CUDA-DENSE-046, proving the artifact fetch and model verify gates pass, the 5070 Ti backend is visible, and the existing dense-gguf-all-layer-plan command fails closed with strict_cuda_ready=false instead of inheriting Qwen2.5 0.5B Q8_0 CUDA proof; update model coverage to require Q4_K_M CUDA route support or an explicit unsupported-route receipt before any larger-Qwen CUDA answer claim."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- model fetch qwen2.5-1.5b-instruct-q4_k_m --json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- model verify qwen2.5-1.5b-instruct-q4_k_m --json",
  "nvidia-smi --query-gpu=name,driver_version,memory.total --format=csv,noheader",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-all-layer-plan --model <verified-qwen2.5-1.5b-q4_k_m-gguf> --device nvidia-rtx-5070-ti-cuda --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-11\\dense-gguf-all-layer-plan-qwen25-15b-q4km.json",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check model-artifacts",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/model-artifacts/model-coverage-matrix.toml",
  "docs/model-artifacts/MODEL_COVERAGE_MATRIX.md",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "runtime behavior",
  "tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "Qwen one-token, short decode, or chat implementation",
  "new CUDA-ready hardware receipt for Qwen2.5 1.5B Q4_K_M",
]
may_claim = [
  "The verified Qwen2.5 1.5B Q4_K_M artifact is cached on the 5070 Ti box and passes model verify by SHA.",
  "The existing strict dense GGUF all-layer CUDA planner fails closed for Qwen2.5 1.5B Q4_K_M with strict_cuda_ready=false.",
  "The larger Qwen row remains CPU-answer-ready only until Q4_K_M CUDA route support or a validated unsupported-route receipt lands.",
]
must_not_claim = [
  "Qwen2.5 1.5B Q4_K_M is dense CUDA answer-ready.",
  "Qwen2.5 1.5B Q4_K_M has a strict CUDA all-layer plan receipt.",
  "Qwen2.5 0.5B Q8_0 CUDA proof applies to Qwen2.5 1.5B Q4_K_M.",
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "CUDA speedup exists.",
  "Full CUDA residency is proven.",
  "Server readiness exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-UX-003"
status = "merged"
branch = "codex/cuda-ux-003-dense-ask"
pr = 4424
head_sha = "fe94761879d48afa26a3d3b149e9ed74b4c0e72d"
merge_sha = "66b36643daf1d46a5587aa2f14e6bfdd833431f1"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-046", "CUDA-UX-002"]
acceptance = "Promote the validated dense Qwen CUDA runtime receipts into the user-facing `bitnet ask --device cuda --model qwen2.5-0.5b-instruct-q8_0` path, resolving the verified artifact/cache entry, selecting dense_regular_llm_cuda, rejecting hidden fallback, producing a compact proof summary and receipt path, and preserving chat UX, speedup, full-residency, server, BitNet packed proof, QK256, tokenizer behavior, loader behavior, transformer runtime behavior, and CUDA kernel math non-claims."
commands = [
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- model verify qwen2.5-0.5b-instruct-q8_0 --json",
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- ask --device cuda --model qwen2.5-0.5b-instruct-q8_0 --max-new-tokens 8 \"What is 2+2?\"",
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- receipts explain --latest",
  "cargo test --locked -p bitnet-cli --bin bitnet --no-default-features --features cpu,cuda,full-cli dense_qwen_ask -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features qwen_ask -- --nocapture",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
]
may_claim = [
  "`bitnet ask --device cuda --model qwen2.5-0.5b-instruct-q8_0` may produce a bounded dense Qwen CUDA answer if the receipt validates.",
  "The ask proof summary may expose the dense_regular_llm_cuda route, backend, fallback=false, generated-token evidence, timing/transfer fields, claim boundary, and receipt path.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete beyond the bounded ask gate.",
  "Qwen chat UX works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists beyond the scoped ask receipt.",
  "Server readiness exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-UX-004"
status = "merged"
branch = "codex/cuda-ux-004-dense-chat"
pr = 4427
head_sha = "2cb7d4aba46968aaad5154fcc9f52939606950d6"
merge_sha = "3082f2905b2162c6dfe7f994195f357984fb1042"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-046", "CUDA-UX-003"]
acceptance = "Promote the validated dense Qwen CUDA warm-session runtime receipts into the user-facing `bitnet chat --device cuda --model qwen2.5-0.5b-instruct-q8_0` path, resolving the verified artifact/cache entry, selecting dense_regular_llm_cuda, accepting 2-4 bounded user turns with 5-16 generated tokens per turn, rejecting hidden fallback, producing assistant responses plus a compact proof summary and receipt path, and preserving speedup, full-residency, server, broad dense GGUF inference, BitNet packed proof, QK256, tokenizer behavior, loader behavior, transformer runtime behavior, and CUDA kernel math non-claims."
commands = [
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- model verify qwen2.5-0.5b-instruct-q8_0 --json",
  "@(\"What is 2+2?\", \"Name the previous answer in words.\") | cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- chat --device cuda --model qwen2.5-0.5b-instruct-q8_0 --max-tokens 8",
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- receipts explain --latest",
  "cargo test --locked -p bitnet-cli --bin bitnet --no-default-features --features cpu,cuda,full-cli dense_qwen_chat -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features qwen_chat -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo fmt -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
]
may_claim = [
  "`bitnet chat --device cuda --model qwen2.5-0.5b-instruct-q8_0` may produce a bounded dense Qwen CUDA chat session if the receipt validates.",
  "The chat proof summary may expose the dense_regular_llm_cuda route, backend, fallback=false, generated-token evidence, timing/transfer fields, claim boundary, and receipt path.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete beyond the bounded chat gate.",
  "Persistent-session or full dense regular LLM CUDA residency exists beyond the scoped warm-session/chat receipt.",
  "Server readiness exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-UX-005"
status = "merged"
branch = "codex/cuda-ux-005-benchmark-fail-closed"
pr = 4448
head_sha = "4f7dfaba5273880fa457c7fbaf46c5cee96f34ef"
merge_sha = "70e4f44ef4c3443fbd481c2c59b7757ad2f91529"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-PERF-004"]
acceptance = "Make the legacy `bitnet bench/benchmark --device cuda` path fail closed instead of silently benchmarking CPU, with help/error text pointing users to receipt-backed CUDA ask/chat and governed benchmark receipts while preserving speedup, full-residency, dense broad-inference, BitNet packed proof, QK256, tokenizer, loader, transformer runtime, CUDA kernel math, and server non-claims."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests --no-default-features --features cpu,full-cli bench_ -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo fmt -p bitnet-cli -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
]
may_claim = [
  "`bitnet bench --device cuda` no longer silently falls back to CPU through the legacy simulated benchmark path.",
  "The legacy benchmark help/error text directs users to receipt-backed CUDA paths for governed CUDA evidence.",
]
must_not_claim = [
  "`bitnet bench --device cuda` runs a receipt-backed CUDA benchmark.",
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete beyond the bounded ask/chat gates.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "Server readiness exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-UX-006"
status = "merged"
branch = "codex/cuda-ux-006-bench-receipt-report"
pr = 4454
head_sha = "f6312b2169a8bd4ed4c8153564547ab2cbea7a14"
merge_sha = "b64737310fed807743e32cd84d37378e5f9473ea"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-UX-005", "CUDA-DENSE-PERF-006"]
acceptance = "Add a governed receipt-reporting path for `bitnet bench --device cuda --cuda-benchmark-receipt <path>` that validates accepted CUDA benchmark qualification receipt kinds, prints text/json/csv benchmark evidence, preserves fail-closed behavior for CUDA bench without a receipt, and keeps speedup, fresh benchmark execution, full-residency, server, BitNet packed proof, QK256, tokenizer, loader, transformer runtime, and CUDA kernel math claims false."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests --no-default-features --features cpu,full-cli bench_ -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo fmt -p bitnet-cli -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
]
may_claim = [
  "`bitnet bench --device cuda --cuda-benchmark-receipt <path>` validates and reports governed CUDA benchmark receipts.",
  "`bitnet bench --device cuda` still fails closed without a governed CUDA benchmark receipt.",
  "The receipt-reporting path can render text, JSON, or CSV summaries from existing CUDA benchmark qualification receipts.",
]
must_not_claim = [
  "`bitnet bench --device cuda` executes a fresh CUDA benchmark.",
  "Dense regular LLM CUDA speedup exists.",
  "Any profile has benchmark-qualified speedup unless the input receipt proves it.",
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Full CUDA residency exists.",
  "Server readiness exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-UX-007"
status = "merged"
branch = "codex/cuda-ux-007-receipts-explain-benchmark"
pr = 4456
head_sha = "df98280d167e1909729309d49e81ee7fc260cf69"
merge_sha = "a36a048af4c677a4b8b2067887f2094f5278d423"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-UX-006", "CUDA-DENSE-PERF-006"]
acceptance = "Extend `bitnet receipts explain` so governed CUDA benchmark qualification receipts show qualification status, accepted/blocked profiles, per-profile CPU/CUDA timing means, transfer timing sources, H2D envelope non-transfer-overhead labeling, and blockers while preserving ask/chat receipt summaries and keeping speedup, fresh benchmark execution, full-residency, server, BitNet packed proof, QK256, tokenizer, loader, transformer runtime, and CUDA kernel math claims false."
commands = [
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,full-cli receipts -- --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet --no-default-features --features cpu,full-cli receipts -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-10\\dense-qwen-perf-006-h2d-envelope-qualification\\dense-gguf-qwen-benchmark-qualification-h2d-envelope.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-10\\dense-qwen-perf-006-h2d-envelope-qualification\\dense-gguf-qwen-benchmark-qualification-h2d-envelope.json --json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo fmt -p bitnet-cli -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "runtime tokenizer behavior changes",
  "model loader behavior changes",
  "transformer runtime behavior changes",
  "server runtime behavior",
]
may_claim = [
  "`bitnet receipts explain` surfaces governed CUDA benchmark qualification decisions and blockers.",
  "Dense Qwen and strict BitNet CUDA benchmark qualification receipts remain speedup-unqualified unless their receipt says otherwise.",
  "Existing ask/chat proof summaries keep their generic receipt explanation fields.",
]
must_not_claim = [
  "`bitnet receipts explain` executes a fresh CUDA benchmark.",
  "`bitnet bench --device cuda` executes a fresh CUDA benchmark.",
  "Dense regular LLM CUDA speedup exists.",
  "Any profile has benchmark-qualified speedup unless the input receipt proves it.",
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Full CUDA residency exists.",
  "Server readiness exists.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, CUDA kernel math, or server behavior changed.",
]

[[work_item]]
id = "CUDA-DENSE-021"
status = "merged"
branch = "codex/cuda-dense-021-attention-score-parity"
pr = 4251
head_sha = "a1ed103a33877f52590ddc795361904aa6d985ad"
merge_sha = "087a32e18a79a4f56a84cc05d6dc43e21a4444b8"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-020"]
acceptance = "Run metadata-derived Qwen2.5 0.5B Q8_0 dense GGUF attention scores through a strict RTX 5070 Ti CUDA F32 kernel, compare against the CPU-reference fixture, record transfer accounting and parity receipt evidence, and keep dense GGUF inference, Qwen token/decode/chat, speedup, persistent-session, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, and server claims false."
commands = [
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda attention_score -- --nocapture",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli attention_score -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features attention_score -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-attention-score-cuda-parity --model <verified-qwen2.5-q8-gguf> --layer-index 0 --seq-len 4 --position-offset 1 --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-attention-score-cuda-parity-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-attention-score-cuda-parity-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo fmt -p bitnet-kernels -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/cuda/attention.rs",
  "crates/bitnet-kernels/src/cuda/mod.rs",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
  "speedup or full CUDA residency claim upgrades",
  "dense planner one-layer route promotion",
]
may_claim = [
  "A metadata-derived Qwen2.5 0.5B Q8_0 dense GGUF attention-score fixture runs through dense_attention_scores_f32_cuda on the RTX 5070 Ti.",
  "The dense_gguf_attention_score_cuda_parity receipt records CPU/CUDA parity, compared score counts, causal mask counts, transfer bytes, selected_backend=nvidia-rtx-5070-ti-cuda, runtime_api=cuda, and fallback_used=false.",
  "The next dense CUDA proof can consider planner routing or the attention softmax/V-mix gap.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "The one-layer planner route has been promoted for attention_scores.",
]

[[work_item]]
id = "CUDA-DENSE-022"
status = "merged"
branch = "codex/cuda-dense-022-attention-score-route"
pr = 4254
head_sha = "0ac991b39c38bfb5facf979e0acb8f3fb56e4c38"
merge_sha = "f1a932da0fc2b9012730837468b7f3c0ed6f1ae4"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-021"]
acceptance = "Update the dense GGUF one-layer planner and gap receipt to mark verified attention_scores as dense_regular_llm_cuda routable after CUDA-DENSE-021, reducing unsupported strict CUDA gaps to attention_softmax, attention_v_mix, and mlp_activation while keeping dense GGUF inference, Qwen one-token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, server, and CUDA kernel math claims false."
commands = [
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda model_aware_dense -- --nocapture",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_one_layer -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features dense_gguf_one_layer -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-one-layer-plan --model <verified-qwen2.5-q8-gguf> --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo fmt -p bitnet-kernels -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/dispatch_planner.rs",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "The dense GGUF one-layer planner now routes verified attention_scores to dense_regular_llm_cuda based on CUDA-DENSE-021 parity evidence.",
  "The one-layer gap receipt records 11 CUDA-routable dense ops, including 7 linears, 2 RMSNorm ops, 1 RoPE op, and 1 attention-score op, with 3 unsupported strict CUDA gaps remaining.",
  "The next governed unsupported one-layer gap is attention_softmax.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-023"
status = "merged"
branch = "codex/cuda-dense-023-attention-softmax-fixture"
pr = 4257
head_sha = "7a73a0bc85de90d1acc3758ff82fbdcdd453e8da"
merge_sha = "306ce613955415ee4e731ba6e17f7243658550c4"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-022"]
acceptance = "Extract a dense GGUF attention-softmax CPU-reference fixture after CUDA-DENSE-022 made attention_scores routable, record probability hashes, causal zero-probability counts, row-sum error, and the missing CUDA softmax kernel gap, while keeping dense GGUF inference, Qwen token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, server, and CUDA kernel math claims false."
commands = [
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli attention_softmax -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features attention_softmax -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-attention-softmax-fixture --model <verified-qwen2.5-q8-gguf> --layer-index 0 --seq-len 4 --position-offset 1 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-attention-softmax-fixture-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-attention-softmax-fixture-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo fmt -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
  "speedup or full CUDA residency claim upgrades",
  "dense planner one-layer route promotion",
]
may_claim = [
  "A metadata-derived Qwen2.5 0.5B Q8_0 dense GGUF attention-softmax CPU-reference fixture exists after attention-score CUDA parity and planner routing.",
  "The dense_gguf_attention_softmax_fixture receipt records attention-score dependency, probability count, causal zero-probability count, row-sum error, probability SHA256, and the missing CUDA attention-softmax kernel gap.",
  "The next dense CUDA proof can implement CUDA attention-softmax kernel parity for this fixture.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "Attention softmax CUDA parity exists.",
  "The one-layer planner route has been promoted for attention_softmax.",
]

[[work_item]]
id = "CUDA-DENSE-024"
status = "merged"
branch = "codex/cuda-dense-024-attention-softmax-parity"
pr = 4264
head_sha = "2993babab551b5054e088d1228fb2e1ce0f70714"
merge_sha = "16abe9af023f444920b983659a5e05db2fceeea3"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-023"]
acceptance = "Run the CUDA-DENSE-023 attention-softmax fixture through a strict RTX 5070 Ti CUDA F32 softmax kernel, prove parity against the CPU-reference probabilities, record kernel/transfer/residency receipt fields, and keep dense GGUF inference, Qwen token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, server, and planner route-promotion claims false."
commands = [
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda attention_softmax -- --nocapture",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli attention_softmax -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features attention_softmax -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-attention-softmax-cuda-parity --model <verified-qwen2.5-q8-gguf> --layer-index 0 --seq-len 4 --position-offset 1 --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-attention-softmax-cuda-parity-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-attention-softmax-cuda-parity-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo fmt -p bitnet-kernels -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/cuda/attention.rs",
  "crates/bitnet-kernels/src/cuda/mod.rs",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
  "speedup or full CUDA residency claim upgrades",
  "dense planner one-layer route promotion",
]
may_claim = [
  "A metadata-derived Qwen2.5 0.5B Q8_0 dense GGUF attention-softmax fixture runs through a strict RTX 5070 Ti CUDA F32 softmax kernel.",
  "The dense_gguf_attention_softmax_cuda_parity receipt records CUDA backend identity, kernel identity, tensor residency, transfer bytes, and no CPU fallback.",
  "CUDA probabilities match the CPU-reference probabilities for the committed fixture within the governed tolerance.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "The one-layer planner route has been promoted for attention_softmax.",
]

[[work_item]]
id = "CUDA-DENSE-025"
status = "merged"
branch = "codex/cuda-dense-025-attention-softmax-route"
pr = 4269
head_sha = "d5903340908b46e1ec4c1648bf285c4cdea1c123"
merge_sha = "7dbffde9ac54cf61eccd4435356b2303eb64bd49"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-024"]
acceptance = "Update the dense GGUF one-layer planner and gap receipt to mark verified attention_softmax as dense_regular_llm_cuda routable after CUDA-DENSE-024, reducing unsupported strict CUDA gaps to attention_v_mix and mlp_activation while keeping dense GGUF inference, Qwen token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, server, and CUDA kernel math claims false."
commands = [
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda model_aware_dense -- --nocapture",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_one_layer -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features dense_gguf_one_layer -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-one-layer-plan --model <verified-qwen2.5-q8-gguf> --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo fmt -p bitnet-kernels -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/dispatch_planner.rs",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
  "speedup or full CUDA residency claim upgrades",
]
may_claim = [
  "The dense GGUF one-layer planner now routes verified attention_softmax to dense_regular_llm_cuda based on CUDA-DENSE-024 parity evidence.",
  "The one-layer gap receipt records 12 CUDA-routable dense ops, including 7 linears, 2 RMSNorm ops, 1 RoPE op, 1 attention-score op, and 1 attention-softmax op, with 2 unsupported strict CUDA gaps remaining.",
  "The next governed unsupported one-layer gap is attention_v_mix.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "Attention V-mix CUDA parity exists.",
]

[[work_item]]
id = "CUDA-DENSE-026"
status = "merged"
branch = "codex/cuda-dense-026-attention-v-mix-fixture"
pr = 4281
head_sha = "b2ce397304f69a98b27b45cbf6b77f95600541b2"
merge_sha = "2bcc96656dcc486a6851f00970a40038ecbab113"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-025"]
acceptance = "Extract a dense GGUF attention V-mix CPU-reference fixture after CUDA-DENSE-025 made attention_softmax CUDA-routable, record context-vector hashes, dependency authority for attention_softmax and attention_v, and the missing CUDA V-mix kernel gap, while keeping dense GGUF inference, Qwen token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, server, and CUDA kernel math claims false."
commands = [
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli attention_v_mix -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features attention_v_mix -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-attention-v-mix-fixture --model <verified-qwen2.5-q8-gguf> --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-attention-v-mix-fixture-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-attention-v-mix-fixture-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo fmt -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-kernels/src/dispatch_planner.rs",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
  "speedup or full CUDA residency claim upgrades",
]
may_claim = [
  "A dense GGUF attention V-mix CPU-reference fixture exists for the verified Qwen2.5 0.5B Q8_0 artifact.",
  "The fixture records deterministic context-vector hashes and dependency authority for verified attention-softmax and attention-V inputs.",
  "The next governed dense CUDA proof is attention V-mix kernel parity.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "Attention V-mix CUDA parity exists.",
]

[[work_item]]
id = "CUDA-DENSE-027"
status = "merged"
branch = "codex/cuda-dense-027-attention-v-mix-parity"
pr = 4290
head_sha = "11a71d798ba54000ff7ab7755e9900eaa29a6658"
merge_sha = "c00a8873c862fa86b1fb2c870038353d98ede15a"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-026"]
acceptance = "Run the CUDA-DENSE-026 attention V-mix fixture through a strict RTX 5070 Ti CUDA F32 V-mix kernel, prove parity against the CPU-reference context vectors, record kernel/transfer/residency receipt fields, and keep dense GGUF inference, Qwen token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, server, and planner route-promotion claims false."
commands = [
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda attention_v_mix -- --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet --no-default-features --features cpu,cuda,full-cli attention_v_mix -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features attention_v_mix -- --nocapture",
  "$env:PATH = 'C:\\Program Files\\NVIDIA GPU Computing Toolkit\\CUDA\\v12.9\\bin;' + $env:PATH; cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-attention-v-mix-cuda-parity --model <verified-qwen2.5-q8-gguf> --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-attention-v-mix-cuda-parity-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-attention-v-mix-cuda-parity-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo fmt -p bitnet-kernels -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/dispatch_planner.rs",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
  "speedup or full CUDA residency claim upgrades",
  "planner route-promotion claim upgrades",
]
may_claim = [
  "The dense GGUF attention V-mix fixture runs through a strict RTX 5070 Ti CUDA F32 V-mix kernel.",
  "The CUDA V-mix context output matches the CPU reference within the governed fixture tolerance.",
  "The receipt records kernel invocation, fallback, transfer-byte, and single-fixture tensor residency fields for attention V-mix.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "The one-layer planner route has been promoted.",
]

[[work_item]]
id = "CUDA-DENSE-028"
status = "merged"
branch = "codex/cuda-dense-028-attention-v-mix-route"
pr = 4296
head_sha = "48aeb3e2949d4bb83e67bf8a8441264ed4cb193f"
merge_sha = "e8af5be9307de2192943058a50b7b668cd30f2c3"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-027"]
acceptance = "Update the dense GGUF one-layer planner and gap receipt to mark verified attention_v_mix as dense_regular_llm_cuda routable after CUDA-DENSE-027, reducing unsupported strict CUDA gaps to mlp_activation while keeping dense GGUF inference, Qwen token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, server, and CUDA kernel math claims false."
commands = [
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda attention_v_mix -- --nocapture",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_one_layer -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features one_layer -- --nocapture",
  "$env:PATH = 'C:\\Program Files\\NVIDIA GPU Computing Toolkit\\CUDA\\v12.9\\bin;' + $env:PATH; cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-one-layer-plan --model <verified-qwen2.5-q8-gguf> --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo fmt -p bitnet-kernels -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/dispatch_planner.rs",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA kernel math changes",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
  "speedup or full CUDA residency claim upgrades",
]
may_claim = [
  "The dense GGUF one-layer planner routes verified attention_v_mix to dense_regular_llm_cuda after CUDA-DENSE-027 parity evidence.",
  "The refreshed one-layer receipt records 13 CUDA-routable dense ops, including attention_v_mix, with mlp_activation as the remaining unsupported strict CUDA gap.",
  "Strict CUDA still rejects CPU fallback and does not claim dense GGUF inference.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, BitNet CUDA behavior, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
  "The remaining mlp_activation gap is CUDA-routable.",
]

[[work_item]]
id = "CUDA-DENSE-015"
status = "merged"
branch = "codex/cuda-dense-015-norm-fixture-audit"
pr = 4218
head_sha = "6f446f7cec87eab569d0518ea47380cb7fb73555"
merge_sha = "e4f0e605d49645d69e09211887de8796ea0bd9d8"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-014"]
acceptance = "Extract dense GGUF RMSNorm fixtures for attention_norm and ffn_norm from the verified Qwen2.5 0.5B Q8_0 artifact, compute deterministic CPU RMSNorm reference outputs, validate a dense_gguf_norm_fixture_extraction receipt, and record the current CUDA norm gap as missing_cuda_kernel while keeping dense GGUF inference, Qwen token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, kernel, and server claims false."
commands = [
  "cargo test --locked -p bitnet-models --lib --no-default-features dense_gguf_norm -- --nocapture",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,full-cli dense_gguf_norm -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features dense_gguf_norm -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- dense-gguf-norm-fixture --model <verified-qwen2.5-q8-gguf> --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-norm-fixture-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-norm-fixture-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "Verified Qwen2.5 0.5B Q8_0 dense GGUF attention_norm and ffn_norm tensors can be extracted.",
  "The extracted norm tensors can be materialized as F32 and evaluated by a deterministic CPU RMSNorm reference.",
  "The dense norm fixture receipt records the next CUDA gap as missing_cuda_kernel.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Dense CUDA RMSNorm parity exists.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-016"
status = "merged"
branch = "codex/cuda-dense-016-rmsnorm-parity"
pr = 4220
head_sha = "5263370e2eb1859efb37025acd3511b43625e3ca"
merge_sha = "a0773640130f2f88d8d35f0ec8267348e600d169"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-015"]
acceptance = "Add strict RTX 5070 Ti CUDA parity for dense GGUF RMSNorm fixtures by routing the verified Qwen2.5 0.5B Q8_0 attention_norm and ffn_norm CPU reference fixtures through a dense_rmsnorm_f32_cuda kernel, validating a dense_gguf_norm_cuda_parity receipt, and keeping dense GGUF inference, Qwen one-token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, and server claims false."
commands = [
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda rmsnorm -- --nocapture",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_norm -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features dense_gguf_norm -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-norm-cuda-parity --model <verified-qwen2.5-q8-gguf> --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-rmsnorm-cuda-parity-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-rmsnorm-cuda-parity-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "Verified Qwen2.5 0.5B Q8_0 dense GGUF attention_norm and ffn_norm fixtures pass strict RTX 5070 Ti CUDA RMSNorm parity against CPU references.",
  "The dense_gguf_norm_cuda_parity receipt records dense_regular_llm_cuda routing, dense_rmsnorm_f32_cuda kernel use, transfer byte accounting, and fallback_used=false.",
  "The dense RMSNorm CUDA parity receipt remains rejected as BitNet packed I2S/QK256 proof.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-017"
status = "merged"
branch = "codex/cuda-dense-017-one-layer-rmsnorm-route"
pr = 4222
head_sha = "b4404b75db4299b8140477ee9689c926f90956c1"
merge_sha = "7f25a615d8f696cf34bcf13da772126af1a024ef"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-016"]
acceptance = "Update the dense GGUF one-layer planner and gap receipt to mark verified attention_norm and ffn_norm RMSNorm ops as dense_regular_llm_cuda routable after CUDA-DENSE-016, reducing unsupported strict CUDA gaps to RoPE, attention score/softmax/V mix, and MLP activation while keeping dense GGUF inference, Qwen one-token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, and server claims false."
commands = [
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda model_aware_dense -- --nocapture",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_one_layer -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features dense_gguf_one_layer -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-one-layer-plan --model <verified-qwen2.5-q8-gguf> --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/dispatch_planner.rs",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "The dense GGUF one-layer planner now routes attention_norm and ffn_norm RMSNorm ops to dense_regular_llm_cuda based on CUDA-DENSE-016 parity evidence.",
  "The one-layer gap receipt records 9 CUDA-routable dense ops, including 7 linears and 2 RMSNorm ops, with 5 unsupported strict CUDA gaps remaining.",
  "The next governed unsupported one-layer gap is RoPE.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-018"
status = "merged"
branch = "codex/cuda-dense-018-rope-parity"
pr = 4228
head_sha = "67e3ea577ac8408bfc1507b7614957866f07c62e"
merge_sha = "210a338c0257eb656fadf3a2c4df352e1ab3fde5"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-017"]
acceptance = "Add a strict RTX 5070 Ti CUDA RoPE fixture parity proof for the verified Qwen2.5 0.5B Q8_0 dense GGUF metadata, using metadata-derived Q/K head counts and RoPE parameters, recording dense_rope_f32_cuda kernel launches, transfer accounting, CPU reference comparison, and claim boundaries while keeping dense GGUF inference, Qwen one-token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, and server claims false."
commands = [
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda rope -- --nocapture",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_rope -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features dense_gguf_rope -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-rope-cuda-parity --model <verified-qwen2.5-q8-gguf> --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-rope-cuda-parity-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-rope-cuda-parity-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo fmt -p bitnet-kernels -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/cuda/rope.rs",
  "crates/bitnet-kernels/src/cuda/mod.rs",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "Verified metadata-derived Q/K RoPE fixtures from Qwen2.5 0.5B Q8_0 dense GGUF metadata pass strict RTX 5070 Ti CUDA parity against CPU references.",
  "The dense_gguf_rope_cuda_parity receipt records dense_regular_llm_cuda routing, dense_rope_f32_cuda kernel launches, transfer byte accounting, and fallback_used=false.",
  "A later governed one-layer planner update may mark RoPE CUDA-routable after this parity evidence.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-019"
status = "merged"
branch = "codex/cuda-dense-019-rope-route"
pr = 4241
head_sha = "a13164098a54007c381643ce91c037d5cf3ea99d"
merge_sha = "77e9497324ce5a60cf4cdb8590f834204919bac4"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-018"]
acceptance = "Update the dense GGUF one-layer planner and gap receipt to mark verified RoPE ops as dense_regular_llm_cuda routable after CUDA-DENSE-018, reducing unsupported strict CUDA gaps to attention score/softmax/V mix and MLP activation while keeping dense GGUF inference, Qwen one-token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, and server claims false."
commands = [
  "cargo test --locked -p bitnet-kernels --lib --no-default-features --features cuda model_aware_dense -- --nocapture",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_one_layer -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features dense_gguf_one_layer -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-one-layer-plan --model <verified-qwen2.5-q8-gguf> --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo fmt -p bitnet-kernels -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/dispatch_planner.rs",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "The dense GGUF one-layer planner now routes the verified RoPE op to dense_regular_llm_cuda based on CUDA-DENSE-018 parity evidence.",
  "The one-layer gap receipt records 10 CUDA-routable dense ops, including 7 linears, 2 RMSNorm ops, and 1 RoPE op, with 4 unsupported strict CUDA gaps remaining.",
  "The next governed unsupported one-layer gap is attention_scores.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-DENSE-020"
status = "merged"
branch = "codex/cuda-dense-020-attention-score-fixture"
pr = 4246
head_sha = "c9aeb2715847365abc8fe5cf88f05dfd8bc9d14d"
merge_sha = "939ee1aa2712c47f1dc38d1d144905b1643f15f5"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-019"]
acceptance = "Extract a CPU-reference dense GGUF attention-score fixture from verified Qwen2.5 0.5B Q8_0 metadata-derived RoPE Q/K outputs, record the missing CUDA attention-score kernel gap, and keep dense GGUF inference, CUDA attention parity, Qwen one-token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, QK256, and server claims false."
commands = [
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli attention_score -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features attention_score -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-attention-score-fixture --model <verified-qwen2.5-q8-gguf> --layer-index 0 --seq-len 4 --position-offset 1 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-attention-score-fixture-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-attention-score-fixture-qwen25-q8.json",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo fmt -p bitnet-cli -p bitnet-receipts-core -p bitnet-receipts -- --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA attention-score kernel implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "A metadata-derived Qwen2.5 0.5B Q8_0 dense GGUF attention-score CPU-reference fixture exists after verified RoPE Q/K fixture generation.",
  "The dense_gguf_attention_score_fixture_extraction receipt records q_heads, kv_heads, head_dim, causal mask counts, score SHA256, and the missing CUDA attention-score kernel gap.",
  "The next required dense CUDA proof is cuda_attention_score_kernel_parity.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA attention-score parity exists.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, QK256 math, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
]

[[work_item]]
id = "CUDA-UX-002"
status = "merged"
branch = "codex/cuda-ux-002-ask-proof-summary"
pr = 4191
head_sha = "6960b2bf1005bec92b8a1e06005b63a28d46ef9b"
merge_sha = "99b938bfd06cf06fb0bcca98ee1b2f601ee58749"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-UX-001", "CUDA-PROD-001"]
acceptance = "Reuse the `bitnet receipts explain` normalization layer for strict `bitnet ask` proof summaries so the live ask path prints a compact proof covering model, planner route, backend, runtime, kernels, fallback, quality, upload-once weight status, measured kernel/transfer fields, full-residency claim, speed claim, and receipt path without changing generation, tokenizer, loader, transformer, kernel, benchmark, dense GGUF inference, or server behavior."
commands = [
  "cargo fmt -p bitnet-cli -- --check",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,full-cli receipts -- --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet --no-default-features --features cpu,full-cli receipts -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci/hardware/windows-9950x3d-rtx5070ti/2026-05-08/cuda-bitnet-perf-003-warm-session-benchmark.json",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/cuda/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "Strict `bitnet ask` proof summaries reuse the same normalized receipt explanation object as `bitnet receipts explain`.",
  "Strict ask summaries expose route, backend, kernel, fallback, quality, measured timing/transfer fields, upload-once status, speed claim, and receipt path in the user-facing output.",
]
must_not_claim = [
  "Inference behavior changed.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, or server behavior changed.",
  "BitNet QK256 CUDA math changed.",
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "General dense GGUF CUDA inference is complete.",
  "CUDA speedup is accepted.",
  "Full CUDA residency is proven.",
]

[[work_item]]
id = "CUDA-DENSE-014"
status = "merged"
branch = "codex/cuda-dense-014-gap-audit"
pr = 4216
head_sha = "d271eddb75d0aa2c132f3d67f07a3b0549932561"
merge_sha = "b9a05ef5bbc4f00462566fbce576d66e8917cc98"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CUDA-DENSE-013"]
acceptance = "Extend the dense GGUF one-layer planner receipt with a gap_audit section that records unsupported strict CUDA non-linear ops, dependency notes, candidate order, and not-executed residency/timing status while keeping dense GGUF inference, Qwen token/decode/chat, speedup, full-residency, BitNet packed proof, tokenizer, loader, transformer, kernel, and server claims false."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli -- dense-gguf-one-layer-plan --model <verified-qwen2.5-q8-gguf> --device-index 0 --json-out ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- receipts explain ci\\hardware\\windows-9950x3d-rtx5070ti\\2026-05-09\\dense-gguf-one-layer-plan-qwen25-q8.json",
  "cargo test --locked -p bitnet-cli --lib --no-default-features --features cpu,cuda,full-cli dense_gguf_one_layer -- --nocapture",
  "cargo test --locked -p bitnet-receipts --test cuda_receipt_validation --no-default-features dense_gguf_one_layer -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,cuda,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check nvidia-5070ti",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-receipts/**",
  "ci/hardware/windows-9950x3d-rtx5070ti/**",
  "docs/reports/**",
  "docs/tracking/campaigns/nvidia-5070ti/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-models/**",
  "crates/bitnet-server/**",
  "BitNet QK256 CUDA math changes",
  "dense CUDA math implementation",
  "dense GGUF full-model inference implementation",
  "Qwen one-token, short decode, or chat implementation",
  "server runtime behavior",
]
may_claim = [
  "The dense GGUF one-layer planner receipt records dependency notes for unsupported strict CUDA non-linear ops.",
  "The gap audit records strict CUDA rejection with cpu_fallback_ops_total=0, strict_cuda_ready=false, and not-executed residency/timing status for blocked ops.",
  "The dense one-layer gap audit remains rejected as BitNet packed I2S/QK256 proof.",
]
must_not_claim = [
  "Dense regular LLM CUDA proves BitNet packed I2S inference.",
  "Dense regular LLM CUDA proves QK256 inference.",
  "Dense regular LLM CUDA speedup exists.",
  "General dense GGUF CUDA inference is complete.",
  "Runtime tokenizer behavior, prompt-template behavior, transformer behavior, loader behavior, or server behavior changed.",
  "A full Qwen layer, token, short decode, or chat path works on CUDA.",
  "Persistent-session or full dense regular LLM CUDA residency exists.",
]
