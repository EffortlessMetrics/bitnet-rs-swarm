id = "cpu-proof"
title = "BitNet CPU proof"
status = "active"

objective = "Make real BitNet CPU inference strict, receipt-backed, and measurable without routing around model, tokenizer, layout, or fallback truth."

end_state = [
  "Real GGUF loading is authoritative in strict proof mode.",
  "Tokenizer resolution is strict and receipt-backed.",
  "Packed BitNet layout is canonical.",
  "Scalar truth kernels exist before SIMD performance work.",
  "AVX2 dispatch and decode work are parity checked.",
  "AVX-512 work is separated into detection, dispatch, execution, parity, profile, and sustained proof before speed claims.",
  "Strict receipts record model, tokenizer, quantization, kernel family, selected backend, phase, and fallback status.",
]

hard_constraints = [
  "258V CPU is the lead BitNet CPU reference; no GPU or NPU claims.",
  "No GPU or NPU claims.",
  "No silent GGUF fallback.",
  "No coherent local-answer claim from an artifact that is not answer_ready under docs/model-artifacts/ANSWER_ARTIFACT_GATE.md.",
  "No performance claim without receipt artifacts.",
  "No helper-only SIMD work unless it is wired to real inference or explicitly scoped as preparation.",
  "No AVX-512 speed or auto-selection claim from CPUID detection, receipt labels, or AVX2 proof alone.",
]

[[work_item]]
id = "CPU-BITNET-000"
status = "merged"
branch = "codex/cpu-proof/CPU-BITNET-000-path-plan"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Document the real BitNet CPU path implementation plan and sequence strict loader, tokenizer, layout, scalar, AVX2, receipts, and benchmarks."
commands = [
  "cargo fmt --all -- --check",
  "Parse TOML tracker files",
  "git diff --check",
]
allowed_paths = [
  "docs/bitnet/**",
  "docs/tracking/campaigns/cpu-proof/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The real BitNet CPU path plan is documented.",
]
must_not_claim = [
  "Strict loader authority is implemented.",
  "Tokenizer authority is complete.",
  "CPU performance is proven.",
]

[[work_item]]
id = "CPU-BITNET-001"
status = "merged"
branch = "codex/cpu-proof/CPU-BITNET-001-loader-authority"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-BITNET-000"]
acceptance = "Strict CPU inference has one authoritative real GGUF loader path for BitNet models, and minimal fallback is impossible in strict proof mode."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-models --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
]
allowed_paths = [
  "crates/bitnet-models/src/formats/gguf/loader.rs",
  "crates/bitnet-models/src/gguf_simple.rs",
  "crates/bitnet-models/src/lib.rs",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts/**",
  "docs/bitnet/**",
  "docs/tracking/campaigns/cpu-proof/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-qk256-dispatch/**",
]
may_claim = [
  "Strict BitNet GGUF loader authority exists after merge.",
]
must_not_claim = [
  "Tokenizer authority is complete.",
  "Packed kernels are complete.",
  "Transformer decode is complete.",
  "CPU performance is proven.",
]

[[work_item]]
id = "CPU-BITNET-002"
status = "merged"
branch = "codex/cpu-proof/CPU-BITNET-002-tokenizer-authority"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-BITNET-001"]
acceptance = "Strict tokenizer resolution uses explicit override, GGUF metadata, sibling tokenizer assets, then strict failure."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-tokenizers --no-default-features --features cpu",
]
allowed_paths = [
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-cli/**",
  "docs/bitnet/**",
  "docs/tracking/campaigns/cpu-proof/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-qk256-dispatch/**",
]
may_claim = [
  "Strict tokenizer authority exists after merge.",
]
must_not_claim = [
  "Packed kernels are complete.",
  "Transformer decode is complete.",
  "CPU performance is proven.",
]

[[work_item]]
id = "CPU-BITNET-003"
status = "merged"
branch = "codex/cpu-bitnet-003-canonical-packed-layout"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-BITNET-002"]
acceptance = "Canonical block geometry, alignment, stride, and row/block iteration API are defined."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-qk256-layout-core --no-default-features --features cpu",
  "cargo test --locked -p bitnet-quantization --no-default-features --features cpu",
  "cargo test --locked -p bitnet-models --no-default-features --features cpu",
]
allowed_paths = [
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-quantization/src/i2s_qk256.rs",
  "crates/bitnet-quantization/src/qk256_dispatch.rs",
  "crates/bitnet-models/src/quant/**",
  "crates/bitnet-models/src/qk256_utils.rs",
  "docs/bitnet/**",
  "docs/tracking/campaigns/cpu-proof/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Canonical packed layout exists after merge.",
]
must_not_claim = [
  "Optimized kernel performance is proven.",
  "Scalar truth kernels are complete.",
  "AVX2 dispatch is complete.",
]

[[work_item]]
id = "CPU-BITNET-004"
status = "merged"
branch = "codex/cpu-bitnet-004-scalar-packed-truth"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-BITNET-003"]
acceptance = "Canonical scalar packed QK256 GEMV/GEMM kernels are deterministic correctness oracles for decode and prefill."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-quantization --no-default-features --features cpu i2s_qk256 --lib",
]
allowed_paths = [
  "crates/bitnet-quantization/src/i2s_qk256.rs",
  "docs/bitnet/**",
  "docs/tracking/campaigns/cpu-proof/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-qk256-dispatch/**",
]
may_claim = [
  "Scalar packed QK256 GEMV/GEMM correctness oracles exist after merge.",
]
must_not_claim = [
  "Optimized kernel performance is proven.",
  "AVX2 dispatch is complete.",
  "Transformer decode is complete.",
]

[[work_item]]
id = "CPU-BITNET-005a"
status = "merged"
branch = "codex/cpu-bitnet-005a-avx2-fma-gating"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-BITNET-004"]
acceptance = "AVX2/FMA feature plumbing is explicit, FMA-using QK256 helpers are target-feature gated, and scalar fallback remains unchanged."
commands = [
  "rustfmt crates/bitnet-cpu-detect/src/lib.rs crates/bitnet-quantization/src/i2s_qk256.rs crates/bitnet-quantization/src/i2s_qk256_avx2.rs --edition 2024 --check",
  "cargo test --locked -p bitnet-cpu-detect --no-default-features --features avx2",
  "cargo test --locked -p bitnet-quantization --no-default-features --features cpu,avx2 i2s_qk256",
  "cargo test --locked -p bitnet-quantization --no-default-features --features cpu,avx2 --test qk256_avx2_parity_tests",
  "cargo clippy --locked -p bitnet-quantization --all-targets --no-default-features --features cpu,avx2 -- -D warnings",
]
allowed_paths = [
  "Cargo.lock",
  "crates/bitnet-cpu-detect/src/lib.rs",
  "crates/bitnet-quantization/Cargo.toml",
  "crates/bitnet-quantization/src/i2s_qk256.rs",
  "crates/bitnet-quantization/src/i2s_qk256_avx2.rs",
  "crates/bitnet-quantization/tests/qk256_avx2_parity_tests.rs",
  "docs/bitnet/**",
  "docs/tracking/campaigns/cpu-proof/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-receipts/**",
]
may_claim = [
  "QK256 AVX2 execution is gated on both AVX2 and FMA after merge.",
  "FMA-using AVX2 helpers advertise target_feature(avx2,fma) after merge.",
]
must_not_claim = [
  "Requested versus selected kernel dispatch is complete.",
  "Transformer decode is complete.",
  "Strict receipts are complete.",
  "Benchmark or throughput performance is proven.",
  "GPU or NPU execution is involved.",
]

[[work_item]]
id = "CPU-BITNET-005b"
status = "merged"
branch = "codex/cpu-bitnet-005b-kernel-selection"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-BITNET-005a"]
acceptance = "QK256 decode GEMV selection reports requested kernel, selected kernel, fallback status, fallback reason, and CPU features with strict AVX2 fallback failure."
commands = [
  "rustfmt crates/bitnet-quantization/src/i2s_qk256.rs crates/bitnet-quantization/src/i2s_qk256_avx2.rs --edition 2024 --check",
  "cargo test --locked -p bitnet-quantization --no-default-features --features cpu,avx2 i2s_qk256 --lib",
  "cargo test --locked -p bitnet-quantization --no-default-features --features cpu,avx2 --test qk256_avx2_parity_tests",
  "cargo clippy --locked -p bitnet-quantization --all-targets --no-default-features --features cpu,avx2 -- -D warnings",
]
allowed_paths = [
  "crates/bitnet-quantization/src/i2s_qk256.rs",
  "crates/bitnet-quantization/src/i2s_qk256_avx2.rs",
  "crates/bitnet-quantization/tests/qk256_avx2_parity_tests.rs",
  "docs/bitnet/**",
  "docs/tracking/campaigns/cpu-proof/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-receipts/**",
]
may_claim = [
  "QK256 requested and selected kernel identity is explicit after merge.",
  "Strict requested AVX2 fallback failure exists after merge.",
]
must_not_claim = [
  "Transformer decode is complete.",
  "Strict receipts are complete.",
  "Benchmark or throughput performance is proven.",
  "GPU or NPU execution is involved.",
]

[[work_item]]
id = "CPU-BITNET-005c"
status = "merged"
branch = "codex/cpu-bitnet-005c-avx2-parity-hardening"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-BITNET-005b"]
acceptance = "AVX2 decode GEMV parity is hardened against scalar across rows, tail-column shapes, deterministic patterns, and repeated-run equality."
commands = [
  "cargo test --locked -p bitnet-quantization --tests --no-default-features --features cpu,avx2",
  "cargo bench --locked -p bitnet-quantization --bench qk256_gemv --features cpu,avx2 --no-run",
]
allowed_paths = [
  "crates/bitnet-quantization/src/i2s_qk256.rs",
  "crates/bitnet-quantization/src/i2s_qk256_avx2.rs",
  "crates/bitnet-quantization/tests/qk256_avx2_parity_tests.rs",
  "crates/bitnet-quantization/benches/qk256_gemv.rs",
  "docs/bitnet/**",
  "docs/tracking/campaigns/cpu-proof/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-receipts/**",
]
may_claim = [
  "AVX2 decode GEMV parity against scalar is hardened after merge.",
]
must_not_claim = [
  "Transformer decode is complete.",
  "Strict receipts are complete.",
  "Benchmark or throughput performance is proven.",
  "GPU or NPU execution is involved.",
]

[[work_item]]
id = "CPU-BITNET-006"
status = "merged"
pr = 3793
branch = "codex/cpu-bitnet-006-cpu-decode-step"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-BITNET-005c"]
acceptance = "Embedding gather, RMSNorm/subln, RoPE, attention scores, softmax, A/V, FFN, KV-cache, output head, logits handoff, and sampling handoff are authoritative for CPU decode; prefill and decode scheduling are separated; one real-model decode step can run with real tensors; missing ops fail explicitly; KV-cache append/read is deterministic."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-inference --no-default-features --features cpu",
  "cargo test --locked -p bitnet-kernels --no-default-features --features cpu",
]
allowed_paths = [
  "crates/bitnet-kernels/src/cpu/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/src/backends.rs",
  "crates/bitnet-inference/**",
  "tests/**",
  "docs/bitnet/**",
  "docs/tracking/campaigns/cpu-proof/**",
  "docs/tracking/bitnet-alignment/status.md",
  "docs/tracking/bitnet-alignment/workstream-ledger.yaml",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "CPU decode op coverage exists after merge.",
]
must_not_claim = [
  "Full inference quality or performance is proven without receipts.",
  "GPU or NPU execution is involved.",
]

[[work_item]]
id = "CPU-BITNET-007"
status = "merged"
pr = 3800
branch = "codex/cpu-bitnet-007-strict-receipts"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-BITNET-006"]
acceptance = "Strict CPU proof receipts tie loader mode, tokenizer source, requested/selected backend, requested/selected kernel, CPU features, quant format, decode/prefill phase, prompt/generated tokens, and fallback status into one machine-checkable record; validation fails on hidden fallback, non-real GGUF loader mode, guessed tokenizer source, mock/diagnostic kernels, or requested AVX2 silently selecting scalar."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-receipts --no-default-features",
  "cargo test --locked -p bitnet-receipts-core --no-default-features",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
]
allowed_paths = [
  "crates/bitnet-common/src/backend_selection.rs",
  "crates/bitnet-inference/src/backends.rs",
  "crates/bitnet-inference/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "crates/bitnet-bench-receipts/**",
  "crates/bitnet-cli/**",
  "docs/bitnet/**",
  "docs/tracking/campaigns/cpu-proof/**",
  "docs/tracking/bitnet-alignment/status.md",
  "docs/tracking/bitnet-alignment/workstream-ledger.yaml",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Strict CPU proof receipts record loader, tokenizer, backend, kernel, phase, and fallback truth after merge.",
]
must_not_claim = [
  "Benchmark or throughput performance is proven.",
  "Server inference is complete.",
  "GPU or NPU execution is involved.",
]

[[work_item]]
id = "CPU-BITNET-008"
status = "merged"
branch = "codex/cpu-bitnet-008-phase-benchmark-receipts"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-BITNET-007"]
acceptance = "CPU proof benchmark profiles emit receipt-backed micro, layer, prefill, first-token, and decode measurements with selected backend/kernel, fallback status, workload shape, model identity, quant format, prompt/generated token counts, and hardware context."
commands = [
  "cargo fmt --all -- --check",
  "cargo bench --locked -p bitnet-kernels --bench kernel_benchmarks --features cpu --no-run",
  "cargo bench --locked -p bitnet-quantization --bench qk256_gemv --features cpu,avx2 --no-run",
  "cargo test --locked -p bitnet-bench-receipts --no-default-features",
]
allowed_paths = [
  "crates/bitnet-kernels/benches/**",
  "crates/bitnet-quantization/benches/**",
  "crates/bitnet-bench-receipts/**",
  "crates/bitnet-cli/**",
  "docs/bitnet/**",
  "docs/tracking/campaigns/cpu-proof/**",
  "ci/hardware/intel-i5-8250u-cpu-avx2/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "CPU benchmark artifacts record selected backend/kernel, fallback state, workload, and hardware context after merge.",
]
must_not_claim = [
  "Server inference is complete.",
  "GPU or NPU execution is involved.",
  "Sustained Kaby Lake throughput is proven from cold-turbo-only runs.",
]

[[work_item]]
id = "CPU-PHASE-TIMING-001"
status = "merged"
pr = 3872
branch = "codex/cpu-bitnet-008-phase-timing-followup"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-BITNET-008"]
acceptance = "CPU phase benchmark receipt generation prefers strict prompt-prefill, first-token decode, and steady-decode timing fields when present; regenerated Kaby Lake evidence records prefill, first_token, and decode as measured from strict proof timing while micro and layer remain explicit not_run gaps."
commands = [
  "cargo test --locked -p bitnet-bench-receipts --no-default-features",
  "Parse ci/hardware/intel-i5-8250u-cpu-avx2/2026-05-07/cpu-phase-benchmark-decode2.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-bench-receipts/src/bin/cpu_phase_benchmark_receipt.rs",
  "ci/hardware/intel-i5-8250u-cpu-avx2/**",
  "docs/tracking/campaigns/cpu-proof/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-cli/**",
]
may_claim = [
  "Kaby Lake phase receipt timing source is tightened for prefill, first_token, and decode after merge.",
]
must_not_claim = [
  "micro and layer profiles are measured by this slice.",
  "Sustained Kaby Lake throughput is proven.",
  "Server inference is complete.",
  "GPU or NPU execution is involved.",
]

[[work_item]]
id = "CPU-ANSWER-001"
status = "merged"
pr = 3898
branch = "codex/cpu-answer-001-strict-answer-readiness"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-BITNET-008", "CPU-PHASE-TIMING-001"]
acceptance = "Strict real-model CPU runs prove answer readiness on a tiny deterministic corpus by recording prompt token IDs, generated token IDs, decoded text, selected backend/kernel, fallback=false, and pass/fail answer quality; scalar and AVX2 full-decode runs can be compared before blaming the AVX2 lane."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo test --locked -p bitnet-inference --no-default-features --features cpu",
  "cargo test --locked -p bitnet-receipts --no-default-features",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "ci/quality/**",
  "docs/bitnet/**",
  "docs/tracking/campaigns/cpu-proof/**",
  "ci/hardware/intel-i5-8250u-cpu-avx2/**",
  "tests/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The Rust CPU path can be checked for deterministic answer readiness on tiny prompts after merge.",
  "Scalar versus AVX2 full-decode divergence can be separated from shared decode correctness after merge.",
]
must_not_claim = [
  "General chat quality is proven.",
  "Sustained Kaby Lake throughput is proven.",
  "Server inference is complete.",
  "GPU or NPU execution is involved.",
]

[[work_item]]
id = "CPU-ANSWER-002"
status = "merged"
pr = 3906
branch = "codex/cpu-answer-002-full-decode-parity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-ANSWER-001"]
acceptance = "Strict CPU answer runs can compare scalar and AVX2 full-decode outputs for the same real GGUF, tokenizer, prompt, greedy settings, prompt token IDs, generated token IDs, decoded text, and per-step logits/top-k evidence so AVX2 divergence is separated from shared decode correctness; the 258V CPU is the lead machine for new BitNet CPU answer parity artifacts; runs against a non-answer-ready artifact are diagnostic-only."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo test --locked -p bitnet-inference --no-default-features --features cpu",
  "cargo test --locked -p bitnet-receipts --no-default-features",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "ci/quality/**",
  "ci/hardware/intel-258v/**",
  "docs/bitnet/**",
  "docs/tracking/campaigns/cpu-proof/**",
  "tests/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Scalar versus AVX2 full-decode answer parity can be audited after merge.",
  "First divergence evidence can separate AVX2 kernel issues from shared decode, prompt, tokenizer, logits, or sampler issues after merge.",
  "258V CPU answer parity artifacts can become the lead BitNet CPU reference for Arc 140V and Intel NPU comparison after merge.",
]
must_not_claim = [
  "General chat quality is proven.",
  "258V sustained throughput is proven.",
  "Server inference is complete.",
  "GPU or NPU execution is involved.",
]

[[work_item]]
id = "CPU-ANSWER-003"
status = "merged"
pr = 3993
branch = "codex/cpu-answer-003-reference-comparator"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-ANSWER-002"]
acceptance = "Strict BitNet CPU answer runs can be compared against a known-good reference artifact for the same model SHA, tokenizer, prompt bytes/template/BOS policy, prompt token IDs, generated token IDs, decoded text, and first-step top-k/logit evidence so the first divergence is attributed to prompt/tokenizer/template, shared decode math, logits/sampler, or backend-specific execution; scalar-vs-AVX2 parity remains preserved and non-answer-ready artifacts stay diagnostic-only."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo test --locked -p bitnet-inference --no-default-features --features cpu",
  "cargo test --locked -p bitnet-receipts --no-default-features",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "ci/quality/**",
  "ci/hardware/intel-258v/**",
  "docs/bitnet/**",
  "docs/model-artifacts/**",
  "docs/tracking/campaigns/cpu-proof/**",
  "tests/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "BitNet CPU answer failures can be triaged against a known-good reference artifact after merge.",
  "The first divergence can be attributed to prompt/template/tokenizer, shared decode, logits/sampler, or backend-specific execution after merge.",
  "Scalar-versus-AVX2 full-decode parity remains a preserved diagnostic input after merge.",
]
must_not_claim = [
  "General chat quality is proven.",
  "A rejected or non-answer-ready artifact is coherent local-answer evidence.",
  "258V sustained throughput is proven.",
  "Server inference is complete.",
  "GPU or NPU execution is involved.",
]

[[work_item]]
id = "CPU-ANSWER-004"
status = "merged"
pr = 3995
branch = "codex/cpu-answer-004-bitnetcpp-template"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-ANSWER-003"]
acceptance = "Strict Rust CPU answer-corpus runs use the MODEL-ARTIFACT-007 answer-ready authority: the Microsoft BitNet.cpp reference prompt envelope `User: <question><|eot_id|>Assistant:`, external tokenizer/pretokenizer provenance, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel, fallback=false, and reference-divergence evidence before any BitNet answer-quality claim."
commands = [
  "cargo test --locked -p bitnet-prompt-templates-core --no-default-features bitnetcpp",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli answer_corpus",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-prompt-templates-core/**",
  "crates/bitnet-cli/**",
  "ci/quality/**",
  "ci/hardware/intel-258v/**",
  "docs/model-artifacts/**",
  "docs/tracking/campaigns/cpu-proof/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Strict BitNet CPU answer-corpus runs use the accepted Microsoft BitNet.cpp answer-ready prompt envelope after merge.",
  "The answer-corpus prompt template no longer uses the rejected llama3-chat envelope for Microsoft BitNet I2_S evidence after merge.",
  "Reference-divergence evidence can now compare Rust CPU against the MODEL-ARTIFACT-007 prompt authority after merge.",
]
must_not_claim = [
  "General chat quality is proven.",
  "Rust CPU answer quality is proven without a fresh strict corpus artifact.",
  "258V sustained throughput is proven.",
  "Server inference is complete.",
  "GPU or NPU execution is involved.",
]

[[work_item]]
id = "CPU-ANSWER-005"
status = "merged"
pr = 4003
branch = "codex/cpu-answer-005-tokenizer-authority"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-ANSWER-004"]
acceptance = "Strict Rust CPU answer receipts record the MODEL-ARTIFACT-007 external Llama-BPE tokenizer/pre-tokenizer authority instead of `unknown` when an explicit or sibling Llama-3 tokenizer is used; failed answer-corpus artifacts remain diagnostic and no answer-quality, throughput, server, GPU, or NPU claim is made."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli pretokenizer_authority",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli answer_corpus",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/tracking/campaigns/cpu-proof/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Strict Rust CPU answer receipts record external Llama-BPE tokenizer authority after merge.",
  "MODEL-ARTIFACT-007 tokenizer/pre-tokenizer authority is no longer collapsed to unknown in run receipts after merge.",
]
must_not_claim = [
  "General chat quality is proven.",
  "Rust CPU answer quality is proven.",
  "258V or Kaby Lake sustained throughput is proven.",
  "Server inference is complete.",
  "GPU or NPU execution is involved.",
]

[[work_item]]
id = "CPU-ANSWER-006"
status = "merged"
pr = 4005
branch = "codex/cpu-answer-006-reference-token-artifact"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-ANSWER-005"]
acceptance = "A Microsoft BitNet.cpp reference-divergence artifact records the MODEL-ARTIFACT-007 prompt envelope, BOS policy, external Llama-BPE tokenizer/pre-tokenizer authority, prompt token IDs, generated token IDs, decoded text, and first-step top-k/logit evidence where available, so strict Rust CPU failures can be classified as prompt/tokenizer divergence, shared decode/logits divergence, or backend-specific execution without claiming answer quality."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli reference_divergence",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/hardware/intel-i5-8250u-cpu-avx2/**",
  "ci/hardware/intel-258v/**",
  "ci/quality/**",
  "docs/bitnet/**",
  "docs/model-artifacts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/cpu-proof/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Strict BitNet CPU answer failures can be compared against a reference artifact with prompt IDs and generated IDs after merge.",
  "Reference evidence can separate prompt/tokenizer/template mismatch from decode/logits or backend-specific divergence after merge.",
]
must_not_claim = [
  "General chat quality is proven.",
  "Rust CPU answer quality is proven.",
  "A reference text-only output is enough to prove logits parity.",
  "258V or Kaby Lake sustained throughput is proven.",
  "Server inference is complete.",
  "GPU or NPU execution is involved.",
]

[[work_item]]
id = "CPU-ANSWER-007"
status = "merged"
pr = 4019
branch = "codex/cpu-answer-007-bitnet-subnorms"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-ANSWER-006"]
acceptance = "Strict Rust CPU answer-corpus runs pass the MODEL-ARTIFACT-007 official Microsoft I2_S answer-ready artifact by aligning BitNet B1.58 model math with BitNet.cpp: RMSNorm/ReLU^2 defaults, attention and FFN sub-layernorms, BitNet.cpp I2_S QK256 layout, inline scale and I8_S GEMV semantics, and GGML token-major F16 token embeddings."
commands = [
  "cargo fmt -p bitnet-common -p bitnet-models -p bitnet-quantization -p bitnet-qk256-dispatch -p bitnet-transformer -p bitnet-cli -- --check",
  "cargo test --locked -p bitnet-common --no-default-features --features cpu",
  "cargo test --locked -p bitnet-models qk256_token_embedding_rows -- --nocapture",
  "cargo test --locked -p bitnet-models f16_embedding_payload_is_token_major -- --nocapture",
  "cargo test --locked -p bitnet-quantization --no-default-features --features cpu qk256 -- --nocapture",
  "cargo test --locked -p bitnet-qk256-dispatch --no-default-features --features cpu --test forward_qk256 forward_qk256_with_scale -- --nocapture",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli answer_corpus -- --nocapture",
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run --release --locked -p xtask --no-default-features -- campaign check cpu-proof",
  "cargo run --release --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --release --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-common/**",
  "crates/bitnet-models/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-cli/**",
  "docs/tracking/campaigns/cpu-proof/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Strict Rust CPU answer-corpus runs pass the MODEL-ARTIFACT-007 official Microsoft I2_S artifact after merge.",
  "BitNet B1.58 CPU model math now matches the evidenced BitNet.cpp norm, activation, sub-layernorm, QK256 scale/layout, and token-embedding layout requirements after merge.",
]
must_not_claim = [
  "General chat quality is proven.",
  "CUDA answer readiness is proven.",
  "GPU, NPU, or server inference is involved.",
  "A speedup or throughput claim is proven.",
]

[[work_item]]
id = "CPU-AVX512-000"
status = "merged"
pr = 5554
head_sha = "18ec173922bea3c858aaaa9f8194fe62cd6ac1b9"
merge_sha = "f3e1986ecb48b55cc0258127d2e378d50400e8ce"
branch = "codex/cpu-avx512-kernel-contract"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-ANSWER-007"]
acceptance = "Add the AVX-512 kernel contract, strict CPU ISA selection rails, implementation plan, 9950X3D proof requirements, and kernel-matrix claim boundary before runtime AVX-512 implementation starts."
commands = [
  "git diff --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check cpu-proof",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
]
allowed_paths = [
  "docs/specs/BITNET-SPEC-CPU-AVX512-KERNEL-CONTRACT.md",
  "docs/specs/BITNET-SPEC-CPU-ISA-SELECTION.md",
  "docs/specs/amd-9950x3d-cpu-roadmap.md",
  "docs/specs/INDEX.md",
  "docs/bitnet/BITNET_KERNEL_MATRIX.md",
  "plans/cpu-avx512/implementation-plan.md",
  "docs/tracking/campaigns/cpu-proof/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/hardware/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The AVX-512 CPU kernel contract and strict CPU ISA selection rails are documented after merge.",
  "The 9950X3D AVX-512 lane now has a receipt-gated implementation sequence after merge.",
]
must_not_claim = [
  "An AVX-512 optimized kernel is implemented.",
  "AVX-512 is auto-selected.",
  "AVX-512 speedup or sustained performance is proven.",
  "CUDA, GPU, NPU, OpenCL, OpenVINO, Metal, WGPU, server, or answer-quality readiness is proven.",
]

[[work_item]]
id = "CPU-SCALAR-000"
status = "merged"
pr = 5559
head_sha = "3d33c2324e9474753cd0fd2b29344168528fb2ce"
merge_sha = "2fa488420140fd80f532781c18340adda88cc116"
branch = "codex/cpu-scalar-000-docs-contract"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-ANSWER-007"]
acceptance = "Docs-only scalar CPU contracts define precise scalar kernel IDs, hot-path counters, parity direction, performance profiles, implementation sequencing, and claim boundaries without runtime changes."
commands = [
  "cargo run --locked -p xtask --no-default-features -- campaign check cpu-proof",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/specs/BITNET-SPEC-CPU-SCALAR-KERNEL-CONTRACT.md",
  "docs/specs/BITNET-SPEC-CPU-SCALAR-HOTPATH.md",
  "docs/specs/BITNET-SPEC-CPU-SCALAR-PARITY.md",
  "docs/specs/BITNET-SPEC-CPU-SCALAR-PERFORMANCE.md",
  "plans/cpu-scalar/implementation-plan.md",
  "docs/bitnet/BITNET_CPU_PATH_PLAN.md",
  "docs/bitnet/BITNET_KERNEL_MATRIX.md",
  "docs/tracking/campaigns/cpu-proof/active.toml",
  "docs/tracking/campaigns/cpu-proof/generated/status.md",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The scalar CPU BitNet contract and implementation sequence are documented after merge.",
  "Precise scalar kernel IDs, hot-path counters, parity direction, and scalar performance receipt fields are specified after merge.",
]
must_not_claim = [
  "Runtime scalar kernel IDs are implemented.",
  "Scaled scalar kernel selection metadata is implemented.",
  "Strict scalar answer-corpus proof exists.",
  "Scalar performance is proven.",
  "A speedup is proven.",
  "GPU, NPU, or server inference is involved.",
]

[[work_item]]
id = "CPU-AVX2-HOTPATH-000"
status = "merged"
pr = 5547
head_sha = "c54e80f230fcf8acdb9f2631002a71c89dcc1161"
merge_sha = "4be4bce6e9573a665de0bf2469bc2c7a2fdad053"
branch = "codex/cpu-avx2-hotpath-000-docs-plan"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-ANSWER-007"]
acceptance = "Add documentation-only CPU AVX2 BitNet hot-path rails: repo-local plan, implementation sequence, behavior spec, status page, and ready follow-on tracker item for hot-path counters."
commands = [
  "cargo run --locked -p xtask --no-default-features -- campaign check cpu-proof",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "plans/cpu-avx2-bitnet/**",
  "docs/specs/BITNET-SPEC-CPU-AVX2-HOTPATH.md",
  "docs/bitnet/BITNET_CPU_AVX2_STATUS.md",
  "docs/tracking/campaigns/cpu-proof/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The CPU AVX2 BitNet hot-path implementation plan and claim boundary are documented after merge.",
  "The next ready runtime work item is hot-path counter evidence after merge.",
]
must_not_claim = [
  "Strict AVX2 scaled I2S-I8S execution is proven.",
  "A speedup or throughput claim is proven.",
  "Server inference is complete.",
  "GPU, NPU, OpenVINO, CUDA, dense SLM, Qwen, or broad chat quality is involved.",
]

[[work_item]]
id = "CPU-AVX2-HOTPATH-001"
status = "merged"
pr = 5963
head_sha = "455ff1268289308b0da5b896524467160130d514"
merge_sha = "0789396eb13476c576a9c4860353e5be13478e11"
branch = "codex/cpu-avx2-hotpath-001-counters"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["CPU-AVX2-HOTPATH-000"]
acceptance = "Strict scalar and strict AVX2 BitNet answer-corpus receipts include QK256 hot-path counters that distinguish no-scale F32 GEMV, scaled I2S-I8S scalar GEMV, scaled I2S-I8S AVX2 GEMV, and audited tensor materialization without changing math or making speed claims."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-quantization --no-default-features --features cpu,avx2 --test qk256_avx2_parity_tests",
  "cargo test --locked -p bitnet-quantization --no-default-features --features cpu,avx2 i2s_qk256 --lib",
  "cargo test --locked -p bitnet-qk256-dispatch --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli answer_corpus",
  "cargo run --locked -p xtask --no-default-features -- campaign check cpu-proof",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-common/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "ci/quality/**",
  "ci/hardware/intel-258v/**",
  "docs/bitnet/**",
  "docs/tracking/campaigns/cpu-proof/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Strict BitNet CPU receipts expose actual QK256 hot-path counters after merge.",
  "Receipts distinguish no-scale F32 GEMV from scaled I2S-I8S GEMV after merge.",
]
must_not_claim = [
  "Scaled I2S-I8S AVX2 is optimized or promoted.",
  "A speedup or throughput claim is proven.",
  "Generated-token changes are acceptable without a divergence receipt.",
  "Server inference is complete.",
  "GPU, NPU, OpenVINO, CUDA, dense SLM, Qwen, or broad chat quality is involved.",
]
