id = "apple-m4-inference-excellence"
title = "Apple M4 inference excellence"
status = "active"

objective = "Move the M4 Mac mini from complete evidence lanes to excellent, repeatable local inference across supported dense SLMs and BitNet: deeper deterministic accuracy, fuller benchmark envelopes, enough matching-history receipts to remove important insufficient-history gaps, BitNet-specific product proof, reproducible run and artifact identity, service-surface conformance, clearer operator UX, and phase-scoped acceleration discipline without broad Apple Silicon or unsupported backend claims."

end_state = [
  "Every supported dense M4 SLM model has repeated matching-history eval and benchmark receipts under the same model/tokenizer/backend/fallback identity.",
  "The accepted BitNet artifact has repeated matching-history eval, benchmark, and variable warm-session receipts before any chat or serve claim changes.",
  "Dense SLM and BitNet eval corpora are large enough to report useful mechanical pass rates by task family without required LLM judging.",
  "Benchmark reports include cold load, tokenizer load, prompt tokenization, prefill/input throughput, TTFT, output/decode throughput, sampling overhead, total wall time, peak memory, and memory drift with p50/p90/p99 and min/max where supported.",
  "Every committed M4 receipt records reproducible run identity: machine, OS, git commit, binary/build profile, model artifact, tokenizer authority, prompt template, backend, fallback state, corpus/profile seed, and exact command class.",
  "Supported model artifacts have provenance manifests and cache verification receipts that separate file identity, tokenizer authority, prompt template identity, license/source metadata, and local cache state.",
  "Corpus, scorer, normalization, and golden-token canary versions are recorded before pass rates or drift are interpreted.",
  "Benchmark summaries record environment preflight and repeatability variance before p50/p90/p99 drift thresholds are treated as meaningful.",
  "Operator docs translate receipts into local operating classes such as interactive, advisory, batch, and unsupported.",
  "Dense SLM and BitNet service surfaces have explicit health, ready, streaming, timeout, cancellation, and per-request receipt conformance before any user-facing service claim expands.",
  "Release gates state exactly which eval, benchmark, stability, service, and operator checks must pass before updating the public M4 expectation envelope.",
  "Operator UX exposes supported models, cache/disk health, last dense and BitNet reports, current regressions, unsupported claim boundaries, and recommended next commands.",
  "Metal work remains phase-scoped with CPU parity, fallback-free phase receipts, and explicit CPU/NEON remainder until a full route is separately proven.",
]

hard_constraints = [
  "This is an M4 Mac mini inference-excellence campaign.",
  "Do not reopen completed Apple M4 dense SLM, BitNet local-answer, BitNet eval/benchmark, BitNet productization, inference-ops, local server, durable evidence, or Metal campaigns unless a regression proves they are wrong.",
  "Do not use dense Qwen evidence as BitNet evidence.",
  "Do not use MacBook artifact-only evidence as M4 runtime proof.",
  "Do not enable BitNet chat or BitNet serve until the campaign item for that surface has matching receipts and gates.",
  "Do not claim full apple-m4-metal inference, QK256 support, Neural Engine execution, MPSGraph model inference, MacBook evidence, broad Apple Silicon performance, broad model quality, or speedup.",
  "Do not add live model downloads, hardware timing runs, BitNet runtime runs, or long resident soaks to generic required PR CI.",
  "Never commit model binaries.",
  "Do not let eval, benchmark, serve, or status commands contact external services except for explicit model fetch commands.",
]

[[work_item]]
id = "M4-EXCELLENCE-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-EXCELLENCE-001-dense-eval-refresh"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
pr = 5057
merge_sha = "ec739821b35df5b00ddd24477502fa7bce5a3711"
acceptance = "Run and commit a second dense SLM eval-v2 refresh for every supported M4 dense model identity so dense_slm_eval_v2 dashboard groups can move from insufficient_history to comparable matching history without changing the corpus or broadening quality claims."
commands = [
  "target/release/bitnet --device apple-m4-cpu-neon answer-corpus --model <verified-cache-path>/<model-file>.gguf --model-id <model-id> --corpus ci/quality/apple-m4-slm-eval-seeded-corpus-v2.yaml --json-out ci/hardware/apple-m4-mac-mini/<date>/slm-eval-v2/<model-id>/answer-corpus.json --per-prompt-timeout-seconds 240",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/slm-eval-v2/<model-id>/summary.json --json",
  "target/release/bitnet mac regression ci/hardware/apple-m4-mac-mini/<date>/slm-eval-v2/<model-id>/summary.json --baseline ci/hardware/apple-m4-mac-mini/<baseline-date>/slm-eval-v2/<model-id>/summary.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/slm-eval-v2/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Dense SLM eval-v2 reports have matching-history comparisons for the recorded supported M4 model identities.",
]
must_not_claim = [
  "The reports prove broad dense model quality.",
  "The reports prove BitNet behavior.",
  "The reports are broad Apple Silicon benchmarks.",
]

[[work_item]]
id = "M4-EXCELLENCE-002"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-EXCELLENCE-002-bitnet-warm-refresh"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
pr = 5066
merge_sha = "290abbc1e2dc78c02f2b041b3a7e8825a13b7b10"
acceptance = "Run and commit a second BitNet variable warm-session refresh for the accepted artifact/tokenizer identity so the BitNet warm dashboard group can move from insufficient_history to comparable matching history while chat and serve remain disabled."
commands = [
  "target/release/bitnet model verify microsoft-bitnet-b1.58-2B-4T-i2s --path models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf",
  "target/release/bitnet --device apple-m4-cpu-neon mac bitnet-warm --model-id microsoft-bitnet-b1.58-2B-4T-i2s --model-path models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --prompt 'Answer with a single digit: 2+2=' --prompt 'Name the capital of France. Answer with one word.' --prompt 'Return exactly: ready' --prompt 'Answer with a single digit: 3+1=' --prompt 'Answer with a single digit: 2+2=' --json-out ci/hardware/apple-m4-mac-mini/<date>/bitnet-productization/variable-warm-session.json --progress",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/bitnet-productization --json",
  "target/release/bitnet mac report-refresh --json-out target/apple-m4-inference-excellence/M4-EXCELLENCE-002-report-refresh.json --json",
  "target/release/bitnet mac regression-dashboard --json-out target/apple-m4-inference-excellence/M4-EXCELLENCE-002-regression-dashboard.json --markdown-out target/apple-m4-inference-excellence/M4-EXCELLENCE-002-regression-dashboard.md --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/bitnet-productization/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "BitNet variable warm-session reports have repeated matching-history receipts for the accepted M4 artifact/tokenizer identity.",
]
must_not_claim = [
  "BitNet chat is enabled.",
  "BitNet serve is enabled.",
  "Full Metal BitNet inference works.",
]

[[work_item]]
id = "M4-EXCELLENCE-003"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-EXCELLENCE-003-dashboard-trends"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-EXCELLENCE-001", "M4-EXCELLENCE-002"]
pr = 5078
merge_sha = "56c54df2ed3a0637f94b62e4d33044e91b8f5d19"
acceptance = "Regenerate report-refresh and regression-dashboard outputs so all important dense SLM and BitNet M4 report families have explicit comparable or insufficient-history status, with no dense/BitNet evidence mixing."
commands = [
  "bitnet mac report-refresh --json-out target/apple-m4-inference-excellence/report-refresh-manifest.json --json",
  "bitnet mac regression-dashboard --json-out target/apple-m4-inference-excellence/regression-dashboard.json --markdown-out target/apple-m4-inference-excellence/regression-dashboard.md --json",
  "bitnet mac receipts-check target/apple-m4-inference-excellence/regression-dashboard.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The M4 regression dashboard reports matching-history trend status for dense SLM and BitNet evidence families separately.",
]
must_not_claim = [
  "A trend exists for any group still marked insufficient_history.",
  "Dense SLM evidence proves BitNet behavior.",
]

[[work_item]]
id = "M4-EXCELLENCE-004"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-EXCELLENCE-004-drift-thresholds"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-EXCELLENCE-003"]
pr = 5089
merge_sha = "38706c2f8ca57d2b0fb332ee1bca2bb4bc1e2ec4"
acceptance = "Publish model-family drift thresholds for M4 dense SLM and BitNet reports, separating identity mismatches, quality failures, timing warnings, memory drift warnings, timeout failures, and unsupported claim boundaries."
commands = [
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The M4 operator docs define family-specific drift thresholds for matching-history reports.",
]
must_not_claim = [
  "Threshold docs prove new runtime quality or speed.",
  "The thresholds apply to unproven Apple backends.",
]

[[work_item]]
id = "M4-ACCURACY-000"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-ACCURACY-000-corpus-scorer-contract"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
pr = 5099
merge_sha = "eee9866d94a53c336971689c74d1126042619655"
acceptance = "Freeze the dense SLM and BitNet corpus/scorer version contract before large-corpus expansion: corpus IDs, seed generation rules, expected-output provenance, normalization rules, scoring schema, scorer self-tests, and version fields in receipts."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_validate -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/quality/**",
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 eval corpora and scorers have explicit version, seed, normalization, and expected-output provenance contracts.",
]
must_not_claim = [
  "The scorer contract proves broad model quality.",
  "Dense and BitNet corpora are interchangeable.",
]

[[work_item]]
id = "M4-ACCURACY-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-ACCURACY-001-dense-100"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-ACCURACY-000"]
pr = 5112
merge_sha = "5514215c2f317d232d82f8483633e436eb3b8ba5"
acceptance = "Expand the seeded dense SLM mechanical eval corpus to 100 deterministic cases across arithmetic, table QA, JSON/schema, extraction, sorting, rewrite, classification, summary-keyword, and instruction-following families without required LLM judging."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_validate -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/quality/**",
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "The dense M4 eval corpus covers 100 deterministic mechanically scored cases.",
]
must_not_claim = [
  "The 100-case corpus is a broad model-quality benchmark.",
  "The corpus proves BitNet behavior.",
]

[[work_item]]
id = "M4-ACCURACY-002"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-ACCURACY-002-dense-500"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-ACCURACY-001"]
pr = 5119
merge_sha = "23eca8792e12946a0bdb99c9aa4d4be42436617c"
acceptance = "Expand the seeded dense SLM corpus to 500 deterministic cases after 100-case scoring is stable, keeping all scoring mechanical and reproducible."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_validate -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/quality/**",
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "The dense M4 eval corpus covers 500 deterministic mechanically scored cases.",
]
must_not_claim = [
  "The 500-case corpus is a broad model-quality benchmark.",
  "The corpus proves BitNet behavior.",
]

[[work_item]]
id = "M4-ACCURACY-003"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-ACCURACY-003-stop-template-scoring"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-ACCURACY-001"]
pr = 5128
merge_sha = "39d81c8062b8cb0e33a256768363fddd2a70489e"
acceptance = "Fix dense SLM stop-token, prompt-template, normalization, and scoring misses found by the expanded corpus while preserving deterministic scoring and receipt schema."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_validate -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/quality/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-tokenizers/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "Expanded dense SLM eval scoring handles the documented stop/template/normalization cases.",
]
must_not_claim = [
  "Scoring fixes prove broad model quality.",
  "Dense scoring fixes prove BitNet behavior.",
]

[[work_item]]
id = "M4-ACCURACY-004"
status = "merged"
pr = 5141
branch = "codex/apple-m4-inference-excellence/M4-ACCURACY-004-task-family-report"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-ACCURACY-002", "M4-ACCURACY-003"]
acceptance = "Publish dense SLM pass rates by deterministic task family for every supported M4 model identity, with exact model/tokenizer/backend/fallback context and no broad quality claim."
commands = [
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/slm-eval-v2/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "Supported dense M4 models have task-family pass-rate reports for the deterministic corpus.",
]
must_not_claim = [
  "Task-family pass rates are broad model benchmarks.",
  "Dense task-family reports prove BitNet behavior.",
]

[[work_item]]
id = "M4-ACCURACY-005"
status = "merged"
pr = 5154
branch = "codex/apple-m4-inference-excellence/M4-ACCURACY-005-failure-taxonomy"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-ACCURACY-004"]
acceptance = "Add mechanical dense SLM failure taxonomy fields for formatting, factual/table, extraction, refusal, timeout, schema, and normalization failures so regressions can distinguish quality drift from harness drift."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_validate -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/quality/**",
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "Dense SLM eval receipts classify mechanical failure types for regression triage.",
]
must_not_claim = [
  "Failure taxonomy is a broad semantic quality judge.",
  "Dense taxonomy proves BitNet behavior.",
]

[[work_item]]
id = "M4-ACCURACY-006"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-ACCURACY-006-dense-500-live"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-ACCURACY-005"]
pr = 5243
merge_sha = "7246c7be632c321eabbd440f925776a441f0a177"
acceptance = "Run and publish full 500-case dense SLM eval-v2 answer-corpus receipts for every supported M4 dense model identity, preserving exact model/tokenizer/backend/fallback context, task-family pass rates, failure taxonomy, generated text, and token IDs."
commands = [
  "target/release/bitnet --device apple-m4-cpu-neon answer-corpus --model <verified-cache-path>/<model-file>.gguf --model-id <model-id> --corpus ci/quality/apple-m4-slm-eval-seeded-corpus-v2.yaml --json-out ci/hardware/apple-m4-mac-mini/<date>/slm-eval-v2/<model-id>/answer-corpus.json --per-prompt-timeout-seconds 240",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/slm-eval-v2/<model-id>/summary.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/slm-eval-v2/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Supported dense M4 models have full 500-case deterministic eval-v2 receipts for the recorded identities.",
]
must_not_claim = [
  "500-case dense receipts are broad model benchmarks.",
  "500-case dense receipts prove BitNet behavior.",
]

[[work_item]]
id = "M4-ACCURACY-007"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-ACCURACY-007-dense-500-history"
pr = 5285
merge_sha = "cd36bfb3c4d18be23a1e0e0f38a624f13407f518"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-ACCURACY-006"]
acceptance = "Run a second matching full 500-case dense SLM eval-v2 refresh for every supported M4 dense model identity and update dashboards so the 500-case dense eval groups have comparable repeat-history before trend or envelope claims use them."
commands = [
  "target/release/bitnet --device apple-m4-cpu-neon answer-corpus --model <verified-cache-path>/<model-file>.gguf --model-id <model-id> --corpus ci/quality/apple-m4-slm-eval-seeded-corpus-v2.yaml --json-out ci/hardware/apple-m4-mac-mini/<date>/slm-eval-v2/<model-id>/answer-corpus.json --per-prompt-timeout-seconds 240",
  "target/release/bitnet mac regression ci/hardware/apple-m4-mac-mini/<date>/slm-eval-v2/<model-id>/summary.json --baseline ci/hardware/apple-m4-mac-mini/<baseline-date>/slm-eval-v2/<model-id>/summary.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/slm-eval-v2/**",
  "ci/hardware/apple-m4-mac-mini/**/report-refresh/**",
  "ci/hardware/apple-m4-mac-mini/**/regression-dashboard/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Full 500-case dense M4 eval-v2 groups have comparable matching-history receipts for the recorded identities.",
]
must_not_claim = [
  "Repeat-history receipts are broad model benchmarks.",
  "Dense repeat-history receipts prove BitNet behavior.",
]

[[work_item]]
id = "M4-CANARY-001"
status = "merged"
pr = 5584
head_sha = "40b0e9fe053c52216a83decf611fd6f59171c45a"
merge_sha = "b8adc15da2ea0f8626faff193f3afadb2322975e"
branch = "codex/apple-m4-inference-excellence/M4-CANARY-001-golden-token-traces"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-ACCURACY-000", "M4-REPRO-003"]
acceptance = "Add a small dense SLM plus BitNet golden-token canary set recording prompt text, template identity, tokenized input IDs, generated token IDs/text, stop reason, sampler configuration, backend, fallback state, and exact artifact/tokenizer identity for fast drift localization."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_validate -- --nocapture",
  "target/release/bitnet mac receipts-check <canary-receipt.json> --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/quality/**",
  "ci/hardware/apple-m4-mac-mini/**/canaries/**",
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "M4 dense SLM and BitNet canaries record golden token traces for fast drift localization.",
]
must_not_claim = [
  "Canaries certify full corpus quality.",
  "Dense canaries prove BitNet behavior.",
]

[[work_item]]
id = "M4-DENSE-REF-000"
status = "merged"
pr = 5640
head_sha = "7f3736afb8b435dfba7d124704fc505b6a0f4149"
merge_sha = "8938b70f6e4a20c7164de7acbd2e21a46360021a"
branch = "codex/apple-m4-inference-excellence/M4-DENSE-REF-000-reference-receipt-contract"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-CANARY-001", "M4-REPRO-003", "M4-REPRO-004"]
acceptance = "Define and validate the dense SLM reference-runner vs Rust M4 comparison receipt contract before committing live comparison evidence: artifact kind, supported Qwen identity fields, reference-runner identity and command shape, prompt template and tokenizer authority, Rust generated token IDs, generated text, mechanical scores, summary totals, token-ID availability status, and claim-boundary flags."
commands = [
  "cargo test --locked -p bitnet-cli dense_slm_reference_vs_rust -- --nocapture",
  "cargo build --locked -p bitnet-cli --release",
  "target/release/bitnet mac receipts-check <dense-reference-vs-rust-fixture.json> --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Dense SLM reference-vs-Rust comparison receipts have a validated schema and claim-boundary contract.",
]
must_not_claim = [
  "Receipt-contract fixtures are live dense SLM runtime proof.",
  "Dense reference-vs-Rust validation proves BitNet behavior.",
  "Reference-only output proves Rust M4 behavior.",
  "The receipt contract is a broad model-quality benchmark.",
]

[[work_item]]
id = "M4-DENSE-REF-001"
status = "merged"
pr = 5653
head_sha = "4d2ae8b071ddb6b3cc966f76bdb5326a518ad96c"
merge_sha = "e0f16fc8e47ffac7fa7858f604b892bb43b1e088"
branch = "codex/apple-m4-inference-excellence/M4-DENSE-REF-001-reference-vs-rust"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-ACCURACY-001", "M4-CANARY-001", "M4-DENSE-REF-000", "M4-REPRO-004"]
acceptance = "Add a bounded dense SLM reference-runner vs Rust M4 comparison for supported Qwen model identities, recording prompt template, token IDs where available, generated text, mechanical scores, and deltas so Rust/template/tokenizer regressions can be separated from model behavior."
commands = [
  "cargo build --locked -p bitnet-cli --release",
  "Reference runner prompt-suite command for each supported dense Qwen identity",
  "target/release/bitnet --device apple-m4-cpu-neon mac validate --model-id <model-id> --json-out ci/hardware/apple-m4-mac-mini/<date>/slm-reference-vs-rust/<model-id>/summary.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/slm-reference-vs-rust/<model-id>/summary.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/quality/**",
  "ci/hardware/apple-m4-mac-mini/**/slm-reference-vs-rust/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Supported dense M4 SLM outputs are compared against a bounded reference-runner control for the recorded model identities.",
]
must_not_claim = [
  "Dense reference evidence proves BitNet behavior.",
  "Reference-only output proves Rust M4 behavior.",
  "The comparison is a broad model-quality benchmark.",
]

[[work_item]]
id = "M4-DENSE-CHAT-001"
status = "merged"
pr = 5675
head_sha = "b0f572012a2e332eebd025a57117673c0270a0d6"
merge_sha = "13b5eea5b0c8155ed4f32ebb74c3936b6646ab6d"
branch = "codex/apple-m4-inference-excellence/M4-DENSE-CHAT-001-cli-chat-conformance"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-CANARY-001", "M4-REPRO-003"]
acceptance = "Prove dense SLM CLI ask/chat conformance on M4 for supported model identities: prompt template and stop behavior, bounded multi-turn history, timeout/cancel behavior, per-turn receipts, generated text, token IDs, backend, fallback state, and model/tokenizer identity."
commands = [
  "cargo build --locked -p bitnet-cli --release",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_chat -- --nocapture",
  "target/release/bitnet --device apple-m4-cpu-neon mac chat-smoke --model-id <model-id> --json-out ci/hardware/apple-m4-mac-mini/<date>/slm-chat/<model-id>/chat-smoke.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/slm-chat/<model-id>/chat-smoke.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/slm-chat/**",
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Supported dense M4 ask/chat routes have bounded CLI conformance receipts.",
]
must_not_claim = [
  "Dense chat conformance proves BitNet chat behavior.",
  "CLI conformance is broad model quality proof.",
]

[[work_item]]
id = "M4-ROBUSTNESS-001"
status = "merged"
pr = 5699
head_sha = "150459610c0e6166c502662668ec6c2ced860288"
merge_sha = "7578f1de8ded04742fd491174b813789ac6ebbdc"
branch = "codex/apple-m4-inference-excellence/M4-ROBUSTNESS-001-negative-corpus"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-ACCURACY-007", "M4-BITNET-EX-012", "M4-CANARY-001", "M4-REPRO-003"]
acceptance = "Add a dense SLM and BitNet negative/robustness eval lane with false-premise, ambiguous, instruction-conflict, prompt-injection-style, format-trap, and unsupported-request cases, mechanically scored and reported separately by model family without required LLM judging."
commands = [
  "target/release/bitnet mac eval --suite m4-robustness --dry-run --json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/robustness/summary.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/quality/**",
  "ci/hardware/apple-m4-mac-mini/**/robustness/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "M4 dense SLM and BitNet robustness/negative cases are tracked with mechanical scoring and separate receipt families.",
]
must_not_claim = [
  "Robustness cases prove broad safety, alignment, or factuality.",
  "Dense SLM robustness evidence proves BitNet robustness.",
]

[[work_item]]
id = "M4-BENCH-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-BENCH-001-profile-contract"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
pr = 5288
merge_sha = "cc0400a3fddb5af5bbca18b6bf8abdae131ffe89"
acceptance = "Ensure the M4 benchmark profile contract records cold load, tokenizer load, prompt tokenization, prefill/input tokens per second, TTFT, output/decode tokens per second, sampling overhead, total wall time, peak memory, and memory drift for the supported profile set."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_benchmark -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "The M4 benchmark contract records the full operator timing and memory metric set.",
]
must_not_claim = [
  "Any new speed or memory result exists before a live receipt is recorded.",
  "The contract is a broad Apple Silicon benchmark.",
]

[[work_item]]
id = "M4-BENCH-004"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-BENCH-004-environment-preflight"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-REPRO-001"]
acceptance = "Add benchmark environment preflight evidence for comparable M4 timing runs: git/binary identity, macOS build, SoC, memory pressure, thermal pressure when available, power state, disk/cache state, model cache root, background-load notes, and skipped/invalid comparison reasons."
commands = [
  "target/release/bitnet mac benchmark-preflight --json",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_benchmark -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 benchmark reports include environment preflight fields and invalid-comparison reasons.",
]
must_not_claim = [
  "Environment preflight proves a speed result.",
  "Preflight makes nonmatching runs comparable.",
]

[[work_item]]
id = "M4-BENCH-002"
status = "merged"
pr = 5887
merge_sha = "7b8c7ad8b8594d376fb08ac94089363781bab1ea"
branch = "codex/apple-m4-inference-excellence/M4-BENCH-002-p50-p90-p99"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BENCH-001", "M4-BENCH-004", "M4-BENCH-007"]
acceptance = "Publish p50/p90/p99 and min/max benchmark summaries for all supported dense M4 profiles and model identities, with exact matching identity context."
commands = [
  "target/release/bitnet --device apple-m4-cpu-neon mac benchmark --model-id <model-id> --profile short_prompt_16_out --profile short_prompt_64_out --profile long_prompt_16_out --profile long_prompt_128_out --profile context_1k --profile context_4k --profile resident_25 --profile resident_50 --profile resident_100 --json-out ci/hardware/apple-m4-mac-mini/<date>/slm-benchmark-v2/<model-id>/summary.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/slm-benchmark-v2/<model-id>/summary.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/slm-benchmark-v2/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "Supported dense M4 benchmark receipts include p50/p90/p99 and min/max summaries for the recorded profile set.",
]
must_not_claim = [
  "The receipts are broad Apple Silicon benchmarks.",
  "The receipts prove BitNet performance.",
]

[[work_item]]
id = "M4-BENCH-009"
status = "merged"
pr = 5917
merge_sha = "014885a85dc13c6c11b19e4afe9ce6fc24b89832"
branch = "codex/apple-m4-inference-excellence/M4-BENCH-009-profile-timeout-enforcement"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BENCH-007", "M4-BENCH-008"]
acceptance = "Enforce the calibrated M4 dense SLM benchmark profile timeout rules in `bitnet mac benchmark`, write a profile timeout receipt when a child warm-session profile exceeds its boundary, validate that receipt, and keep timed-out profiles invalid for envelope comparison before M4-BENCH-005 publishes live variance envelopes."
commands = [
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli benchmark_profile -- --nocapture",
  "cargo build --release --locked -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet",
  "target/release/bitnet --device apple-m4-cpu-neon mac benchmark --model-id qwen2.5-0.5b-instruct-q8_0 --profile short_prompt_16_out --repeat 2 --quiet --json-out ci/hardware/apple-m4-mac-mini/<date>/benchmark-timeout-enforcement/harness-smoke.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/benchmark-timeout-enforcement/harness-smoke.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/mac.rs",
  "ci/hardware/apple-m4-mac-mini/**/benchmark-timeout-enforcement/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "M4 dense SLM benchmark profiles now execute behind calibrated child-process timeout enforcement.",
  "Timed-out dense benchmark profiles write a profile timeout receipt and are invalid for envelope comparison.",
]
must_not_claim = [
  "Timeout enforcement publishes the final dense variance envelope.",
  "Timeout enforcement proves a speedup.",
  "Timeout enforcement proves BitNet timing variance.",
]

[[work_item]]
id = "M4-BENCH-005"
status = "merged"
pr = 6023
merge_sha = "419e56d9662ef9b599cc1258976470067fe5c0e3"
branch = "codex/apple-m4-inference-excellence/M4-BENCH-005-repeatability-variance"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BENCH-002", "M4-BENCH-008", "M4-BENCH-009", "M4-BENCH-010"]
acceptance = "Publish M4 dense SLM benchmark repeatability and variance envelopes for the supported dense Qwen timing families, including run count, sample count, variance band, outlier handling, threshold derivation, and when timing drift is advisory instead of failure. BitNet one-shot and warm-session variance remains scoped to M4-BENCH-006."
commands = [
  "target/release/bitnet --device apple-m4-cpu-neon mac benchmark --repeat <n> --json-out ci/hardware/apple-m4-mac-mini/<date>/benchmark-variance/summary.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/benchmark-variance/summary.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/benchmark-variance/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "M4 dense SLM benchmark comparisons include repeatability and variance envelopes for recorded dense Qwen timing families.",
]
must_not_claim = [
  "Variance envelopes are broad Apple Silicon benchmarks.",
  "Variance envelopes prove a speedup.",
  "Dense SLM variance envelopes prove BitNet timing variance.",
]

[[work_item]]
id = "M4-BENCH-010"
status = "merged"
pr = 5929
merge_sha = "86068afee5e279eea06c4e7b879ef6fb3ece8a7a"
branch = "codex/apple-m4-inference-excellence/M4-BENCH-010-timeout-aware-variance"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BENCH-008", "M4-BENCH-009"]
acceptance = "Make `bitnet mac benchmark --repeat` produce timeout-aware dense benchmark aggregate receipts when one or more calibrated profiles time out: include timeout profiles as `invalid_for_comparison` entries in child `apple_m4_slm_benchmark_v2` summaries, propagate invalid-comparison reasons into `apple_m4_benchmark_variance_v1`, skip timed-out profiles from timing/throughput samples, validate child and aggregate receipts, and keep M4-BENCH-005 blocked until the live dense variance run can publish without aborting."
commands = [
  "cargo check --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli benchmark_profile -- --nocapture",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli timeout_profile -- --nocapture",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli timeout_aware -- --nocapture",
  "cargo build --release --locked -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet",
  "target/release/bitnet --device apple-m4-cpu-neon mac benchmark --model-id qwen2.5-0.5b-instruct-q8_0 --profile short_prompt_16_out --repeat 2 --quiet --json-out ci/hardware/apple-m4-mac-mini/<date>/benchmark-timeout-aware-variance/harness-smoke.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/benchmark-timeout-aware-variance/harness-smoke.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/mac.rs",
  "ci/hardware/apple-m4-mac-mini/**/benchmark-timeout-aware-variance/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "M4 dense SLM benchmark variance receipts can record timeout profiles as invalid-for-comparison instead of aborting the aggregate.",
]
must_not_claim = [
  "Timeout-aware aggregation publishes the final dense variance envelope.",
  "Timeout-aware aggregation proves a speedup.",
  "Timeout-aware aggregation proves BitNet timing variance.",
]

[[work_item]]
id = "M4-BENCH-008"
status = "merged"
pr = 5902
merge_sha = "1cf5aedda58ef50919bdb943778aca4fe10a51f3"
branch = "codex/apple-m4-inference-excellence/M4-BENCH-008-repeat-harness"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BENCH-002"]
acceptance = "Implement the repeat-run M4 benchmark variance harness required before M4-BENCH-005 can publish live variance evidence: expose `bitnet mac benchmark --repeat <n>` or a documented equivalent, write a benchmark-variance receipt with run count, sample count, variance band, outlier handling, threshold derivation, invalid-comparison reasons, model/profile identity, backend/fallback state, and receipt validation/tests."
commands = [
  "target/release/bitnet mac benchmark --help | rg -- '--repeat'",
  "target/release/bitnet --device apple-m4-cpu-neon mac benchmark --model-id qwen2.5-0.5b-instruct-q8_0 --profile short_prompt_16_out --repeat 2 --json-out ci/hardware/apple-m4-mac-mini/<date>/benchmark-variance/harness-smoke.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/benchmark-variance/harness-smoke.json --json",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_benchmark -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/hardware/apple-m4-mac-mini/**/benchmark-variance/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The M4 benchmark CLI has a repeat-run variance harness and receipt validator ready for live variance evidence.",
]
must_not_claim = [
  "The repeat harness itself publishes the final dense or BitNet variance envelope.",
  "A two-repeat harness smoke is a broad performance benchmark.",
]

[[work_item]]
id = "M4-BENCH-006"
status = "merged"
pr = 6033
branch = "codex/apple-m4-inference-excellence/M4-BENCH-006-bitnet-variance"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BENCH-005", "M4-BITNET-EX-003", "M4-BITNET-EX-004"]
acceptance = "Publish explicit BitNet one-shot and warm-session repeatability/variance receipts for the accepted artifact/tokenizer identity, including run count, sample count, timeout-stage accounting, variance band, outlier handling, and advisory-vs-failure thresholds."
commands = [
  "target/release/bitnet --device apple-m4-cpu-neon mac bitnet-benchmark --model-id microsoft-bitnet-b1.58-2B-4T-i2s --model-path models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --repeat <n> --progress --json-out ci/hardware/apple-m4-mac-mini/<date>/bitnet-benchmark-variance/summary.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/bitnet-benchmark-variance/summary.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/mac.rs",
  "ci/hardware/apple-m4-mac-mini/**/bitnet-benchmark-variance/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The accepted BitNet M4 one-shot and warm routes have bounded repeatability and variance receipts.",
]
must_not_claim = [
  "BitNet variance receipts are broad Apple Silicon benchmarks.",
  "BitNet variance receipts prove dense SLM behavior.",
]

[[work_item]]
id = "M4-BENCH-003"
status = "merged"
pr = 6047
branch = "codex/apple-m4-inference-excellence/M4-BENCH-003-regression-refresh"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BENCH-005", "M4-BENCH-006", "M4-EXCELLENCE-003"]
acceptance = "Wire benchmark summaries into matching-identity regression comparisons so timing and memory drift are reported separately from quality and identity failures."
commands = [
  "bitnet mac regression <current.json> --baseline <matching-baseline.json>",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 benchmark regression reports separate identity, timing, memory, and quality drift for matching receipts.",
]
must_not_claim = [
  "Regression wiring proves a new speedup.",
  "Regression wiring applies to mismatched identities.",
]

[[work_item]]
id = "M4-BITNET-REG-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-REG-001-warm-regression"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-EXCELLENCE-002", "M4-EXCELLENCE-004"]
pr = 5299
merge_sha = "173a59055f11a5677712942abc2a79358d1cf307"
acceptance = "Add direct `bitnet mac regression --baseline` support for `bitnet_apple_m4_warm_session` receipts, matching accepted artifact/tokenizer identity, backend, fallback state, prompt set, warm profile, timeout policy, and receipt schema before reporting drift."
commands = [
  "bitnet mac regression ci/hardware/apple-m4-mac-mini/<date>/bitnet-productization/variable-warm-session.json --baseline ci/hardware/apple-m4-mac-mini/<baseline-date>/bitnet-productization/variable-warm-session.json",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_regression -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "BitNet M4 warm-session receipts can be compared directly by matching identity through mac regression.",
]
must_not_claim = [
  "Warm regression support enables BitNet chat or serve.",
  "Warm regression support makes mismatched receipts comparable.",
]

[[work_item]]
id = "M4-BITNET-EX-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-EX-001-bitnet-100"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
merge_sha = "17c25469420d92bc0fee8e10b1fdafc407ffa4fb"
acceptance = "Add a BitNet-specific 100-case deterministic corpus for the accepted artifact/tokenizer identity, mechanically scored and separate from dense SLM Qwen evidence."
commands = [
  "target/release/bitnet --device apple-m4-cpu-neon answer-corpus --model models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --corpus ci/quality/<bitnet-corpus>.yaml --json-out ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval/answer-corpus.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval/answer-corpus.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/quality/**",
  "ci/hardware/apple-m4-mac-mini/**/bitnet-eval/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The accepted BitNet artifact has a BitNet-specific 100-case deterministic M4 eval corpus.",
]
must_not_claim = [
  "Dense Qwen evidence proves BitNet behavior.",
  "The corpus enables BitNet chat or serve.",
]

[[work_item]]
id = "M4-BITNET-EX-002"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-EX-002-reference-vs-rust"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-EX-001"]
pr = 5306
merge_sha = "058ae7fa08a3537f454f467d4ae5f5e6f0bce895"
acceptance = "Compare reference-runner and Rust M4 BitNet answers for the accepted artifact/tokenizer identity, recording prompt template, token IDs where available, generated text, and mechanical scoring deltas."
commands = [
  "$HOME/.cache/bitnet_cpp/build/bin/llama-cli -m models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --override-kv tokenizer.ggml.pre=str:llama-bpe --no-mmap -n <case.max_new_tokens> --temp 0 --top-k 1 --top-p 1 --min-p 0 --seed 42 --no-display-prompt --no-warmup -p 'User: <case.question><|eot_id|>Assistant:'",
  "cargo run --locked -p bitnet-cli -- mac receipts-check ci/hardware/apple-m4-mac-mini/2026-05-17T0810Z/bitnet-eval/answer-corpus-reference-comparison.json --json",
  "cargo test --locked -p bitnet-cli bitnet_reference_comparison_records_matched_text_and_tokens",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/quality/**",
  "ci/hardware/apple-m4-mac-mini/**/bitnet-eval/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Reference and Rust BitNet M4 answers are compared for the accepted artifact/tokenizer identity.",
]
must_not_claim = [
  "Reference-only output proves M4 Rust behavior.",
  "The comparison enables BitNet chat or serve.",
]

[[work_item]]
id = "M4-BITNET-EX-003"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-EX-003-one-shot-benchmark"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-EX-001"]
pr = 5309
merge_sha = "1d1e98c890dd18ee328b4258de055d590692a50c"
acceptance = "Publish a BitNet one-shot ask benchmark envelope for the accepted artifact/tokenizer identity, including load, tokenize, prefill, TTFT, decode, total wall, memory, generated text, token IDs, and fallback=false."
commands = [
  "target/release/bitnet --device apple-m4-cpu-neon mac bitnet-benchmark --model-id microsoft-bitnet-b1.58-2B-4T-i2s --model-path models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --json-out ci/hardware/apple-m4-mac-mini/<date>/bitnet-benchmark/summary.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/bitnet-benchmark/summary.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/bitnet-benchmark/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The accepted BitNet M4 one-shot route has a bounded benchmark envelope.",
]
must_not_claim = [
  "BitNet chat or serve is enabled.",
  "The envelope proves broad Apple Silicon performance.",
]

[[work_item]]
id = "M4-BITNET-EX-004"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-EX-004-warm-25-50-100"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-EXCELLENCE-002"]
pr = 5316
merge_sha = "1a6a438357fd70dfe251982c742eafeaf460d6b8"
acceptance = "Add BitNet variable warm-session 25/50/100 prompt soaks with per-turn receipts, aggregate timing/memory, determinism checks, timeout evidence, and chat/serve-disabled boundaries."
commands = [
  "target/release/bitnet --device apple-m4-cpu-neon mac bitnet-warm --model-id microsoft-bitnet-b1.58-2B-4T-i2s --model-path models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --profile resident_25 --profile resident_50 --profile resident_100 --timeout-seconds 1200 --progress --json-out ci/hardware/apple-m4-mac-mini/<date>/bitnet-warm/variable-warm-session.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/bitnet-warm/variable-warm-session.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/bitnet-warm/**",
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The accepted BitNet M4 warm route has bounded 25/50/100 prompt receipt evidence.",
]
must_not_claim = [
  "BitNet chat is enabled.",
  "BitNet serve is enabled.",
]

[[work_item]]
id = "M4-BITNET-EX-005"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-EX-005-progress-timeout"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-EX-004"]
pr = 5323
merge_sha = "64ba64379f05c022c047cc348086bcc770660cd1"
acceptance = "Harden BitNet operator progress, timeout, partial-failure, and repair UX for one-shot and warm-session routes, with failure receipts preserving backend, fallback, model, tokenizer, timeout stage, and partial generation."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_bitnet -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "BitNet M4 one-shot and warm routes report progress, timeout, and repair state with receipts.",
]
must_not_claim = [
  "BitNet chat is enabled.",
  "BitNet serve is enabled.",
]

[[work_item]]
id = "M4-BITNET-EX-006"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-EX-006-chat-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-EX-004", "M4-BITNET-EX-005"]
pr = 5330
merge_sha = "7f4d2adfdd8f668f121ecc9ab56008e8d10fdeef"
acceptance = "Enable BitNet chat only after variable warm receipts, determinism, timeout/failure receipts, streaming semantics, strict backend/fallback fields, and chat claim boundaries pass the gate."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_bitnet_chat -- --nocapture",
  "target/release/bitnet --device apple-m4-cpu-neon mac bitnet-chat-gate --model-id microsoft-bitnet-b1.58-2B-4T-i2s --warm-receipt ci/hardware/apple-m4-mac-mini/<date>/bitnet-warm/variable-warm-session.json --failure-receipt ci/hardware/apple-m4-mac-mini/<date>/bitnet-chat-gate/failure.json --streaming-receipt ci/hardware/apple-m4-mac-mini/<date>/bitnet-chat-gate/streaming-semantics.json --json-out ci/hardware/apple-m4-mac-mini/<date>/bitnet-chat-gate/gate.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/bitnet-chat-gate/**",
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "BitNet chat is enabled only after the recorded M4 chat gate passes.",
]
must_not_claim = [
  "BitNet serve is enabled.",
  "Full Metal BitNet inference works.",
]

[[work_item]]
id = "M4-BITNET-EX-007"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-EX-007-serve-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-EX-006"]
pr = 5339
merge_sha = "8590563daaeb00decd6fe8fdc3570e141c69933c"
acceptance = "Enable BitNet serve only after BitNet chat, streaming request/response semantics, health/ready endpoints, per-request receipts, timeout behavior, and failure-mode receipts pass on M4."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_bitnet_serve -- --nocapture",
  "target/release/bitnet --device apple-m4-cpu-neon mac bitnet-serve-gate --model-id microsoft-bitnet-b1.58-2B-4T-i2s --chat-receipt ci/hardware/apple-m4-mac-mini/<date>/bitnet-chat/chat-session.json --streaming-receipt ci/hardware/apple-m4-mac-mini/<date>/bitnet-serve-gate/streaming.json --failure-receipt ci/hardware/apple-m4-mac-mini/<date>/bitnet-serve-gate/failure.json --serve-check-receipt ci/hardware/apple-m4-mac-mini/<date>/bitnet-serve-gate/serve-check.json --json-out ci/hardware/apple-m4-mac-mini/<date>/bitnet-serve-gate/gate.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/bitnet-serve-gate/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-server/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "BitNet serve is enabled only after the recorded M4 serve gate passes.",
]
must_not_claim = [
  "BitNet serve is production hosting.",
  "Full OpenAI compatibility is proven.",
  "Full Metal BitNet inference works.",
]

[[work_item]]
id = "M4-BITNET-EX-008"
status = "merged"
pr = 5371
merge_sha = "850ad7073f5318ccc4d28188c9082fb1fa3e527a"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-EX-008-task-family-taxonomy"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-EX-001", "M4-BITNET-EX-002", "M4-BITNET-EX-005"]
acceptance = "Publish BitNet-specific task-family pass-rate and mechanical failure-taxonomy reports for the accepted artifact/tokenizer identity, separate from dense Qwen evidence and preserving reference-vs-Rust deltas where available."
commands = [
  "target/release/bitnet --device apple-m4-cpu-neon answer-corpus --model models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --corpus ci/quality/<bitnet-corpus>.yaml --json-out ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval/answer-corpus.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval/answer-corpus.json --json",
  "jq -e '.artifact_kind == \"apple_m4_bitnet_eval_task_family_pass_rates\" and .work_item == \"M4-BITNET-EX-008\" and .report_scope.bitnet_only == true and .claim_boundary.dense_slm_evidence_used == false and .accuracy.cases_total == 100 and .accuracy.cases_passed == 75 and (.task_families | length) == 10 and .reference_vs_rust.summary.mechanical_scoring_matches == 77' ci/hardware/apple-m4-mac-mini/2026-05-17T0810Z/bitnet-eval/task-family-pass-rates.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/bitnet-eval/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The accepted BitNet M4 artifact has task-family and failure-taxonomy reports for its deterministic corpus.",
]
must_not_claim = [
  "BitNet task-family reports are dense SLM evidence.",
  "BitNet task-family reports enable chat or serve.",
]

[[work_item]]
id = "M4-BITNET-EX-009"
status = "merged"
pr = 5386
merge_sha = "86dc319569946cd3896b5e8eacfbc5b155e9ed28"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-EX-009-eval-history"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-EX-008", "M4-BITNET-REG-001"]
acceptance = "Run a second matching BitNet deterministic eval refresh for the accepted artifact/tokenizer identity and decide, with recorded evidence, whether to stage a larger 250/500-case BitNet corpus before any broader BitNet quality envelope claim."
commands = [
  "target/release/bitnet --device apple-m4-cpu-neon answer-corpus --model models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --corpus ci/quality/apple-m4-bitnet-eval-seeded-corpus.yaml --json-out ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval/answer-corpus.json --per-prompt-timeout-seconds 300",
  "bitnet mac regression ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval/answer-corpus.json --baseline ci/hardware/apple-m4-mac-mini/<baseline-date>/bitnet-eval/answer-corpus.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/quality/**",
  "ci/hardware/apple-m4-mac-mini/**/bitnet-eval/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The accepted BitNet M4 eval path has matching-history receipts and a recorded larger-corpus decision.",
]
must_not_claim = [
  "BitNet repeat-history receipts enable chat or serve.",
  "The larger-corpus decision is itself a broad BitNet quality claim.",
]

[[work_item]]
id = "M4-BITNET-EX-010"
status = "merged"
pr = 5395
merge_sha = "e323fbc6a5838fe540c130c61821b0bf4fc9fbd0"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-EX-010-bitnet-250-corpus"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-EX-009"]
acceptance = "Define the deterministic 250-case BitNet corpus expansion staged by M4-BITNET-EX-009, focused on weak or variable families from the 100-case history, with accepted artifact/tokenizer identity metadata, corpus/scorer contract, mechanical scoring only, and no runtime quality claim."
commands = [
  "target/release/bitnet answer-corpus --dry-run --device apple-m4-cpu-neon --model missing.gguf --corpus ci/quality/apple-m4-bitnet-eval-seeded-corpus-250.yaml --json-out target/apple-m4-inference-excellence/bitnet-250-dry-run.json",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests answer_corpus_bitnet_250_dry_run_preserves_task_family_and_reference_schema -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/quality/**",
  "crates/bitnet-cli/tests/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The BitNet 250-case deterministic corpus and scorer contract are tracked and mechanically dry-run checked.",
]
must_not_claim = [
  "The 250-case corpus definition is a fresh runtime proof.",
  "The 250-case corpus definition is a broad BitNet quality envelope.",
  "BitNet 250-case planning enables chat or serve.",
]

[[work_item]]
id = "M4-BITNET-EX-011"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-EX-011-bitnet-250-run"
pr = 5452
head_sha = "17462920105f0b1e8aa738356bab0205da47089e"
merge_sha = "0c4691bd906b66a1057f61803c50837bd3c14e52"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-EX-010"]
acceptance = "Run the accepted Microsoft I2_S BitNet GGUF and accepted external tokenizer on the 250-case BitNet corpus through apple-m4-cpu-neon, publishing aggregate and child receipts, task-family rollup, timing, token IDs, fallback=false evidence, receipt validation, and either a compatible regression comparison or an explicit context-mismatch reason for the 100-case baseline."
commands = [
  "target/release/bitnet answer-corpus --device apple-m4-cpu-neon --model models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --corpus ci/quality/apple-m4-bitnet-eval-seeded-corpus-250.yaml --json-out ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval-250/answer-corpus.json --per-prompt-timeout-seconds 300",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval-250/answer-corpus.json --json",
  "target/release/bitnet mac regression ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval-250/answer-corpus.json --baseline ci/hardware/apple-m4-mac-mini/<baseline-date>/bitnet-eval/answer-corpus.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/mac.rs",
  "ci/quality/**",
  "ci/hardware/apple-m4-mac-mini/**/bitnet-eval-250/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The accepted BitNet M4 path has bounded 250-case receipts for the recorded artifact/tokenizer/backend identity.",
]
must_not_claim = [
  "250-case BitNet receipts are a broad BitNet quality benchmark.",
  "250-case BitNet receipts prove dense SLM behavior.",
  "250-case BitNet receipts enable chat, serve, or Metal.",
]

[[work_item]]
id = "M4-BITNET-EX-012"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-EX-012-bitnet-500-decision"
pr = 5456
head_sha = "28edc7140530a8af0e01afd6bd9031a330430032"
merge_sha = "0f7b63aabe19bcb29168a89d3039cc35bf5d8333"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-EX-011"]
acceptance = "Compare the 100-case BitNet matching history and 250-case BitNet evidence, including task-family variance, reference-vs-Rust deltas where available, timing, timeout behavior, fallback status, and failure taxonomy, then record whether to expand to 500 cases or repair corpus/scorer/template issues first."
commands = [
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval-250/larger-corpus-decision.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/mac.rs",
  "ci/hardware/apple-m4-mac-mini/**/bitnet-eval-250/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The BitNet 500-case expansion or repair decision is recorded from bounded 100-case and 250-case evidence.",
]
must_not_claim = [
  "A 500-case BitNet expansion is approved before M4-BITNET-EX-012 records it.",
  "The 250-case decision is a broad BitNet quality envelope.",
  "BitNet decision evidence enables chat, serve, or Metal.",
]

[[work_item]]
id = "M4-BITNET-EX-013"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-EX-013-bitnet-250-repair"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-EX-012", "M4-REPRO-003"]
acceptance = "Repair the BitNet 250-case corpus/scorer/template path called out by M4-BITNET-EX-012: numeric-tolerance format normalization, fixed-table QA expected-answer authority, rewrite normalization, and reference-vs-Rust 250-case comparison plumbing, while keeping the accepted artifact/tokenizer identity and mechanical scoring only."
commands = [
  "target/release/bitnet answer-corpus --dry-run --device apple-m4-cpu-neon --model missing.gguf --corpus ci/quality/apple-m4-bitnet-eval-seeded-corpus-250.yaml --json-out target/apple-m4-inference-excellence/M4-BITNET-EX-013-bitnet-250-dry-run.json",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests answer_corpus_bitnet_250 -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/quality/**",
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The BitNet 250-case corpus/scorer/template repair is staged and dry-run checked for the accepted artifact/tokenizer identity.",
]
must_not_claim = [
  "The repair is a fresh BitNet runtime proof.",
  "The repair approves 500-case BitNet expansion.",
  "The repair enables BitNet chat, serve, or Metal.",
]

[[work_item]]
id = "M4-BITNET-EX-014"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-EX-014-bitnet-250-repaired-run"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-EX-013"]
pr = 5795
acceptance = "Run the repaired BitNet 250-case corpus on the M4 Mac mini through the accepted Microsoft I2_S GGUF, accepted external tokenizer, and apple-m4-cpu-neon backend; publish aggregate and child receipts, generated token IDs, task-family pass rates, deterministic failure taxonomy, timing summary, reference-vs-Rust comparison where available, receipt validation, and an explicit new-baseline or context-mismatch statement."
commands = [
  "target/release/bitnet answer-corpus --device apple-m4-cpu-neon --model models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --corpus ci/quality/apple-m4-bitnet-eval-seeded-corpus-250.yaml --json-out ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval-250-repaired/answer-corpus.json --per-prompt-timeout-seconds 300",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval-250-repaired --json",
  "target/release/bitnet mac regression ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval-250-repaired/answer-corpus.json --baseline ci/hardware/apple-m4-mac-mini/<baseline-date>/bitnet-eval-250/answer-corpus.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/bitnet-eval-250-repaired/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The accepted BitNet artifact has a bounded repaired 250-case M4 CPU/NEON runtime receipt set.",
]
must_not_claim = [
  "One repaired 250-case run is enough matching history for a broad BitNet quality claim.",
  "The repaired 250-case run approves 500-case BitNet expansion.",
  "The repaired 250-case run enables BitNet chat, serve, full Metal, QK256, Neural Engine, or MPSGraph.",
]

[[work_item]]
id = "M4-BITNET-EX-015"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-BITNET-EX-015-bitnet-250-repaired-history"
pr = 6115
head_sha = "4e4a09936fb025f78134f715e3664e5a674d53c9"
merge_sha = "9ceb75a6b0d4244e096006e402f0f13c85ddc03f"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BITNET-EX-014", "M4-BENCH-006"]
acceptance = "Repeat the repaired BitNet 250-case M4 CPU/NEON run under the same artifact/tokenizer/backend/template identity, compare against the repaired 250-case baseline with matched_context=true, refresh task-family and dashboard trend evidence, and record the next decision: approve 500-case BitNet expansion, keep repairing, or freeze the current BitNet quality envelope."
commands = [
  "target/release/bitnet answer-corpus --device apple-m4-cpu-neon --model models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --corpus ci/quality/apple-m4-bitnet-eval-seeded-corpus-250.yaml --json-out ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval-250-repaired/answer-corpus.json --per-prompt-timeout-seconds 300",
  "target/release/bitnet mac regression ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval-250-repaired/answer-corpus.json --baseline ci/hardware/apple-m4-mac-mini/<baseline-date>/bitnet-eval-250-repaired/answer-corpus.json --json",
  "target/release/bitnet mac regression-dashboard --json-out target/apple-m4-inference-excellence/M4-BITNET-EX-015-regression-dashboard.json --markdown-out target/apple-m4-inference-excellence/M4-BITNET-EX-015-regression-dashboard.md --json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/bitnet-eval-250-repaired --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/bitnet-eval-250-repaired/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The repaired BitNet 250-case path has matching-history M4 CPU/NEON evidence for the recorded artifact/tokenizer/backend identity.",
]
must_not_claim = [
  "The repaired 250-case trend is broad BitNet quality evidence.",
  "Dense SLM evidence contributes to the BitNet repaired-history decision.",
  "The repaired-history decision enables BitNet chat, serve, full Metal, QK256, Neural Engine, or MPSGraph.",
]

[[work_item]]
id = "M4-OPS-UX-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-OPS-UX-001-evidence-command"
pr = 5460
head_sha = "3311bdd0d6e12c0fb1fa1567f14eeb7b53da1268"
merge_sha = "a08566cb34db58cad82ee26e160b686dacf32830"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-EXCELLENCE-003"]
acceptance = "Add an operator-facing M4 evidence summary that reports default model, supported models, cache state, disk pressure, last dense report, last BitNet report, current regressions, unsupported claims, and recommended next command without running live inference."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_evidence -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "The M4 operator evidence summary reports committed receipt state without live inference.",
]
must_not_claim = [
  "Evidence summary output is a new runtime proof.",
  "Dense and BitNet evidence are interchangeable.",
]

[[work_item]]
id = "M4-OPS-UX-002"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-OPS-UX-002-explain-open"
pr = 5478
head_sha = "3ce6f87063d344e89c74bb17dccb6f827c104a4e"
merge_sha = "594cf82b8936f40d90e7f740a5132967a753b135"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-OPS-UX-001"]
acceptance = "Add explanation/open affordances for report-refresh and regression-dashboard outputs so operators can see why a group is comparable, warning, failed, or insufficient_history."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_report_refresh -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 report-refresh and regression-dashboard commands explain comparable, warning, failed, and insufficient-history states.",
]
must_not_claim = [
  "Explanation affordances run live inference.",
  "Explanation affordances prove new quality or performance.",
]

[[work_item]]
id = "M4-OPS-UX-003"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-OPS-UX-003-doctor-bitnet"
pr = 5491
head_sha = "d1d8f9b9239a882ff0c83f35da0622bdb5a75a41"
merge_sha = "6cbc0a8ce08cdbc181e3d10be65ed166cf7e8917"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-OPS-UX-001"]
acceptance = "Extend M4 doctor/status UX to report dense SLM and BitNet readiness separately, including cache repair guidance, disk pressure, last matching receipts, disabled chat/serve state, and unsupported claim boundaries."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_doctor -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 doctor/status separates dense SLM and BitNet readiness for operators.",
]
must_not_claim = [
  "Doctor readiness enables BitNet chat or serve.",
  "Doctor readiness is a new runtime proof.",
]

[[work_item]]
id = "M4-OPS-SLO-001"
status = "merged"
pr = 6123
head_sha = "4f8504e2c0bdaee3f22384457893713dde244548"
merge_sha = "e2213309360ff8af4689035f02d09f90ed2af316"
branch = "codex/apple-m4-inference-excellence/M4-OPS-SLO-001-envelope-classes"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-ACCURACY-007", "M4-BENCH-005", "M4-BENCH-006", "M4-BITNET-EX-003", "M4-OPS-UX-003"]
acceptance = "Translate M4 receipts into operator-facing envelope classes per model family and route: interactive, advisory, batch, disabled, or unsupported, with max context/profile guidance, timing expectations, memory/disk expectations, and exact evidence date/identity."
commands = [
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "M4 operator docs classify supported routes by local operating envelope and evidence identity.",
]
must_not_claim = [
  "Envelope classes are broad quality or performance claims.",
  "A disabled BitNet route is enabled.",
]

[[work_item]]
id = "M4-CONTEXT-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-CONTEXT-001-long-context-guardrails"
pr = 6130
head_sha = "67e31a2ab752ef8c65f23db4be92acd6c7e7685f"
merge_sha = "ecd95c40bc155a64e1c7e8b3f85e982a2495f11d"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BENCH-002", "M4-OPS-SLO-001"]
acceptance = "Add long-context envelope guardrails for M4 CLI and server routes so requests beyond recorded dense SLM or BitNet context/profile evidence produce clear advisory, batch, disabled, or unsupported states with receipt fields instead of silent overclaiming."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_context -- --nocapture",
  "cargo test --locked -p bitnet-server -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-server/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 CLI and server routes enforce or explain long-context limits against recorded evidence envelopes.",
]
must_not_claim = [
  "Long-context guardrails prove new context-length quality.",
  "Guardrails make unsupported contexts supported.",
]

[[work_item]]
id = "M4-CONTEXT-HARNESS-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-CONTEXT-HARNESS-001-long-context-command-contract"
pr = 6137
head_sha = "9c04e76f4f4ef5a12f7a862ba7fa491f3dac44bd"
merge_sha = "1280d5b06715954d5980bc8812b1bb873effd485"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-CONTEXT-001", "M4-BENCH-007", "M4-REPRO-003"]
acceptance = "Align the tracker-facing long-context command surface before live proof: register `bitnet mac eval --suite m4-long-context` as a mechanical dry-run corpus contract with receipt validation and explicit no-proof boundaries, and make `bitnet mac benchmark --profile context` expand to the measured `context_1k` and `context_4k` benchmark profiles without creating a new benchmark contract profile."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_eval_long_context -- --nocapture",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_eval_robustness -- --nocapture",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_benchmark_accepts_context_profile_alias_before_release_gate -- --nocapture",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_eval_help_documents_robustness_dry_run -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/quality/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "ci/hardware/apple-m4-mac-mini/**/context/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "The M4 long-context command contract and context benchmark alias are registered and receipt-checked for future proof runs.",
]
must_not_claim = [
  "M4 long-context quality or timing proof has been produced.",
  "Long-context proof applies to untested context lengths or model identities.",
  "Dense SLM long-context evidence proves BitNet long-context behavior.",
]

[[work_item]]
id = "M4-CONTEXT-002"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-CONTEXT-002-long-context-proof"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-CONTEXT-001", "M4-BENCH-007", "M4-REPRO-003", "M4-CONTEXT-HARNESS-001"]
pr = 6145
merge_sha = "e3370f16c7c876a1cca732f95d7121e83271c95b"
acceptance = "Run and publish long-context M4 quality and timing receipts for supported dense SLMs and the accepted BitNet artifact where supported, covering retrieval/copy, table extraction, late-context instruction following, truncation behavior, input throughput, TTFT, decode throughput, memory, fallback state, and explicit unsupported-context boundaries."
commands = [
  "target/release/bitnet mac eval --suite m4-long-context --json-out ci/hardware/apple-m4-mac-mini/<date>/context/answer-corpus.json",
  "target/release/bitnet mac benchmark --profile context --json-out ci/hardware/apple-m4-mac-mini/<date>/context/benchmark.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/context/answer-corpus.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/quality/**",
  "ci/hardware/apple-m4-mac-mini/**/context/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 long-context behavior has explicit quality, timing, memory, and unsupported-boundary receipts for the tested identities.",
]
must_not_claim = [
  "Long-context proof applies to untested context lengths or model identities.",
  "Dense SLM long-context evidence proves BitNet long-context behavior.",
]

[[work_item]]
id = "M4-REPRO-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-REPRO-001-run-identity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
pr = 5502
merge_sha = "19dc1f11c8f85792d156ab7f466517540c347ccc"
acceptance = "Define and enforce a reusable M4 run-identity contract for eval, benchmark, warm-session, chat-gate, serve-gate, and dashboard receipts: machine_id, SoC, OS version, git commit, binary hash or build profile, command class, model ID, model SHA, tokenizer authority/SHA, prompt template hash, backend, fallback state, seed/corpus/profile identity, and timing source."
commands = [
  "cargo test --locked -p bitnet-receipts -- --nocapture",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_receipts_check -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 receipt families share a documented run-identity contract for reproducible comparisons.",
]
must_not_claim = [
  "Run-identity fields are new runtime proof.",
  "Run-identity fields make mismatched receipts comparable.",
]

[[work_item]]
id = "M4-REPRO-002"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-REPRO-002-artifact-provenance"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-REPRO-001"]
acceptance = "Publish and validate M4 supported-model provenance manifests that record artifact source, license or redistribution boundary, file size, SHA256, tokenizer authority/SHA, prompt template identity, local cache path, symlink target where used, and repair command for each supported dense SLM and accepted BitNet artifact."
commands = [
  "target/release/bitnet model verify <model-id> --json",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests model_verify -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "Supported M4 model artifacts have provenance and cache-verification manifests.",
]
must_not_claim = [
  "A provenance manifest proves runtime quality or performance.",
  "A candidate artifact is supported before its M4 quality gates pass.",
]

[[work_item]]
id = "M4-REPRO-003"
status = "merged"
pr = 5549
head_sha = "ce0cbd5fe5824525b527b71b2a73f432950b1aa2"
merge_sha = "7ef399bdc5692374a2344b2e76b624bfbea275ba"
branch = "codex/apple-m4-inference-excellence/M4-REPRO-003-template-registry"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-REPRO-001"]
acceptance = "Add prompt-template and stop-sequence registry evidence so dense SLM and BitNet receipts record the exact template family, template hash, tokenizer authority, stop criteria, and generation parameter set used by eval, ask, chat, warm, and serve paths."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_validate -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/quality/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-tokenizer-model-core/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 receipts identify prompt template, stop criteria, and generation parameters used for comparison.",
]
must_not_claim = [
  "Template identity alone proves answer quality.",
  "Dense and BitNet templates are interchangeable.",
]

[[work_item]]
id = "M4-REPRO-004"
status = "merged"
pr = 5610
head_sha = "dcbba6b2c52bd2f49d9709044fc8541b015ed050"
merge_sha = "585ad99d26a00eefd195838fe68f3e5c1d757d56"
branch = "codex/apple-m4-inference-excellence/M4-REPRO-004-template-alias-validation"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-REPRO-003"]
acceptance = "Repair prompt-generation identity validation so canonical template aliases such as qwen2.5 and qwen25-chat compare as the same Qwen2.5 ChatML template while still rejecting genuinely different template families, unblocking M4 dense validation receipts that record prompt-generation identity."
commands = [
  "cargo fmt --check",
  "cargo test --locked -p bitnet-cli prompt_generation_identity -- --nocapture",
  "cargo build --locked -p bitnet-cli --release",
  "target/release/bitnet --device apple-m4-cpu-neon mac validate --model-id qwen2.5-0.5b-instruct-q8_0 --quiet --json-out target/apple-m4-inference-excellence/M4-REPRO-004-template-alias-validation/qwen25-template-alias.json",
  "target/release/bitnet mac receipts-check target/apple-m4-inference-excellence/M4-REPRO-004-template-alias-validation/qwen25-template-alias.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- check-no-panic-family --report-dir target/bitnet/reports",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "M4 prompt-generation identity validation accepts canonical aliases for the same Qwen2.5 ChatML template.",
]
must_not_claim = [
  "Template alias validation proves answer quality.",
  "Dense and BitNet templates are interchangeable.",
]

[[work_item]]
id = "M4-RECEIPT-001"
status = "merged"
pr = 5811
merge_sha = "c8cbe29a0bd21cc5cb77680df81224f404a1306a"
branch = "codex/apple-m4-inference-excellence/M4-RECEIPT-001-schema-compat"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-REPRO-001"]
acceptance = "Add receipt-schema compatibility and negative fixtures for M4 eval, benchmark, warm, chat-gate, serve-gate, dashboard, and failure receipts so missing identity fields, fallback ambiguity, malformed timing, absent token IDs, and unsupported claim fields fail validation clearly."
commands = [
  "cargo test --locked -p bitnet-receipts -- --nocapture",
  "target/release/bitnet mac receipts-check <fixture.json> --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 receipt validation has compatibility and negative fixtures for the excellence report families.",
]
must_not_claim = [
  "Receipt fixtures are live runtime proof.",
  "Receipt validation makes unsupported backends supported.",
]

[[work_item]]
id = "M4-GATE-HYGIENE-001"
status = "merged"
pr = 5815
branch = "codex/apple-m4-inference-excellence/M4-GATE-HYGIENE-001-no-panic-family"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-RECEIPT-001"]
acceptance = "Keep the repository gate surface clean for the M4 excellence lane by repairing the current check-no-panic-family failures without widening runtime or platform claims, documenting any allowlist decision, and ensuring receipt-core fixtures do not rely on unallowlisted unwrap/expect paths."
commands = [
  "cargo test --locked -p bitnet-receipts-core -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- check-no-panic-family --report-dir target/bitnet/reports",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-receipts-core/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The M4 excellence gate set includes a clean no-panic-family hygiene check.",
]
must_not_claim = [
  "No-panic hygiene is runtime quality or performance proof.",
  "No-panic hygiene changes dense SLM, BitNet, Metal, QK256, Neural Engine, MPSGraph, MacBook, or broad Apple Silicon support.",
]

[[work_item]]
id = "M4-STABILITY-HARNESS-001"
status = "merged"
pr = 6131
head_sha = "6dcf19ce033000e42628ead5ea6aa05e50c6d847"
merge_sha = "17288cc07e2f78e724b3a76d166cf493a9ed9a3b"
branch = "codex/apple-m4-inference-excellence/M4-STABILITY-HARNESS-001-mixed-model-profile"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BENCH-002", "M4-EXCELLENCE-003"]
acceptance = "Implement the `bitnet mac benchmark --profile mixed_model_switch` harness and receipt validation required by M4-STABILITY-001 before any mixed dense-model switch soak evidence is recorded: run one resident_25 child benchmark summary per supported dense M4 model identity, preserve per-model child receipts, validate cache/model identity separation, fallback=false backend selection, and memory summary fields, and keep the change harness-only without publishing live soak evidence."
commands = [
  "cargo test --locked -p bitnet-cli mac_receipts_check_accepts_mixed_model_switch_soak -- --nocapture",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_benchmark_accepts_mixed_model_switch_profile_before_release_gate -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/mac.rs",
  "crates/bitnet-cli/tests/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "ci/hardware/apple-m4-mac-mini/**/slm-soak/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The M4 benchmark CLI exposes and validates a harness for future mixed dense-model switch soak receipts.",
]
must_not_claim = [
  "The harness publishes or proves a live mixed-model switch soak.",
  "The harness proves BitNet behavior, BitNet chat or serve, full Metal, QK256, Neural Engine, MPSGraph, MacBook behavior, speedup, broad quality, or broad Apple Silicon performance.",
]

[[work_item]]
id = "M4-STABILITY-001"
status = "merged"
pr = 6134
merge_sha = "7832622eb2d10921f549d677ad594db88caeb1cc"
branch = "codex/apple-m4-inference-excellence/M4-STABILITY-001-mixed-model-soak"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BENCH-002", "M4-EXCELLENCE-003", "M4-STABILITY-HARNESS-001"]
acceptance = "Run and record a mixed dense-model switch soak across supported M4 dense SLM identities, proving cache reuse, model unload/reload behavior, fallback=false backend selection, bounded memory drift, and receipt separation per model identity."
commands = [
  "target/release/bitnet --device apple-m4-cpu-neon mac benchmark --profile mixed_model_switch --json-out ci/hardware/apple-m4-mac-mini/<date>/slm-soak/mixed-model-switch.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/slm-soak/mixed-model-switch.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/slm-soak/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Supported dense M4 model switching has bounded soak evidence for the recorded identities.",
]
must_not_claim = [
  "Mixed-model soak proves BitNet behavior.",
  "Mixed-model soak is a broad Apple Silicon benchmark.",
]

[[work_item]]
id = "M4-STABILITY-002"
status = "merged"
pr = 5839
merge_sha = "818446f0fb7eb7d4e1f92102a2718ed3fb104583"
branch = "codex/apple-m4-inference-excellence/M4-STABILITY-002-cache-disk-repair"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-REPRO-002", "M4-OPS-UX-003"]
acceptance = "Add cache and disk-pressure repair receipts for M4 operator flows, covering missing model, bad SHA, missing tokenizer, stale symlink, low free disk, prune dry-run, and repair guidance without deleting user data or running live inference by default."
commands = [
  "target/release/bitnet mac doctor --json --include-bitnet",
  "target/release/bitnet model prune --dry-run --json",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_doctor -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 doctor and prune flows explain cache and disk repair states without live inference by default.",
]
must_not_claim = [
  "Repair guidance deletes user data automatically.",
  "Doctor repair checks are runtime quality proof.",
]

[[work_item]]
id = "M4-STABILITY-003"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-STABILITY-003-trend-retention"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-STABILITY-001", "M4-STABILITY-002", "M4-EXCELLENCE-004"]
acceptance = "Define the scheduled M4 trend-retention policy for dense SLM and BitNet reports, including which receipts are retained, which summaries are regenerated, how stale identities age out, and when operator envelopes must be refreshed."
commands = [
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  ".github/workflows/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "M4 scheduled evidence has an explicit retention and stale-identity policy.",
]
must_not_claim = [
  "A schedule policy proves new runtime quality or performance.",
  "Stale identities remain comparable after they age out.",
]

[[work_item]]
id = "M4-RELIABILITY-001"
status = "merged"
pr = 6154
branch = "codex/apple-m4-inference-excellence/M4-RELIABILITY-001-recovery-drills"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-STABILITY-001", "M4-STABILITY-002", "M4-BITNET-EX-005"]
acceptance = "Add M4 recovery-drill receipts for interrupted generation, cancellation, timeout, interrupted receipt write, missing or corrupt cache, low disk, process restart, and retry guidance across dense SLM and BitNet one-shot or warm routes."
commands = [
  "target/release/bitnet --device apple-m4-cpu-neon mac reliability-drill --json-out ci/hardware/apple-m4-mac-mini/<date>/reliability-drills/summary.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/reliability-drills/summary.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/reliability-drills/**",
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "M4 dense SLM and BitNet one-shot or warm routes have bounded recovery-drill receipts.",
]
must_not_claim = [
  "Recovery drills prove service production readiness.",
  "Recovery drills enable BitNet chat or serve.",
]

[[work_item]]
id = "M4-OBS-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-OBS-001-trace-correlation"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-RECEIPT-001", "M4-RELIABILITY-001", "M4-SERVE-EX-002"]
pr = 6169
merge_sha = "911e7ce33e8f8f93f825140ce1f56e0ddfebbf85"
acceptance = "Add M4 observability correlation for CLI and server routes: trace IDs linking progress events, logs, per-run or per-request receipts, failure receipts, cancellation/timeout stages, and operator-facing diagnostics without leaking prompts or cache paths beyond the documented redaction policy."
commands = [
  "bitnet mac ask --trace --json-out ci/hardware/apple-m4-mac-mini/<date>/observability/ask.json <prompt>",
  "bitnet mac serve --trace --receipt-dir ci/hardware/apple-m4-mac-mini/<date>/observability/serve",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/observability/ask.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-server/**",
  "ci/hardware/apple-m4-mac-mini/**/observability/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 CLI and server diagnostics can correlate progress, logs, receipts, and failure stages for tested routes.",
]
must_not_claim = [
  "Trace correlation proves model quality or performance.",
  "Trace output may expose unredacted prompt, secret, or cache-path data.",
]

[[work_item]]
id = "M4-SERVE-EX-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-SERVE-EX-001-closeout"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-REPRO-001", "M4-BENCH-003"]
pr = 6165
acceptance = "Refresh dense SLM local-server conformance on M4 for supported model identities, including health, ready, one-shot request, streaming completion, per-request receipt export, timeout behavior, fallback=false backend fields, and service claim boundaries."
commands = [
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_serve -- --nocapture",
  "target/release/bitnet --device apple-m4-cpu-neon mac serve-smoke --model-id <model-id> --json-out ci/hardware/apple-m4-mac-mini/<date>/slm-serve/<model-id>/serve-smoke.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/slm-serve/<model-id>/serve-smoke.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/slm-serve/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-server/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "Supported dense M4 local-server routes have bounded conformance receipts.",
]
must_not_claim = [
  "The local server is production hosting.",
  "Full OpenAI compatibility is proven.",
  "Dense serve receipts prove BitNet serve.",
]

[[work_item]]
id = "M4-SERVE-EX-002"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-SERVE-EX-002-streaming-failure-semantics"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-SERVE-EX-001", "M4-BITNET-EX-007"]
pr = 6167
merge_sha = "3cfbaf7d051674e0080972ccbb6ed0d9d0c28b94"
acceptance = "Prove local-server streaming and failure semantics across dense SLM and enabled BitNet serve paths: partial token streaming, client cancellation, timeout stage, invalid request, missing cache, per-request receipt export, and no-response failure receipts."
commands = [
  "cargo test --locked -p bitnet-server -- --nocapture",
  "target/release/bitnet --device apple-m4-cpu-neon mac serve-failure-smoke --json-out ci/hardware/apple-m4-mac-mini/<date>/serve-failure-semantics/summary.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/serve-failure-semantics/summary.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/serve-failure-semantics/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-server/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 local-server streaming and failure semantics have bounded dense and BitNet receipt evidence.",
]
must_not_claim = [
  "The local server is production hosting.",
  "Full OpenAI compatibility is proven.",
]

[[work_item]]
id = "M4-SERVE-EX-003"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-SERVE-EX-003-local-safety-defaults"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-SERVE-EX-001"]
pr = 6171
merge_sha = "c345ed7d72620903d7aeb62eb6f2d9266be0e59f"
acceptance = "Document and test local-server safety defaults for the M4 appliance: localhost binding by default, explicit opt-in for wider binding, no telemetry, predictable CORS behavior, request-size limits, cache-path disclosure boundaries, and receipt redaction rules."
commands = [
  "cargo test --locked -p bitnet-server -- --nocapture",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_serve -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-server/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 local-server defaults are documented and tested for local appliance operation.",
]
must_not_claim = [
  "The local server is production hosting.",
  "The local server has undergone a full security audit.",
]

[[work_item]]
id = "M4-SERVE-EX-004"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-SERVE-EX-004-queue-backpressure"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-SERVE-EX-001", "M4-SERVE-EX-003", "M4-CONTEXT-001"]
pr = 6173
head_sha = "6eb57f141113ab1ee37c9098baaeddaf470a321e"
merge_sha = "f22d40c3124485f51ec90d6feefb66ff801143a7"
acceptance = "Add bounded local-server queue, backpressure, and resident-state evidence for the M4 appliance: concurrent local requests, queue limits, busy/timeout responses, resident model reuse, per-request receipts, and failure receipts without production-hosting or full OpenAI-compatibility claims."
commands = [
  "cargo test --locked -p bitnet-server -- --nocapture",
  "target/release/bitnet --device apple-m4-cpu-neon mac serve-backpressure-smoke --json-out ci/hardware/apple-m4-mac-mini/<date>/serve-backpressure/summary.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/serve-backpressure/summary.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/serve-backpressure/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-server/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 local-server queue, backpressure, and resident-state behavior have bounded local-appliance receipts.",
]
must_not_claim = [
  "The local server is production hosting.",
  "Queue/backpressure evidence proves full OpenAI compatibility.",
]

[[work_item]]
id = "M4-CI-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-CI-001-evidence-lanes"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-GATE-HYGIENE-001", "M4-RECEIPT-001", "M4-STABILITY-003"]
pr = 6175
head_sha = "86156087b0923d7a682cd6c5e197fa6492508d24"
merge_sha = "e01d5cc42dad096aee68f5bea66592b08a43256a"
acceptance = "Codify the M4 evidence CI lanes and retention contracts: generic PR model-free checks, advisory local M4 runs, scheduled M4 refresh, release-gate M4 refresh, artifact retention, dashboard generation, and explicit non-blocking behavior for hardware-only timing jobs."
commands = [
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  ".github/workflows/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "M4 evidence lanes and retention behavior are documented for PR, advisory, scheduled, and release workflows.",
]
must_not_claim = [
  "Hardware timing is required in generic PR CI.",
  "CI lane docs prove new runtime performance.",
]

[[work_item]]
id = "M4-SETUP-001"
status = "merged"
pr = 5854
branch = "codex/apple-m4-inference-excellence/M4-SETUP-001-first-run"
merge_sha = "8e06f84e526f18999610d1ebf022f34eb6dabe22"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-REPRO-002", "M4-STABILITY-002", "M4-OPS-UX-003"]
acceptance = "Prove the first-run M4 setup path for supported dense SLMs and the accepted BitNet artifact: discover model state, fetch or repair cache entries, verify SHA/tokenizer authority, report disk pressure, then run the documented smoke command with receipts or a clear non-runtime dry-run boundary."
commands = [
  "bitnet mac models",
  "bitnet model verify <model-id>",
  "bitnet mac doctor --include-bitnet",
  "bitnet mac smoke --model-id <supported-model-id>",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/setup/summary.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/hardware/apple-m4-mac-mini/**/setup/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "The M4 first-run setup path is receipt-backed for supported model identities and accepted BitNet artifacts.",
]
must_not_claim = [
  "First-run setup evidence proves broad answer quality.",
  "A dry-run setup receipt proves live inference completed.",
]

[[work_item]]
id = "M4-BENCH-007"
status = "merged"
pr = 5869
merge_sha = "80995ff7fdb0365ab1d21bffb69d67192dc7a116"
branch = "codex/apple-m4-inference-excellence/M4-BENCH-007-harness-calibration"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-BENCH-004", "M4-REPRO-001"]
acceptance = "Calibrate the M4 benchmark harness before using timing envelopes: record clock source and resolution, runner overhead, warm-up policy, sample discard policy, synthetic timing fixtures, profile timeout rules, and invalid-comparison reasons."
commands = [
  "bitnet mac benchmark --calibrate --json-out ci/hardware/apple-m4-mac-mini/<date>/benchmark/calibration.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/benchmark/calibration.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/hardware/apple-m4-mac-mini/**/benchmark/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "M4 benchmark harness overhead, timing source, warm-up, timeout, and invalid-comparison rules are calibrated before envelope refreshes.",
]
must_not_claim = [
  "Harness calibration proves model speed.",
  "Synthetic timing fixtures are real inference benchmarks.",
]

[[work_item]]
id = "M4-ROUTE-MATRIX-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-ROUTE-MATRIX-001-route-states"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-DENSE-CHAT-001", "M4-BITNET-EX-006", "M4-BITNET-EX-007", "M4-SERVE-EX-002", "M4-OPS-SLO-001"]
pr = 6177
head_sha = "998ced351da0bf94d3c365fb861f5b7cb4f2feed"
merge_sha = "bd0a2e3fdfa740645bf2b7e6d9768d46f5f65e6f"
acceptance = "Publish a route-state matrix for M4 dense SLM and BitNet paths covering ask, chat, warm session, serve, streaming, disabled, batch-only, and unsupported states, with the exact evidence item and receipt family required for each enabled state."
commands = [
  "bitnet mac status --json",
  "bitnet mac evidence --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 route availability is explicit by model family, command surface, evidence source, and disabled or unsupported state.",
]
must_not_claim = [
  "A route-state matrix enables a route without runtime receipts.",
  "Disabled BitNet chat or serve states are supported.",
]

[[work_item]]
id = "M4-WORKLOAD-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-WORKLOAD-001-source-promotion"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-DENSE-CHAT-001", "M4-BITNET-EX-006", "M4-SERVE-EX-002", "M4-ROUTE-MATRIX-001", "M4-REPRO-003"]
pr = 6180
head_sha = "8ab28785b39cf92263f03e1d3bbd792842c8986d"
merge_sha = "a86d13cd9e643b54ca1b42bde75fdb214c4d1900"
acceptance = "Add an end-to-end M4 operator workload suite across ask, chat, warm-session, and serve routes for supported dense SLMs and enabled BitNet routes, covering summarize, extract, classify, JSON, rewrite, and table-QA workflows with receipts, mechanical checks where possible, route-state boundaries, and no broad quality claim."
commands = [
  "target/release/bitnet mac workload --suite m4-operator --json-out ci/hardware/apple-m4-mac-mini/<date>/workload/summary.json",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/<date>/workload/summary.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-server/**",
  "ci/quality/**",
  "ci/hardware/apple-m4-mac-mini/**/workload/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 operator workflows have route-level receipts and mechanical checks for the tested dense SLM and BitNet identities.",
]
must_not_claim = [
  "The operator workload suite proves broad assistant quality.",
  "A route is enabled for BitNet unless its route-state evidence permits it.",
]

[[work_item]]
id = "M4-EVIDENCE-REPLAY-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-EVIDENCE-REPLAY-001-replay-bundles"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-RECEIPT-001", "M4-CI-001", "M4-REPRO-001"]
pr = 6182
head_sha = "22cc5eb0da30a090a7b96b41031c1354c7344724"
merge_sha = "62a5f3a30523c619a371a536a778cc8601236938"
acceptance = "Add replayable M4 evidence bundles that list the exact commands, git/binary identity, model/tokenizer identity, receipt inputs, dashboard outputs, expected regression result, and claim boundary so an operator can reproduce or audit a dense SLM or BitNet evidence refresh."
commands = [
  "bitnet mac evidence replay --bundle ci/hardware/apple-m4-mac-mini/<date>/<bundle>/manifest.json --dry-run --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/hardware/apple-m4-mac-mini/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 evidence refreshes are replayable or auditable from committed bundle manifests and receipt inputs.",
]
must_not_claim = [
  "Dry-run replay proves live model inference.",
  "Replay bundles validate uncommitted local artifacts.",
]

[[work_item]]
id = "M4-TREND-001"
status = "merged"
branch = "codex/apple-m4-inference-excellence/M4-TREND-001-seven-day-history"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-CI-001", "M4-BENCH-003", "M4-BITNET-REG-001", "M4-ACCURACY-007", "M4-BITNET-EX-012"]
pr = 6184
head_sha = "393420a13b9e8f9f788805478374e5b3c084b6cf"
merge_sha = "8ba3057c1169662f2d0b95b71be12715cc109103"
acceptance = "Publish a seven-day M4 trend history for dense SLM and BitNet receipt families with matching identity, skipped-day reasons, dashboard status, threshold outcomes, and operator-envelope impact separated from any one-off benchmark or quality claim."
commands = [
  "bitnet mac report-refresh --since 7d --json",
  "bitnet mac regression-dashboard --since 7d --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/trend/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "M4 dense SLM and BitNet evidence has multi-day trend history for matching identities and recorded skipped-day reasons.",
]
must_not_claim = [
  "Seven-day trend history proves future performance.",
  "Trend summaries replace per-run receipts.",
]

[[work_item]]
id = "M4-MODEL-LIFECYCLE-001"
status = "in_progress"
branch = "codex/apple-m4-inference-excellence/M4-MODEL-LIFECYCLE-001-supported-model-policy"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-REPRO-002", "M4-OPS-SLO-001", "M4-CI-001"]
acceptance = "Define the supported-model lifecycle for M4: candidate, diagnostic-only, supported non-default, default, deprecated, rejected, and retired states, including required evidence, cache migration behavior, operator warnings, downgrade/rollback guidance, and claim-boundary updates."
commands = [
  "bitnet mac models --json",
  "bitnet model verify <model-id>",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 supported-model state changes have explicit evidence, operator warning, cache, rollback, and claim-boundary requirements.",
]
must_not_claim = [
  "A model lifecycle policy supports a new model.",
  "A deprecated or diagnostic-only model is user-ready.",
]

[[work_item]]
id = "M4-COMPAT-001"
status = "proposed"
branch = "codex/apple-m4-inference-excellence/M4-COMPAT-001-update-compat"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-SETUP-001", "M4-MODEL-LIFECYCLE-001", "M4-CI-001", "M4-REPRO-001"]
acceptance = "Define and run M4 compatibility-refresh receipts after macOS, Rust toolchain, binary build-profile, or supported-model manifest changes, including smoke/regression commands, expected invalidation of timing baselines, cache repair behavior, rollback guidance, and claim-boundary updates."
commands = [
  "bitnet mac doctor --json-out ci/hardware/apple-m4-mac-mini/<date>/compat/doctor.json",
  "bitnet mac smoke --json-out ci/hardware/apple-m4-mac-mini/<date>/compat/smoke.json",
  "bitnet mac regression-dashboard --json-out ci/hardware/apple-m4-mac-mini/<date>/compat/regression-dashboard.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/hardware/apple-m4-mac-mini/**/compat/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
]
may_claim = [
  "M4 OS, toolchain, binary-profile, and model-manifest changes have explicit compatibility refresh and rollback evidence.",
]
must_not_claim = [
  "Compatibility refresh proves unchanged performance unless matching benchmark identities also pass.",
  "Compatibility evidence applies to MacBook or broad Apple Silicon targets.",
]

[[work_item]]
id = "M4-CLAIM-LINT-001"
status = "proposed"
branch = "codex/apple-m4-inference-excellence/M4-CLAIM-LINT-001-claim-boundary-check"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-COMPAT-001", "M4-REPRO-001"]
acceptance = "Add a static claim-boundary checker for M4 docs, generated tracker status, operator envelope text, and operator-facing command strings that rejects unsupported Apple Silicon, MacBook, Metal, Neural Engine, MPSGraph, QK256, dense-as-BitNet, broad quality/performance, and speedup wording unless explicitly tied to a matching accepted receipt gate."
commands = [
  "cargo run --locked -p xtask --no-default-features -- claim-lint --scope apple-m4 --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "xtask/**",
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "M4 claim-boundary wording is statically checked before release-envelope updates.",
]
must_not_claim = [
  "Claim lint is runtime proof.",
  "Claim lint authorizes unsupported Apple Silicon, MacBook, full Metal, Neural Engine, MPSGraph, QK256, dense-as-BitNet, broad quality/performance, or speedup claims.",
]

[[work_item]]
id = "M4-RELEASE-001"
status = "proposed"
branch = "codex/apple-m4-inference-excellence/M4-RELEASE-001-go-no-go"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-ACCURACY-007", "M4-BENCH-003", "M4-BENCH-007", "M4-BITNET-EX-005", "M4-BITNET-EX-015", "M4-BITNET-REG-001", "M4-CANARY-001", "M4-CLAIM-LINT-001", "M4-COMPAT-001", "M4-CONTEXT-001", "M4-CONTEXT-002", "M4-DENSE-CHAT-001", "M4-DENSE-REF-001", "M4-EVIDENCE-REPLAY-001", "M4-GATE-HYGIENE-001", "M4-MODEL-LIFECYCLE-001", "M4-OBS-001", "M4-OPS-SLO-001", "M4-OPS-UX-003", "M4-RELIABILITY-001", "M4-ROBUSTNESS-001", "M4-ROUTE-MATRIX-001", "M4-SERVE-EX-004", "M4-SETUP-001", "M4-TREND-001", "M4-WORKLOAD-001", "M4-CI-001"]
acceptance = "Publish the M4 inference release go/no-go matrix that states which dense SLM, BitNet, benchmark, stability, service, operator, and claim-boundary gates must pass before updating the public M4 expectation envelope or marking a model family route excellent."
commands = [
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The public M4 expectation envelope has explicit go/no-go inputs for future excellent-route claims.",
]
must_not_claim = [
  "The go/no-go matrix itself proves an excellent route.",
  "Unmet BitNet chat or serve gates are satisfied.",
]

[[work_item]]
id = "M4-METAL-EX-001"
status = "proposed"
branch = "codex/apple-m4-inference-excellence/M4-METAL-EX-001-phase-choice"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-EXCELLENCE-004"]
acceptance = "Choose one named future Apple M4 Metal phase from existing proof boundaries, document CPU reference parity requirements, fallback=false receipt fields, phase-local timing, and explicit CPU/NEON remainder before any code route changes."
commands = [
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/specs/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The next Apple M4 Metal work is limited to one named phase with documented parity and receipt requirements.",
]
must_not_claim = [
  "Full apple-m4-metal inference works.",
  "QK256, Neural Engine, or MPSGraph inference is supported.",
]

[[work_item]]
id = "M4-METAL-EX-002"
status = "proposed"
branch = "codex/apple-m4-inference-excellence/M4-METAL-EX-002-phase-parity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-METAL-EX-001"]
acceptance = "Implement the selected Metal phase fixture with CPU reference parity, same generated token IDs/text where required by the phase, fallback=false phase receipts, and phase-local timing only."
commands = [
  "cargo test --locked -p bitnet-kernels --features metal <selected-phase-test>",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-inference-excellence",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-kernels/tests/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-inference-excellence/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "One named Apple M4 Metal phase has parity and phase-local receipt evidence.",
]
must_not_claim = [
  "Full apple-m4-metal inference works.",
  "The phase proves end-to-end speedup.",
  "QK256, Neural Engine, or MPSGraph inference is supported.",
]
