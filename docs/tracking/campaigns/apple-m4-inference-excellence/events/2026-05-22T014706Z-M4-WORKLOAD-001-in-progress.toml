timestamp = "2026-05-22T01:47:06Z"
campaign = "apple-m4-inference-excellence"
item = "M4-WORKLOAD-001"
event = "in_progress"
actor = "codex"
previous_status = "proposed"
new_status = "in_progress"
branch = "codex/apple-m4-inference-excellence/M4-WORKLOAD-001-source-promotion"

notes = [
  "Started M4-WORKLOAD-001 source promotion with a model-free `bitnet mac workload --suite m4-operator` suite contract and receipt validator.",
  "The first slice enumerates summarize, extract, classify, JSON/schema, rewrite, and table-QA workloads across enabled dense SLM ask/chat/warm/serve and BitNet ask/warm routes.",
  "This slice does not run live generation, fetch models, start local serve, enable BitNet chat or serve, or make broad quality, performance, Metal, QK256, Neural Engine, MPSGraph, MacBook, speedup, or broad Apple Silicon claims.",
]
