id = "slm-cpu"
title = "Small dense model CPU proof"
status = "active"

objective = "Make the Intel i5-8250U a strict CPU proof host for small dense transformer GGUF models by proving metadata, tokenizer, architecture adapter, CPU execution, and tiny answer evidence without claiming throughput."

end_state = [
  "A dense GGUF candidate is selected by verified architecture and tokenizer metadata.",
  "Tokenizer source and strictness are recorded.",
  "Dense architecture metadata maps into a typed adapter contract.",
  "Tiny CPU answer receipts record prompt IDs, generated IDs, decoded text, selected backend/kernel, and fallback=false.",
  "8250U evidence is correctness and operability evidence, not performance evidence.",
]

hard_constraints = [
  "Do not edit BitNet QK256/I2_S kernels.",
  "Do not change BitNet proof receipt semantics.",
  "Do not claim sustained 8250U throughput.",
  "Do not claim server, GPU, OpenVINO, UHD 620, or NPU execution.",
  "Do not use model name recognition as proof of GGUF compatibility.",
]

[[work_item]]
id = "SLM-CPU-000"
status = "merged"
pr = 3902
branch = "codex/slm-cpu-000-8250u-lane"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Define a separate 8250U dense SLM CPU proof lane with explicit target policy, dense GGUF requirements, tokenizer authority rules, architecture adapter boundaries, receipt fields, and claim boundaries. The current first candidate policy prefers official Qwen3-0.6B-GGUF Q8_0 after exact artifact metadata verification."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The 8250U dense SLM CPU lane contract exists after merge.",
  "Qwen3-0.6B-GGUF is the preferred first candidate class, pending exact GGUF metadata verification.",
]
must_not_claim = [
  "A dense GGUF artifact is verified.",
  "Dense CPU inference works.",
  "Answer quality is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
]

[[work_item]]
id = "SLM-CPU-001"
status = "merged"
pr = 3905
branch = "codex/slm-cpu-001-model-manifest"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-000"]
acceptance = "Add a model candidate manifest, artifact policy, and 8250U runbook. The target policy was updated by SLM-CPU-002 to prefer official Qwen3-0.6B-GGUF Q8_0, with Qwen2.5-0.5B-Instruct as a conservative fallback only after exact artifact metadata is verified."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "ci/slm-cpu/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Dense SLM model candidate policy and 8250U runbook exist after merge.",
]
must_not_claim = [
  "The first GGUF artifact is verified.",
  "Dense CPU inference works.",
  "Answer quality is proven.",
  "Sustained 8250U throughput is proven.",
]

[[work_item]]
id = "SLM-CPU-002"
status = "merged"
pr = 3910
branch = "codex/slm-cpu-002-metadata-preflight"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-001"]
acceptance = "Add strict SLM CPU metadata preflight that verifies model path, pinned SHA256, GGUF architecture, tokenizer source, context cap, quant format, adapter selection, and fallback=false without running inference."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo test --locked -p bitnet-models --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "docs/slm/**",
  "ci/slm-cpu/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Dense SLM metadata preflight exists after merge.",
]
must_not_claim = [
  "Dense CPU inference works.",
  "Answer quality is proven.",
  "Sustained 8250U throughput is proven.",
]

[[work_item]]
id = "SLM-CPU-002A"
status = "merged"
pr = 3917
merge_sha = "a2b9b468a266683279e16bee2f8d12ab03cf4350"
branch = "codex/slm-cpu-002a-qwen-strict-blocker"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-002"]
acceptance = "Recognize qwen2/qwen3 in the authoritative GGUF loader metadata path and make strict dense Qwen Q8_0/Q*_K GGUF attempts fail at an explicit unsupported-quantization boundary before compatibility fallback or misleading tensor validation."
commands = [
  "cargo check --locked -p bitnet-models --no-default-features",
  "cargo test --locked -p bitnet-models --no-default-features dense_qwen --lib",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The official Qwen3-0.6B Q8_0 artifact reaches an explicit dense-quantization blocker in strict mode.",
]
must_not_claim = [
  "Dense CPU inference works.",
  "Q8_0 dense kernels or dequantization are implemented.",
  "Answer quality is proven.",
  "Sustained 8250U throughput is proven.",
]

[[work_item]]
id = "SLM-CPU-002B"
status = "merged"
pr = 3926
merge_sha = "726e2efd8fb0224d740f608ef5e4079475653ae4"
branch = "codex/slm-cpu-002b-dense-q8-gguf"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-002A"]
acceptance = "Add dense standard GGUF Q8_0/Q*_K adapter or dequantization support sufficient for the verified Qwen3-0.6B Q8_0 artifact to pass strict load without compatibility fallback, while preserving explicit failure for unsupported dense quantization variants."
commands = [
  "cargo check --locked -p bitnet-models --no-default-features",
  "cargo test --locked -p bitnet-models --no-default-features dense_qwen --lib",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/src/i2s_qk256*",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Qwen3-0.6B Q8_0 can pass strict dense GGUF load after merge.",
]
must_not_claim = [
  "Tiny dense CPU inference works.",
  "Answer quality is proven.",
  "Sustained 8250U throughput is proven.",
]

[[work_item]]
id = "SLM-CPU-003"
status = "merged"
pr = 3940
merge_sha = "566472d3be1965d958154b2c2f5cc364ef25e52d"
branch = "codex/slm-cpu-003-tiny-strict-receipt"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-002B"]
acceptance = "Run one tiny strict dense CPU receipt on the verified candidate with real GGUF, strict tokenizer, dense architecture adapter, CPU backend, fallback=false, prompt IDs, generated IDs, and decoded text. Correct answer is not required for bring-up if the receipt is diagnosable."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-common/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-receipts/**",
  "xtask/src/campaign.rs",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "A tiny dense SLM CPU run receipt exists after merge.",
]
must_not_claim = [
  "Answer quality is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
]

[[work_item]]
id = "SLM-CPU-004"
status = "merged"
pr = 3957
merge_sha = "a9e7835e6480b9277b80bf76cda6be6653942eec"
branch = "codex/slm-cpu-004-answer-corpus"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-003"]
acceptance = "Add a tiny SLM answer corpus with receipt rows recording model SHA, architecture, tokenizer source, prompt IDs, generated IDs, decoded text, selected backend, fallback status, quality pass/fail, and failed rules."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/quality/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Tiny SLM answer corpus evidence exists after merge.",
]
must_not_claim = [
  "General chat quality is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
]

[[work_item]]
id = "SLM-CPU-005"
status = "merged"
pr = 3969
merge_sha = "ae2fc47e742100c47c40b0723e234b3191b115d1"
branch = "codex/slm-cpu-005-reference-divergence"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-004"]
acceptance = "Add a machine-checkable reference divergence artifact schema and validator comparing bitnet-rs against a known-good external run by model SHA, prompt/template/BOS policy, prompt IDs, generated IDs, decoded text, top-k when available, and first divergence."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM reference divergence artifacts can be validated after merge.",
]
must_not_claim = [
  "The repo can run the external reference engine.",
  "General chat quality is proven.",
  "Sustained 8250U throughput is proven.",
]

[[work_item]]
id = "SLM-CPU-006A"
status = "merged"
pr = 4051
merge_sha = "9974bbcc66ad6abd5c7867a4193744186283bc2a"
branch = "codex/slm-cpu-006-first-token-capture"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-005"]
acceptance = "Land first-token divergence support tooling for the Qwen3 i5-8250U lane: --logits-dump-steps alias support, reference-compare first-token failure classification, and documented first-token workflow. This support slice does not capture the actual bitnet-rs and external-reference artifact pair."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli reference_divergence",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The first-token divergence tooling and runbook support landed in #4051.",
  "The actual Qwen3 first-token artifact capture remains open as SLM-CPU-006B.",
]
must_not_claim = [
  "The actual i5-8250U Qwen3 first-token artifact pair is captured.",
  "Qwen answer quality is proven.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-006B"
status = "merged"
pr = 4096
merge_sha = "ee8e7ec0f4c0a7ed5cee1a10729177d5f823d02d"
branch = "codex/slm-cpu-006b-first-token-artifact"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-006A"]
acceptance = "Capture a real first-token divergence artifact for Qwen3-0.6B Q8_0 on the i5-8250U by running bitnet-rs and a known-good external reference with identical model SHA, prompt text, Qwen template, BOS policy, prompt IDs, generated IDs, decoded text, chosen token, and first-step top-k/logit evidence where available. The artifact classifies the first divergence as prompt/tokenizer/template, logits/sampler, output-head/vocab indexing, or shared transformer math without claiming answer quality, throughput, server, GPU, NPU, or Qwen3.5 support."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli reference_divergence",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Qwen3 first-token divergence can be localized against reference prompt/token/logit evidence after merge.",
  "The SLM-CPU lane remains a correctness and operability lane for the i5-8250U, not a throughput lane.",
]
must_not_claim = [
  "Qwen answer quality is proven.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-007A"
status = "merged"
pr = 4132
merge_sha = "afb6a84bd66ea638b8122240869ee35a0eb4032e"
branch = "codex/slm-cpu-007-checkpoint-probes"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-006B"]
acceptance = "Expose the bounded Qwen3 first-token checkpoint trace surface through bitnet run flags for JSONL output, trace layer selection, full-prompt trace, and prompt-ID override, and document the first-drift workflow. This is a support slice only and does not claim first-drift localization, first-token parity, answer quality, throughput, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli run_help_documents_qwen_trace_flags",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli reference_divergence",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The bitnet run command exposes bounded Qwen3 checkpoint trace controls after merge.",
  "The first-drift workflow is documented for dense Qwen3 correctness triage after merge.",
]
must_not_claim = [
  "The Qwen3 first-token logits divergence is localized.",
  "Qwen answer quality is proven.",
  "Qwen first-token parity is restored.",
  "Qwen3 tiny answer corpus is green.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-007B"
status = "merged"
pr = 4138
merge_sha = "fcc0f992a00e39fb97bb5d0770a9b1b2295322ad"
branch = "codex/slm-cpu-007b-first-drift-capture"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-007A"]
acceptance = "Capture bitnet-rs Qwen3 checkpoint JSONL plus known-good reference prompt/top-logit evidence for the same Qwen3-0.6B Q8_0 model SHA, prompt/template/BOS policy, prompt IDs, greedy settings, and selected CPU backend as SLM-CPU-006B; validate and classify the first comparable divergence as logits and the first internal drift as reference-missing until reference internal checkpoint dumps exist, without claiming answer quality, throughput, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli reference_divergence",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The Qwen3 first comparable divergence is localized to first-token top logits after merge.",
  "Bounded bitnet-rs checkpoint evidence exists for dense Qwen3 correctness triage after merge.",
]
must_not_claim = [
  "Qwen answer quality is proven.",
  "Qwen first-token parity is restored.",
  "Qwen3 tiny answer corpus is green.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008"
status = "merged"
pr = 4434
branch = "codex/slm-cpu-008-qwen3-first-token-parity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-007B"]
acceptance = "Fix the first Qwen3-0.6B Q8_0 divergent operation using the SLM-CPU-007B checkpoint evidence and any required known-good reference checkpoints so bitnet-rs matches the reference first generated token for the same model SHA, prompt/template/BOS policy, prompt IDs, greedy settings, and fallback=false. The target reference token is 19 / '4'. No answer-quality, throughput, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, quant-expansion, or BitNet QK256 claim is allowed."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli reference_divergence",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Qwen3 first-token parity for the fixed prompt exists after merge.",
  "The fix is bounded to dense Qwen3 CPU correctness.",
]
must_not_claim = [
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008R"
status = "merged"
pr = 4572
merge_sha = "5414b66dc07921e2d9c4eafa6f9140d2e6b8eef6"
branch = "codex/slm-cpu-008r-post008-artifact-revalidation"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008"]
acceptance = "Run the verified Qwen3-0.6B Q8_0 GGUF on the actual i5-8250U after #4434 and record whether first-token parity is actually achieved with real GGUF, strict tokenizer from GGUF metadata, selected_backend=cpu-rust, fallback_used=false, rendered prompt, prompt IDs, generated IDs, first-step top-k, and reference comparison. This is an artifact revalidation item only and must not claim answer quality, sustained throughput, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, quant expansion, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/model-artifacts/model-coverage-matrix.toml",
  "ci/slm-cpu/**",
  "docs/model-artifacts/MODEL_COVERAGE_MATRIX.md",
  "docs/slm/**",
  "docs/tracking/campaigns/nvidia-5070ti/CAMPAIGN.md",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
  "plans/cuda-5070ti-productization/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Post-#4434 first-token parity has been revalidated on the actual i5-8250U artifact after merge.",
  "The post-#4434 artifact records strict loader/tokenizer provenance, prompt IDs, generated IDs, top-k, selected CPU backend, and fallback=false.",
]
must_not_claim = [
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008S"
status = "merged"
pr = 4606
merge_sha = "5dfec8ceb794f01aded1939ef72b98aac50c224a"
branch = "codex/slm-cpu-008s-qwen3-output-head-root-cause"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008R"]
acceptance = "Use the SLM-CPU-008R post-#4434 artifact and trace to localize the remaining Qwen3-0.6B Q8_0 first-token divergence, prioritizing output head/vocab indexing and shared transformer math. The item must identify the next concrete drift point or add the smallest missing diagnostic needed to do so, while preserving strict loader/tokenizer provenance and fallback=false. No answer-quality, tiny corpus, sustained throughput, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, Q4/Q5 quant expansion, or BitNet QK256 claim is allowed."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features \"cpu,full-cli\" reference_compare",
  "cargo test --locked -p bitnet-inference --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-common/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The post-#4434 Qwen3 first-token divergence is being localized from actual i5-8250U evidence.",
  "The next drift domain is output-head/vocab-indexing-or-shared-transformer-math unless new diagnostics prove otherwise.",
]
must_not_claim = [
  "Qwen first-token parity is restored.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008T"
status = "merged"
pr = 4611
merge_sha = "35be945a7ddaf9f593d8d405a961e08c553fe7fc"
branch = "codex/slm-cpu-008t-first-token-parity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008S"]
acceptance = "Fix the post-008 Qwen3-0.6B Q8_0 first-token parity candidate by selecting the dedicated GGUF output.weight head before tied-embedding fallback, then require a real i5-8250U artifact refresh to prove whether bitnet-rs now matches the reference token 19 / '4' for the same model SHA, rendered prompt, prompt IDs, greedy settings, strict tokenizer, selected CPU backend, and fallback=false. This slice must not claim answer quality, tiny corpus, multi-token stability, warm-session performance, Q4/Q5 quant expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu transformer_uses_dedicated_output_weight_when_present",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu transformer_uses_dedicated_lm_head_when_present",
  "cargo check --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The transformer now selects a dedicated GGUF output.weight head before tied-embedding fallback.",
  "The post-008 first-token parity candidate is ready for real i5-8250U artifact refresh after merge.",
]
must_not_claim = [
  "Qwen first-token parity is restored before the real artifact refresh proves it.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008U"
status = "merged"
pr = 4617
merge_sha = "edac33c25311d457ed6bfacb1642daaf76b2f8ae"
branch = "codex/slm-cpu-008u-output-weight-layout"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008T"]
acceptance = "Fix the remaining Qwen3 output-head layout candidate by treating GGUF output.weight with hidden/vocab dimensions as token-major storage that must be reshaped to vocab/hidden without transposing values, while preserving true transposed lm_head.weight handling. This slice must not claim Qwen3 first-token parity until a real i5-8250U artifact refresh proves bitnet-rs matches the reference token 19 / '4' with strict loader/tokenizer provenance and fallback=false."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu transformer_reshapes_gguf_output_weight_hidden_vocab_without_transpose",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu transformer_keeps_hidden_vocab_lm_head_transposed",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu transformer_uses_dedicated_output_weight_when_present",
  "cargo check --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "GGUF output.weight hidden/vocab dims are handled as token-major storage instead of a transposed lm head after merge.",
  "The post-008 first-token parity candidate is ready for real i5-8250U artifact refresh after merge.",
]
must_not_claim = [
  "Qwen first-token parity is restored before the real artifact refresh proves it.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008V"
status = "merged"
pr = 4633
merge_sha = "45db888b376ffd85a3be3cdc56af2fb3cbb0a8b0"
branch = "codex/slm-cpu-008v-post-008u-artifact-refresh"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008U"]
acceptance = "Refresh the real i5-8250U Qwen3-0.6B Q8_0 first-token artifact after SLM-CPU-008U, validate it against the known-good reference token 19 / '4', and audit the output-head/tied-head boundary. The artifact must record real GGUF, strict GGUF tokenizer authority, prompt IDs, generated IDs, first-step top-k, selected CPU backend, fallback=false, and whether first-token parity is restored. This slice must not claim answer quality, tiny corpus, multi-token stability, warm-session performance, Q4/Q5 quant expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "Get-FileHash C:\\Code\\Rust\\BitNet-rs\\models\\slm\\Qwen3-0.6B-Q8_0.gguf -Algorithm SHA256",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu run --model C:\\Code\\Rust\\BitNet-rs\\models\\slm\\Qwen3-0.6B-Q8_0.gguf --prompt-template qwen --prompt \"What is 2+2? Answer with only the number.\" --max-new-tokens 1 --temperature 0.0 --greedy --deterministic --strict-loader --strict-tokenizer --logits-dump-steps 1 --logits-topk 10 --assert-greedy --qwen-trace-jsonl ci\\slm-cpu\\intel-i5-8250u\\2026-05-07\\qwen3-post-008u-original-trace.jsonl --qwen-trace-layer 0 --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-07\\qwen3-post-008u-original-first-token.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- output-head-logits-audit --machine-id intel-i5-8250u-cpu-avx2 --model C:\\Code\\Rust\\BitNet-rs\\models\\slm\\Qwen3-0.6B-Q8_0.gguf --avx2-answer-corpus ci\\slm-cpu\\intel-i5-8250u\\2026-05-07\\qwen3-post-008u-original-first-token.json --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-07\\qwen3-post-008u-output-head-audit.json",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli output_head",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- reference-compare --artifact ci\\slm-cpu\\intel-i5-8250u\\2026-05-07\\qwen3-post-008u-reference-compare.json --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-07\\qwen3-post-008u-reference-validation.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/commands/output_head_logits_audit.rs",
  "crates/bitnet-cli/tests/cli_arg_tests.rs",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Post-SLM-CPU-008U first-token parity was revalidated on the real i5-8250U artifact.",
  "The official Qwen3-0.6B Q8_0 GGUF output-head/tied-head boundary was audited from GGUF metadata.",
  "The next root-cause domain is tied-token-embedding logits or shared transformer math if parity still fails.",
]
must_not_claim = [
  "Qwen first-token parity is restored unless the artifact emits the reference token 19 / 4.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Multi-token stability is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008W"
status = "merged"
pr = 4641
merge_sha = "08530ef97765b99bd89d2526c5dfd1714caa0583"
branch = "codex/slm-cpu-008w-tied-head-shared-math-root-cause"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008V"]
acceptance = "Use the post-SLM-CPU-008U artifact refresh and output-head audit to localize the remaining first-token divergence after the official Qwen3 GGUF is proven to use tied token embeddings rather than a dedicated output.weight head. Prioritize tied embedding logits, final norm input, vocab-index handoff, and shared transformer math; only move backward into RoPE, GQA, MLP ordering, tensor orientation, or Q8_0 dequant if the new evidence points there. This item must not claim answer quality, corpus success, performance, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-models/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The next Qwen3 first-token drift point is narrowed beyond the dedicated output.weight hypothesis.",
]
must_not_claim = [
  "Qwen first-token parity is restored before a real artifact proves token 19 / 4.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008X"
status = "merged"
pr = 4655
merge_sha = "a6e3bf2c846363c8dfc7e3282d3c0f3575169ac0"
branch = "codex/slm-cpu-008x-reference-checkpoints"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008W"]
acceptance = "Capture or validate known-good internal Qwen3 checkpoints for the same Qwen3-0.6B Q8_0 model SHA, rendered prompt, prompt IDs, and greedy first-token settings used by the i5-8250U artifact, then compare them against bitnet-rs checkpoints to identify the first shared-transformer-math drift before lm_head.top_logits. The minimum useful checkpoints are decode.input_embedding, block.attention_norm, attention.q_proj, attention.k_proj, attention.v_proj, attention.q_rope, model.final_norm, and lm_head.logits. This item must not claim first-token parity, answer quality, tiny corpus success, multi-token stability, warm-session performance, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The reference-compare command can validate checkpoint-aware SLM reference artifacts.",
  "The next Qwen3 first-token proof step requires a real known-good internal checkpoint pack.",
  "The output-head/tied-head boundary has been audited and did not restore first-token parity.",
]
must_not_claim = [
  "Qwen first-token parity is restored.",
  "A real known-good Qwen3 reference checkpoint pack has been captured.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Multi-token stability is proven.",
  "Warm-session usability is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008YA"
status = "merged"
pr = 4669
merge_sha = "04ac4b17713fdb9e9aa5950d8b3f582b2225aceb"
branch = "codex/slm-cpu-008ya-qwen3-no-think-prompt"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008X"]
acceptance = "Expose an explicit Qwen3 no-thinking prompt policy on `bitnet run` so first-token parity artifacts can be regenerated against an exact rendered prompt. The flag must be accepted only for `--prompt-template qwen`, be receipt-visible as `qwen_no_think`, update the 8250U runbook/reference-divergence docs, and must not claim first-token parity, answer quality, tiny corpus success, multi-token stability, warm-session performance, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli no_think",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Qwen3 no-thinking prompt authority is explicit and receipt-visible after merge.",
  "The next Qwen3 first-token reference can be regenerated with exact no-thinking prompt IDs after merge.",
]
must_not_claim = [
  "Qwen first-token parity is restored.",
  "A real known-good Qwen3 reference checkpoint pack has been captured.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Multi-token stability is proven.",
  "Warm-session usability is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008YB"
status = "merged"
pr = 4699
merge_sha = "0f06153ae25d3777cfdbb1c3a01109f0d53218cf"
branch = "codex/slm-cpu-008yb-qwen-think-special-tokenizer"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008YA"]
acceptance = "Parse Qwen thinking control tokens `<think>` and `</think>` from the GGUF BPE vocabulary as special tokens when `parse_special=true`, so `--no-think` rendered prompts can be compared against known-good reference tokenization without literalizing thinking markers. This support slice must not claim first-token parity, known-good checkpoint capture, answer quality, tiny corpus success, multi-token stability, warm-session performance, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-tokenizers bpe_special -- --nocapture",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli no_think",
  "cargo run --locked -p xtask --no-default-features -- check-no-panic-family --report-dir target/bitnet/reports",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-cli/tests/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Qwen thinking control tokens are preserved as GGUF-authoritative BPE special tokens after merge.",
  "A refreshed no-thinking Qwen3 reference can compare prompt IDs without literalized `<think>` markers after merge.",
]
must_not_claim = [
  "Qwen first-token parity is restored.",
  "A real known-good Qwen3 reference checkpoint pack has been captured.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Multi-token stability is proven.",
  "Warm-session usability is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008Y"
status = "merged"
pr = 4778
merge_sha = "a7f65ca5008a4fedaa3e352b2fe368a639dcfea2"
branch = "codex/slm-cpu-008y-reference-checkpoints"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
acceptance = "Capture or ingest a real known-good internal Qwen3 checkpoint pack for the same Qwen3-0.6B Q8_0 model SHA, rendered prompt, prompt IDs, and greedy first-token settings used by the i5-8250U artifact, then validate it with the SLM checkpoint-aware reference-compare path from SLM-CPU-008X. The artifact must identify the first divergent checkpoint before lm_head.top_logits when compared with bitnet-rs. This item must not claim first-token parity, answer quality, tiny corpus success, multi-token stability, warm-session performance, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- reference-compare --artifact ci/slm-cpu/intel-i5-8250u/2026-05-07/qwen3-reference-checkpoint-compare.json --json-out target/slm-cpu/qwen3-reference-checkpoint-validation.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "A real known-good Qwen3 internal checkpoint artifact is available for the i5-8250U SLM lane after merge.",
  "The checkpoint-aware reference comparator identifies the first checkpoint drift before corpus work resumes.",
]
must_not_claim = [
  "Qwen first-token parity is restored.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Multi-token stability is proven.",
  "Warm-session usability is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-017"
status = "merged"
pr = 5041
branch = "codex/slm-cpu-017-second-model-sanity"
merge_sha = "8f05904886c6124d3355f360d0f79a7d5237a68b"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-016"]
acceptance = "Add a bounded second small dense GGUF model sanity check after the Qwen3-0.6B Q8_0 appliance profile. The slice must select a non-Qwen3 candidate only by verified artifact metadata, pinned SHA256, GGUF architecture, tokenizer authority, quant format, and tensor naming; run the strict CPU preflight and the smallest diagnosable strict CPU answer or failure receipt the current command surface supports; record prompt IDs, generated IDs when generated, decoded text when generated, selected backend/kernel, loader mode, tokenizer source/strictness, and fallback_used=false. It must preserve the Qwen3 Q8_0 appliance profile as the baseline and must not claim broad answer quality, sustained throughput, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm",
  "cargo test --locked -p bitnet-models --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "ci/slm-cpu/**",
  "ci/quality/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "A second small dense GGUF candidate has bounded strict CPU sanity evidence only after exact metadata, tokenizer authority, selected CPU backend, and fallback=false are recorded.",
  "The Qwen3-0.6B Q8_0 appliance profile remains the established Kaby Lake baseline.",
]
must_not_claim = [
  "Broad answer quality is proven for the second model.",
  "Sustained 8250U throughput is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-017A"
status = "merged"
pr = 5060
branch = "codex/slm-cpu-017a-qwen25-second-model-sanity"
merge_sha = "76a10d534be7292f7dac24e9ec995e05ee08435d"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-017"]
acceptance = "Record a positive bounded second-model sanity receipt for qwen2.5-0.5b-instruct-q8_0 after SLM-CPU-017's SmolLM2 strict preflight blocker slice. The slice must verify the exact Qwen2.5 Q8_0 GGUF artifact by pinned SHA256, run the smallest strict CPU answer-corpus case supported by the current command surface, and record prompt IDs, generated IDs, decoded text, selected backend/kernel, loader mode, tokenizer source/strictness, logits boundary evidence, and fallback_used=false. It must preserve the Qwen3 Q8_0 appliance profile as the Kaby baseline and must not claim broad answer quality, sustained throughput, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- model verify qwen2.5-0.5b-instruct-q8_0 --cache-dir target\\slm-cpu-017\\cache --json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- answer-corpus --corpus ci\\quality\\slm-second-model-sanity-corpus.yaml --model target\\slm-cpu-017\\cache\\qwen2.5-0.5b-instruct-q8_0\\qwen2.5-0.5b-instruct-q8_0.gguf --model-id qwen2.5-0.5b-instruct-q8_0 --device cpu --threads 4 --case-id math_2_plus_2_brief --dump-logit-steps 1 --logits-topk 5 --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-16\\qwen25-second-model-sanity.json",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "ci/slm-cpu/**",
  "ci/quality/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "qwen2.5-0.5b-instruct-q8_0 has bounded strict CPU second-model sanity evidence on the i5-8250U path.",
  "The Qwen3-0.6B Q8_0 appliance profile remains the established Kaby Lake baseline.",
]
must_not_claim = [
  "Broad answer quality is proven for Qwen2.5.",
  "Sustained 8250U throughput is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008AA"
status = "merged"
pr = 4817
merge_sha = "083b536bae306f6c886ceafdb1f3094e7e8ba517"
branch = "codex/slm-cpu-008aa-no-think-artifact"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008Z"]
acceptance = "Capture or ingest a known-good Qwen3 no-thinking first-token checkpoint/reference artifact for the verified Qwen3-0.6B Q8_0 model SHA, exact rendered `--prompt-template qwen --no-think` prompt, prompt IDs, greedy settings, strict tokenizer policy, selected CPU backend identity, and fallback=false bitnet-rs comparison. The artifact must distinguish prompt-policy mismatch from logits/shared-transformer-math drift before SLM-CPU-009 corpus work begins. It must not claim Qwen first-token parity unless bitnet-rs and the known-good reference choose the same token for the exact no-thinking prompt, and must not claim answer quality, tiny corpus success, multi-token stability, warm-session performance, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- run --device cpu --model models/slm/Qwen3-0.6B-Q8_0.gguf --prompt-template qwen --no-think --prompt \"What is 2+2? Answer with only the number.\" --max-new-tokens 1 --temperature 0.0 --greedy --deterministic --strict-loader --strict-tokenizer --logits-dump-steps 1 --logits-topk 10 --assert-greedy --qwen-trace-layer 0 --json-out target/slm-cpu/qwen3-no-think-first-token.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- reference-compare --artifact ci/slm-cpu/intel-i5-8250u/2026-05-07/qwen3-no-think-reference-checkpoint-compare.json --json-out target/slm-cpu/qwen3-no-think-reference-checkpoint-validation.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-models/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The default-prompt Qwen3 reference evidence from SLM-CPU-008Z is not sufficient for no-thinking constrained-answer parity.",
  "The exact no-thinking rendered prompt has a known-good Candle reference artifact.",
  "For that exact no-thinking prompt, bitnet-rs and the Candle reference both choose token 17 / '2'.",
  "Numeric top-k logits still differ, so strict logits parity is not proven.",
  "The older token 19 / '4' target is not the known-good first token for the exact no-thinking rendered prompt captured in this item.",
]
must_not_claim = [
  "Strict Qwen first-step logits parity is restored.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Multi-token stability is proven.",
  "Warm-session usability is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008AB"
status = "merged"
pr = 4822
merge_sha = "dba4ca6d0714cf70aa4d144a86ac98fe0a06bb5e"
branch = "codex/slm-cpu-008ab-constrained-answer-target"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008AA"]
acceptance = "Use the SLM-CPU-008AA no-thinking reference artifact to calibrate the constrained-answer target before SLM-CPU-009 corpus work begins. The slice must either produce a known-good reference prompt policy where the verified Qwen3-0.6B Q8_0 model emits the expected constrained answer for the 2+2 seed, or explicitly record that the current first-token no-thinking prompt is unsuitable because both Candle and bitnet-rs choose token 17 / '2'. It must preserve prompt IDs, rendered prompt, generated IDs, decoded text, selected backend/kernel identity, strict tokenizer policy, and fallback=false evidence. It must not claim tiny corpus success, broad answer quality, multi-token stability, warm-session performance, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- reference-compare --artifact ci/slm-cpu/intel-i5-8250u/2026-05-07/qwen3-no-think-reference-checkpoint-compare.json --json-out target/slm-cpu/qwen3-no-think-reference-checkpoint-validation.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The exact no-thinking 2+2 first-token seed currently chooses token 17 / '2' in both Candle and bitnet-rs.",
  "SLM-CPU-009 remains blocked until the constrained-answer target is calibrated against a known-good reference.",
]
must_not_claim = [
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Multi-token stability is proven.",
  "Warm-session usability is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008AC"
status = "merged"
pr = 4826
merge_sha = "c69927f37d9b19300e36f09ac60c5105621b59c4"
branch = "codex/slm-cpu-008ac-constrained-answer-scoring"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008AB"]
acceptance = "Define and validate the Qwen3 constrained-answer scoring policy that can safely unblock SLM-CPU-009. The slice must use the verified Qwen3-0.6B Q8_0 model SHA, strict GGUF tokenizer authority, selected_backend=cpu-rust, fallback=false, and a known-good reference for the exact rendered prompt policy. It must either find a prompt policy where the reference emits the expected constrained answer as the first token, or switch the seed to a bounded multi-token score where the reference and bitnet-rs generated IDs/text agree on the constrained answer. It must preserve prompt IDs, generated IDs, decoded text, selected backend/kernel, strict tokenizer policy, and failure rules. It must not claim the tiny corpus is green, broad answer quality, multi-token stability beyond the bounded seed, warm-session performance, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- reference-compare --artifact ci/slm-cpu/intel-i5-8250u/2026-05-07/qwen3-no-think-say-ok-reference-compare.json --json-out target/slm-cpu/qwen3-no-think-say-ok-reference-validation.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "ci/quality/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The first-token `2+2=` and sentence-form no-thinking seeds are unsuitable as token-19 constrained-answer gates because the known-good reference chooses token 17 / '2'.",
  "The `Say exactly: OK` no-thinking seed is selected as the first constrained-answer scoring gate because the known-good reference and bitnet-rs both choose token 3925 / 'OK'.",
  "SLM-CPU-009 can start from the selected bounded scoring policy without claiming the full tiny corpus is green.",
]
must_not_claim = [
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Unbounded multi-token stability is proven.",
  "Warm-session usability is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-009A"
status = "merged"
pr = 4832
merge_sha = "05a7bb16a94231e3dfd97e09bfbc161cf9683a85"
branch = "codex/slm-cpu-009-qwen-no-think-corpus"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008AC"]
acceptance = "Carry the selected Qwen no-thinking prompt policy into the answer-corpus runner, record qwen_no_think in per-case receipts, constrain the `say_ok` seed to the bounded one-token scoring policy, and add the strict i5-8250U CPU receipt showing `Say exactly: OK` passes with generated token 3925 / `OK`, selected_backend=cpu-rust, tokenizer.source=gguf_metadata, and fallback_used=false. This support slice must not claim the full tiny answer corpus is green, broad Qwen answer quality, unbounded multi-token stability, warm-session usability, sustained throughput, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli qwen_no_think_defaults_to_false_and_can_be_set",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli answer_corpus",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- answer-corpus --corpus ci/quality/slm-answer-corpus.yaml --model models/slm/Qwen3-0.6B-Q8_0.gguf --device cpu --case-id say_ok --dump-logit-steps 1 --json-out target/slm-cpu/qwen3-answer-corpus-say-ok.json",
  "cargo run --locked -p xtask --no-default-features -- check-no-panic-family --report-dir target/bitnet/reports",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "ci/quality/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The answer-corpus runner can reproduce the selected Qwen no-thinking `say_ok` bounded scoring seed under strict i5-8250U CPU receipts.",
  "The `say_ok` case passes as a one-token constrained-answer seed with generated token 3925 / `OK` and fallback_used=false.",
]
must_not_claim = [
  "Qwen3 tiny answer corpus is green.",
  "Broad Qwen answer quality is proven.",
  "Unbounded multi-token stability is proven.",
  "Warm-session usability is proven.",
  "Sustained 8250U throughput is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-009B"
status = "merged"
pr = 4843
merge_sha = "92e7010a3e8f40e9030d57f6f7df7f4f3ce5017c"
branch = "codex/slm-cpu-009-tiny-corpus-evidence"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-009A"]
acceptance = "Run the full Qwen3-0.6B Q8_0 tiny answer corpus through the strict i5-8250U CPU path under the selected qwen no-thinking policy, record per-case receipts with prompt IDs, generated IDs, decoded text, selected backend/kernel, loader.mode=real_gguf, tokenizer.source=gguf_metadata, tokenizer.strict=true, and fallback_used=false, and preserve explicit failed_rules for remaining misses. This evidence slice records 4/5 passing cases and keeps math_2_plus_2 as a real gate_exact_trimmed miss; it must not claim the full tiny corpus is green, broad answer quality, unbounded multi-token stability, warm-session usability, sustained throughput, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli answer_corpus",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- answer-corpus --corpus ci/quality/slm-answer-corpus.yaml --model models/slm/Qwen3-0.6B-Q8_0.gguf --device cpu --json-out ci/slm-cpu/intel-i5-8250u/2026-05-07/qwen3-answer-corpus.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "ci/quality/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The strict i5-8250U Qwen3 no-thinking corpus artifact records 4/5 passing cases.",
  "math_2_plus_2 remains a real gate_exact_trimmed content miss with generated token 17 / '2' followed by EOS.",
  "SLM-CPU-009 remains open until the remaining math case is fixed, replaced with a calibrated constrained-answer case, or explicitly scoped out by a follow-up tracker decision.",
]
must_not_claim = [
  "Qwen3 tiny answer corpus is fully green.",
  "Broad Qwen answer quality is proven.",
  "Unbounded multi-token stability is proven.",
  "Warm-session usability is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-009"
status = "merged"
pr = 4846
merge_sha = "92b1848370a0ac43c41091e7894a261d0c00395d"
branch = "codex/slm-cpu-009-tiny-corpus-green"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-009B"]
acceptance = "Run the Qwen3-0.6B Q8_0 tiny answer corpus through the strict i5-8250U CPU path using the SLM-CPU-008AC constrained-answer scoring policy. The slice must record prompt IDs, generated IDs, decoded text, selected backend/kernel, loader.mode=real_gguf, tokenizer.source=gguf_metadata, tokenizer.strict=true, and fallback_used=false for every case. It may adjust the corpus prompt policy to qwen no-thinking or score cases one at a time, but it must preserve explicit failed_rules for any miss. It must not claim broad chat quality, unbounded multi-token stability, warm-session usability, sustained throughput, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli answer_corpus",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- answer-corpus --corpus ci/quality/slm-answer-corpus.yaml --model models/slm/Qwen3-0.6B-Q8_0.gguf --device cpu --json-out target/slm-cpu/qwen3-answer-corpus.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "ci/quality/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The selected tiny corpus cases pass only when their explicit gates pass under strict CPU receipts.",
  "Per-case failures are acceptable evidence when failed_rules remain visible and the item is not marked merged.",
]
must_not_claim = [
  "Broad Qwen answer quality is proven.",
  "Unbounded multi-token stability is proven.",
  "Warm-session usability is proven.",
  "Sustained 8250U throughput is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-010"
status = "merged"
pr = 4851
merge_sha = "42e4d04ae78c372569315b7fbb7b1548b7f17326"
branch = "codex/slm-cpu-010-multitoken-stability"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-009"]
acceptance = "Prove bounded Qwen3-0.6B Q8_0 multi-token decode stability on the strict i5-8250U CPU path after the calibrated tiny corpus is green. The slice must run fixed prompts at max_new_tokens 4, 8, and 16, record prompt IDs, generated IDs, decoded text, stop reason, KV-cache position metadata where available, selected backend/kernel, loader.mode=real_gguf, tokenizer.source=gguf_metadata, tokenizer.strict=true, deterministic repeated-run evidence, and fallback_used=false. It must not claim broad chat quality, warm-session usability, sustained throughput, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli answer_corpus",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "ci/quality/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The selected bounded multi-token prompts are deterministic only when repeated-run generated IDs match under strict CPU receipts.",
  "KV-cache and stop-reason metadata are visible for the bounded stability cases.",
]
must_not_claim = [
  "Broad Qwen answer quality is proven.",
  "Warm-session usability is proven.",
  "Sustained 8250U throughput is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-011"
status = "merged"
pr = 4858
merge_sha = "3bd117480c92a155a70be80efcfcd37826012f2e"
branch = "codex/slm-cpu-011-warm-session-receipts"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-010"]
acceptance = "Add bounded Qwen3-0.6B Q8_0 warm-session receipts for the strict i5-8250U CPU path after multi-token stability. The slice must load the model and tokenizer once, run multiple fixed prompts in one process where the available command surface supports it, emit per-case and aggregate receipts, and record model SHA, tokenizer authority, selected backend/kernel, fallback_used=false, prompt/generated IDs, load time, prefill time, first-token time, decode timing, resident memory or explicit unavailable fields, thread count, and thermal/power context where available. It must not claim sustained throughput without warm/cold and thermal boundaries, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "ci/quality/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Warm-session behavior is proven only for the fixed Qwen3 Q8_0 prompts and the recorded i5-8250U strict CPU receipts.",
  "Timing is bounded to recorded warm/cold conditions, thread count, and available thermal/power context.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven without warm/cold and thermal boundaries.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-012"
status = "merged"
pr = 4876
merge_sha = "6c766f8f020cd892cfbf15574691450cafe6a3c6"
branch = "codex/slm-cpu-012-allocation-kv-cleanup"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-011"]
acceptance = "Reduce Qwen3-0.6B Q8_0 strict CPU warm-session allocation and KV/layout waste on the i5-8250U without changing answer behavior. The slice must preserve the SLM-CPU-011 warm-session receipts and deterministic generated IDs, add bounded allocation or buffer-reuse evidence where available, and document any unavailable measurement fields explicitly. It must not claim sustained throughput, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Allocation or KV/layout cleanup is bounded to the recorded Qwen3 Q8_0 strict CPU warm-session evidence.",
  "Answer behavior is preserved only when generated IDs match the SLM-CPU-011 baseline for the fixed prompts.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-013"
status = "merged"
pr = 4891
merge_sha = "17793ceb829ab6d7319bce2a66905df66ea5fea3"
branch = "codex/slm-cpu-013-q8-gemv-hot-path"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-012"]
acceptance = "Improve the Qwen3-0.6B Q8_0 strict CPU warm-session hot path after SLM-CPU-012 by reducing dense Q8_0 dequant/GEMV waste without changing generated IDs or strict receipt provenance. The slice must preserve the SLM-CPU-011/012 behavior oracle, record bounded timing or allocation evidence where available, and keep any speed statement scoped to the fixed model, corpus, backend, thread count, and measured artifact. It must not claim sustained 8250U throughput, Q4/Q5 quant expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-models --no-default-features --features cpu",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Q8_0 dense CPU hot-path cleanup is bounded to the recorded Qwen3 Q8_0 strict CPU warm-session evidence.",
  "Generated IDs and strict provenance are preserved for the fixed SLM warm-session prompts.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-014"
status = "merged"
pr = 4900
merge_sha = "4faa85e2f999f1fb90112d733a71efdc2bf32e33"
branch = "codex/slm-cpu-014-dense-avx2-hot-path"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-013"]
acceptance = "Add the next bounded Qwen3-0.6B Q8_0 dense CPU hot-path improvement after SLM-CPU-013, focusing on AVX2/FMA-safe dense GEMV, RMSNorm, RoPE, or adjacent allocation-free decode cleanup only where generated IDs and strict receipt provenance remain unchanged. The slice must preserve the SLM-CPU-011/012/013 behavior oracle, record bounded timing or allocation evidence where available, and scope any speed statement to the fixed model, corpus, backend, thread count, and measured artifact. It must not claim sustained 8250U throughput, Q4/Q5 quant expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-common/**",
  "crates/bitnet-cpu-detect/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Dense CPU hot-path cleanup is bounded to the recorded Qwen3 Q8_0 strict CPU warm-session evidence.",
  "Generated IDs and strict provenance are preserved for the fixed SLM warm-session prompts.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-015"
status = "merged"
pr = 4911
merge_sha = "d139ae72bc749eb6a6f06bea7c90248316f59465"
branch = "codex/slm-cpu-015-thread-thermal-envelope"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-014"]
acceptance = "Add bounded i5-8250U Qwen3-0.6B Q8_0 thread and timing envelope evidence after SLM-CPU-014 by running or documenting strict warm-session measurements for 1, 2, 4, and 8 CPU threads where available. The slice must preserve generated IDs, strict GGUF tokenizer authority, selected CPU backend, and fallback=false, record warm/cold, prompt/corpus, thread count, timing, and any thermal/power fields as measured or explicitly unavailable, and keep claims bounded to the recorded host/model/corpus/backend. It must not claim sustained 8250U throughput beyond recorded thermal boundaries, Q4/Q5 quant expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Thread and timing evidence is bounded to the recorded Qwen3 Q8_0 strict CPU warm-session artifacts.",
  "A recommended thread-count default may be proposed only when supported by recorded evidence.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven without warm/cold and thermal boundaries.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-016"
status = "merged"
pr = 4922
merge_sha = "7c3064db8ce56df92f0cf632a898b9febf2f3c66"
branch = "codex/slm-cpu-016-kaby-operator-profile"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-015"]
acceptance = "Add the Kaby Lake SLM operator appliance profile after SLM-CPU-015 by documenting and, where the command surface supports it, emitting a single strict Qwen3-0.6B Q8_0 operator receipt bundle that uses the selected thread envelope, records model SHA, GGUF tokenizer authority, prompt IDs, generated IDs, selected CPU backend/kernel identity, fallback_used=false, warm-session timing, resident memory, power, thermal, storage/free-space, and unsupported-path fields as measured or explicitly unavailable. The slice must preserve the SLM-CPU-009/010/011/015 generated-ID behavior oracle, keep any timing claim bounded to the recorded i5-8250U host/model/corpus/backend/thread context, and must not claim sustained throughput, Q4/Q5 quant expansion, a second model, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The 8250U Qwen3 Q8_0 operator profile is bounded to the recorded strict CPU warm-session artifact bundle.",
  "A default operator thread count may be recommended only if the SLM-CPU-015 envelope and SLM-CPU-016 host-context fields support it.",
  "Resident memory, power, thermal, storage, or free-space fields may be recorded as explicitly unavailable when the local host cannot sample them.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven without warm/cold and thermal boundaries.",
  "Q4/Q5 quant expansion is in scope.",
  "A second dense SLM model is supported by this item.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008Y1"
status = "merged"
pr = 4769
merge_sha = "7770c8b764425909b72962c4194d096e397e6e28"
branch = "codex/slm-cpu-008y1-reference-checkpoint-method"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008YB"]
acceptance = "Document the non-interactive reference checkpoint capture method required before SLM-CPU-008Y can land: the method must produce a `slm_reference_checkpoint_compare` artifact for the verified Qwen3-0.6B Q8_0 model SHA, exact rendered prompt, prompt IDs, greedy first-token settings, and required internal checkpoint stages. It must explicitly reject interactive llama.cpp output, top-k-only evidence, and any artifact that lacks internal reference checkpoint summaries. This support item must not claim first-token parity, answer quality, tiny corpus success, multi-token stability, warm-session performance, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "ci/slm-cpu/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The SLM-CPU-008Y reference checkpoint capture method is documented.",
  "SLM-CPU-008Y remains blocked until a real known-good internal checkpoint pack is captured or ingested.",
]
must_not_claim = [
  "A real known-good Qwen3 internal checkpoint artifact is available.",
  "Qwen first-token parity is restored.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Multi-token stability is proven.",
  "Warm-session usability is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-008Z"
status = "merged"
pr = 4789
merge_sha = "a478272489b59b7c608f9986f2515052409a3e1d"
branch = "codex/slm-cpu-008z-qwen3-qproj-drift"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-008Y"]
acceptance = "Use the SLM-CPU-008Y checkpoint artifact to fix or further localize the first required-stage Qwen3 drift at `attention.q_proj` for the verified Qwen3-0.6B Q8_0 model SHA and exact rendered prompt. The slice must compare the bitnet-rs projection path against the known-good reference checkpoint evidence and identify whether the remaining fault is q_proj input, q_proj weight/dequantization, bias handling, matmul orientation, accumulation precision, or tensor mapping. It must not claim first-token parity, answer quality, tiny corpus success, multi-token stability, warm-session performance, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu qwen",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu q_proj",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- reference-compare --artifact ci/slm-cpu/intel-i5-8250u/2026-05-07/qwen3-reference-checkpoint-compare.json --json-out target/slm-cpu/qwen3-reference-checkpoint-validation.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-models/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The first required-stage Qwen3 drift at `attention.q_proj` is fixed or narrowed to a specific projection-path subcomponent after merge.",
  "The SLM-CPU-008Y checkpoint artifact remains the reference evidence for the Qwen3 first-token correctness lane.",
]
must_not_claim = [
  "Qwen first-token parity is restored.",
  "Qwen answer quality is proven.",
  "Qwen3 tiny answer corpus is green.",
  "Multi-token stability is proven.",
  "Warm-session usability is proven.",
  "Sustained 8250U throughput is proven.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid Qwen architecture support is in scope.",
  "Q4/Q5 quant expansion is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-018"
status = "merged"
pr = 5067
head_sha = "04de329278126312b13eca85d8d59a8ad8e43a9c"
merge_sha = "1f9dd9cb1d0d76e40077ffd82e98715e15c7ce71"
branch = "docs/slm-cpu-018-smollm2-normalization-policy-linear"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-017"]
acceptance = "Record the governed SmolLM2 360M normalization-policy decision after the SLM-CPU-017 strict CPU preflight blocker. The item must preserve the generic llama strict LayerNorm/RMSNorm gamma guard as fail-closed, define the exact metadata required before any SmolLM2-family exception can be implemented, and commit a machine-readable policy audit. It must not change loader behavior, retry CPU answer sanity, start CUDA planning, or claim CPU answer readiness, throughput, Q4/Q5 expansion, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, broad dense GGUF support, or BitNet QK256 behavior."
commands = [
  "python -m json.tool ci/slm-cpu/windows-9950x3d-rtx5070ti/2026-05-16/smollm2-360m-normalization-policy-audit.json",
  "cargo run --locked -p xtask --no-default-features -- campaign check slm-cpu",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The SmolLM2 360M strict CPU preflight blocker has a governed normalization-policy audit after merge.",
  "The next SmolLM2 proof must implement exact metadata-scoped loader validation before CPU sanity can be retried.",
]
must_not_claim = [
  "SmolLM2 strict CPU loading is fixed.",
  "SmolLM2 CPU answer readiness is proven.",
  "SmolLM2 broad answer quality is proven.",
  "Sustained 8250U or 9950X3D throughput is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "CUDA planning or CUDA execution is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-019"
status = "merged"
pr = 5081
head_sha = "c93ad85786934dc93b3be4ef4eb7aa31694d19cd"
merge_sha = "11ca6d7ceee41cfc4ec073976a15f5c0fb5b21d8"
branch = "model/slm-cpu-019-smollm2-normalization-validation"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-018"]
acceptance = "Implement exact metadata-scoped SmolLM2 360M normalization validation in the strict GGUF loader while preserving generic llama strict LayerNorm/RMSNorm gamma fail-closed behavior. The accepted SmolLM2 exception must require the pinned artifact SHA plus exact GGUF architecture, tokenizer pre-tokenizer, tensor dimensions, vocab size, block count, and intermediate size, and must be receipt-visible. This item must not retry CPU answer sanity, start CUDA planning, or claim CPU answer readiness, throughput, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, broad dense GGUF support, or BitNet QK256 behavior."
commands = [
  "cargo test --locked -p bitnet-models --no-default-features --features cpu --lib test_ln_gamma_validator_envelope",
  "cargo test --locked -p bitnet-models --no-default-features --features cpu --lib smollm2_norm_policy_requires_exact_metadata_scope",
  "python -m json.tool ci/slm-cpu/windows-9950x3d-rtx5070ti/2026-05-16/smollm2-360m-normalization-validation-implementation.json",
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "cargo run --locked -p xtask --no-default-features -- campaign check slm-cpu",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "ci/model-artifacts/model-coverage-matrix.toml",
  "ci/slm-cpu/**",
  "docs/model-artifacts/MODEL_COVERAGE_MATRIX.md",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "Exact metadata-scoped SmolLM2 360M normalization validation is implemented after merge.",
  "Generic llama strict LayerNorm/RMSNorm gamma validation remains fail-closed.",
  "The accepted SmolLM2 normalization exception is receipt-visible.",
  "The next proof is a strict CPU sanity retry with fallback_used=false.",
]
must_not_claim = [
  "SmolLM2 CPU answer readiness is proven.",
  "SmolLM2 broad answer quality is proven.",
  "SmolLM2 strict CPU sanity was retried.",
  "Sustained 8250U or 9950X3D throughput is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "CUDA planning or CUDA execution is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-020"
status = "merged"
pr = 5108
head_sha = "3c99ddb9a078a21ae40ccc73f2bba25c9ce2e160"
merge_sha = "8590c563628f0f855de6d2bb682b5097757c210e"
branch = "model/slm-cpu-020-smollm2-cpu-sanity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-019"]
acceptance = "Retry strict SmolLM2 360M Q8_0 CPU sanity after exact metadata-scoped normalization validation. The slice must keep the generic llama normalization guard fail-closed, preserve exact SmolLM2 artifact and metadata scope, record tokenizer/prompt/generation reachability or the next blocker with fallback_used=false, and keep CPU answer readiness false unless the bounded quality gate passes."
commands = [
  "cargo test --locked -p bitnet-models --no-default-features --features cpu --lib test_ln_gamma_validator_envelope",
  "cargo test --locked -p bitnet-models --no-default-features --features cpu --lib smollm2_norm_policy_requires_exact_metadata_scope",
  "python -m json.tool ci/slm-cpu/windows-9950x3d-rtx5070ti/2026-05-16/smollm2-360m-strict-cpu-sanity-retry.json",
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "cargo run --locked -p xtask --no-default-features -- campaign check slm-cpu",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "ci/model-artifacts/model-coverage-matrix.toml",
  "ci/slm-cpu/**",
  "docs/model-artifacts/MODEL_COVERAGE_MATRIX.md",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SmolLM2 360M exact metadata-scoped normalization validation reaches strict CPU tokenizer, prompt rendering, and one-token generation after merge.",
  "The strict CPU retry is fallback-free and records a quality blocker when the math prompt generates the wrong first token.",
  "Generic llama strict LayerNorm/RMSNorm gamma validation remains fail-closed.",
]
must_not_claim = [
  "SmolLM2 CPU answer readiness is proven.",
  "SmolLM2 broad answer quality is proven.",
  "SmolLM2 all-layer CUDA planning can start.",
  "SmolLM2 CUDA execution exists.",
  "Sustained 8250U or 9950X3D throughput is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-083"
status = "merged"
pr = 393
head_sha = "1db5d185d0895aba705ea161c25c50b089f9b125"
merge_sha = "107409f014e234f8af1a77d0491ee30899ba7b4b"
branch = "codex/slm-cpu-083-prefill-forward-allocation-target"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-082", "SLM-CPU-041"]
acceptance = "Queue the next Kaby Lake Qwen3 Q8_0 performance slice after the repeated packed-Q8 sidecar timing regression. The implementation must return to the allocation/layout target identified by the dashboard and SLM-CPU-035 through SLM-CPU-041: reduce or narrowly classify one prompt_prefill.forward / model-forward owned-output allocation boundary using the existing typed transformer-forward workspace surfaces. Any runtime change must preserve the Qwen3 Q8_0 appliance behavior oracle: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, and fallback_used=false. The item must not promote packed_q8_sidecar, claim speedup without before/after receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "The next Kaby Lake Qwen3 Q8_0 performance target returns to prompt_prefill.forward / model-forward allocation or layout work after the packed-Q8 sidecar repeated timing regression.",
  "Any future implementation must preserve the established Qwen3 Q8_0 generated-ID and strict-receipt behavior oracle before claiming even a bounded improvement.",
]
must_not_claim = [
  "A runtime optimization is implemented by this tracker item alone.",
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "End-to-end speedup is proven without before/after receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-084"
status = "merged"
pr = 399
head_sha = "8c28874c867989c5f0dd8c0107c4defbd708f9b5"
merge_sha = "47e3d657917981701d980d32cf4850fd3d0d096b"
branch = "codex/slm-cpu-084-model-forward-output-storage-api"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-083"]
acceptance = "Burn down or further narrow the SLM-CPU-083 model.forward.output owned-output boundary by adding a behavior-preserving model-forward output-storage API surface or a machine-checkable blocker proving the remaining Candle Tensor ownership/API gap. Any runtime change must preserve the established Qwen3 Q8_0 appliance behavior oracle: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, and fallback_used=false. The slice must not promote packed_q8_sidecar, claim speedup without before/after receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "The next Kaby Lake Qwen3 Q8_0 performance target is the model.forward.output storage API boundary identified by SLM-CPU-083.",
  "A valid SLM-CPU-084 slice must preserve generated IDs, decoded text, strict tokenizer authority, selected CPU backend, model SHA, dense hook identity where applicable, and fallback=false before claiming even a bounded allocation improvement.",
]
must_not_claim = [
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "End-to-end speedup is proven without before/after receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-085"
status = "merged"
pr = 409
head_sha = "dc1f42d1e979e08a17b69407b65a8725e4e2b18a"
merge_sha = "e10f3b190e0d7999450899aaa41d56575068cd09"
branch = "codex/slm-cpu-085-final-norm-output-storage-boundary"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-084"]
acceptance = "Burn down or further narrow the post-SLM-CPU-084 final-norm/layer-output caller-output-storage boundary for the Qwen3 Q8_0 Kaby performance lane. A valid slice may add a behavior-preserving output-storage API surface or a machine-checkable blocker proving the remaining Candle Tensor ownership/API gap, but any runtime change must preserve the established Qwen3 Q8_0 appliance behavior oracle: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, and fallback_used=false. The slice must not promote packed_q8_sidecar, claim speedup without before/after receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "The next Kaby Lake Qwen3 Q8_0 performance target is the final-norm/layer-output caller-output-storage boundary identified by SLM-CPU-084.",
  "A valid SLM-CPU-085 slice must preserve generated IDs, decoded text, strict tokenizer authority, selected CPU backend, model SHA, dense hook identity where applicable, and fallback=false before claiming even a bounded allocation improvement.",
]
must_not_claim = [
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "End-to-end speedup is proven without before/after receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-086"
status = "merged"
pr = 425
head_sha = "6494cfb4c4209123f367aa3da037ad403bb94783"
merge_sha = "959f5240216bb74d81e531a34a498fc8c9aee426"
branch = "codex/slm-cpu-086-final-norm-output-storage-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-085"]
acceptance = "Burn down or further narrow the SLM-CPU-085 `model.final_norm.output` caller-output-storage blocker for the Qwen3 Q8_0 Kaby performance lane. A valid slice may add a behavior-preserving final-norm output-storage helper or record a more precise machine-checkable LayerNorm/RMSNorm API blocker, but any runtime change must preserve the established Qwen3 Q8_0 appliance behavior oracle: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, and fallback_used=false. The slice must not move to residual-add/layer-output runtime changes, promote packed_q8_sidecar, claim speedup without before/after receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "The next Kaby Lake Qwen3 Q8_0 performance target is the `model.final_norm.output` caller-output-storage boundary identified by SLM-CPU-085.",
  "A valid SLM-CPU-086 slice must preserve generated IDs, decoded text, strict tokenizer authority, selected CPU backend, model SHA, dense hook identity where applicable, and fallback=false before claiming even a bounded allocation improvement.",
]
must_not_claim = [
  "The residual-add/layer-output runtime path is optimized.",
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "End-to-end speedup is proven without before/after receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-080"
status = "merged"
pr = 332
head_sha = "a289b083fd612aa760b142f40d85fc497713151b"
merge_sha = "f8f10d2176389fa358943fb30940ad90aa538b96"
branch = "codex/slm-cpu-080-kaby-performance-dashboard-refresh"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-079", "SLM-CPU-032", "SLM-CPU-035"]
acceptance = "Refresh the Kaby Lake Qwen3 Q8_0 CPU performance dashboard after the merged prompt-token cache, KV-cache reuse, prefill-attribution, and post-aligned packed-matvec evidence. The dashboard must preserve the Qwen3 Q8_0 behavior oracle, record that the current operator default is evidence-scoped to 4 threads, identify the current allocation and packed-matvec next targets, and keep speedup, sustained-throughput, Q4/Q5 runtime, server, accelerator, Qwen3.5, and BitNet QK256 claims false."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "The Kaby Lake Qwen3 Q8_0 performance dashboard is refreshed against the latest merged strict receipt evidence.",
  "The current operator default remains evidence-scoped to the recorded 4-thread operator profile.",
  "The next Kaby SLM performance targets are allocation/layout work around prompt_prefill.forward or exact-tensor packed Q8_0 matvec compute, both gated by generated-ID and strict-receipt preservation.",
]
must_not_claim = [
  "A new runtime optimization is implemented.",
  "End-to-end speedup is proven.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-081"
status = "merged"
pr = 349
head_sha = "2b996d200112070d9c98d77057759810f4723fc3"
merge_sha = "149247d66b04b8297f32d8b7d4096b401bd9f73c"
branch = "codex/slm-cpu-081-repeated-packed-q8-timing"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-079", "SLM-CPU-080"]
acceptance = "Collect or define the repeated before/after i5-8250U Qwen3 Q8_0 warm-session timing evidence needed after SLM-CPU-079's aligned packed-Q8 exact-tensor matvec counter improvement. The slice must compare the established eager-F32 or accepted sidecar oracle against the opt-in exact-tensor packed_q8_sidecar path across repeated runs, preserve model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, and fallback_used=false, and classify timing as improved, regressed, inconclusive, or not claimed. It must keep packed_q8_sidecar opt-in and exact-tensor scoped, keep the default runtime unchanged, and must not claim end-to-end speedup unless repeated receipts prove it within the bounded host/model/thread/corpus context."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "ci/slm-cpu/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "The next Kaby packed-Q8 performance evidence target is repeated before/after warm-session timing for the exact-tensor opt-in sidecar path.",
  "Any timing classification is bounded to the recorded i5-8250U host, Qwen3 Q8_0 model, thread count, prompt corpus, and receipt set.",
  "The default runtime remains unchanged unless a later promotion item proves behavior-preserving receipt equivalence and bounded timing evidence.",
]
must_not_claim = [
  "A new runtime optimization is implemented.",
  "The packed-Q8 sidecar is enabled by default.",
  "Packed-Q8 execution is generalized beyond the exact tensor scope.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-082"
status = "merged"
pr = 374
head_sha = "2d3e2b9cc0d002600aa1c24336fe757948e30938"
merge_sha = "a3e5a98395adb769c94433738fb3bf09f102867a"
branch = "codex/slm-cpu-082-receipt-capture"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-081"]
acceptance = "Capture or ingest the repeated i5-8250U Qwen3 Q8_0 warm-session receipt pack required by SLM-CPU-081 for the exact-tensor packed_q8_sidecar path. The slice must provide at least three baseline receipts and three candidate receipts for the same host, model SHA, GGUF tokenizer authority, prompt corpus, 4-thread operator profile, selected CPU backend/kernel identity, dense hook identity, and fallback_used=false; preserve prompt IDs, generated IDs, and decoded text; emit qwen3-slm-cpu-081-repeated-packed-q8-timing-classification.json with result improved, regressed, inconclusive, or not_claimed; and keep the default runtime unchanged unless a later promotion item accepts the repeated evidence. It must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-082 is the next evidence item for repeated before/after packed-Q8 warm-session receipts after the SLM-CPU-081 gate.",
  "Any timing classification must be bounded to the recorded i5-8250U host, Qwen3 Q8_0 model, 4-thread corpus run, backend identity, dense hook identity, and repeated receipt set.",
  "Behavior preservation is required before any timing result can be accepted.",
]
must_not_claim = [
  "A new runtime optimization is implemented.",
  "The packed-Q8 sidecar is enabled by default.",
  "Packed-Q8 execution is generalized beyond the exact tensor scope.",
  "End-to-end speedup is proven without the required repeated receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-059"
status = "merged"
pr = 6078
head_sha = "223f87e471f43911bb5757f934c0a3151352eb7f"
merge_sha = "5265445b4e9a5cd4f70679ec62c9bdd47036a5a8"
branch = "codex/slm-cpu-059-packed-q8-kernel-proof"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-058"]
acceptance = "Add the next packed Q8_0 sidecar compute proof gate after SLM-CPU-058. The slice must either add the first narrow production-safe packed Q8_0 dense-linear compute-kernel boundary for an evidence-scoped Qwen3 Q8_0 tensor path, or emit a concrete blocker artifact naming the exact kernel/API/layout gap. Any runtime candidate must compare against the eager_f32_candle Qwen3 Q8_0 Kaby behavior oracle with before/after warm-session receipts proving identical model SHA, tokenizer source/strictness, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook-selection identity, and fallback_used=false before claiming even bounded timing improvement. If timing is recorded, it must be bounded to the fixed i5-8250U host/model/corpus/thread context and preserve speedup_claim=false unless a separate timing item accepts the evidence. The item must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-models --no-default-features --features cpu dense_gguf_q8",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu dense_linear_runtime_hook",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-059 is the next packed Q8_0 sidecar compute proof gate after SLM-CPU-058.",
  "A valid SLM-CPU-059 slice may add a narrow evidence-scoped packed Q8_0 dense-linear compute-kernel boundary, or may emit a concrete blocker artifact if the current kernel/API/layout contract still prevents safe runtime use.",
  "Any bounded timing evidence is valid only when before/after receipts preserve model SHA, tokenizer source/strictness, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook-selection identity, and fallback=false.",
]
must_not_claim = [
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "Generated IDs or decoded text changed.",
  "Strict GGUF tokenizer authority, selected CPU backend/kernel identity, model SHA, dense hook-selection identity, or fallback=false provenance changed.",
  "A sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-060"
status = "merged"
pr = 6085
head_sha = "cca8b5385672f8150021a88ab9ffca078fe5e561"
merge_sha = "6e2f7837e0f90317122ffe34eca355fe285f4abe"
branch = "codex/slm-cpu-060-payload-bearing-q8-sidecar-hook"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-059"]
acceptance = "Add the first evidence-scoped payload-bearing packed Q8_0 sidecar hook contract for one Qwen3 Q8_0 dense-linear tensor path, or emit an implementation blocker if the transformer/model API cannot safely carry payload bytes to the dense-linear callsite. Any runtime candidate must remain opt-in/disabled by default, preserve the eager_f32_candle behavior oracle, and require before/after warm-session receipts proving identical model SHA, tokenizer source/strictness, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook-selection identity, and fallback_used=false before broader selection. The item must not claim speedup, sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-models --no-default-features --features cpu dense_gguf_q8",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu dense_linear_runtime_hook",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-060 is the next evidence-scoped hook-contract slice after SLM-CPU-059 identified the metadata-only sidecar blocker.",
  "A valid SLM-CPU-060 slice may add a payload-bearing packed Q8_0 sidecar hook contract for one dense-linear tensor path, or may emit a concrete blocker if the current model/transformer API cannot safely carry packed payload bytes.",
  "Runtime compute remains gated on before/after receipts that preserve generated IDs, decoded text, strict tokenizer authority, selected CPU backend/kernel identity, dense hook-selection identity, model SHA, and fallback=false.",
]
must_not_claim = [
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "Generated IDs or decoded text changed.",
  "Strict GGUF tokenizer authority, selected CPU backend/kernel identity, model SHA, dense hook-selection identity, or fallback=false provenance changed.",
  "A sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-061"
status = "merged"
pr = 6111
head_sha = "d304e5fe1cee6bc92d4008f84e904ec924a8f826"
merge_sha = "8952dd2a3cf1c134134560d71f12d5b6072c4322"
branch = "codex/slm-cpu-061-real-q8-sidecar-payload-candidate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-060"]
acceptance = "Wire one exact Qwen3 Q8_0 dense-linear tensor from real GGUF sidecar bytes into DenseLinearPackedQ8Payload behind an explicit opt-in gate, then emit before/after warm-session receipts proving model SHA, tokenizer source/strictness, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook-selection identity, and fallback_used=false are unchanged. Packed Q8 runtime compute must remain disabled unless the before/after evidence is present and selected only for the single candidate tensor. The item must not claim speedup, sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "python -m json.tool ci/slm-cpu/intel-i5-8250u/2026-05-20/qwen3-real-q8-sidecar-payload-candidate.json",
  "cargo test --locked -p bitnet-models --no-default-features --features cpu dense_gguf_q8",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu dense_linear_runtime_hook",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-061 is the next evidence-scoped slice after SLM-CPU-060 added the payload-bearing hook contract.",
  "One exact Qwen3 Q8_0 dense-linear tensor may carry real sidecar payload bytes into DenseLinearPackedQ8Payload behind an explicit opt-in gate.",
  "Any runtime selection remains gated on before/after receipts that preserve generated IDs, decoded text, strict tokenizer authority, selected CPU backend/kernel identity, dense hook-selection identity, model SHA, and fallback=false.",
]
must_not_claim = [
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "Generated IDs or decoded text changed.",
  "Strict GGUF tokenizer authority, selected CPU backend/kernel identity, model SHA, dense hook-selection identity, or fallback=false provenance changed.",
  "A sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-062"
status = "merged"
pr = 6122
head_sha = "42e5ed1a9dc88b1d29f210432589e09011bc7e30"
merge_sha = "06b82a17e8ee2dda2dd926bd5affc9d692c2cafd"
branch = "codex/slm-cpu-062-release-surface-swarm-handoff"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-061"]
acceptance = "Checkpoint the post-SLM-CPU-061 Kaby Lake SLM state as a BitNet-rs release-surface boundary and direct the next packed Q8_0 compute-candidate development to bitnet-rs-swarm. The slice must preserve the existing Qwen3 Q8_0 and Qwen2.5 Q8_0 strict CPU receipt oracle, record that BitNet-rs remains the release/evidence surface, and avoid runtime code changes. It must not claim speedup, sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/slm-cpu/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-061 is the last merged BitNet-rs release-surface runtime-adjacent packed Q8_0 sidecar slice in this sequence.",
  "BitNet-rs remains the release/evidence surface for the Kaby Qwen3 Q8_0 and Qwen2.5 Q8_0 strict CPU oracle receipts.",
  "Future packed Q8_0 compute-candidate development should happen in bitnet-rs-swarm and return to BitNet-rs only as audited release/evidence artifacts.",
]
must_not_claim = [
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "A new packed Q8_0 compute path is implemented in BitNet-rs by this item.",
  "Generated IDs or decoded text changed.",
  "Strict GGUF tokenizer authority, selected CPU backend/kernel identity, model SHA, dense hook-selection identity, or fallback=false provenance changed.",
  "A sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-063"
status = "merged"
pr = 6127
head_sha = "94b9d4835"
merge_sha = "f09d559c8ad575e80b6c1f2e706b4f49e419e7f4"
branch = "codex/slm-cpu-063-swarm-artifact-intake-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-062"]
acceptance = "Define the BitNet-rs release-surface intake gate for Kaby SLM artifacts produced by bitnet-rs-swarm after SLM-CPU-062. The gate must specify the required before/after evidence for any returned packed Q8_0 compute candidate: identical model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook-selection identity, fallback_used=false, and speedup_claim=false unless a separate bounded timing artifact is accepted. The slice must be docs/tracker only and must not implement runtime compute or claim speedup, sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/slm-cpu/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-063 is the BitNet-rs release-surface intake gate for audited Kaby SLM artifacts returning from bitnet-rs-swarm.",
  "Returned packed Q8_0 compute candidates must preserve the Qwen3 Q8_0 and Qwen2.5 Q8_0 strict CPU behavior oracle before release-surface promotion.",
  "BitNet-rs remains the release/evidence surface, while implementation experiments belong in bitnet-rs-swarm.",
]
must_not_claim = [
  "A new packed Q8_0 compute path is implemented in BitNet-rs by this item.",
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "Generated IDs or decoded text changed.",
  "Strict GGUF tokenizer authority, selected CPU backend/kernel identity, model SHA, dense hook-selection identity, or fallback=false provenance changed.",
  "A sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-064"
status = "merged"
pr = 6138
head_sha = "43f8484698877813297ecb3d406c9efec005f8bc"
merge_sha = "189dd6ceee45f6a41328000edc5ebe5f1fa5fe8a"
branch = "codex/slm-cpu-064-first-swarm-intake-review"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-063"]
acceptance = "Review the first audited Kaby SLM artifact package returned from bitnet-rs-swarm under the SLM-CPU-063 intake gate. The package must include candidate_summary.json, before_receipt.json, after_receipt.json, equivalence_report.json, timing_report.json or timing_not_claimed.json, and source_commit.txt. The review must record whether the package preserves model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook-selection identity, and fallback_used=false before any BitNet-rs runtime promotion item is opened. The slice is release/evidence intake only and must not implement runtime compute or claim speedup, sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
  "ci/slm-cpu/**",
]
forbidden_paths = [
  "crates/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-064 is the first BitNet-rs release-surface review gate for an audited bitnet-rs-swarm Kaby SLM artifact package.",
  "A returned package is accepted, rejected, or blocked based on the SLM-CPU-063 intake evidence contract.",
  "Accepted evidence preserves the existing Qwen3 Q8_0 and Qwen2.5 Q8_0 strict CPU behavior oracle before any runtime promotion item is opened.",
]
must_not_claim = [
  "A swarm candidate is promoted into BitNet-rs runtime code by this item.",
  "A new packed Q8_0 compute path is implemented in BitNet-rs by this item.",
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "Generated IDs or decoded text changed.",
  "Strict GGUF tokenizer authority, selected CPU backend/kernel identity, model SHA, dense hook-selection identity, or fallback=false provenance changed.",
  "A sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-065"
status = "merged"
pr = 6155
head_sha = "545c7ba19"
merge_sha = "67918be87a31b5733b0c5340e65c8e31f86a7f44"
branch = "codex/slm-cpu-065-single-tensor-runtime-promotion-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-064"]
acceptance = "Open the first BitNet-rs release-surface runtime-promotion gate for the accepted single-tensor packed Q8_0 sidecar candidate returned from bitnet-rs-swarm. The item may implement or explicitly block opt-in exact-tensor runtime promotion for `layers.0.attention.q_proj.weight` only, and must preserve the Qwen3 Q8_0 strict CPU oracle with before/after receipts proving identical model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook-selection identity, and fallback_used=false. Runtime promotion must remain disabled by default unless the receipt gate proves behavior, and the item must not claim speedup, sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
  "ci/slm-cpu/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-065 is the first BitNet-rs runtime-promotion gate after accepting a bitnet-rs-swarm package as release-surface evidence.",
  "The scope is limited to the exact `layers.0.attention.q_proj.weight` packed Q8_0 sidecar candidate.",
  "A later runtime-promotion decision is accepted, rejected, or blocked based on before/after strict CPU receipts.",
]
must_not_claim = [
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "Runtime promotion is enabled by default without matching before/after generated-ID and decoded-text receipts.",
  "Generated IDs or decoded text changed.",
  "Strict GGUF tokenizer authority, selected CPU backend/kernel identity, model SHA, dense hook-selection identity, or fallback=false provenance changed.",
  "A speedup or sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-066"
status = "superseded"
branch = "codex/slm-cpu-066-swarm-runtime-hook-handoff"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-065"]
acceptance = "Superseded by SLM-CPU-067, which directly implemented the bitnet-rs-swarm exact-tensor runtime hook candidate and committed before/after strict CPU receipts. Retain this item only as the skipped handoff placeholder after the BitNet-rs release-surface promotion gate; do not implement it as a separate runtime or tracker slice."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
  "ci/slm-cpu/**",
]
forbidden_paths = [
  "crates/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-066 was superseded by SLM-CPU-067's direct swarm runtime-hook implementation.",
  "The release-surface promotion discipline still requires before/after strict CPU receipts before any BitNet-rs release-surface runtime promotion.",
]
must_not_claim = [
  "BitNet-rs release surface has enabled packed Q8_0 runtime compute.",
  "Runtime promotion is enabled by default without matching before/after generated-ID and decoded-text receipts.",
  "A speedup or sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-067"
status = "merged"
pr = 109
head_sha = "fbad3de113db4e25140874e48a6d159b08741bbe"
merge_sha = "ec2145727cf48dcb5ea47da35acf85d5e427c8d4"
branch = "codex/slm-cpu-067-exact-tensor-q8-runtime-hook"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-066"]
acceptance = "Implement or produce a blocker for an exact-tensor packed Q8_0 runtime compute hook for `layers.0.attention.q_proj.weight` only, using the accepted SLM-CPU-065/066 sidecar evidence and preserving eager F32 Candle as the default path. Any opt-in runtime candidate must emit before/after strict CPU receipts proving unchanged model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook-selection identity, and fallback_used=false before it can be exported back to BitNet-rs release-surface review. The slice must not enable packed Q8_0 compute by default or claim speedup, sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-common/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/swarm/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-067 is the next swarm development item after the SLM-CPU-066 release-gate handoff.",
  "Any candidate remains exact-tensor scoped to `layers.0.attention.q_proj.weight` and must preserve the Qwen3 Q8_0 strict CPU behavior oracle.",
  "A blocker artifact is acceptable if the current runtime API still cannot select packed Q8_0 compute for the exact tensor without weakening receipt invariants.",
]
must_not_claim = [
  "Packed Q8_0 runtime compute is enabled by default.",
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "Generated IDs or decoded text changed.",
  "Strict GGUF tokenizer authority, selected CPU backend/kernel identity, model SHA, dense hook-selection identity, or fallback=false provenance changed.",
  "A speedup or sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-068"
status = "merged"
pr = 120
head_sha = "95919954f6d403d9fae9f416bf960977e84c47fe"
merge_sha = "287ef74a8e8345cb26429bddd191cb712467c45d"
branch = "codex/slm-cpu-068-exact-hook-timing-classification"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-067"]
acceptance = "Classify the SLM-CPU-067 exact-tensor packed Q8_0 runtime hook timing evidence without broadening the runtime claim. The slice must compare the committed before/after Qwen3 Q8_0 warm-session receipts for `layers.0.attention.q_proj.weight`, record that generated IDs, decoded text, model SHA, strict GGUF tokenizer authority, selected CPU backend, dense hook-selection identity, and fallback_used=false are preserved, and state whether the opt-in packed sidecar path improves, regresses, or is inconclusive for the bounded two-prompt 4-thread artifact. It must keep eager F32 Candle as the default path and must not claim speedup unless the measured artifact supports it under the existing claim boundary."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-067 merged an opt-in exact-tensor packed Q8_0 runtime hook for `layers.0.attention.q_proj.weight` only.",
  "The committed SLM-CPU-067 before/after receipts preserve generated IDs, decoded text, strict tokenizer authority, selected CPU backend, and fallback=false.",
  "A bounded timing classification exists for the exact two-prompt 4-thread artifact after merge.",
]
must_not_claim = [
  "Packed Q8_0 runtime compute is enabled by default.",
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "A broad speedup or sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-069"
status = "merged"
pr = 124
head_sha = "77cf8ce3cf96d1fdfdaa74e8f35c626af44a1348"
merge_sha = "78bc57c50a347c7f1f47b6bbf178b3c9c3c8040c"
branch = "codex/slm-cpu-069-packed-q8-locality-root-cause"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-068"]
acceptance = "Use the SLM-CPU-068 timing regression evidence to localize why the exact-tensor packed Q8_0 sidecar path for `layers.0.attention.q_proj.weight` is slower than eager F32 Candle on the bounded Qwen3 Q8_0 4-thread i5-8250U artifact. The slice must preserve eager_f32_candle as the default runtime, keep packed sidecar execution opt-in and exact-tensor scoped, identify whether the next fix belongs in packed-block decode, matvec locality, scratch allocation, selector overhead, or receipt/timing instrumentation, and emit a machine-checkable root-cause or next-target artifact. It must not change generated IDs/text, weaken strict tokenizer/backend/fallback receipts, claim speedup, enable packed Q8_0 by default, start Q4/Q5 runtime support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "The SLM-CPU-067 opt-in exact-tensor packed Q8_0 hook preserves behavior but regresses timing on the SLM-CPU-068 bounded two-prompt 4-thread artifact.",
  "The next optimization target is localized from existing strict receipts before any new runtime promotion claim.",
]
must_not_claim = [
  "Packed Q8_0 runtime compute is enabled by default.",
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "A speedup or sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-070"
status = "merged"
pr = 129
head_sha = "05f89de29308ef205af5d723b65e401240d6ad18"
merge_sha = "67e63d7b2b0f2b2c3cdc8697f71b43e41695cfd0"
branch = "codex/slm-cpu-070-packed-q8-block-local-matvec"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-069"]
acceptance = "Use the SLM-CPU-069 root-cause artifact to prototype or explicitly block a block-local packed Q8_0 matvec path for the exact `layers.0.attention.q_proj.weight` sidecar. The slice must preserve eager_f32_candle as the default runtime, keep packed sidecar execution opt-in and exact-tensor scoped, decode each Q8_0 block scale once per block rather than per weight where a runtime prototype is attempted, and compare against the existing eager F32 behavior oracle with identical prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook-selection identity, and fallback_used=false before any timing interpretation. It must not claim speedup unless a separate bounded timing artifact supports it, enable packed Q8_0 by default, broaden beyond the exact tensor, start Q4/Q5 runtime support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu exact_q8_sidecar_runtime_hook_matches_dense_linear_reference",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu exact_q8_sidecar_runtime_hook_matches_reference_across_q8_blocks",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu exact_q8_sidecar_runtime_hook_matches_reference_when_rows_split_q8_blocks",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-069 localized the exact-tensor packed Q8_0 timing regression to packed-block decode and matvec locality.",
  "A block-local packed Q8_0 matvec prototype is behavior-gated against the eager F32 oracle before any timing interpretation.",
]
must_not_claim = [
  "Packed Q8_0 runtime compute is enabled by default.",
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "A speedup or sustained 8250U throughput claim is proven without a separate bounded timing artifact.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-071"
status = "merged"
pr = 177
head_sha = "d402440e0d17c056d1f0141bf92d1f9a199a35fb"
merge_sha = "6f37670d3902c65dc69ba58ff70fa97ff4f8938c"
branch = "codex/slm-cpu-071-post070-real-timing-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-070"]
acceptance = "Regenerate the verified Qwen3-0.6B Q8_0 i5-8250U strict warm-session before/after artifact pack after the SLM-CPU-070 block-local packed Q8_0 sidecar prototype. The slice must run the established 4-thread appliance profile with the default eager_f32_candle path and with the opt-in exact-tensor `layers.0.attention.q_proj.weight` packed_q8_sidecar candidate, prove identical prompt IDs, generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel identity, dense hook-selection identity, model SHA, and fallback_used=false before any timing interpretation, then classify timing as improved, regressed, or inconclusive. It must not enable packed Q8_0 by default, broaden beyond the exact tensor, claim sustained throughput, claim broad answer quality, start Q4/Q5 runtime support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu slm-warm-session --model models\\slm\\Qwen3-0.6B-Q8_0.gguf --corpus ci\\quality\\slm-warm-session-corpus.yaml --corpus-repeat-runs 2 --max-new-tokens 8 --temperature 0.0 --greedy --deterministic --threads 4 --strict-loader --strict-tokenizer --prompt-template qwen --require-determinism --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-21\\qwen3-slm-cpu-071-before-warm-session.json",
  "cmd /C \"set BITNET_DENSE_Q8_RUNTIME_ENABLE=1&& set BITNET_DENSE_Q8_RUNTIME_TENSOR=blk.0.attn_q.weight&& cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu slm-warm-session --model models\\slm\\Qwen3-0.6B-Q8_0.gguf --corpus ci\\quality\\slm-warm-session-corpus.yaml --corpus-repeat-runs 2 --max-new-tokens 8 --temperature 0.0 --greedy --deterministic --threads 4 --strict-loader --strict-tokenizer --prompt-template qwen --require-determinism --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-21\\qwen3-slm-cpu-071-after-warm-session.json\"",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "SLM-CPU-071 is the required real i5-8250U artifact timing gate after the SLM-CPU-070 block-local packed Q8_0 prototype.",
  "Timing classification is allowed only after generated IDs, decoded text, strict tokenizer authority, selected CPU backend/kernel, model SHA, hook identity, and fallback=false match the Qwen3 Q8_0 appliance oracle.",
]
must_not_claim = [
  "Packed Q8_0 runtime compute is enabled by default.",
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "A speedup or sustained 8250U throughput claim is proven before the real before/after artifact pack is validated.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-072"
status = "merged"
pr = 192
head_sha = "cc575cbd73a1deee2dfaeae073ddd23928a52123"
merge_sha = "f40802d0c53140b2d8a24d70fd504f52a6674867"
branch = "codex/slm-cpu-072-real-artifact-capture"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-071"]
acceptance = "Capture or ingest the real i5-8250U Qwen3-0.6B Q8_0 post-SLM-CPU-070 before/after warm-session artifact pack that SLM-CPU-071 defined as the required timing gate. The slice must run or commit the established 4-thread appliance profile with the default eager_f32_candle path and the opt-in exact-tensor `layers.0.attention.q_proj.weight` packed_q8_sidecar candidate, prove identical prompt IDs, generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel identity, dense hook-selection identity, model SHA, and fallback_used=false before timing interpretation, then classify timing as improved, regressed, or inconclusive. If the verified Qwen3 GGUF is unavailable in the execution environment, the PR must record that as a blocker and must not fabricate artifacts. It must not enable packed Q8_0 by default, broaden beyond the exact tensor, claim sustained throughput, claim broad answer quality, start Q4/Q5 runtime support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu slm-warm-session --model models\\slm\\Qwen3-0.6B-Q8_0.gguf --corpus ci\\quality\\slm-warm-session-corpus.yaml --corpus-repeat-runs 2 --max-new-tokens 8 --temperature 0.0 --greedy --deterministic --threads 4 --strict-loader --strict-tokenizer --prompt-template qwen --require-determinism --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-21\\qwen3-slm-cpu-072-before-warm-session.json",
  "cmd /C \"set BITNET_DENSE_Q8_PAYLOAD_ENABLE=1&& set BITNET_DENSE_Q8_PAYLOAD_TENSOR=blk.0.attn_q.weight&& set BITNET_DENSE_Q8_RUNTIME_ENABLE=1&& set BITNET_DENSE_Q8_RUNTIME_TENSOR=blk.0.attn_q.weight&& cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu slm-warm-session --model models\\slm\\Qwen3-0.6B-Q8_0.gguf --corpus ci\\quality\\slm-warm-session-corpus.yaml --corpus-repeat-runs 2 --max-new-tokens 8 --temperature 0.0 --greedy --deterministic --threads 4 --strict-loader --strict-tokenizer --prompt-template qwen --require-determinism --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-21\\qwen3-slm-cpu-072-after-warm-session.json\"",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "SLM-CPU-072 is the real artifact capture/classification follow-up to the SLM-CPU-071 timing gate.",
  "Timing classification is allowed only after generated IDs, decoded text, strict tokenizer authority, selected CPU backend/kernel, model SHA, hook identity, and fallback=false match the Qwen3 Q8_0 appliance oracle.",
]
must_not_claim = [
  "Packed Q8_0 runtime compute is enabled by default.",
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "A speedup or sustained 8250U throughput claim is proven before the real before/after artifact pack is validated.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-073"
status = "merged"
pr = 195
head_sha = "92c464e790f7a7f2297d55c9612d3861898544d3"
merge_sha = "d429f515514e4680442df0a2dc7d70020ae1a485"
branch = "codex/slm-cpu-073-packed-q8-regression-root-cause"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-072"]
acceptance = "Use the SLM-CPU-072 real six-prompt i5-8250U timing evidence to localize why the opt-in exact-tensor `layers.0.attention.q_proj.weight` packed_q8_sidecar path remains slower than the default eager_f32_candle path after the SLM-CPU-070 block-local matvec prototype. The slice must preserve eager_f32_candle as the default runtime, keep packed sidecar execution opt-in and exact-tensor scoped, identify whether the next fix belongs in payload decode, packed-block matvec locality, scratch allocation, selector/dispatch overhead, tensor layout, or timing instrumentation, and emit a machine-checkable root-cause or next-target artifact. It must not change generated IDs/text, weaken strict tokenizer/backend/fallback receipts, claim speedup, enable packed Q8_0 by default, start Q4/Q5 runtime support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-073 is the root-cause follow-up to the SLM-CPU-072 real six-prompt timing regression.",
  "The artifact may identify the next exact packed-Q8 optimization target after preserving the Qwen3 Q8_0 appliance behavior oracle.",
]
must_not_claim = [
  "Packed Q8_0 runtime compute is enabled by default.",
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "A speedup or sustained 8250U throughput claim is proven.",
  "Generated IDs, decoded text, model SHA, tokenizer authority, selected CPU backend/kernel identity, dense hook identity, or fallback=false provenance changed.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-074"
status = "merged"
pr = 219
head_sha = "9f8b37c7e2bbd114ac32faa9089e9c3911222af1"
merge_sha = "91634fb46366674f178fd51dde87af8feeb379cb"
branch = "codex/slm-cpu-074-packed-q8-sidecar-instrumentation"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-073"]
acceptance = "Add the packed Q8_0 sidecar matvec instrumentation and scratch-elimination gate after SLM-CPU-073. The slice must keep eager_f32_candle as the default runtime, keep packed sidecar execution opt-in and exact-tensor scoped to `layers.0.attention.q_proj.weight`, measure or eliminate input flatten/to_vec, output Vec allocation, and bias extraction costs before any broader packed-Q8 runtime selection, add per-call or aggregate timing counters around sidecar input materialization, packed matvec compute, output tensor construction, and selector dispatch, and preserve identical model SHA, tokenizer source/strictness, prompt IDs, generated IDs, decoded text, selected CPU backend identity, dense hook identity, and fallback_used=false. It must not claim speedup unless a separate bounded before/after timing artifact proves it, enable packed Q8_0 by default, broaden beyond the exact tensor, start Q4/Q5 runtime support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu exact_q8_sidecar_runtime_hook_matches_dense_linear_reference",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu exact_q8_sidecar_runtime_hook_matches_reference_across_q8_blocks",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu exact_q8_sidecar_runtime_hook_matches_reference_when_rows_split_q8_blocks",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-073 localized the remaining exact-tensor packed Q8_0 sidecar regression to host tensor materialization, scalar matvec, and scratch allocation.",
  "SLM-CPU-074 may measure or eliminate those exact costs only while preserving the Qwen3 Q8_0 appliance behavior oracle.",
]
must_not_claim = [
  "Packed Q8_0 runtime compute is enabled by default.",
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "A speedup or sustained 8250U throughput claim is proven without a separate bounded before/after timing artifact.",
  "Generated IDs, decoded text, model SHA, tokenizer authority, selected CPU backend/kernel identity, dense hook identity, or fallback=false provenance changed.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-075"
status = "merged"
pr = 225
branch = "codex/slm-cpu-075-packed-q8-instrumentation-artifact"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-074"]
acceptance = "Consume the SLM-CPU-074 packed Q8_0 sidecar instrumentation on the exact `layers.0.attention.q_proj.weight` Qwen3 Q8_0 path and emit a bounded diagnostic artifact classifying selector dispatch, input materialization, bias extraction, packed matvec compute, and output tensor construction costs before any further runtime promotion. The slice must preserve eager_f32_candle as the default runtime, keep packed sidecar execution opt-in and exact-tensor scoped, prove generated IDs, decoded text, model SHA, tokenizer source/strictness, selected CPU backend identity, dense hook identity, and fallback_used=false remain unchanged where a real run is available, and record an explicit blocker if the verified GGUF or hardware run is unavailable. It must not claim speedup, enable packed Q8_0 by default, broaden beyond the exact tensor, start Q4/Q5 runtime support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu exact_q8_sidecar_runtime_hook_matches_dense_linear_reference",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu exact_q8_sidecar_runtime_hook_matches_reference_across_q8_blocks",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu exact_q8_sidecar_runtime_hook_matches_reference_when_rows_split_q8_blocks",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-074 exposes aggregate instrumentation for the exact packed Q8_0 sidecar runtime hook.",
  "SLM-CPU-075 may classify exact-tensor sidecar costs or record a concrete artifact-capture blocker.",
]
must_not_claim = [
  "Packed Q8_0 runtime compute is enabled by default.",
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "A speedup or sustained 8250U throughput claim is proven.",
  "Generated IDs, decoded text, model SHA, tokenizer authority, selected CPU backend/kernel identity, dense hook identity, or fallback=false provenance changed.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-076"
status = "merged"
pr = 229
head_sha = "93d04ab2fe53e3424aa39ceb27b3129f4cf8d3ee"
merge_sha = "3436ba44928865157be1cd94f03858ebc3a01d12"
branch = "codex/slm-cpu-076-sidecar-instrumentation-receipt-bridge"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-075"]
acceptance = "Add the Qwen3 Q8_0 warm-session sidecar instrumentation receipt bridge identified by SLM-CPU-075. The slice must reset the `bitnet-transformer` packed Q8_0 sidecar instrumentation counters before the opt-in exact-tensor `layers.0.attention.q_proj.weight` warm-session run, snapshot them afterward, and serialize selector dispatch, input materialization, bias extraction, packed matvec compute, and output tensor construction counters into the warm-session receipt or a bounded companion diagnostic artifact. It must preserve eager_f32_candle as the default runtime, keep packed sidecar execution opt-in and exact-tensor scoped, and prove prompt IDs, generated IDs, decoded text, model SHA, tokenizer source/strictness, selected CPU backend identity, dense hook identity, and fallback_used=false remain unchanged before any counter-driven optimization or speed claim. It must not enable packed Q8_0 by default, broaden beyond the exact tensor, start Q4/Q5 runtime support, claim sustained throughput or broad answer quality, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu exact_q8_sidecar_runtime_hook_matches_dense_linear_reference",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "Cargo.lock",
  "crates/bitnet-cli/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "The warm-session receipt path records packed Q8_0 sidecar instrumentation counters when the exact-tensor opt-in path is selected.",
  "SLM-CPU-076 may produce a bounded instrumentation counter artifact or record a concrete receipt-bridge blocker.",
]
must_not_claim = [
  "Packed Q8_0 runtime compute is enabled by default.",
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "A speedup or sustained 8250U throughput claim is proven.",
  "Generated IDs, decoded text, model SHA, tokenizer authority, selected CPU backend/kernel identity, dense hook identity, or fallback=false provenance changed.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-077"
status = "merged"
pr = 264
head_sha = "85dd24b86c0f573a12202a4011b002fe5acfd23f"
merge_sha = "5dfdd69190930738f8edace5ef860bd2301cfefd"
branch = "codex/slm-cpu-077-post-bridge-counter-artifact"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-076"]
acceptance = "Capture or ingest the first real i5-8250U Qwen3 Q8_0 warm-session receipt after the SLM-CPU-076 packed Q8_0 sidecar instrumentation bridge, using the exact opt-in `layers.0.attention.q_proj.weight` sidecar path. The artifact must serialize `dense_q8_sidecar_instrumentation` counters for selector dispatch, input materialization, bias materialization, packed matvec compute, and output tensor construction, prove prompt IDs, generated IDs, decoded text, model SHA, tokenizer source/strictness, selected CPU backend identity, dense hook identity, and fallback_used=false remain unchanged against the eager_f32_candle oracle, and classify the next evidence-scoped optimization target or record a concrete verified-GGUF/hardware blocker. It must not enable packed Q8_0 by default, broaden beyond the exact tensor, start Q4/Q5 runtime support, claim speedup, sustained throughput, or broad answer quality, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu slm-warm-session --model models\\slm\\Qwen3-0.6B-Q8_0.gguf --corpus ci\\quality\\slm-warm-session-corpus.yaml --corpus-repeat-runs 1 --max-new-tokens 8 --temperature 0.0 --greedy --deterministic --threads 4 --strict-loader --strict-tokenizer --prompt-template qwen --require-determinism --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-21\\qwen3-slm-cpu-077-post-bridge-counter-artifact.json",
  "python -m json.tool ci/slm-cpu/intel-i5-8250u/2026-05-21/qwen3-slm-cpu-077-post-bridge-counter-artifact.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-077 is the real post-bridge counter artifact gate after SLM-CPU-076.",
  "A valid artifact may classify the next exact-tensor packed Q8_0 sidecar optimization target from serialized warm-session instrumentation counters.",
]
must_not_claim = [
  "Packed Q8_0 runtime compute is enabled by default.",
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "A speedup or sustained 8250U throughput claim is proven.",
  "Generated IDs, decoded text, model SHA, tokenizer authority, selected CPU backend/kernel identity, dense hook identity, or fallback=false provenance changed.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-078"
status = "merged"
pr = 277
head_sha = "a5b7b8c4081a0639fd23bdf3cc6d25c26fa67e3a"
merge_sha = "3b39511e9e1a1d42ffad31de95c7faf248a47130"
branch = "codex/slm-cpu-078-packed-matvec-aligned-path"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-077"]
acceptance = "Use the SLM-CPU-077 real post-bridge counter artifact to reduce or tightly classify the dominant packed Q8_0 exact-tensor matvec compute cost for `layers.0.attention.q_proj.weight`. The slice may optimize only the opt-in exact-tensor packed sidecar path and must keep eager_f32_candle as the default runtime. Any runtime change must preserve prompt IDs, generated IDs, decoded text, model SHA, tokenizer source/strictness, selected CPU backend/kernel identity, dense hook identity, and fallback_used=false against the Qwen3 Q8_0 appliance oracle before claiming even a bounded improvement. If no safe optimization lands, emit a concrete blocker or next-target artifact. It must not enable packed Q8_0 by default, broaden beyond the exact tensor, start Q4/Q5 runtime support, claim sustained throughput or broad answer quality, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu dense_q8",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu slm-warm-session --model models\\slm\\Qwen3-0.6B-Q8_0.gguf --corpus ci\\quality\\slm-warm-session-corpus.yaml --corpus-repeat-runs 1 --max-new-tokens 8 --temperature 0.0 --greedy --deterministic --threads 4 --strict-loader --strict-tokenizer --prompt-template qwen --require-determinism --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-22\\qwen3-slm-cpu-078-packed-matvec-compute-cleanup.json",
  "python -m json.tool ci/slm-cpu/intel-i5-8250u/2026-05-22/qwen3-slm-cpu-078-packed-matvec-compute-cleanup.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-078 is the evidence-scoped packed matvec compute cleanup target after SLM-CPU-077.",
  "A valid implementation may improve or further classify only the opt-in exact-tensor packed Q8_0 sidecar path when generated IDs and strict provenance remain unchanged.",
]
must_not_claim = [
  "Packed Q8_0 runtime compute is enabled by default.",
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-079"
status = "merged"
pr = 291
branch = "codex/slm-cpu-079-post-aligned-matvec-artifact"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-078"]
acceptance = "Capture or ingest the first real i5-8250U Qwen3 Q8_0 warm-session artifact after the SLM-CPU-078 aligned packed Q8_0 exact-tensor matvec implementation. The artifact must prove prompt IDs, generated IDs, decoded text, model SHA, tokenizer source/strictness, selected CPU backend/kernel identity, dense hook identity, and fallback_used=false remain unchanged against the Qwen3 Q8_0 appliance oracle, and must serialize enough packed-sidecar instrumentation counters to classify whether aligned packed matvec compute improved, regressed, or remains blocked. It must not claim speedup or sustained throughput without a real before/after artifact, must not enable packed Q8_0 by default, must not broaden beyond the exact tensor, and must not touch Q4/Q5, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu slm-warm-session --model models\\slm\\Qwen3-0.6B-Q8_0.gguf --corpus ci\\quality\\slm-warm-session-corpus.yaml --corpus-repeat-runs 1 --max-new-tokens 8 --temperature 0.0 --greedy --deterministic --threads 4 --strict-loader --strict-tokenizer --prompt-template qwen --require-determinism --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-22\\qwen3-slm-cpu-079-post-aligned-matvec-artifact.json",
  "python -m json.tool ci/slm-cpu/intel-i5-8250u/2026-05-22/qwen3-slm-cpu-079-post-aligned-matvec-artifact.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-079 is the post-SLM-CPU-078 real artifact gate for the exact-tensor aligned packed Q8_0 sidecar path.",
  "A valid artifact may classify bounded aligned packed matvec behavior only when generated IDs and strict provenance remain unchanged.",
]
must_not_claim = [
  "SLM-CPU-078 proves a speedup without real post-merge artifact evidence.",
  "Packed Q8_0 runtime compute is enabled by default.",
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-033"
status = "merged"
pr = 5603
head_sha = "8896bf4c9"
merge_sha = "03523b8120a41bf148d64d584b3bdc7394af383f"
branch = "codex/slm-cpu-033-forward-allocation-boundary"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-032"]
acceptance = "Record the next Qwen3 Q8_0 Kaby warm-session allocation boundary after prompt-token caching by making aggregate allocation receipts name the dominant measured hotspot and next evidence-scoped optimization target. The slice must preserve generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false when real artifacts are regenerated. It must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit_names_next_target",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_allocation_audit_reports_prompt_setup_breakdown",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "Aggregate Qwen3 Q8_0 Kaby warm-session allocation receipts name the dominant measured hotspot.",
  "Aggregate receipts name the next evidence-scoped optimization target without changing generation behavior.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "A runtime speedup is proven by this slice.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-034"
status = "merged"
pr = 5612
head_sha = "d939ba3e4"
merge_sha = "8418b2ca505a23b0ba7a23edbd3f67fbe9e651ea"
branch = "codex/slm-cpu-034-prefill-forward-attribution"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-033"]
acceptance = "Add bounded Qwen3 Q8_0 Kaby warm-session attribution for the dominant prompt_prefill/model.forward allocation boundary identified by SLM-CPU-033. The slice should split model-forward allocation counters into the narrowest practical decode/prefill subcomponents before changing dense math, preserving generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false when real artifacts are regenerated. It must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit_names_next_target",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The Qwen3 Q8_0 Kaby warm-session allocation receipt attributes the dominant prompt_prefill/model.forward boundary more precisely.",
  "The slice identifies the next measured allocation target without changing generated IDs or strict provenance.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "A runtime speedup is proven before before/after receipts exist.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-035"
status = "merged"
pr = 5615
head_sha = "d25b49bed9c2bdf505439ae3e4d91fe1b39afba9"
merge_sha = "a6d69818e3099c71b21a33c3428bd085bedb2119"
branch = "codex/slm-cpu-035-prefill-attribution-artifact"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-034"]
acceptance = "Regenerate the real i5-8250U Qwen3 Q8_0 warm-session allocation artifact after SLM-CPU-034 so the receipt records prompt_prefill_breakdown.embed and prompt_prefill_breakdown.forward, ranked prompt_prefill subcomponent hotspots, generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false. The validation must preserve the SLM-CPU-032/034 behavior oracle and must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu slm-warm-session --model models\\slm\\Qwen3-0.6B-Q8_0.gguf --corpus ci\\quality\\slm-warm-session-corpus.yaml --corpus-repeat-runs 2 --max-new-tokens 8 --temperature 0.0 --greedy --deterministic --threads 4 --strict-loader --strict-tokenizer --prompt-template qwen --require-determinism --allocation-audit --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-18\\qwen3-prefill-attribution.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The real i5-8250U Qwen3 Q8_0 warm-session receipt records prompt_prefill embed/forward allocation attribution.",
  "Generated IDs, decoded text, strict tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false remain preserved.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "A runtime speedup is proven by attribution evidence alone.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-036"
status = "merged"
pr = 5625
head_sha = "5874d82525cecdb4b9de10f471f70d4ecf0710d3"
merge_sha = "e03b877d3340ebf235d3739a731fb8d4d3351756"
branch = "codex/slm-cpu-036-prefill-forward-buffer-boundary"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-035"]
acceptance = "Use the SLM-CPU-035 artifact to narrow the Qwen3 Q8_0 Kaby prompt_prefill.forward allocation boundary before changing dense math. Identify the first reusable allocation surface inside model.forward/prompt prefill, add a bounded buffer-reuse or allocation-classification change only if it preserves generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false, and record before/after allocation evidence. The slice must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit_names_next_target",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The next Qwen3 Q8_0 Kaby allocation target is scoped to prompt_prefill.forward/model.forward evidence.",
  "Any implemented allocation change preserves the strict Qwen3 Q8_0 behavior oracle and fallback=false if real artifacts are regenerated.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-037"
status = "merged"
pr = 5643
head_sha = "fc18f1147d93c62c4cf3e94764c775cf6aa55c3a"
merge_sha = "724a42346fb38130c868def7faaedd1655bd0078"
branch = "codex/slm-cpu-037-transformer-forward-buffer-reuse"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-036"]
acceptance = "Use the SLM-CPU-036 prompt_prefill.forward boundary classification to add the first bounded reusable transformer-forward buffer/workspace, or explicitly prove the owned tensor-output boundary cannot be safely reused yet, while preserving generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false. Record before/after allocation evidence or an explicit no-reuse classification artifact. The slice must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The next Qwen3 Q8_0 Kaby allocation slice targets transformer-forward buffer reuse after prompt_prefill.forward classification.",
  "Any implemented allocation change preserves the strict Qwen3 Q8_0 behavior oracle and fallback=false if real artifacts are regenerated.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-038"
status = "merged"
pr = 5679
head_sha = "b1eb461a4c48b22a83d1586d91fbe4dd9a950fc6"
merge_sha = "5d8df74514d757c5566de475cd87e9378e46cbf0"
branch = "codex/slm-cpu-038-typed-transformer-forward-workspace"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-037"]
acceptance = "Use the SLM-CPU-037 no-reuse classification to introduce the first typed transformer forward workspace API boundary, or explicitly prove the next narrower API hook needed before replacing owned tensor outputs. The slice must preserve generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false when behavior artifacts are regenerated. It must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The next Qwen3 Q8_0 Kaby allocation slice is scoped to a typed transformer forward workspace API boundary.",
  "Any implemented API or workspace change preserves the strict Qwen3 Q8_0 behavior oracle and fallback=false if real artifacts are regenerated.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-039"
status = "merged"
pr = 5693
head_sha = "34af62e419025a9b1dddfc863359ccda1ea82bd3"
merge_sha = "a6a3466cca749a45f92f6373c67dbde735d13acc"
branch = "codex/slm-cpu-039-workspace-owned-tensor-output"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-038"]
acceptance = "Use the SLM-CPU-038 typed transformer forward workspace API boundary to replace or narrowly classify one owned transformer tensor output with workspace-owned storage. The slice must preserve generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false when behavior artifacts are regenerated, and must update allocation-boundary evidence without claiming speedup or sustained throughput."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The next Qwen3 Q8_0 Kaby allocation slice is scoped to one workspace-owned transformer tensor output or a narrower no-reuse classification.",
  "The SLM-CPU-038 typed transformer forward workspace API boundary is available for behavior-preserving allocation work.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-040"
status = "merged"
pr = 5715
head_sha = "153ea5a22ad0d6949c27c7229eba068a2ed89b12"
merge_sha = "1060a032a7a0c01e1e48b8676328874a873ef88d"
branch = "codex/slm-cpu-040-workspace-down-proj-storage"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-039"]
acceptance = "Use the SLM-CPU-039 `feed_forward.output` workspace-owned boundary to attempt one reusable workspace-backed `FeedForward::down_proj` output storage hook, or explicitly prove why Candle's current linear/output API still prevents safe reuse at that exact boundary. The slice must preserve generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false when behavior artifacts are regenerated; it must update allocation-boundary evidence and must not claim speedup or sustained throughput."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The next Qwen3 Q8_0 Kaby allocation slice is scoped to the exact FeedForward::down_proj output storage hook behind the existing workspace-owned feed_forward.output boundary.",
  "A valid implementation may either enable one reusable workspace-backed output slot or record a narrower no-reuse proof for this API boundary.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "A runtime speedup is proven without before/after behavior-preserving receipts.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-041"
status = "merged"
pr = 5754
head_sha = "f9ee7725af17b63a66e9c3bfab104ff80fcdece0"
merge_sha = "40a80c69125aad71b487891a781b979d59a95d48"
branch = "codex/slm-cpu-041-linear-output-storage-api"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-040"]
acceptance = "Add or adopt a behavior-preserving dense linear output-storage API boundary that can fill a caller-provided workspace slot for the Qwen3 Q8_0 Kaby FeedForward::down_proj output, or prove with a narrower implementation note why Candle's current Tensor/Linear surface still prevents this without changing math semantics. The slice must preserve generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false when behavior artifacts are regenerated; it must keep SLM-CPU-040's no-speedup/no-throughput boundary unless reusable storage is actually implemented and proven with before/after receipts."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The next Qwen3 Q8_0 Kaby allocation slice is scoped to a behavior-preserving dense linear output-storage API boundary for FeedForward::down_proj output.",
  "A valid implementation may enable one reusable workspace-backed output slot only if generated IDs/text and strict receipts remain unchanged.",
  "A valid no-reuse slice may narrow the blocker if Candle's current linear API still prevents safe caller-provided output storage.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "A runtime speedup is proven without before/after behavior-preserving receipts.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-042"
status = "merged"
pr = 5773
head_sha = "60c2d6397fe6e831728e466d6dff365e705c28cb"
merge_sha = "a2c869122d3930379437f67d7fc8abba9699d766"
branch = "codex/slm-cpu-042-q8-dense-linear-locality"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-041"]
acceptance = "Start the next Kaby Qwen3 Q8_0 dense-math performance slice after SLM-CPU-041 proved reusable FeedForward::down_proj output storage is blocked by Candle Tensor matmul/bias-add owned returns. The slice must identify and instrument the first behavior-preserving Q8_0 dense linear locality or matmul/dequant boundary that can be optimized without changing generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel, model SHA, or fallback=false; if no safe runtime change is made, it must produce a concrete implementation boundary for the next slice. It must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The next Kaby SLM performance slice is scoped to Qwen3 Q8_0 dense linear locality or matmul/dequant boundary evidence after the Candle output-storage API blocker.",
  "A valid runtime optimization must preserve generated IDs/text, strict tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false before claiming any bounded improvement.",
  "A valid no-change slice may identify the first concrete dense-math implementation boundary for the next behavior-preserving optimization.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "A runtime speedup is proven without before/after behavior-preserving receipts.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-043"
status = "merged"
pr = 5794
head_sha = "2661f94be70693fc3ffad5d8fb9b2bad947775ad"
merge_sha = "f2087460628299e8474860c33633475aa7a35cf1"
branch = "codex/slm-cpu-043-q8-packed-linear-prototype"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-042"]
acceptance = "Use the SLM-CPU-042 eager Q8_0 dequant-to-F32-before-Candle boundary to attempt the first behavior-preserving Q8_0 dense linear locality implementation, such as a packed Q8_0 sidecar or dequant-fused dense-linear prototype on a narrow path. The slice must compare against the existing eager F32 path and preserve generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false before claiming any bounded improvement. If no safe runtime change is made, it must produce a concrete no-change blocker artifact naming the API or layout gap for the next slice. It must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-models --no-default-features --features cpu",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "A Q8_0 dense linear locality implementation or no-change blocker is scoped to the SLM-CPU-042 eager dequant-to-F32-before-Candle boundary.",
  "Any bounded improvement is valid only if generated IDs/text, strict tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false are preserved against the existing eager F32 path.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "A runtime speedup is proven without before/after behavior-preserving receipts.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-044"
status = "merged"
pr = 5810
head_sha = "9265ecf265f11035b0a7371844ad2809c0f0bc55"
merge_sha = "3663cb0ad4e8b54460a0716d08ac676dbbb91f64"
branch = "codex/slm-cpu-044-q8-sidecar-runtime-plan"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-043"]
acceptance = "Define the first production-integration boundary after the SLM-CPU-043 fixture-level Q8_0 packed sidecar prototype. The slice must specify how a packed Q8_0 sidecar can be carried from GGUF load metadata toward a runtime dense-linear path without replacing the existing eager F32 Candle tensors yet, including exact behavior-preservation gates for prompt IDs, generated IDs, decoded text, model SHA, strict GGUF tokenizer authority, selected CPU backend/kernel, and fallback=false. It must name any remaining API/layout blockers before runtime use and must not claim speedup, sustained throughput, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign check slm-cpu",
  "cargo run -p xtask --no-default-features -- campaign generate --check",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The next Q8_0 packed sidecar runtime-integration boundary is ready after the fixture-level prototype.",
  "Packed Q8_0 sidecar runtime use remains gated on preserving generated IDs/text, strict tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false against the Qwen3 Q8_0 Kaby behavior oracle.",
]
must_not_claim = [
  "Packed Q8_0 sidecars replace the production dense runtime.",
  "A runtime speedup is proven.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-045"
status = "merged"
pr = 5845
head_sha = "bd8cae5c28771202169b9f4551de1988bfe4a5fb"
merge_sha = "78fe24de69bb8ac320869927009e0b6ff2200669"
branch = "codex/slm-cpu-045-q8-sidecar-carrier"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-044"]
acceptance = "Add the first production-facing Qwen3 Q8_0 packed sidecar carrier after SLM-CPU-044 by preserving packed Q8_0 block metadata from strict GGUF tensor loading into an inert model-side sidecar registry or descriptor while keeping the existing eager F32 Candle tensors as the only runtime compute path. The slice must prove the sidecar carrier is metadata-only/inert for generation, preserve prompt IDs, generated IDs, decoded text, model SHA, strict GGUF tokenizer authority, selected CPU backend/kernel, and fallback=false in behavior evidence or fixture-equivalent gates, and name the next runtime API hook needed before any dense-linear sidecar compute can be selected. It must not claim speedup, sustained throughput, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-models dense_gguf_q8",
  "cargo test --locked -p bitnet-models --no-default-features",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "Packed Q8_0 sidecar metadata can be carried alongside the existing eager F32 dense tensors for the next runtime-integration slice.",
  "The production runtime still uses the existing eager F32 Candle tensors unless a later sidecar compute selector preserves generated IDs/text and strict receipts.",
]
must_not_claim = [
  "Packed Q8_0 sidecars replace the production dense runtime.",
  "A runtime speedup is proven.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-046"
status = "merged"
pr = 5860
head_sha = "51fa63018fe0189777fcb596665e8bfcbe6a66ad"
merge_sha = "4bfa68cbc5577bb0a81b709639a74af28788a134"
branch = "codex/slm-cpu-046-q8-sidecar-dispatch-selector"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-045"]
acceptance = "Add the first behavior-preserving dense-linear dispatch selector contract after SLM-CPU-045 by making the runtime choose the existing eager F32 path explicitly while exposing a packed Q8_0 sidecar candidate only as unavailable until generated-ID, decoded-text, strict tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false equivalence gates pass. The slice may add selector structs, reasons, fixture tests, and dashboard/tracker documentation, but must not execute packed Q8_0 sidecar compute, claim speedup, alter generated IDs/text, change tokenizer or backend semantics, start Q4/Q5 runtime support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-models dense_gguf_q8 --no-default-features",
  "cargo test --locked -p bitnet-models --no-default-features",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The dense-linear runtime selector contract exists and currently selects the eager F32 path explicitly.",
  "Packed Q8_0 sidecar descriptors are visible to the selector as candidates but remain unavailable for runtime compute until equivalence gates pass in a later slice.",
  "The next Q8_0 sidecar runtime slice has a concrete selector API boundary to attach behavior-preserving compute behind.",
]
must_not_claim = [
  "Packed Q8_0 sidecars execute in production runtime.",
  "A runtime speedup is proven.",
  "Sustained 8250U throughput is proven.",
  "Generated IDs or decoded text changed.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-047"
status = "merged"
pr = 5868
head_sha = "632bd95ab5fc3ec1fae21129c6379d75723d42c7"
merge_sha = "d0faced083c3ea119752a7f3a070c85469062ec4"
branch = "codex/slm-cpu-047-q8-sidecar-equivalence-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-046"]
acceptance = "Add the behavior-preserving packed Q8_0 sidecar equivalence gate after SLM-CPU-046. The gate must connect the fixture-level Q8_0 sidecar prototype to the dense-linear selector by recording fixture-vs-eager-F32 parity, selector identity, remaining generated-ID/text receipt blockers, and sidecar_runtime_compute_allowed=false. It must preserve the existing eager F32 Candle runtime as the selected production path and must not execute packed Q8_0 sidecar compute, claim speedup, alter generated IDs/text, change tokenizer or backend semantics, start Q4/Q5 runtime support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-models --lib dense_gguf_q8 --no-default-features",
  "cargo test --locked -p bitnet-models --no-default-features",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The packed Q8_0 sidecar fixture equivalence gate exists.",
  "The gate records fixture-vs-eager-F32 parity and the remaining generated-ID/text receipt blocker before runtime use.",
  "The production dense-linear selector still chooses the eager F32 Candle path.",
]
must_not_claim = [
  "Packed Q8_0 sidecars execute in production runtime.",
  "A runtime speedup is proven.",
  "Sustained 8250U throughput is proven.",
  "Generated IDs or decoded text changed.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-048"
status = "merged"
pr = 5873
head_sha = "4b1f335ec95ff23a5fd457711e21249ea6750546"
merge_sha = "eb3fb38d17d5b545448492c65535c7c187b50a34"
branch = "codex/slm-cpu-048-q8-sidecar-runtime-preflight"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-047"]
acceptance = "Add the first non-executing packed Q8_0 sidecar runtime preflight after SLM-CPU-047. The slice must consume the sidecar equivalence gate and selector evidence to produce an explicit runtime eligibility report naming each remaining blocker before packed sidecar compute can be selected. It must keep selected_path=eager_f32_candle and sidecar_runtime_compute_allowed=false unless generated-ID/text receipt equivalence and a production compute hook both exist. It must not execute packed Q8_0 sidecar compute, claim speedup, alter generated IDs/text, change tokenizer or backend semantics, start Q4/Q5 runtime support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-models --lib dense_gguf_q8 --no-default-features",
  "cargo test --locked -p bitnet-models --no-default-features",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The next packed Q8_0 sidecar runtime preflight reports eligibility blockers before runtime selection.",
  "The production dense-linear selector still chooses the eager F32 Candle path until the gate is fully satisfied and a production compute hook exists.",
]
must_not_claim = [
  "Packed Q8_0 sidecars execute in production runtime.",
  "A runtime speedup is proven.",
  "Sustained 8250U throughput is proven.",
  "Generated IDs or decoded text changed.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-049"
status = "merged"
pr = 5879
head_sha = "363f24398a71b6e363fbf38a85a1cbd4507ff438"
merge_sha = "f76c93862c5feb94d7688061f571f6704d9b01e1"
branch = "codex/slm-cpu-049-generated-id-equivalence-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-048"]
acceptance = "Add the generated-ID/text receipt equivalence gate required after the packed Q8_0 sidecar runtime preflight. The slice must define or implement a machine-checkable equivalence surface that compares the eager F32 behavior oracle against any future packed Q8_0 sidecar candidate by model SHA, tokenizer source/strictness, prompt IDs, generated IDs, decoded text, selected backend/kernel identity, fallback=false, corpus/prompt identity, and claim boundaries. It must keep selected_path=eager_f32_candle and sidecar_runtime_compute_allowed=false until both generated-ID/text receipt equivalence and a production compute hook exist. It must not execute packed Q8_0 sidecar compute, claim speedup, alter generated IDs/text, change tokenizer or backend semantics, start Q4/Q5 runtime support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-models --lib dense_gguf_q8 --no-default-features",
  "cargo test --locked -p bitnet-models --no-default-features",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The packed Q8_0 sidecar runtime lane has a generated-ID/text receipt equivalence gate before runtime selection.",
  "The production dense-linear selector still chooses the eager F32 Candle path until generated-ID/text equivalence and a production compute hook both exist.",
]
must_not_claim = [
  "Packed Q8_0 sidecars execute in production runtime.",
  "A runtime speedup is proven.",
  "Sustained 8250U throughput is proven.",
  "Generated IDs or decoded text changed.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-050"
status = "merged"
pr = 5885
head_sha = "c11508411e022dbd5809239d3f0f1adb47d0ae02"
merge_sha = "29ffb758ba57d78c21968f07b74d4563611ef4b6"
branch = "codex/slm-cpu-050-q8-production-hook-availability"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-049"]
acceptance = "Add the first production-compute-hook availability surface for the packed Q8_0 sidecar lane after SLM-CPU-049. The slice must make the production hook explicit and machine-checkable so runtime preflight can distinguish generated-ID/text equivalence from compute-hook availability, while still keeping selected_path=eager_f32_candle and sidecar_runtime_compute_allowed=false until a later selector update proves behavior-preserving generated-ID/text receipts against the established Qwen3 Q8_0 oracle. It may add hook structs, eligibility reports, fixture tests, and dashboard/tracker documentation. It must not execute packed Q8_0 sidecar compute in production, claim speedup, alter generated IDs/text, change tokenizer/backend semantics, start Q4/Q5 runtime support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-models --lib dense_gguf_q8 --no-default-features",
  "cargo test --locked -p bitnet-models --no-default-features",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The packed Q8_0 sidecar runtime lane has an explicit production-compute-hook availability surface.",
  "The production dense-linear selector still chooses the eager F32 Candle path until generated-ID/text equivalence and a later selector update both allow sidecar runtime compute.",
]
must_not_claim = [
  "Packed Q8_0 sidecars execute in production runtime.",
  "A runtime speedup is proven.",
  "Sustained 8250U throughput is proven.",
  "Generated IDs or decoded text changed.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-051"
status = "merged"
pr = 5897
head_sha = "8d86f804c9165e13eb5745ee101b3d4254c371a7"
merge_sha = "8e2d23c1f00aeec898f3f386a0e07eddfc307f87"
branch = "codex/slm-cpu-051-selector-readiness-impl"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-050"]
acceptance = "Add the behavior-preserving selector-readiness gate for the packed Q8_0 sidecar lane after SLM-CPU-050. The slice must consume the generated-ID/text equivalence gate and production-compute-hook availability report to produce a machine-checkable selector update readiness artifact that can say whether all evidence is present before a later runtime selector change. It must keep the production selector on eager_f32_candle, keep sidecar_runtime_compute_allowed=false, and make any remaining blockers explicit. It may add readiness structs, fixture tests, and dashboard/tracker documentation. It must not execute packed Q8_0 sidecar compute in production, claim speedup, alter generated IDs/text, change tokenizer/backend semantics, start Q4/Q5 runtime support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-models --lib dense_gguf_q8 --no-default-features",
  "cargo test --locked -p bitnet-models --no-default-features",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The packed Q8_0 sidecar runtime lane has a selector-readiness gate that names the evidence required before a later runtime selector update.",
  "The production dense-linear selector still chooses the eager F32 Candle path until a separate selector update PR changes runtime behavior under generated-ID/text receipt discipline.",
]
must_not_claim = [
  "Packed Q8_0 sidecars execute in production runtime.",
  "A runtime speedup is proven.",
  "Sustained 8250U throughput is proven.",
  "Generated IDs or decoded text changed.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-052"
status = "merged"
pr = 5921
head_sha = "d803a63446c6aec5935cd39b35885f91f00d8ba7"
merge_sha = "d9bd53cbdc87207698a6d8d58604731a405604c7"
branch = "codex/slm-cpu-052-selector-update-impl"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-051"]
acceptance = "Implement the first behavior-preserving packed Q8_0 sidecar selector update after SLM-CPU-051. The slice may allow the dense-linear selector to choose a packed Q8_0 sidecar candidate only for the exact evidence-scoped Qwen3 Q8_0 appliance profile where generated-ID/text equivalence, strict tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false are proven against the eager F32 Candle oracle. If runtime sidecar execution remains blocked, the slice must record the blocker explicitly. Any before/after receipt must preserve prompt IDs, generated IDs, decoded text, loader/tokenizer provenance, backend/kernel identity, and fallback=false before claiming even a bounded improvement. It must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-models --lib dense_gguf_q8 --no-default-features",
  "cargo test --locked -p bitnet-models --no-default-features",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "SLM-CPU-052 is the next Qwen3 Q8_0 Kaby dense-linear selector update gate after the selector-readiness report.",
  "Any packed Q8_0 sidecar selector update must preserve the eager F32 Candle behavior oracle before it can claim a bounded improvement.",
]
must_not_claim = [
  "Packed Q8_0 sidecar runtime execution is enabled before generated-ID/text receipt equivalence and strict provenance are preserved.",
  "A runtime speedup is proven without before/after receipts.",
  "Sustained 8250U throughput is proven.",
  "Generated IDs or decoded text changed.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-053"
status = "merged"
pr = 5943
head_sha = "2ee3ddd8932af8d12054ddfcc6772e81549ee833"
merge_sha = "570ae6c50cbd76d01469d9f136b46583df1c59b8"
branch = "codex/slm-cpu-053-runtime-proof-blocker"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-052"]
acceptance = "Add the first evidence-scoped packed Q8_0 sidecar runtime execution proof after SLM-CPU-052. The slice may route only the exact proven dense-linear tensor or minimal tensor set through packed Q8_0 sidecar compute when selector-update proof is supplied, while preserving the eager F32 Candle path as the default and preserving the Qwen3 Q8_0 appliance behavior oracle. It must emit or validate before/after receipts proving identical model SHA, tokenizer source/strictness, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, fallback_used=false, and speedup_claim=false before claiming even a bounded runtime improvement. If runtime execution cannot be enabled safely, the slice must instead record the precise blocker and keep packed sidecar compute disabled. It must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-models --lib dense_gguf_q8 --no-default-features",
  "cargo test --locked -p bitnet-models --no-default-features",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu slm-warm-session --model models\\slm\\Qwen3-0.6B-Q8_0.gguf --corpus ci\\quality\\slm-warm-session-corpus.yaml --corpus-repeat-runs 2 --max-new-tokens 8 --temperature 0.0 --greedy --deterministic --threads 4 --strict-loader --strict-tokenizer --prompt-template qwen --require-determinism --allocation-audit --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-19\\qwen3-packed-q8-sidecar-runtime-proof.json",
  "python -m json.tool ci/slm-cpu/intel-i5-8250u/2026-05-19/qwen3-packed-q8-sidecar-runtime-proof-validation.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-053 is the first possible packed Q8_0 sidecar runtime execution proof gate after the behavior-preserving selector update.",
  "Any runtime improvement is bounded to the exact receipt-proven Qwen3 Q8_0 Kaby appliance profile and must keep speedup_claim=false unless a later benchmark item records a separate performance claim.",
]
must_not_claim = [
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "Generated IDs or decoded text changed.",
  "Strict GGUF tokenizer authority, selected CPU backend/kernel identity, model SHA, or fallback=false provenance changed.",
  "A sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-054"
status = "merged"
pr = 5992
head_sha = "db81e20444019f253b612f6e53bb6291201f5524"
merge_sha = "8157d160c4a66d3982f90589debef9406c3ace19"
branch = "codex/slm-cpu-054-sidecar-runtime-hook"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-053"]
acceptance = "Burn down the SLM-CPU-053 packed Q8_0 sidecar runtime blocker by adding the narrow production dense-linear runtime hook or proving the exact remaining hook/API gap. The slice must keep eager_f32_candle as the default Qwen3 Q8_0 Kaby behavior oracle unless before/after receipts prove identical model SHA, tokenizer source/strictness, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, and fallback_used=false. If packed sidecar compute is not safely enabled, emit an updated blocker artifact that names the remaining gap and keeps sidecar_runtime_compute_allowed=false. It must preserve speedup_claim=false unless a later performance item records separate bounded timing evidence, and must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-models --lib dense_gguf_q8 --no-default-features",
  "cargo test --locked -p bitnet-models --no-default-features",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu slm-warm-session --model models\\slm\\Qwen3-0.6B-Q8_0.gguf --corpus ci\\quality\\slm-warm-session-corpus.yaml --corpus-repeat-runs 2 --max-new-tokens 8 --temperature 0.0 --greedy --deterministic --threads 4 --strict-loader --strict-tokenizer --prompt-template qwen --require-determinism --allocation-audit --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-19\\qwen3-packed-q8-sidecar-runtime-hook.json",
  "python -m json.tool ci/slm-cpu/intel-i5-8250u/2026-05-19/qwen3-packed-q8-sidecar-runtime-hook-validation.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-054 is the next packed Q8_0 sidecar runtime-hook blocker burn-down after SLM-CPU-053.",
  "The default Qwen3 Q8_0 Kaby runtime path remains eager_f32_candle unless before/after generated-ID and decoded-text receipts prove a sidecar path is behavior-preserving.",
  "Any enabled sidecar runtime path is evidence-scoped to the exact Qwen3 Q8_0 Kaby appliance profile and remains speedup_claim=false until a later bounded performance item records timing evidence.",
]
must_not_claim = [
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "Generated IDs or decoded text changed.",
  "Strict GGUF tokenizer authority, selected CPU backend/kernel identity, model SHA, or fallback=false provenance changed.",
  "A sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-055"
status = "merged"
pr = 6008
head_sha = "b8823a533829db1f199a02ebe9c70201fa87463d"
merge_sha = "d6a99278d8a931a3e0ba1d38acf4a541a97b3705"
branch = "codex/slm-cpu-055-production-dense-linear-hook-contract"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-054"]
acceptance = "Add the first production dense-linear hook contract after SLM-CPU-054 so transformer dense linear calls can receive an explicit eager-F32 selection or a selected Q8_0 sidecar descriptor without enabling packed sidecar compute by default. The slice must preserve eager_f32_candle as the default Qwen3 Q8_0 Kaby behavior oracle unless before/after receipts prove identical model SHA, tokenizer source/strictness, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, and fallback_used=false. If packed runtime compute remains disabled, it must emit a machine-checkable hook-contract or blocker artifact naming the remaining dispatch/receipt gap. It must not claim speedup, sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-models --lib dense_gguf_q8 --no-default-features",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-common/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-models/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-055 is the next packed Q8_0 sidecar production-hook contract gate after SLM-CPU-054.",
  "The transformer dense-linear path has an explicit contract for eager-F32 selection or a selected Q8_0 sidecar descriptor before any packed compute can be enabled.",
  "The default Qwen3 Q8_0 Kaby runtime path remains eager_f32_candle unless before/after generated-ID and decoded-text receipts prove a sidecar path is behavior-preserving.",
]
must_not_claim = [
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "Generated IDs or decoded text changed.",
  "Strict GGUF tokenizer authority, selected CPU backend/kernel identity, model SHA, or fallback=false provenance changed.",
  "A speedup or sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-056"
status = "merged"
pr = 6018
head_sha = "0abc84dde3f2a03b8ae691f1ac04c087bc041a2e"
merge_sha = "3f56b26f9a1dc510667f8c93d9ff0bdcec01e286"
branch = "codex/slm-cpu-056-production-dense-linear-hook-impl"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-055"]
acceptance = "Implement the first production dense-linear hook boundary defined by SLM-CPU-055 without enabling packed Q8_0 sidecar compute by default. The hook must let transformer dense linear calls report an explicit eager-F32 selection or a selected Q8_0 sidecar descriptor while preserving the current Qwen3 Q8_0 Kaby behavior oracle. Any before/after artifact must prove identical model SHA, tokenizer source/strictness, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, and fallback_used=false. If packed runtime compute remains disabled, emit a machine-checkable blocker artifact naming the remaining runtime or receipt gap. It must not claim speedup, sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-models --lib dense_gguf_q8 --no-default-features",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-common/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-models/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-056 is the first implementation slice for the production dense-linear hook contract added by SLM-CPU-055.",
  "The transformer dense-linear path can report explicit eager-F32 selection or selected Q8_0 sidecar descriptor state without enabling packed compute by default.",
  "The default Qwen3 Q8_0 Kaby runtime path remains eager_f32_candle unless before/after generated-ID and decoded-text receipts prove a sidecar path is behavior-preserving.",
]
must_not_claim = [
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "Generated IDs or decoded text changed.",
  "Strict GGUF tokenizer authority, selected CPU backend/kernel identity, model SHA, or fallback=false provenance changed.",
  "A speedup or sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-057"
status = "merged"
pr = 6045
head_sha = "80d707d65973a884066d607fd04f07cc18dbd0c2"
merge_sha = "c19c7a51d4edf11d6a459df6ccda8efcab081ca2"
branch = "codex/slm-cpu-057-dense-hook-receipt-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-056"]
acceptance = "Use the SLM-CPU-056 production dense-linear hook boundary to add or prove the next receipt gate for Qwen3 Q8_0 dense hook selection before any packed Q8_0 sidecar compute can be enabled. The slice must keep eager_f32_candle as the default behavior oracle unless before/after Qwen3 Q8_0 warm-session receipts prove identical model SHA, tokenizer source/strictness, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook selection identity, and fallback_used=false. If packed runtime compute remains disabled, emit a machine-checkable blocker artifact naming the remaining compute-kernel or receipt gap. It must not claim speedup, sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-models --lib dense_gguf_q8 --no-default-features",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-common/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-models/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts/**",
  "crates/bitnet-receipts-core/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-057 is the next receipt/proof gate after the production dense-linear hook boundary added by SLM-CPU-056.",
  "The Qwen3 Q8_0 dense-linear hook selection can be recorded or its remaining receipt gap can be precisely named.",
  "The default Qwen3 Q8_0 Kaby runtime path remains eager_f32_candle unless before/after generated-ID and decoded-text receipts prove a sidecar path is behavior-preserving.",
]
must_not_claim = [
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "Generated IDs or decoded text changed.",
  "Strict GGUF tokenizer authority, selected CPU backend/kernel identity, model SHA, or fallback=false provenance changed.",
  "A speedup or sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-058"
status = "merged"
pr = 6070
head_sha = "69c421955c05a2f16b21dffd0533cbf9dfcd80e9"
merge_sha = "f27ec16ac9892f50d01e0530071d984ee891bf8c"
branch = "codex/slm-cpu-058-dense-hook-before-after-receipts"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-057"]
acceptance = "Capture or ingest the next Qwen3 Q8_0 dense-hook before/after warm-session receipt pack after SLM-CPU-057. The pack must compare the eager_f32_candle Qwen3 Q8_0 Kaby behavior oracle against the selected dense-hook path or explicitly record that packed Q8_0 sidecar compute remains disabled. It must prove identical model SHA, tokenizer source/strictness, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook-selection identity, and fallback_used=false before any packed sidecar compute can be enabled. It must preserve speedup_claim=false unless a separate bounded timing item records timing evidence, and must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "python -m json.tool ci/slm-cpu/intel-i5-8250u/2026-05-19/qwen3-dense-hook-receipt-gate.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-058 is the next receipt-equivalence gate after SLM-CPU-057 made dense hook-selection identity visible in Qwen3 Q8_0 warm-session receipts.",
  "A valid SLM-CPU-058 slice may capture or ingest before/after receipts that prove identical generated IDs, decoded text, strict tokenizer authority, selected CPU backend/kernel identity, dense hook-selection identity, model SHA, and fallback=false.",
  "If packed Q8_0 sidecar compute remains disabled, the slice may record that explicitly as the current safe state.",
]
must_not_claim = [
  "Packed Q8_0 sidecar execution is generally enabled for all dense tensors.",
  "Generated IDs or decoded text changed.",
  "Strict GGUF tokenizer authority, selected CPU backend/kernel identity, model SHA, or fallback=false provenance changed.",
  "A speedup or sustained 8250U throughput claim is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-024"
status = "merged"
pr = 5342
head_sha = "2ea2bfaba0d6ed47151bdd00df87902dee5026cc"
merge_sha = "328fc4b893f505f3d06b64dd7abb5da2f47a53a6"
branch = "codex/slm-cpu-024-greedy-sampler-fast-path"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-023"]
acceptance = "Add a guarded greedy no-penalty sampler fast path that bypasses sampler logits scratch copying for temperature=0.0 and inactive repetition penalty, then record a real i5-8250U Qwen3 Q8_0 warm-session after artifact proving the same model SHA, generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend, fallback=false, and zero sampler decode allocations. This slice must not claim sustained throughput, broad answer quality, Q4/Q5 quant expansion, a second dense model, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-sampling",
  "cargo clippy --locked -p bitnet-sampling -- -D warnings",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu slm-warm-session --model models\\slm\\Qwen3-0.6B-Q8_0.gguf --corpus ci\\quality\\slm-warm-session-corpus.yaml --corpus-repeat-runs 2 --max-new-tokens 8 --temperature 0.0 --greedy --deterministic --threads 4 --strict-loader --strict-tokenizer --prompt-template qwen --require-determinism --allocation-audit --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-17\\qwen3-greedy-sampler-fast-path.json",
  "python -m json.tool ci/slm-cpu/intel-i5-8250u/2026-05-17/qwen3-greedy-sampler-fast-path-validation.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-sampling/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 quant expansion",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "After merge, deterministic greedy sampling without active repetition penalty bypasses sampler logits scratch copying.",
  "The recorded i5-8250U Qwen3 Q8_0 warm-session after artifact preserves generated IDs, decoded text, strict tokenizer authority, selected CPU backend, and fallback=false against the 4-thread baseline.",
  "The after artifact records zero sampler decode allocations for the fixed warm-session corpus.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "Broad Qwen answer quality is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "A second dense model is supported.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-025"
status = "merged"
pr = 5357
merge_sha = "1123eafcf28968a43c9b293994a9ca728ae44ac8"
branch = "codex/slm-cpu-025-logits-extraction-reuse"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-024"]
acceptance = "Remove or explicitly isolate the remaining Qwen3-0.6B Q8_0 warm-session logits extraction allocation after SLM-CPU-024 without changing generated IDs or strict receipt provenance. The slice must preserve the Qwen3 Q8_0 4-thread behavior oracle, keep model SHA, prompt IDs, generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend, and fallback=false identical to the recorded baseline, and emit before/after allocation evidence or explicitly documented unavailable counters. It must not claim sustained throughput, broad answer quality, Q4/Q5 quant expansion, a second dense model, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu slm-warm-session --model models\\slm\\Qwen3-0.6B-Q8_0.gguf --corpus ci\\quality\\slm-warm-session-corpus.yaml --corpus-repeat-runs 2 --max-new-tokens 8 --temperature 0.0 --greedy --deterministic --threads 4 --strict-loader --strict-tokenizer --prompt-template qwen --require-determinism --allocation-audit --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-17\\qwen3-logits-extraction-reuse.json",
  "python -m json.tool ci/slm-cpu/intel-i5-8250u/2026-05-17/qwen3-logits-extraction-reuse-validation.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 quant expansion",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The next optimization target is the remaining Qwen3 Q8_0 logits extraction allocation after sampler scratch cleanup.",
  "Any accepted runtime change must preserve generated IDs, decoded text, strict tokenizer authority, selected CPU backend, and fallback=false against the recorded Qwen3 Q8_0 4-thread oracle.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "Broad Qwen answer quality is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "A second dense model is supported by this item.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-026"
status = "merged"
pr = 5369
merge_sha = "8f44d67b9ee06b97eda96ed0eaca0d1dfd9fcee6"
branch = "codex/slm-cpu-026-repetition-penalty-logits-reuse"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-025"]
acceptance = "Remove or further isolate the remaining Qwen3-0.6B Q8_0 warm-session logits Vec extraction for default repetition-penalty decode steps without changing count-aware repetition-penalty semantics, generated IDs, decoded text, model SHA, prompt IDs, strict GGUF tokenizer authority, selected CPU backend, or fallback=false. The slice must emit before/after allocation evidence or explicitly document unavailable counters and must not claim sustained throughput, broad answer quality, Q4/Q5 quant expansion, a second dense model, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-sampling",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu slm-warm-session --model models\\slm\\Qwen3-0.6B-Q8_0.gguf --corpus ci\\quality\\slm-warm-session-corpus.yaml --corpus-repeat-runs 2 --max-new-tokens 8 --temperature 0.0 --greedy --deterministic --threads 4 --strict-loader --strict-tokenizer --prompt-template qwen --require-determinism --allocation-audit --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-17\\qwen3-repetition-penalty-logits-reuse.json",
  "python -m json.tool ci/slm-cpu/intel-i5-8250u/2026-05-17/qwen3-repetition-penalty-logits-reuse-validation.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-sampling/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 quant expansion",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "SLM-CPU-025 left the default repetition-penalty logits Vec extraction path explicit rather than removed.",
  "A valid SLM-CPU-026 implementation must preserve count-aware repetition-penalty generated IDs and strict CPU receipt provenance.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "Broad Qwen answer quality is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "A second dense model is supported by this item.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-027"
status = "superseded"
branch = "codex/slm-cpu-027-second-dense-sanity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-026"]
acceptance = "Duplicate tracker item retained as blocked because the second-small-dense-GGUF sanity requirement was already satisfied by SLM-CPU-017 and SLM-CPU-017A. Do not implement this item as a new runtime slice; use SLM-CPU-028 for the next non-duplicative Q4 planning gate."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 quant expansion",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The Kaby Lake Qwen3 Q8_0 proof-appliance profile is complete through SLM-CPU-026.",
  "SLM-CPU-017 and SLM-CPU-017A already provide the second-model blocker and positive Qwen2.5 Q8_0 sanity evidence.",
  "SLM-CPU-027 is blocked to prevent duplicate second-model work.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-028"
status = "merged"
pr = 5384
head_sha = "06041a534af0d8fbe02569c75d3b2249f6399a2e"
merge_sha = "8c79a58d28c7a5114556919ab4cfd201474134a8"
branch = "codex/slm-cpu-028-q4-planning-gates"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-026"]
acceptance = "Define the bounded Kaby Lake Q4_K_M/Q4_K_S expansion plan after the Qwen3 Q8_0 proof-appliance profile and Qwen2.5 Q8_0 second-model sanity evidence. The slice must identify candidate GGUF artifacts only by pinned SHA256 and metadata, define the strict load/tokenizer/backend/fallback gates, require constrained corpus, multi-token determinism, warm-session receipts, operator-profile timing/memory/storage context, and before/after generated-ID preservation before any Q4 support claim. It must not implement Q4 runtime support or claim broad answer quality, sustained throughput, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
  "ci/slm-cpu/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The Qwen3 Q8_0 Kaby proof-appliance profile is complete through SLM-CPU-026.",
  "Qwen2.5 Q8_0 second-model sanity evidence already exists via SLM-CPU-017A.",
  "A valid SLM-CPU-028 slice defines Q4_K_M/Q4_K_S expansion gates before runtime support starts.",
]
must_not_claim = [
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-029"
status = "merged"
pr = 5405
head_sha = "025b87143b272561f15235c5c4be31f824121a3d"
merge_sha = "62258e8688211f9dd4ed0f4678bdb54d4734c5c8"
branch = "codex/slm-cpu-029-kv-temp-reuse"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-028"]
acceptance = "Reduce the next Qwen3-0.6B Q8_0 strict CPU warm-session hot-loop allocation or layout boundary after the SLM-CPU-026 logits scratch reuse and SLM-CPU-028 Q4 planning gate. Prioritize KV/cache/temp buffer reuse, prompt/session buffer pre-sizing, avoidable tensor clones/transposes, or keeping tokenizer/stop-policy work out of the decode hot loop only where deterministic generated IDs and strict receipt provenance are preserved. The slice must use the Qwen3 Q8_0 4-thread appliance profile as behavior oracle, record before/after allocation or timing evidence where available, preserve model SHA, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel, tokenizer.source=gguf_metadata, tokenizer.strict=true, and fallback_used=false, and scope any improvement to the fixed model/corpus/thread-count artifact. It must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- --device cpu slm-warm-session --model models\\slm\\Qwen3-0.6B-Q8_0.gguf --corpus ci\\quality\\slm-warm-session-corpus.yaml --corpus-repeat-runs 2 --max-new-tokens 8 --temperature 0.0 --greedy --deterministic --threads 4 --strict-loader --strict-tokenizer --prompt-template qwen --require-determinism --allocation-audit --json-out ci\\slm-cpu\\intel-i5-8250u\\2026-05-17\\qwen3-kv-temp-reuse.json",
  "python -m json.tool ci/slm-cpu/intel-i5-8250u/2026-05-17/qwen3-kv-temp-reuse-validation.json",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The next Kaby SLM optimization target is a bounded Qwen3 Q8_0 warm-session allocation or layout boundary after logits scratch reuse.",
  "A valid SLM-CPU-029 slice must preserve generated IDs, decoded text, strict tokenizer authority, selected CPU backend, model SHA, and fallback=false against the recorded 4-thread appliance oracle.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-023"
status = "merged"
pr = 5312
merge_sha = "1484b7123767d47f280e1757ac8df2cd29ebda6b"
branch = "codex/slm-cpu-019-kaby-performance-dashboard"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-016", "SLM-CPU-017A", "SLM-CPU-022"]
acceptance = "Formalize the Kaby Lake Qwen3 Q8_0 CPU performance dashboard from existing 1/2/4/8-thread envelope and operator-profile receipts. The dashboard must record cold load, warm-session load-once, prefill, first-token latency, steady decode, per-prompt timing boundaries, allocation or buffer-reuse counters where available, memory footprint, storage/free-space context, and thermal/power fields as measured or explicitly unavailable. It may choose an operator default thread count only from recorded evidence and must preserve generated-ID, tokenizer, backend, model SHA, and fallback=false claim boundaries without adding runtime optimization or sustained-throughput claims."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "The Kaby Lake Qwen3 Q8_0 strict CPU performance dashboard exists after merge.",
  "The current operator-profile default thread count is evidence-scoped to the recorded thread envelope and operator-profile receipt.",
  "The dashboard identifies remaining hot-loop allocation and buffer-reuse boundaries for future optimization slices.",
]
must_not_claim = [
  "A new runtime optimization is implemented.",
  "Sustained 8250U throughput is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-022"
status = "merged"
pr = 5140
head_sha = "acee14ad135a88878a7c4af1440c1d0dc19d8b88"
merge_sha = "f4bf3fd38c3d0e4f49d0f39999eabc9bd2a834d6"
branch = "model/slm-cpu-022-smollm2-reference-comparator"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-021"]
acceptance = "Define the SmolLM2 360M reference-compatible first-token/top-k comparator contract after the SLM-CPU-021 wrong-first-token diagnosis, and add CLI fixture coverage showing `reference-compare` validates the exact SmolLM2 model-family/top-k artifact shape and fails closed under `--require-match` when it diverges. This support slice must not claim a fresh external reference run, CPU answer readiness, CUDA planning, CUDA execution, speedup, server readiness, broad dense GGUF support, or BitNet QK256 behavior."
commands = [
  "python -m json.tool ci/slm-cpu/windows-9950x3d-rtx5070ti/2026-05-16/smollm2-360m-reference-comparator-contract.json",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli reference_compare_validates_smollm2_first_token_topk_artifact",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli reference_compare_require_match_fails_smollm2_topk_divergence",
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "cargo run --locked -p xtask --no-default-features -- campaign check slm-cpu",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/model-artifacts/model-coverage-matrix.toml",
  "ci/slm-cpu/**",
  "docs/model-artifacts/MODEL_COVERAGE_MATRIX.md",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
  "plans/cuda-5070ti-productization/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "SmolLM2 CUDA planning",
  "SmolLM2 CUDA execution",
]
may_claim = [
  "The SmolLM2 comparator contract is defined after merge.",
  "`reference-compare` has SmolLM2 first-token/top-k fixture coverage after merge.",
  "The next SmolLM2 proof remains a same-prompt external reference comparator capture before CPU answer readiness or CUDA planning.",
]
must_not_claim = [
  "A fresh SmolLM2 external reference comparator was captured.",
  "SmolLM2 CPU answer readiness is proven.",
  "SmolLM2 broad answer quality is proven.",
  "SmolLM2 all-layer CUDA planning can start.",
  "SmolLM2 CUDA execution exists.",
  "Sustained 8250U or 9950X3D throughput is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-030"
status = "merged"
pr = 5457
head_sha = "9c2400fb4c781c2189a4935f1e3744b85567bd3d"
merge_sha = "87325006e25bb5820fd5c366ee7dad88fd7ea77c"
branch = "codex/slm-cpu-030-prompt-setup-attribution"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-029"]
acceptance = "Add bounded warm-session allocation attribution for the Qwen3 Q8_0 Kaby prompt setup boundary after SLM-CPU-029. The slice must keep the existing prompt_setup total for receipt continuity while breaking it down into buffer reset, token seeding, KV-cache creation, and sampler setup subcomponents so the next optimization target is evidence-scoped. It must preserve generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false when real artifacts are regenerated, and must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_allocation_audit_reports_prompt_setup_breakdown",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The next Kaby SLM optimization target is prompt setup attribution inside the bounded Qwen3 Q8_0 warm-session allocation audit.",
  "A valid SLM-CPU-030 slice may add receipt telemetry that splits prompt_setup into subcomponents without changing generation behavior.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-031"
status = "merged"
pr = 5499
head_sha = "24ba402d3ffb9b01599f4f04d0cb114a3205ab99"
merge_sha = "db0600dd43eb18702fbb766126f07df2bcfca6c4"
branch = "codex/slm-cpu-031-kv-cache-session-reuse"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-030"]
acceptance = "Reuse a single CPU KV cache across Qwen3 Q8_0 Kaby warm-session prompts by clearing it before each prompt. The slice must preserve prompt isolation, generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false when real artifacts are regenerated. Receipts must distinguish the session-level KV cache allocation from per-prompt prompt_setup.kv_cache clear/reset work and must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_speed_receipt_records_warm_and_decode_throughput_without_speedup_claim",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_allocation_audit_reports_prompt_setup_breakdown",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The Qwen3 Q8_0 Kaby warm session reuses one CPU KV cache across prompts and clears it before each prompt for prompt isolation.",
  "Receipts distinguish session-level KV cache allocation from per-prompt KV cache clear/reset attribution.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-032"
status = "merged"
pr = 5514
head_sha = "da6630419a055cac9f8c7d6ddcc499e07617b95a"
merge_sha = "f3cce75aef2948d42174086f164061da7b4ff65a"
branch = "codex/slm-cpu-032-prompt-token-cache"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-031"]
acceptance = "Reuse rendered prompt token IDs across repeated Qwen3 Q8_0 Kaby warm-session prompts so repeated corpus cases avoid redundant tokenizer.encode work while preserving generated IDs, decoded text, strict GGUF tokenizer authority, selected CPU backend/kernel, model SHA, and fallback=false when real artifacts are regenerated. Receipts must record prompt token cache policy, hit/miss counts, and cache entry count, and must not claim sustained throughput, broad answer quality, Q4/Q5 runtime support, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_prompt_token_cache_reuses_rendered_prompt_ids",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli slm_warm_session",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
]
may_claim = [
  "The Qwen3 Q8_0 Kaby warm session reuses rendered prompt token IDs for repeated prompts in the same resident session.",
  "Receipts record prompt token cache hit/miss counts and cache entry count.",
]
must_not_claim = [
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-021"
status = "merged"
pr = 5133
head_sha = "6e280e129ac810442ba51543e9d781b398b4ba4c"
merge_sha = "ceec8dd64366c85a26b7dbb0a5351769c92ac0cd"
branch = "model/slm-cpu-021-smollm2-wrong-token-diagnosis"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-020"]
acceptance = "Diagnose the SmolLM2 360M wrong-first-token strict CPU quality blocker from committed SLM CPU and reference-runner evidence. The item must preserve the exact artifact scope, record that current evidence is insufficient to promote CPU answer readiness, define the next reference-compatible comparator proof, and keep CUDA planning, CUDA execution, speedup, server, broad dense GGUF, and BitNet QK256 claims false."
commands = [
  "python -m json.tool ci/slm-cpu/windows-9950x3d-rtx5070ti/2026-05-16/smollm2-360m-wrong-first-token-diagnosis.json",
  "cargo test --locked -p bitnet-models --no-default-features --features cpu --lib test_ln_gamma_validator_envelope",
  "cargo test --locked -p bitnet-models --no-default-features --features cpu --lib smollm2_norm_policy_requires_exact_metadata_scope",
  "cargo run --locked -p xtask --no-default-features -- check-model-coverage",
  "cargo run --locked -p xtask --no-default-features -- campaign check slm-cpu",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/model-artifacts/model-coverage-matrix.toml",
  "ci/slm-cpu/**",
  "docs/model-artifacts/MODEL_COVERAGE_MATRIX.md",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
  "plans/cuda-5070ti-productization/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "BitNet QK256/I2_S kernels",
  "SmolLM2 CUDA planning",
  "SmolLM2 CUDA execution",
]
may_claim = [
  "The SmolLM2 wrong-first-token blocker is now diagnosed as unresolved after committed reference-runner, prompt/tokenizer, and strict CPU retry evidence.",
  "The strict CPU retry reached generation with fallback_used=false but selected token `The`, so CPU answer readiness remains false.",
  "The next SmolLM2 proof is a reference-compatible first-token/top-k or checkpoint comparator before CPU answer readiness or CUDA planning.",
]
must_not_claim = [
  "SmolLM2 CPU answer readiness is proven.",
  "SmolLM2 broad answer quality is proven.",
  "SmolLM2 all-layer CUDA planning can start.",
  "SmolLM2 CUDA execution exists.",
  "Sustained 8250U or 9950X3D throughput is proven.",
  "Q4/Q5 quant expansion is in scope.",
  "Server, GPU, OpenVINO, UHD 620, or NPU execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-087"
status = "merged"
pr = 437
head_sha = "0a2a391113a47b7b8b46fa524372e33008338cb2"
merge_sha = "550a9ba4d65a55e3c16beba699e916e17ea9b354"
branch = "codex/slm-cpu-087-residual-block-output-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-086"]
acceptance = "Queue the next Kaby Lake Qwen3 Q8_0 allocation-layout slice after SLM-CPU-086 by moving from the final-norm output-storage API blocker to the residual-add / transformer.block.output boundary. A valid implementation slice may add a behavior-preserving block-output or residual-add workspace/storage API surface, or record a machine-checkable blocker proving the exact remaining Candle Tensor ownership/API gap. Any runtime change must preserve the established Qwen3 Q8_0 appliance behavior oracle: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, and fallback_used=false. The slice must not promote packed_q8_sidecar, claim speedup without before/after receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "The next Kaby Lake Qwen3 Q8_0 performance target is the residual-add / transformer.block.output boundary after SLM-CPU-086 narrowed model.final_norm.output to the public Candle LayerNorm/RMSNorm owned-output API gap.",
  "A valid SLM-CPU-087 implementation slice must preserve generated IDs, decoded text, strict tokenizer authority, selected CPU backend, model SHA, dense hook identity where applicable, and fallback=false before claiming even a bounded allocation improvement.",
]
must_not_claim = [
  "The residual-add/layer-output runtime path is optimized by this tracker queue slice.",
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "End-to-end speedup is proven without before/after receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-088"
status = "merged"
pr = 459
head_sha = "5ece9f17394d927845eb806ceccc335138d47c25"
merge_sha = "c1050aa483bac674c26b523dd7b6182a40801827"
branch = "codex/slm-cpu-088-residual-block-output-boundary"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-087"]
acceptance = "Implement or record the residual-add / transformer.block.output allocation-layout boundary queued by SLM-CPU-087. A valid slice may add a behavior-preserving block-output or residual-add workspace/storage API surface, or produce a machine-checkable blocker that identifies the exact remaining Candle Tensor ownership/API gap. Any runtime change must preserve the established Qwen3 Q8_0 appliance oracle: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, and fallback_used=false. The slice must not promote packed_q8_sidecar, claim speedup without before/after receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-087 has landed as the residual-add / transformer.block.output queue gate.",
  "SLM-CPU-088 records residual input and feed-forward branch output shape evidence at the residual-add / transformer.block.output boundary.",
  "SLM-CPU-088 classifies the remaining blocker as Candle residual-add owned Tensor output without caller-provided storage.",
  "SLM-CPU-088 keeps the Qwen3 Q8_0 appliance oracle as the gate before any later bounded allocation improvement can be claimed.",
]
must_not_claim = [
  "The residual-add/layer-output runtime path is optimized by SLM-CPU-088.",
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "End-to-end speedup is proven without before/after receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-089"
status = "merged"
pr = 471
head_sha = "e14a851bb85546e987a47f89114679c92c15dcd1"
merge_sha = "e5f28c991de626f96ed2d3d91446b5c4be98df20"
branch = "codex/slm-cpu-089-residual-add-storage-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-088"]
acceptance = "Define the next behavior-preserving residual-add output-storage gate after SLM-CPU-088. The slice must either add or precisely specify a narrow transformer.block.output residual-add storage API that can reuse caller-owned storage, or emit a machine-checkable blocker proving why Candle still forces an owned Tensor output. Any implementation must preserve the established Qwen3 Q8_0 appliance oracle: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, and fallback_used=false before claiming even a bounded allocation improvement. The slice must not promote packed_q8_sidecar, claim speedup without before/after receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-089 is the next residual-add / transformer.block.output storage gate after SLM-CPU-088.",
  "The current blocker remains Candle residual-add owned Tensor output unless SLM-CPU-089 proves a behavior-preserving reusable-storage API.",
  "Any later allocation improvement remains gated by the Qwen3 Q8_0 appliance oracle.",
]
must_not_claim = [
  "The residual-add/layer-output runtime path is optimized by this tracker queue slice.",
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "End-to-end speedup is proven without before/after receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-090"
status = "merged"
pr = 483
head_sha = "a6704f9acb6db624d20c0ddc770645c9a86500da"
merge_sha = "bf3733505ff7c9f69433f5f5306fca23c25ad743"
branch = "codex/slm-cpu-090-residual-add-output-storage-slice"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-089"]
acceptance = "Implement or conclusively block the narrow residual-add output-storage slice queued by SLM-CPU-089. A valid slice must either add a behavior-preserving transformer.block.output residual-add helper that can write into caller-provided reusable storage, or produce a machine-checkable blocker naming the exact Candle Tensor API limitation that still prevents reusable storage. Any runtime change must preserve the Qwen3 Q8_0 appliance oracle before claiming even bounded allocation improvement: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, and fallback_used=false. The slice must not promote packed_q8_sidecar, claim speedup without before/after receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-090 is the implementation/blocker slice for transformer.block.output residual-add output storage after SLM-CPU-089.",
  "The slice may narrow whether residual-add can reuse caller-owned storage without changing generated IDs.",
  "Any allocation improvement remains gated by Qwen3 Q8_0 before/after behavior receipts.",
]
must_not_claim = [
  "The residual-add/layer-output runtime path is optimized without a behavior-equivalent Qwen3 Q8_0 receipt.",
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "End-to-end speedup is proven without before/after receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-091"
status = "merged"
pr = 507
head_sha = "eb3a8aace3d373d31fd56a46e8ecf992d687be6a"
merge_sha = "c4992d971957ee8b82b0e47668f2fb43b55cb7eb"
branch = "codex/slm-cpu-091-logits-output-boundary"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-090"]
acceptance = "Queue the next Kaby Lake Qwen3 Q8_0 performance slice after the SLM-CPU-090 residual-add output-storage blocker by moving to the remaining model.logits / output-head tensor allocation boundary. A valid implementation slice must either reduce or narrowly classify one logits/output-head owned-output or extraction boundary using the existing strict warm-session allocation-audit surface. Any runtime change must preserve the established Qwen3 Q8_0 appliance oracle: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, and fallback_used=false. The slice must not promote packed_q8_sidecar, claim speedup without before/after receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-091 is the next non-duplicative Kaby Lake Qwen3 Q8_0 performance target after the residual-add storage path was blocked at Candle public Tensor APIs.",
  "The next implementation target is the model.logits / output-head tensor allocation boundary already identified by the Kaby dashboard.",
  "Any later allocation or timing improvement remains gated by Qwen3 Q8_0 before/after behavior receipts.",
]
must_not_claim = [
  "The logits/output-head runtime path is optimized by this tracker queue slice.",
  "The residual-add output-storage blocker is solved.",
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "End-to-end speedup is proven without before/after receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-092"
status = "merged"
pr = 535
head_sha = "c847ad3894821ee80fd6e17d6606dfe87201823c"
merge_sha = "0d9a0f645aa1e0ce1e8011f12100ae81dbdc7dab"
branch = "codex/slm-cpu-092-logits-fused-selection-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-091"]
acceptance = "Implement or conclusively block the logits/output-head fused-selection gate queued by SLM-CPU-091. A valid slice must either add a behavior-preserving output-head/token-selection path that avoids one owned logits tensor or full host logits extraction boundary, or emit a machine-checkable blocker naming the exact Candle Tensor, output-head, or accumulator-order limitation that prevents doing so safely. Any runtime change must preserve the established Qwen3 Q8_0 appliance oracle before claiming even bounded allocation improvement: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, and fallback_used=false. The slice must not promote packed_q8_sidecar, claim speedup without before/after receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu logits_output_storage_boundary_names_exact_candle_logits_blocker",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-092 is the next Kaby Lake Qwen3 Q8_0 performance target after SLM-CPU-091 classified the logits/output-head allocation boundary.",
  "The implementation target is a behavior-preserving logits/output-head fused-selection or exact blocker that avoids speculative transformer rewrites.",
  "Any allocation or timing improvement remains gated by Qwen3 Q8_0 before/after behavior receipts.",
]
must_not_claim = [
  "The logits/output-head fused-selection runtime path is implemented by this tracker queue slice.",
  "The model.logits owned Tensor boundary is solved without behavior-equivalent Qwen3 Q8_0 receipts.",
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "End-to-end speedup is proven without before/after receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-093"
status = "merged"
branch = "codex/slm-cpu-093-logits-fused-selection-boundary"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-092"]
pr = 564
head_sha = "b60db8fe6ddd7b558879ff02f805c1c768e39be6"
merge_sha = "749a2f3532e4bca311fed76d8d98a6afaa2e8b38"
acceptance = "Implement or conclusively block the actual logits/output-head fused-selection implementation slice after the SLM-CPU-092 tracker queue closeout. A valid slice must either add a behavior-preserving output-head/token-selection path that avoids one owned logits tensor or full host logits extraction boundary, or emit a machine-checkable blocker naming the exact Candle Tensor, output-head, accumulator-order, or top-k/argmax API limitation that prevents doing so safely. Any runtime change must preserve the established Qwen3 Q8_0 appliance oracle before claiming even bounded allocation improvement: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, and fallback_used=false. The slice must not promote packed_q8_sidecar, claim speedup without before/after receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli warm_session_aggregate_allocation_audit",
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu logits_output_storage_boundary_names_exact_candle_logits_blocker",
  "cargo clippy --locked -p bitnet-cli --no-default-features --features cpu,full-cli --all-targets -- -D warnings",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-093 is the implementation/blocker slice for the logits/output-head fused-selection gate after SLM-CPU-092 only queued the work.",
  "The target remains one behavior-preserving output-head/token-selection boundary with Qwen3 Q8_0 oracle preservation.",
  "Any allocation or timing improvement remains gated by Qwen3 Q8_0 before/after behavior receipts.",
]
must_not_claim = [
  "The logits/output-head fused-selection runtime path is optimized without behavior-equivalent Qwen3 Q8_0 receipts.",
  "The model.logits owned Tensor boundary is solved by SLM-CPU-092 alone.",
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "End-to-end speedup is proven without before/after receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-094"
status = "merged"
pr = 572
head_sha = "c3ba0feabfcff8c322426c2cb6082afd90ad700d"
merge_sha = "c9d804cb3fd4430e96ff3512cbed8906697d2ff8"
branch = "codex/slm-cpu-094-packed-q8-output-scratch"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-093"]
acceptance = "Queue the next Kaby Lake Qwen3 Q8_0 dense-CPU performance slice after SLM-CPU-093 blocked logits/output-head fused selection at Candle owned tensor materialization. The implementation target is the opt-in exact-tensor packed Q8_0 sidecar matvec output-scratch boundary: a valid slice may add a behavior-preserving caller-owned output/scratch buffer for the packed sidecar path, or emit a machine-checkable blocker naming why scratch reuse cannot be safely attached. Any runtime change must preserve the Qwen3 Q8_0 appliance behavior oracle before claiming even bounded allocation improvement: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, and fallback_used=false. The slice must not promote packed_q8_sidecar to the default runtime, claim speedup without before/after repeated receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "runtime code",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-094 is the next tracker gate after SLM-CPU-093 blocked logits/output-head fused selection at the Candle owned Tensor boundary.",
  "The next target is the opt-in packed Q8_0 sidecar output-scratch boundary, not default runtime promotion.",
  "Any future allocation or timing improvement remains gated by Qwen3 Q8_0 before/after behavior receipts.",
]
must_not_claim = [
  "SLM-CPU-094 implements runtime output-scratch reuse by itself.",
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "End-to-end speedup is proven without before/after repeated receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-095"
status = "merged"
pr = 577
head_sha = "d36745955f73702a17c27c914ead4c019a0ac09c"
merge_sha = "32afbe490290f7ceb34b639d9f66139fb6b729cc"
branch = "codex/slm-cpu-095-packed-q8-matvec-scratch"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-094"]
acceptance = "Implement or precisely classify the packed Q8_0 sidecar matvec output-scratch boundary queued by SLM-CPU-094. A valid slice may add behavior-preserving inner matvec helpers that fill caller-provided output slices for the opt-in exact-tensor packed Q8_0 sidecar path, and must record whether the full runtime can reuse caller-owned output storage through Candle Tensor construction. Any runtime-adjacent change must preserve the established Qwen3 Q8_0 appliance oracle before claiming even bounded allocation improvement: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, and fallback_used=false. The slice must not promote packed_q8_sidecar to the default runtime, claim speedup without before/after repeated receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo test --locked -p bitnet-transformer --lib dense_q8_sidecar",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "The packed Q8_0 sidecar matvec core has a behavior-preserving caller-output-slice helper if tests prove equivalence.",
  "The full packed sidecar runtime still requires an owned Candle Tensor output unless a behavior-preserving Tensor construction path can keep reusable caller storage alive.",
  "No speedup is claimed without before/after repeated Qwen3 Q8_0 receipts.",
]
must_not_claim = [
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "The full runtime output Tensor now reuses caller-owned storage if Candle still consumes an owned Vec.",
  "End-to-end speedup is proven without before/after repeated receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-096"
status = "merged"
pr = 589
head_sha = "8e196da7347b89575643b732f818e9175136def4"
merge_sha = "e3d350069d008cfbb23643b04d4c486c2bfaddca"
branch = "codex/slm-cpu-096-runtime-output-tensor-storage-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-095"]
acceptance = "Queue the next Kaby Lake Qwen3 Q8_0 dense-CPU performance gate after SLM-CPU-095 proved behavior-preserving inner packed Q8_0 matvec caller-output-slice helpers but recorded that full runtime output Tensor reuse remains blocked by Candle Tensor::from_vec owned-storage construction. A valid slice may adopt or wrap a behavior-preserving Candle-compatible output Tensor construction path that keeps reusable caller-owned packed-Q8 output storage alive, or must emit a machine-checkable blocker naming the exact Candle/API/lifetime limitation. Any runtime-adjacent change must preserve the established Qwen3 Q8_0 appliance oracle before claiming even bounded allocation improvement: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, and fallback_used=false. The slice must not promote packed_q8_sidecar to the default runtime, claim speedup without before/after repeated receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-095 proved inner packed Q8_0 matvec helpers can fill caller-owned output slices while preserving the Qwen3 Q8_0 appliance oracle.",
  "SLM-CPU-096 is the next gate for a Candle-compatible runtime output Tensor storage path or an exact API/lifetime blocker.",
  "Any allocation or timing improvement remains gated by Qwen3 Q8_0 before/after behavior receipts.",
]
must_not_claim = [
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "Full runtime output Tensor storage reuse is solved without behavior-preserving before/after receipts.",
  "End-to-end speedup is proven without before/after repeated receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-097"
status = "merged"
branch = "codex/slm-cpu-097-fused-output-consumer-gate"
pr = 600
head_sha = "48dc804d19bfc77bb92c3ba017799df304306512"
merge_sha = "029aefabb7f68cdb037bf7743a7511c7b4c020f5"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-096"]
acceptance = "Queue the next Kaby Lake Qwen3 Q8_0 dense-CPU performance gate after SLM-CPU-096 classified full runtime output Tensor reuse as blocked by public Candle owned-storage construction. A valid slice must either design and prove a fused output consumer path that avoids returning a reusable-storage-hostile Candle Tensor at the packed-Q8 exact-tensor boundary, or emit a machine-checkable blocker explaining why the next fused-consumer boundary is unsafe. Any runtime-adjacent implementation must preserve the established Qwen3 Q8_0 appliance oracle before claiming even bounded allocation or timing improvement: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, and fallback_used=false. The slice must not promote packed_q8_sidecar to the default runtime, claim speedup without before/after repeated receipts, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-096 proved full runtime output Tensor storage reuse remains blocked by public Candle owned-storage construction.",
  "SLM-CPU-097 is the next gate for a fused output consumer path or an exact fused-consumer safety blocker.",
  "Any allocation or timing improvement remains gated by Qwen3 Q8_0 before/after behavior receipts.",
]
must_not_claim = [
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "A fused output consumer path is safe without behavior-preserving before/after Qwen3 Q8_0 receipts.",
  "End-to-end speedup is proven without before/after repeated receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-098"
status = "merged"
pr = 606
head_sha = "01fdf4aa31f3cd9b29d729bf536ad9f118d17f60"
merge_sha = "6fe3ae0eec1d6913cf9cfaea0ac2085133ba9c2d"
merged_at = "2026-05-24T12:27:35Z"
branch = "codex/slm-cpu-098-fused-output-consumer-boundary"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-097"]
acceptance = "Implement or precisely classify the next fused output consumer boundary after SLM-CPU-097. A valid slice must either add a behavior-preserving fused consumer path that avoids returning a reusable-storage-hostile Candle Tensor at the packed-Q8 exact-tensor boundary, or emit a machine-checkable blocker naming the exact API, lifetime, tensor-shape, or receipt-safety limitation. Any implementation must preserve the established Qwen3 Q8_0 appliance oracle before claiming even bounded allocation or timing improvement: model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, and fallback_used=false. Any improvement claim requires before/after repeated receipts in the bounded i5-8250U Qwen3 Q8_0 context. The slice must not promote packed_q8_sidecar to the default runtime, claim sustained throughput, broaden Q4/Q5 support, or touch server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 paths."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-097 queued the fused output consumer gate after public Candle output Tensor storage reuse remained blocked.",
  "SLM-CPU-098 may prove a fused output consumer path or record an exact fused-consumer blocker.",
  "Any allocation or timing improvement remains gated by behavior-preserving repeated Qwen3 Q8_0 before/after receipts.",
]
must_not_claim = [
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "A fused output consumer path is safe without preserving the Qwen3 Q8_0 appliance oracle.",
  "End-to-end speedup is proven without before/after repeated receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-099"
status = "merged"
pr = 617
head_sha = "c1013835f9eea40e006b1881ba0e8d51b264db2c"
merge_sha = "8aba7b29c0b5b4ce7102cc24ffe735061fb9feca"
merged_at = "2026-05-24T13:46:25Z"
branch = "codex/slm-cpu-099-typed-fused-q-consumer-contract"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-098"]
acceptance = "Queue the typed fused Q projection consumer contract after SLM-CPU-098 classified the exact `layers.0.attention.q_proj.weight` packed-Q8 fused-consumer boundary as blocked by downstream Candle Tensor consumers. A valid slice must define the narrow API and receipt-safety contract needed for a future behavior-preserving fused Q projection consumer to own packed-Q8 matvec output slices, q_proj reshape/transpose, optional q_norm, RoPE application, trace/workspace identity, and attention-head handoff without materializing an intermediate returned Candle Tensor. This item is a design/contract gate only: it may add machine-checkable boundary structs, tests, artifacts, and dashboard/tracker documentation, but it must not enable runtime fused consumer execution or claim allocation/timing improvement. Any future implementation remains gated by repeated Qwen3 Q8_0 before/after receipts proving identical model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, and fallback_used=false."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "runtime fused Q projection execution",
  "packed_q8_sidecar default-runtime promotion",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-098 identified the downstream Candle Tensor consumer boundary that blocks safe fused output consumer execution today.",
  "SLM-CPU-099 may define the typed fused Q projection consumer API and receipt-safety contract needed before a later implementation slice.",
  "Any allocation or timing improvement remains unclaimed until behavior-preserving repeated Qwen3 Q8_0 before/after receipts exist.",
]
must_not_claim = [
  "A fused Q projection consumer executes in production.",
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "Allocation reduction or speedup is proven.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-100"
status = "merged"
pr = 631
head_sha = "9949c9f162b06473b00d3cf76d9bf6dbc93eb6b3"
merge_sha = "9ed97a0fc49d1d6910a9389df54974470a52911d"
merged_at = "2026-05-24T16:31:16Z"
branch = "codex/slm-cpu-100-typed-fused-q-consumer-implementation-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-099"]
acceptance = "Implement or precisely block the typed fused Q projection consumer gate defined by SLM-CPU-099. A valid slice may add a behavior-preserving fused consumer for the exact Qwen3 Q8_0 `layers.0.attention.q_proj.weight` packed-Q8 sidecar path that owns the packed matvec output slice, q_proj reshape/transpose, optional q_norm, RoPE application, trace/workspace identity, and attention-head handoff without materializing an intermediate returned Candle Tensor; otherwise it must emit a machine-checkable blocker naming the exact API, lifetime, tensor-layout, accumulator-order, or receipt-safety gap. Any runtime-adjacent change must keep the default Qwen3 Q8_0 behavior oracle intact unless before/after receipts prove identical model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, and fallback_used=false. The slice must not claim allocation reduction, speedup, sustained 8250U throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes without the required evidence."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without before/after Qwen3 Q8_0 receipts",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-099 defined the typed fused Q projection consumer contract for the exact Qwen3 Q8_0 `layers.0.attention.q_proj.weight` packed-Q8 sidecar boundary.",
  "SLM-CPU-100 may implement that exact fused Q consumer or record the concrete blocker that still prevents safe execution.",
  "Any runtime improvement remains gated by behavior-preserving before/after Qwen3 Q8_0 receipts.",
]
must_not_claim = [
  "Allocation reduction or speedup is proven without before/after Qwen3 Q8_0 receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-101"
status = "merged"
pr = 644
head_sha = "48e7c6e490762b7ddf55cc0d1ea6d0539b7e558f"
merge_sha = "444b2167256c357c0e188efc02930c0b7a5bbaa1"
merged_at = "2026-05-24T20:03:35Z"
branch = "codex/slm-cpu-101-typed-attention-head-view-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-100"]
acceptance = "Define or precisely block the typed attention-head buffer/view gate required by SLM-CPU-100 before any exact `layers.0.attention.q_proj.weight` packed-Q8 fused consumer can execute. A valid slice may add a behavior-preserving typed attention-head view or buffer contract that carries the Q projection through reshape, transpose, optional q_norm, RoPE, trace/workspace identity, and attention score handoff without constructing an intermediate returned Candle Tensor; otherwise it must emit a machine-checkable blocker naming the exact API, lifetime, tensor-layout, q_norm/RoPE, trace identity, score-handoff, or receipt-safety gap. This item must keep the default Qwen3 Q8_0 behavior oracle intact and must not enable runtime fused Q execution, promote packed_q8_sidecar, claim allocation reduction, claim speedup, claim sustained 8250U throughput, broaden Q4/Q5 runtime support, or touch server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 paths."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "runtime fused Q projection execution without before/after Qwen3 Q8_0 receipts",
  "packed_q8_sidecar default-runtime promotion",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-100 recorded that the exact typed fused Q projection consumer remains blocked by downstream attention-head Tensor semantics.",
  "SLM-CPU-101 may define the typed attention-head buffer/view contract or record the concrete blocker that still prevents safe fused consumer execution.",
  "Any runtime improvement remains gated by behavior-preserving before/after Qwen3 Q8_0 receipts.",
]
must_not_claim = [
  "A fused Q projection consumer executes in production.",
  "The packed-Q8 sidecar is promoted to the default runtime.",
  "Allocation reduction or speedup is proven.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-102"
status = "merged"
pr = 657
head_sha = "ef21deefa44a6ffd867ae6d247adf39ffc9adbef"
merge_sha = "fbf83e204151c876bf1f08917203e8b3c0530fd9"
merged_at = "2026-05-24T22:04:11Z"
branch = "codex/slm-cpu-102-typed-attention-head-consumer-implementation"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-101"]
acceptance = "Implement or precisely block the typed attention-head consumer gate defined by SLM-CPU-101 for the exact Qwen3 Q8_0 `layers.0.attention.q_proj.weight` packed-Q8 sidecar path. A valid slice may carry the fused Q projection output through the typed attention-head view/buffer contract, q_norm, RoPE, trace/workspace identity, and score-handoff boundary without constructing an intermediate returned Candle Tensor; otherwise it must emit a machine-checkable blocker naming the exact API, lifetime, tensor-layout, q_norm/RoPE, trace identity, score-handoff, accumulator-order, or receipt-safety gap. Any runtime-adjacent change must preserve the Qwen3 Q8_0 and Qwen2.5 Q8_0 strict CPU behavior oracles with before/after receipts proving identical model SHA, tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, and fallback_used=false before claiming allocation or timing improvement. The slice must not promote packed_q8_sidecar to default runtime, claim speedup, claim sustained 8250U throughput, broaden Q4/Q5 runtime support, or touch server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 paths."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-101 defined the typed attention-head buffer/view contract needed before exact packed-Q8 fused Q projection consumer execution.",
  "SLM-CPU-102 may implement the next typed attention-head consumer gate or record the concrete blocker that still prevents safe fused consumer execution.",
  "Any allocation or timing improvement remains gated by behavior-preserving before/after Qwen3 Q8_0 and Qwen2.5 Q8_0 receipts.",
]
must_not_claim = [
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-105"
status = "merged"
pr = 677
head_sha = "c7e8b54131e372df577b1773219a491ed6d75b00"
merge_sha = "0bc0b9e0abb36f4c9c030089f4e914d1b061cf32"
merged_at = "2026-05-25T02:49:14Z"
branch = "codex/slm-cpu-105-qnorm-input-proof-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-104"]
acceptance = "Prove or precisely block the q_norm_input_candle_tensor_boundary selected by SLM-CPU-104 for the exact Qwen3 Q8_0 `layers.0.attention.q_proj.weight` packed-Q8 sidecar path. A valid slice must either add before/after strict CPU receipt evidence for Qwen3 Q8_0 and Qwen2.5 Q8_0 showing identical model SHA, tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, and fallback_used=false across the selected boundary, or emit a machine-checkable blocker naming the missing runtime hook, receipt field, comparator, tensor identity, accumulator-order, or artifact gap. The slice must keep the default runtime `eager_f32_candle`, must not promote packed_q8_sidecar, and must not claim allocation reduction, timing improvement, sustained throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-105 may prove the selected q_norm_input materialization boundary with before/after Qwen3 Q8_0 and Qwen2.5 Q8_0 strict CPU receipts.",
  "SLM-CPU-105 may record the exact blocker preventing that receipt-backed proof.",
]
must_not_claim = [
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-106"
status = "merged"
branch = "codex/slm-cpu-106-qnorm-input-receipt-comparator"
pr = 681
head_sha = "1aa5e52d7cbd753e0c7750f0b25e484451e6bb2b"
merge_sha = "178916effa05fee512da8e6c6a74712846440e2c"
merged_at = "2026-05-25T04:19:36Z"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-105"]
acceptance = "Burn down the SLM-CPU-105 q_norm_input proof blocker by adding or precisely blocking the minimal receipt identity and fail-closed comparator needed for `q_norm_input_candle_tensor_boundary` on the exact Qwen3 Q8_0 `layers.0.attention.q_proj.weight` packed-Q8 sidecar path. A valid slice may add receipt/comparator fields that name the q_norm-input boundary, tensor identity, selected CPU backend/kernel identity, dense hook identity, model SHA, tokenizer authority, prompt IDs, generated IDs, decoded text, and fallback_used=false for before/after Qwen3 Q8_0 and Qwen2.5 Q8_0 proof receipts, or it may emit a machine-checkable blocker naming the remaining runtime hook, tensor identity, comparator, Qwen2.5 artifact, or accumulator-order gap. The slice must keep the default runtime `eager_f32_candle`, must not promote packed_q8_sidecar, and must not claim allocation reduction, timing improvement, sustained throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-105 merged as a blocker for the q_norm-input proof gate.",
  "SLM-CPU-106 may add the receipt identity and comparator surface needed before q_norm-input before/after proof receipts can be trusted.",
  "SLM-CPU-106 may record any remaining blocker preventing that receipt/comparator surface.",
]
must_not_claim = [
  "The q_norm-input materialization boundary is behavior-proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-107"
status = "merged"
branch = "codex/slm-cpu-107-qnorm-runtime-hook-impl"
pr = 692
head_sha = "9c7695f876093010a9eece984f8ca235e4eaad73"
merge_sha = "0e6f61e37e60ad81e2ec2eb90ebf36f909f655ad"
merged_at = "2026-05-25T07:21:22Z"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-106"]
acceptance = "Burn down the next q_norm_input_candle_tensor_boundary blocker after SLM-CPU-106 by adding a runtime-disabled hook and receipt tensor-identity surface for the exact Qwen3 Q8_0 layers.0.attention.q_proj.weight packed-Q8 sidecar path, or precisely blocking the missing API surface that prevents that hook. A valid slice must preserve eager_f32_candle as the default runtime, keep packed_q8_sidecar execution disabled unless Qwen3 Q8_0 and Qwen2.5 Q8_0 before/after strict CPU receipts prove identical model SHA, tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, q_norm_input tensor identity, and fallback_used=false, and must not claim allocation reduction, timing improvement, sustained throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-106 defined the fail-closed q_norm-input receipt comparator contract.",
  "SLM-CPU-107 may add a runtime-disabled q_norm-input hook and receipt tensor identity surface for the exact Qwen3 Q8_0 attention.q_proj path, or record the exact API blocker preventing that hook.",
  "SLM-CPU-107 may name the before/after Qwen3 Q8_0 and Qwen2.5 Q8_0 receipt gates required before any allocation or timing claim.",
]
must_not_claim = [
  "The q_norm-input materialization boundary is behavior-proven without Qwen3 and Qwen2.5 before/after receipts.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-108"
status = "merged"
branch = "codex/slm-cpu-108-qnorm-qwen3-receipt-pair"
pr = 697
merge_sha = "dfe15045117e0119a69c835a48c68a3ae7148bae"
merged_at = "2026-05-25T09:12:10Z"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-107"]
acceptance = "Burn down the next q_norm_input_candle_tensor_boundary blocker after SLM-CPU-107 by collecting the Qwen3 Q8_0 before/after strict CPU receipt pair for the exact layers.0.attention.q_proj.weight packed-Q8 sidecar q_norm_input hook, or record a machine-checkable blocker naming the missing model, host, receipt, tensor identity, generated-token, fallback, or accumulator-order evidence. A valid slice must preserve identical model SHA, tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, q_norm_input tensor identity, and fallback_used=false across before/after receipts before claiming behavior preservation for this boundary. It must keep eager_f32_candle as the default runtime, keep packed_q8_sidecar execution disabled unless all proof gates pass, and must not claim allocation reduction, timing improvement, sustained throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-107 defined the runtime-disabled q_norm-input hook and receipt tensor identity surface for the exact Qwen3 Q8_0 attention.q_proj path.",
  "SLM-CPU-108 may collect or precisely block the Qwen3 Q8_0 before/after strict CPU receipt pair for that q_norm_input boundary.",
  "SLM-CPU-108 may keep the remaining Qwen2.5 receipt-pair and accumulator-order gates open.",
]
must_not_claim = [
  "The q_norm-input materialization boundary is fully behavior-proven without Qwen3 and Qwen2.5 before/after receipts.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-109"
status = "merged"
branch = "codex/slm-cpu-109-warm-session-qnorm-identity"
pr = 703
head_sha = "5f8eebbb727a5d1c02bf94234c9a21689146d8dd"
merge_sha = "d042ea8be0f76c3ee26b5353bb0346b991182af1"
merged_at = "2026-05-25T10:40:03Z"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-108"]
acceptance = "Burn down the SLM-CPU-108 blocker by adding the missing warm-session receipt field for the exact Qwen3 Q8_0 layers.0.attention.q_proj.weight packed-Q8 sidecar q_norm_input tensor identity, or record a narrower machine-checkable blocker if the current CLI/model/transformer API cannot safely expose it. A valid slice must preserve eager_f32_candle as the default runtime, keep packed_q8_sidecar disabled by default, and require before/after strict CPU receipts with identical model SHA, tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, q_norm_input tensor identity, and fallback_used=false before claiming behavior preservation."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-108 verified the local Qwen3 Q8_0 model SHA and precisely blocked receipt-pair collection on the missing q_norm_input tensor identity field.",
  "SLM-CPU-109 may add or precisely block the missing warm-session q_norm_input tensor identity receipt field.",
]
must_not_claim = [
  "The q_norm-input materialization boundary is fully behavior-proven without Qwen3 and Qwen2.5 before/after receipts.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-110"
status = "merged"
branch = "codex/slm-cpu-110-qnorm-receipt-pair-after-identity"
pr = 710
head_sha = "c4af2f83fb3fd93148d057ee16bdf5516c430f8d"
merge_sha = "48e1c758447236ad37dc11a0c44829d75a5e7ad2"
merged_at = "2026-05-25T11:33:00Z"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-109"]
acceptance = "Collect the Qwen3 Q8_0 before/after strict CPU receipt pair now that warm-session receipts emit dense_q8_hook.q_norm_input_tensor_identity, or record the next narrower machine-checkable blocker. A valid proof must preserve identical model SHA, tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, q_norm_input tensor identity, and fallback_used=false across before/after receipts before claiming behavior preservation. It must keep eager_f32_candle as the default runtime, keep packed_q8_sidecar disabled by default unless all proof gates pass, and must not claim allocation reduction, timing improvement, sustained throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without proof gates",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-109 added the missing warm-session q_norm_input tensor identity receipt field.",
  "SLM-CPU-110 may collect or precisely block the Qwen3 Q8_0 before/after strict CPU receipt pair for that q_norm_input boundary.",
]
must_not_claim = [
  "The q_norm-input materialization boundary is fully behavior-proven without Qwen3 and Qwen2.5 before/after receipts.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-111"
status = "merged"
branch = "codex/slm-cpu-111-qnorm-tensor-fingerprint"
pr = 718
head_sha = "afb539d59020d6faadbcc7d19417025ab7527dc4"
merge_sha = "66d779640d2e8c6830491edd5271707052251cc7"
merged_at = "2026-05-25T14:09:29Z"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-110"]
acceptance = "Add or precisely block f32-le q_norm_input tensor fingerprint capture for the Qwen3 Q8_0 `layers.0.attention.q_proj.weight` packed-Q8 sidecar q_norm_input boundary. A valid slice must preserve eager_f32_candle as the default runtime, keep packed_q8_sidecar disabled by default, and must not claim allocation reduction, timing improvement, sustained throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes. If fingerprint capture is blocked, record the exact Tensor API, lifetime, storage-layout, host extraction, or receipt-safety gap in a machine-checkable artifact."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without proof gates",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-110 collected the Qwen3 Q8_0 before/after strict CPU receipt pair for the q_norm_input identity surface.",
  "SLM-CPU-111 may add or precisely block q_norm_input f32-le tensor fingerprint capture.",
]
must_not_claim = [
  "The q_norm-input materialization boundary is fully behavior-proven without tensor fingerprints and Qwen2.5 before/after receipts.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-112"
status = "merged"
branch = "codex/slm-cpu-112-qnorm-fingerprint-diagnostic-capture"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-111"]
acceptance = "Add or precisely block an opt-in diagnostic q_norm_input fingerprint capture path for the exact Qwen3 Q8_0 `layers.0.attention.q_proj.weight` packed-Q8 sidecar boundary. A valid slice may hash only the selected q_norm_input Tensor in logical f32 little-endian order and record shape, dtype, and SHA256 without dumping tensor contents, or it must record the next exact Tensor API, lifetime, storage-layout, host-extraction, or receipt-safety blocker. The slice must preserve eager_f32_candle as the default runtime, keep packed_q8_sidecar disabled by default unless all proof gates pass, and must not claim allocation reduction, timing improvement, sustained throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without proof gates",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-111 precisely blocked q_norm_input f32-le tensor fingerprint capture at the current warm-session receipt boundary.",
  "SLM-CPU-112 may add or precisely block an opt-in diagnostic q_norm_input tensor fingerprint capture path.",
]
must_not_claim = [
  "The q_norm-input materialization boundary is fully behavior-proven without tensor fingerprints and Qwen2.5 before/after receipts.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-113"
status = "merged"
pr = 726
head_sha = "5d2e5fca829e607d8897139ec74db504683fbd8b"
merge_sha = "ae871be6f580ca9375ddd8b7346d435ef979c8b5"
merged_at = "2026-05-25T16:30:00Z"
branch = "codex/slm-cpu-113-qnorm-fingerprint-artifact"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-112"]
acceptance = "Capture or precisely block a real i5-8250U Qwen3 Q8_0 q_norm_input fingerprint artifact using the SLM-CPU-112 opt-in diagnostic trace path. A valid artifact must use the verified Qwen3-0.6B Q8_0 model SHA, strict GGUF tokenizer authority, qwen no-think prompt policy, cpu-rust backend, fallback_used=false, and the exact `layers.0.attention.q_proj.weight` / `attention.q_norm_input` boundary. It must record shape, dtype, source tensor, boundary, and f32-le SHA256 without dumping tensor contents, and it must not claim allocation reduction, timing improvement, sustained throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without proof gates",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-112 added an opt-in trace-only q_norm_input fingerprint diagnostic path.",
  "SLM-CPU-113 may capture or precisely block a real i5-8250U Qwen3 Q8_0 q_norm_input fingerprint artifact.",
]
must_not_claim = [
  "The q_norm-input materialization boundary is fully behavior-proven without Qwen3 and Qwen2.5 before/after receipt comparisons that include tensor fingerprints.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-114"
status = "merged"
pr = 730
head_sha = "224bf7d1d5e7bd7e9f13f36c4c1656dd42845edc"
merge_sha = "baf857aa821a233c2aa3439c6dee3738e6bf3a79"
merged_at = "2026-05-25T17:28:49Z"
branch = "codex/slm-cpu-114-qnorm-fingerprint-receipt-pair"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-113"]
acceptance = "Compare the SLM-CPU-113 Qwen3 Q8_0 q_norm_input fingerprint against a before/after strict CPU receipt pair and add the corresponding Qwen2.5 Q8_0 fingerprint artifact or record the exact blocker. A valid slice must preserve identical model SHA, tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, q_norm_input tensor identity/fingerprint, and fallback_used=false across before/after receipts before any packed-Q8 sidecar runtime promotion. It must keep eager_f32_candle as the default runtime and must not claim allocation reduction, timing improvement, sustained throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without proof gates",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-113 captured a real i5-8250U Qwen3 Q8_0 q_norm_input fingerprint artifact.",
  "SLM-CPU-114 may compare the Qwen3 q_norm_input fingerprint against before/after strict CPU receipts and add or precisely block a Qwen2.5 Q8_0 fingerprint artifact.",
]
must_not_claim = [
  "The q_norm-input materialization boundary is fully behavior-proven without Qwen3 and Qwen2.5 before/after receipt comparisons that include tensor fingerprints.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-115"
status = "merged"
pr = 740
head_sha = "0567dee4c44ac967f126bb1217c207d8d7db2e8e"
merge_sha = "c57341c19d16c310f1545fa8bf16433e90961d6a"
merged_at = "2026-05-25T20:29:43Z"
branch = "codex/slm-cpu-115-qnorm-proof-next-boundary"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-114"]
acceptance = "Resolve the SLM-CPU-114 q_norm-input proof blocker into the next safe evidence boundary. A valid slice must either add before/after receipt-pair f32-le q_norm_input fingerprint capture for the exact Qwen3 Q8_0 `layers.0.attention.q_proj.weight` boundary, or choose and document a comparable Qwen-family boundary that exists on both Qwen3 Q8_0 and Qwen2.5 Q8_0. It must preserve identical model SHA, tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, and fallback_used=false before any allocation or timing claim. It must keep eager_f32_candle as the default runtime and must not claim sustained throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without proof gates",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-114 recorded that Qwen3 has a real q_norm-input fingerprint but the before/after warm-session receipts do not carry f32-le tensor fingerprints.",
  "SLM-CPU-114 recorded that Qwen2.5 Q8_0 has positive second-model sanity evidence but no q_norm-input stage to fingerprint under the current exact qwen2 artifact.",
  "SLM-CPU-115 may add the missing Qwen3 receipt-pair fingerprint capture or choose a comparable Qwen-family boundary shared by Qwen3 and Qwen2.5.",
]
must_not_claim = [
  "The q_norm-input materialization boundary is fully behavior-proven without before/after f32-le tensor fingerprint equality or an accepted comparable boundary.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-116"
status = "merged"
pr = 742
head_sha = "05ec95907776c32bf2b591ec0db186722aadc948"
merge_sha = "990a91500bb7b4831f821dec35ea87f4f69ac64c"
merged_at = "2026-05-25T21:16:09Z"
branch = "codex/slm-cpu-116-qproj-output-pre-qnorm-hook"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-115"]
acceptance = "Add or precisely block a runtime-disabled diagnostic hook and fail-closed receipt/comparator surface for the shared Qwen-family `attention.q_proj_output_pre_optional_qnorm` boundary selected by SLM-CPU-115. A valid slice must cover the exact Qwen3 Q8_0 and Qwen2.5 Q8_0 artifacts, preserve identical model SHA, tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity where applicable, boundary label, source tensor identity, f32-le tensor fingerprint availability or exact blocker, and fallback_used=false before any allocation or timing claim. It must keep eager_f32_candle as the default runtime and must not claim sustained throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without proof gates",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-115 selected `attention.q_proj_output_pre_optional_qnorm` as the next shared Qwen-family proof boundary because the prior q_norm-input boundary was Qwen3-specific on the current artifacts.",
  "SLM-CPU-116 may add or precisely block the diagnostic hook, receipt identity, comparator, and f32-le fingerprint surface for that shared boundary.",
]
must_not_claim = [
  "The shared q_proj-output boundary is behavior-proven without Qwen3 and Qwen2.5 before/after f32-le tensor fingerprint equality.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-117"
status = "merged"
pr = 749
head_sha = "ea3d44547a668ef3691784e350f988abfedda075"
merge_sha = "36d7e48a022b7d1fb57f091a94d33749f813e3f7"
merged_at = "2026-05-25T23:41:38Z"
branch = "codex/slm-cpu-117-qproj-output-pre-qnorm-hook"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-116"]
acceptance = "Implement or precisely block the runtime-disabled `attention.q_proj_output_pre_optional_qnorm` diagnostic hook selected by SLM-CPU-115 and gated by SLM-CPU-116. A valid implementation must keep eager_f32_candle as the default runtime, keep packed_q8_sidecar unpromoted, expose only opt-in trace/receipt identity for the exact Qwen3 Q8_0 and Qwen2.5 Q8_0 artifacts, and preserve identical model SHA, tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, boundary label, source tensor identity, f32-le tensor fingerprint availability or exact blocker, and fallback_used=false before any allocation or timing claim. The slice must include targeted tests or artifacts for the hook/comparator surface and must not claim sustained throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo test --locked -p bitnet-transformer --no-default-features --features cpu qproj_output_pre_optional_qnorm",
  "cargo test --locked -p bitnet-cli --no-default-features --features \"cpu,full-cli\" qproj_output_pre_optional_qnorm",
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts*/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without proof gates",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-116 defined the fail-closed hook/comparator contract and recorded the exact blockers for attention.q_proj_output_pre_optional_qnorm.",
  "SLM-CPU-117 may implement or precisely block the opt-in runtime-disabled diagnostic hook and receipt/comparator surface for that shared Qwen-family boundary.",
]
must_not_claim = [
  "The shared q_proj-output boundary is behavior-proven without Qwen3 and Qwen2.5 before/after f32-le tensor fingerprint equality.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-118"
status = "merged"
pr = 753
head_sha = "ee95430f9c337ba798d0f64be39789a629a48faa"
merge_sha = "e7ea1bbc0767bfb73a2ed3458dae976e227ba6b6"
merged_at = "2026-05-26T00:28:17Z"
branch = "codex/slm-cpu-118-qproj-output-receipt-pair"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-117"]
acceptance = "Capture or precisely block the real Qwen3 Q8_0 and Qwen2.5 Q8_0 strict CPU before/after receipt pairs for the shared `attention.q_proj_output_pre_optional_qnorm` boundary added by SLM-CPU-117. A valid slice must use the exact accepted model artifacts, strict GGUF tokenizer authority, CPU backend, fallback_used=false, identical prompt IDs, generated IDs, decoded text, selected backend/kernel identity, dense hook identity, boundary label, source tensor identity, shape/dtype, and f32-le tensor fingerprint availability or exact blocker. It must keep eager_f32_candle as the default runtime, keep packed_q8_sidecar unpromoted, and must not claim allocation reduction, timing improvement, sustained 8250U throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without proof gates",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-117 added the opt-in runtime-disabled attention.q_proj_output_pre_optional_qnorm diagnostic hook and fail-closed comparator artifact surface.",
  "SLM-CPU-118 may capture real Qwen3 Q8_0 and Qwen2.5 Q8_0 before/after evidence at that shared boundary, or record the exact missing model, hook, receipt, tensor fingerprint, or comparator blocker.",
]
must_not_claim = [
  "The shared q_proj-output boundary is behavior-proven without accepted Qwen3 and Qwen2.5 before/after f32-le tensor fingerprint equality or explicit blockers.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-119"
status = "merged"
pr = 757
head_sha = "b8e38c4b6ef011efa2ac08131b94f5a440dfcfef"
merge_sha = "f67a78f01e43a01436fd90e02585bbe5e2724e53"
merged_at = "2026-05-26T01:19:00Z"
branch = "codex/slm-cpu-119-qproj-output-receipt-pair-capture"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-118"]
acceptance = "Capture the exact Qwen3 Q8_0 and Qwen2.5 Q8_0 before/after strict CPU receipt pairs with the SLM-CPU-117 `attention.q_proj_output_pre_optional_qnorm` hook active, or provide the exact missing GGUF/cache path, trace activation, receipt field, tensor fingerprint, or command transcript blocker. A valid capture must preserve identical model SHA, strict GGUF tokenizer authority, CPU backend, fallback_used=false, prompt IDs, generated IDs, decoded text, selected backend/kernel identity, dense hook identity, boundary label, source tensor identity, shape/dtype, and f32-le tensor fingerprint equality before any allocation or timing claim."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without proof gates",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-118 recorded the exact blocker for the shared q_proj-output receipt-pair proof.",
  "SLM-CPU-119 may capture the exact Qwen3 and Qwen2.5 before/after hook-fingerprint receipt pairs or record a more precise capture blocker.",
]
must_not_claim = [
  "The shared q_proj-output boundary is behavior-proven without accepted Qwen3 and Qwen2.5 before/after f32-le tensor fingerprint equality or explicit blockers.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-120"
status = "merged"
pr = 759
head_sha = "0a749fa014d5896e198c406651c1bf759fc7793d"
merge_sha = "8401eee073967d458e495b5becc45d5866437554"
merged_at = "2026-05-26T04:53:27Z"
branch = "codex/slm-cpu-120-sidecar-transpose-guard"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-119"]
acceptance = "Burn down the exact SLM-CPU-119 q_proj-output proof blocker by fixing or further isolating the sidecar-gated runtime path. A valid slice must address both recorded gaps: the Qwen3 Q8_0 after-run allocation failure before receipt emission and the Qwen2.5 Q8_0 before/after attention.q_proj_output_pre_optional_qnorm f32-le fingerprint mismatch despite preserved generated token behavior. It must keep eager_f32_candle as the default runtime, preserve strict GGUF tokenizer authority, CPU backend, fallback_used=false, prompt IDs, generated IDs, decoded text, selected backend/kernel identity, dense hook identity, boundary label, source tensor identity, shape/dtype, and must not claim allocation reduction, timing improvement, default-runtime promotion, sustained throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes until accepted Qwen3 and Qwen2.5 before/after proof gates pass."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without proof gates",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-119 restored the accepted Qwen2.5 Q8_0 artifact path and captured real q_proj-output hook evidence for Qwen3 and Qwen2.5.",
  "SLM-CPU-120 fail-closes packed-Q8 sidecar runtime execution when payload byte order does not match the runtime matrix shape.",
  "SLM-CPU-120 isolates the remaining Qwen3 Q8_0 allocation failure as pre-boundary after prompt IDs and before post-guard receipt or layer-0 q_proj-output fingerprint emission.",
]
must_not_claim = [
  "The shared q_proj-output boundary is behavior-proven until accepted Qwen3 and Qwen2.5 before/after f32-le tensor fingerprint equality passes.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-121"
status = "merged"
pr = 764
head_sha = "51274c5040a87c1598d28ae91190b3f2e41942ff"
merge_sha = "9d7393b2e31b92ae904c7d6eca1226b1e4bdf6d4"
merged_at = "2026-05-26T06:30:35Z"
branch = "codex/slm-cpu-121-qwen3-preboundary-allocation"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-120"]
acceptance = "Resolve or precisely isolate the remaining Qwen3 Q8_0 167772160-byte allocation failure that occurs after prompt IDs are emitted but before a post-guard receipt or layer-0 attention.q_proj_output_pre_optional_qnorm fingerprint is written. A valid slice must preserve the SLM-CPU-120 fail-closed packed-Q8 sidecar payload-order guard, keep eager_f32_candle as the default runtime, preserve strict GGUF tokenizer authority, CPU backend, fallback_used=false where receipts exist, and must not claim q_proj sidecar behavior proof, allocation reduction, timing improvement, default-runtime promotion, sustained throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes until accepted Qwen3 and Qwen2.5 before/after proof gates pass."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without proof gates",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-120 fail-closes packed-Q8 sidecar runtime execution when payload byte order does not match the runtime matrix shape.",
  "SLM-CPU-121 resolves the remaining Qwen3 pre-boundary allocation failure by using prompt-plus-generation bounded KV cache capacity for strict tiny generation runs.",
  "SLM-CPU-121 reaches post-guard receipt emission and the layer-0 attention.q_proj_output_pre_optional_qnorm fingerprint for the Qwen3 Q8_0 run that SLM-CPU-120 could not complete.",
]
must_not_claim = [
  "The shared q_proj-output boundary is behavior-proven until accepted Qwen3 and Qwen2.5 before/after f32-le tensor fingerprint equality passes.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-122"
status = "merged"
pr = 766
head_sha = "a870059bdaa91348affcb6c4cab32911ab838452"
merge_sha = "95c1e21e84df503122037011785718e532fd0d34"
merged_at = "2026-05-26T07:07:51Z"
branch = "codex/slm-cpu-122-qproj-output-proof-refresh"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-121"]
acceptance = "Resume the shared attention.q_proj_output_pre_optional_qnorm behavior proof after SLM-CPU-121 makes the Qwen3 Q8_0 post-guard receipt and layer-0 fingerprint reachable. A valid slice must compare accepted Qwen3 Q8_0 and Qwen2.5 Q8_0 before/after strict CPU receipts at the shared q_proj-output boundary, preserve strict GGUF tokenizer authority, CPU backend, fallback_used=false, model SHA, prompt IDs, generated IDs, decoded text, selected backend/kernel identity, dense hook identity, boundary label, source tensor identity, shape/dtype, and must fail closed on missing or mismatched f32-le fingerprints. It must not claim packed-Q8 sidecar behavior proof, allocation reduction, timing improvement, default-runtime promotion, sustained throughput, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes unless the accepted cross-model proof gates pass."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without proof gates",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-121 resolves the Qwen3 pre-boundary KV allocation failure and makes post-guard receipt plus q_proj-output fingerprint evidence reachable.",
  "SLM-CPU-122 may prove or fail-close the shared q_proj-output boundary using Qwen3 Q8_0 and Qwen2.5 Q8_0 strict CPU before/after receipt pairs.",
]
must_not_claim = [
  "The shared q_proj-output boundary is behavior-proven until accepted Qwen3 and Qwen2.5 before/after f32-le tensor fingerprint equality passes.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-123"
status = "merged"
pr = 768
head_sha = "014f386478a86ba7850ad94768d157ca46a419a7"
merge_sha = "f717147d34b57a06ce848471e164461b32a3fd10"
merged_at = "2026-05-26T07:41:14Z"
branch = "codex/slm-cpu-123-qwen25-qproj-fingerprint-root-cause"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-122"]
acceptance = "Explain or eliminate the Qwen2.5 Q8_0 attention.q_proj_output_pre_optional_qnorm before/after f32-le fingerprint mismatch recorded by SLM-CPU-122 before any packed-Q8 sidecar behavior proof, allocation reduction, timing improvement, or default-runtime promotion claim. A valid slice must preserve strict GGUF tokenizer authority, CPU backend, fallback_used=false, model SHA, prompt IDs, generated IDs, decoded text, selected backend/kernel identity, dense hook identity, boundary label, source tensor identity, shape/dtype, and must fail closed if the fingerprint mismatch remains unexplained."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without proof gates",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-122 records that Qwen3 Q8_0 remains stable but Qwen2.5 Q8_0 still mismatches at the shared q_proj-output f32-le fingerprint boundary.",
  "SLM-CPU-123 may identify, explain, or eliminate the Qwen2.5 q_proj-output mismatch while preserving the strict CPU receipt contract.",
]
must_not_claim = [
  "The shared q_proj-output boundary is behavior-proven until accepted Qwen3 and Qwen2.5 before/after f32-le tensor fingerprint equality passes.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-124"
status = "merged"
pr = 770
head_sha = "aa6bc330110d4c17783144360bed42c708703475"
merge_sha = "beb8cb6a9cbcb3e152f40f6b0d8532fe2ed820d2"
merged_at = "2026-05-26T09:20:54Z"
branch = "codex/slm-cpu-124-qwen25-qproj-tensor-dump"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-123"]
acceptance = "Capture a bounded Qwen2.5 Q8_0 attention.q_proj_output_pre_optional_qnorm tensor sample or full 896-f32 diagnostic dump for the strict CPU before/after pair, then classify the numeric mismatch behind the SLM-CPU-123 fingerprint delta before any packed-Q8 sidecar behavior proof, allocation reduction, timing improvement, or default-runtime promotion claim. A valid slice must preserve strict GGUF tokenizer authority, CPU backend, fallback_used=false, model SHA, prompt IDs, generated IDs, decoded text, selected backend/kernel identity, dense hook identity, boundary label, source tensor identity, shape/dtype, f32-le fingerprint, and must emit max_abs_diff plus first_differing_index or fail closed with the exact missing dump blocker."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion without proof gates",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-123 identifies the current Qwen2.5 q_proj-output blocker as unresolved from fingerprint-only traces because contents_dumped=false.",
  "SLM-CPU-124 may capture bounded tensor contents and classify the first numeric mismatch while preserving the strict CPU receipt contract.",
]
must_not_claim = [
  "The shared q_proj-output boundary is behavior-proven until accepted Qwen3 and Qwen2.5 before/after f32-le tensor fingerprint equality and numeric-diff gates pass.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-125"
status = "merged"
pr = 772
head_sha = "dae4625ff2740461140dc63f726657bb889ab0ff"
merge_sha = "03ac5609751404d9948220cb45f667140b346d06"
merged_at = "2026-05-26T10:44:20Z"
branch = "codex/slm-cpu-125-qproj-numeric-tolerance-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-124"]
acceptance = "Decide the next shared attention.q_proj_output_pre_optional_qnorm proof gate after SLM-CPU-124 classified the Qwen2.5 before/after mismatch as small f32 numeric drift. A valid slice must either define and validate a conservative numeric tolerance policy using accepted Qwen3 and Qwen2.5 strict CPU before/after evidence, or keep the boundary fail-closed with a machine-checkable blocker naming the next implementation fix. It must preserve strict GGUF tokenizer authority, CPU backend, fallback_used=false, model SHA, prompt IDs, generated IDs, decoded text, selected backend/kernel identity, dense hook identity, boundary label, source tensor identity, shape/dtype, f32-le fingerprint, max_abs_diff, and first_differing_index, and must not claim packed-Q8 sidecar behavior proof, allocation reduction, timing improvement, or default-runtime promotion unless the accepted numeric proof gate passes."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-124 classifies the Qwen2.5 q_proj-output fingerprint mismatch as small f32 numeric drift while generated behavior remains stable.",
  "SLM-CPU-125 may define, validate, reject, or fail-close a conservative numeric tolerance gate for the shared q_proj-output sidecar proof.",
]
must_not_claim = [
  "The shared q_proj-output boundary is behavior-proven until the accepted numeric proof gate passes for Qwen3 and Qwen2.5 strict CPU before/after evidence.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-126"
status = "merged"
pr = 774
head_sha = "4039f84638d50d497a33df32e0a3db0663465100"
merge_sha = "337638b8fa1828f8f5550dbdbfafdd07f24401ae"
merged_at = "2026-05-26T11:10:31Z"
branch = "codex/slm-cpu-126-qproj-allocation-timing-experiment"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-125"]
acceptance = "Use the accepted exact-boundary SLM-CPU-125 numeric gate as a prerequisite for the next Qwen3/Qwen2.5 before/after packed-Q8 sidecar allocation or timing experiment. A valid slice must collect fresh strict CPU receipts before and after the experiment, preserve model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, fallback_used=false, and the accepted layer-0 attention.q_proj_output_pre_optional_qnorm numeric gate, then either record a bounded allocation/timing result or fail closed with the exact blocker. It must not promote packed-Q8 sidecar to default runtime or claim speedup, sustained throughput, broad answer quality, Q4/Q5 runtime support, server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 changes without matching receipts."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-125 accepts a narrow absolute 1e-4 f32 numeric gate for the exact layer-0 attention.q_proj_output_pre_optional_qnorm diagnostic boundary.",
  "SLM-CPU-126 may run a bounded allocation or timing experiment only if fresh before/after strict CPU receipts preserve the accepted behavior and numeric gates.",
]
must_not_claim = [
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-127"
status = "merged"
pr = 776
head_sha = "706f5e6b39aef0494b6299f8fc0231760c57ad3c"
merge_sha = "e8a9928763285a8a91088fa1c2611b89e0d78949"
merged_at = "2026-05-26T11:34:33Z"
branch = "codex/slm-cpu-127-qwen25-fresh-receipt-prereq"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-126"]
acceptance = "Restore or fetch the exact Qwen2.5-0.5B-Instruct Q8_0 GGUF artifact required by SLM-CPU-126, verify its pinned SHA256, and collect or explicitly block the fresh Qwen3/Qwen2.5 before/after strict CPU receipt pack needed before any allocation or timing experiment can claim improvement. A valid slice must not commit model binaries, must preserve strict GGUF tokenizer authority, selected CPU backend/kernel identity, prompt IDs, generated IDs, decoded text, dense hook identity, fallback_used=false, and the SLM-CPU-125 q_proj-output numeric gate, and must fail closed if the artifact or receipt pack cannot be produced."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-126 identifies the missing exact Qwen2.5 Q8_0 artifact as the blocker before fresh cross-model allocation or timing receipts.",
  "SLM-CPU-127 may restore artifact availability and receipt readiness, or fail closed with an exact missing-artifact or missing-receipt blocker.",
]
must_not_claim = [
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-128"
status = "merged"
pr = 778
head_sha = "794d626e5b62a1f3b130ce6ee665da58d40caa6b"
merge_sha = "200e1d67e1c6c6f1fb914e45ee4e46b3aecba083"
merged_at = "2026-05-26T11:43:19Z"
branch = "codex/slm-cpu-128-qproj-allocation-timing-classification"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-127"]
acceptance = "Use the SLM-CPU-125 exact-boundary numeric gate and the SLM-CPU-127 fresh Qwen3/Qwen2.5 before/after receipt pack to run or classify the next bounded exact-tensor packed-Q8 sidecar allocation or timing experiment. A valid slice must preserve model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, fallback_used=false, and the accepted layer-0 attention.q_proj_output_pre_optional_qnorm numeric gate. It may record a bounded allocation/timing result only if matching receipts prove behavior equivalence; otherwise it must fail closed with the exact blocker."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-127 restores exact Qwen2.5 Q8_0 artifact availability and fresh cross-model receipt readiness.",
  "SLM-CPU-128 may classify a bounded exact-tensor allocation or timing result only if fresh behavior-equivalent receipts support it.",
]
must_not_claim = [
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-129"
status = "merged"
pr = 780
head_sha = "e267281b2fe4481834b1a4bcdddee3857af29351"
merge_sha = "3ec39af9f5410426c0ee1e681aded9aa47cac60a"
merged_at = "2026-05-26T12:06:49Z"
branch = "codex/slm-cpu-129-repeated-allocation-audit-receipts"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-128"]
acceptance = "Collect or explicitly block repeated warm-session before/after receipts with allocation-audit counters for the exact-tensor packed-Q8 sidecar boundary after SLM-CPU-128 classified the single-sample timing evidence as mixed. A valid slice must preserve model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, fallback_used=false, and the accepted layer-0 attention.q_proj_output_pre_optional_qnorm numeric gate. It must require at least repeated behavior-equivalent receipts before claiming allocation reduction or timing improvement, and must fail closed if allocation-audit counters are unavailable or behavior drifts."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-128 classifies the single-sample exact-tensor timing evidence as mixed and fail-closed.",
  "SLM-CPU-129 may collect repeated warm-session allocation-audit receipts or fail closed with the exact missing-counter or behavior-drift blocker.",
]
must_not_claim = [
  "Allocation reduction or speedup is proven without repeated behavior-equivalent before/after strict CPU receipts.",
  "The packed-Q8 sidecar is promoted to default runtime without all proof gates passing.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-130"
status = "merged"
pr = 782
head_sha = "3f1234f1b3e3921e71109f57baa5881fcffa6c99"
merge_sha = "af2d4822ed1517b33921d6b630132be79c34cc9a"
merged_at = "2026-05-26T12:20:06Z"
branch = "codex/slm-cpu-130-qproj-selector-convergence-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-129"]
acceptance = "Resolve or explicitly classify the cross-model exact-tensor packed-Q8 selector mismatch after SLM-CPU-129: Qwen3 records allocation counters but stays on eager_f32_candle for the selected path, while Qwen2.5 records opt-in packed_q8_sidecar counter selection for the exact layer-0 attention.q_proj tensor. A valid slice must preserve model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, fallback_used=false, allocation-audit counters, and the accepted q_proj numeric gate. It may narrow the selector or keep the sidecar diagnostic-only, but must not claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-129 records repeated warm-session allocation-audit counters with behavior-equivalent Qwen3 and Qwen2.5 receipts.",
  "SLM-CPU-130 may classify why Qwen3 and Qwen2.5 do not exercise the same packed-Q8 selector boundary, or may narrow the selector contract so the diagnostic surface is explicit.",
]
must_not_claim = [
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-131"
status = "merged"
pr = 784
head_sha = "3fb5c917e8fec7a6b3ed669b2634e36c8f1f9f7b"
merge_sha = "74723fa3abb73c0dcc664c10b5acc11291efbf1b"
merged_at = "2026-05-26T13:07:39Z"
branch = "codex/slm-cpu-131-qwen3-qproj-payload-order-proof"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-130"]
acceptance = "Classify or implement the Qwen3 exact layer-0 attention.q_proj packed-Q8 payload-order proof needed after SLM-CPU-130: Qwen3 must remain on eager_f32_candle unless a machine-checkable payload reorder/runtime-shape proof shows sidecar_payload_order_matches_runtime_shape=true for the same model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, fallback_used=false, allocation-audit counters, and accepted q_proj numeric gate. The slice may add proof artifacts or code for payload-order verification, but must not claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-130 classifies the cross-model q_proj selector mismatch as the intended payload-order guard outcome.",
  "SLM-CPU-131 may prove Qwen3 payload order matches the runtime matrix shape, or may keep Qwen3 fail-closed with the exact blocker.",
]
must_not_claim = [
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-132"
status = "merged"
pr = 786
head_sha = "1ce29b30b9e0be0b3052cd6a634a50dbd45df961"
merge_sha = "caf026d82b4f10052be0f5dd16ebf008e3b042fc"
merged_at = "2026-05-26T13:35:33Z"
branch = "codex/slm-cpu-132-qwen3-qproj-payload-reorder-contract"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-131"]
acceptance = "Define or precisely block the Qwen3 exact layer-0 attention.q_proj packed-Q8 payload reorder/runtime-shape contract after SLM-CPU-131: the slice may specify a machine-checkable transform from GGUF source-order Q8_0 payload shape [1024, 2048] into runtime matrix order [2048, 1024], or it may keep Qwen3 fail-closed with the exact missing proof. It must preserve Qwen3 and Qwen2.5 behavior oracles with model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, fallback_used=false, allocation-audit counters, and accepted q_proj numeric gate before any runtime selection claim. It must not claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-131 proves Qwen3 exact q_proj runtime selection is blocked by source payload order mismatch.",
  "SLM-CPU-132 may define a payload reorder/runtime-shape contract or may keep Qwen3 fail-closed with the exact missing proof.",
]
must_not_claim = [
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-133"
status = "merged"
branch = "codex/slm-cpu-133-qwen3-source-order-qproj-kernel-contract"
pr = 788
head_sha = "f6a6d5527495d72daa81d1121ade529f7e03e7de"
merge_sha = "593342a2e3e8b700b5078a9eeff4c82d09462cf5"
merged_at = "2026-05-26T14:05:08Z"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-132"]
acceptance = "Define or precisely block the source-order Q8_0 layer-0 attention.q_proj kernel contract for the exact Qwen3 Q8_0 tensor after SLM-CPU-132 proved pure byte reorder invalid. A valid slice may add a runtime-disabled contract, tests, and proof artifact for consuming GGUF source-order payload shape [1024, 2048] directly against the runtime input vector without dequantize/requantize, or may fail closed with the exact missing API/layout/accumulator proof. It must preserve the Qwen3 and Qwen2.5 behavior oracles with model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, fallback_used=false, allocation-audit counters, and accepted q_proj numeric gate before any runtime selection claim. It must not claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-132 proves Qwen3 exact q_proj cannot be enabled by pure Q8_0 byte reorder because the transpose would regroup values under the wrong block scales.",
  "SLM-CPU-133 may define a runtime-disabled source-order Q8_0 q_proj kernel contract or may keep Qwen3 fail-closed with the exact blocker.",
]
must_not_claim = [
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-134"
status = "merged"
branch = "codex/slm-cpu-134-source-order-qproj-matvec-prototype"
pr = 790
head_sha = "c494cc998e0796cbd4f03fbfe2f8c6d6f281e50c"
merge_sha = "cc3db3e5717f07eba49738fef5a26b00cfa99ea2"
merged_at = "2026-05-26T15:08:29Z"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-133"]
acceptance = "Implement or precisely block a runtime-disabled source-order Q8_0 layer-0 attention.q_proj matvec prototype for the exact Qwen3 Q8_0 tensor using the GGUF source-order payload directly, after SLM-CPU-133 defined the contract. A valid slice may add a prototype, tests, and proof artifact that compare the source-order matvec output against the accepted eager_f32_candle q_proj boundary for the same prompt/model and preserve the Qwen3 and Qwen2.5 behavior oracles before any selector use. If the prototype cannot be added safely, it must fail closed with the exact missing accumulator, Tensor API, layout, lifetime, trace identity, or receipt-safety blocker. Runtime selection must remain disabled and the default eager path unchanged. It must not claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-133 defines the runtime-disabled source-order Q8_0 q_proj kernel contract for the exact Qwen3 layer-0 tensor.",
  "SLM-CPU-134 may add a runtime-disabled source-order q_proj matvec prototype or keep the path fail-closed with the exact blocker.",
]
must_not_claim = [
  "Runtime selection uses the source-order matvec prototype.",
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-135"
status = "merged"
branch = "codex/slm-cpu-135-source-order-qproj-behavior-gate"
pr = 792
head_sha = "9f120b56b975eb51b2529ef0ea1a73bced267056"
merge_sha = "13fe70d4e953ab66d04b7d789cffbcbac3d9a60f"
merged_at = "2026-05-26T16:08:12Z"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-134"]
acceptance = "Define the exact-model behavior gate required before the SLM-CPU-134 source-order Q8_0 q_proj matvec prototype can be considered for selector use. A valid slice must either capture strict before/after Qwen3 Q8_0 and Qwen2.5 Q8_0 CPU receipts proving unchanged model SHA, tokenizer authority, prompt IDs, generated IDs, decoded text, selected backend/kernel identity, dense hook identity, fallback_used=false, and accepted q_proj numeric evidence, or keep runtime selection disabled with the exact missing receipt or trace blocker. It must not claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-134 added a runtime-disabled source-order Q8_0 q_proj matvec prototype with fixture proof evidence.",
  "SLM-CPU-135 may define or satisfy the exact-model behavior gate required before selector use is considered.",
]
must_not_claim = [
  "Runtime selection uses the source-order matvec prototype.",
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-136"
status = "merged"
branch = "codex/slm-cpu-136-source-order-qproj-receipt-pairs"
pr = 794
head_sha = "65bd87a08b025bca725dd3cf9b5b9a4c3dfcd9a7"
merge_sha = "41316012dd162ce7252137ab61a6f46eabcdfdbc"
merged_at = "2026-05-26T16:37:02Z"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-135"]
acceptance = "Capture or precisely block exact-model strict CPU before/after receipt pairs for considering the SLM-CPU-134 source-order Q8_0 q_proj matvec prototype as an opt-in selector candidate. A valid slice must cover both Qwen3 Q8_0 and Qwen2.5 Q8_0, preserving model SHA, GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected backend/kernel identity, dense hook identity, q_proj numeric evidence, and fallback_used=false. If receipt capture cannot be done safely, emit a machine-checkable blocker naming the missing selector hook, model cache, trace identity, CLI flag, or receipt field. Runtime selection must remain disabled by default and no allocation, timing, speedup, sustained-throughput, Q4/Q5, server/accelerator, Qwen3.5, or BitNet QK256 claim is allowed."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-135 defines the exact-model behavior gate required before source-order q_proj selector use is considered.",
  "SLM-CPU-136 may capture the required Qwen3/Qwen2.5 before/after strict CPU receipt pairs or keep the selector path fail-closed with an exact blocker.",
]
must_not_claim = [
  "Runtime selection uses the source-order matvec prototype by default.",
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-137"
status = "merged"
pr = 796
head_sha = "5b8577687a50f077a605c77084931c5d4d280f00"
merge_sha = "76c50a94031e9b6ffbf2abdf0cff941509dde95b"
merged_at = "2026-05-26T17:35:45Z"
branch = "codex/slm-cpu-137-source-order-qproj-selector-hook"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-136"]
acceptance = "Add or precisely block a default-disabled exact-tensor selector hook and receipt identity surface for the source-order Q8_0 q_proj matvec candidate. A valid slice may introduce an opt-in selector path for only layers.0.attention.q_proj.weight that preserves eager_f32_candle as default, records selected dense hook/kernel identity and q_proj numeric evidence in receipts, and remains unusable for behavior or performance claims until Qwen3/Qwen2.5 before/after strict CPU receipt pairs pass. If implementation is not safe, emit a machine-checkable blocker naming the exact missing Tensor API, selector integration point, trace identity field, receipt field, or hidden-state input binding. It must not claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-136 records that source-order q_proj receipt-pair capture is blocked by missing selector hook, receipt identity, and runtime input binding.",
  "SLM-CPU-137 may add a default-disabled exact-tensor selector hook and receipt identity surface or keep it fail-closed with an exact blocker.",
]
must_not_claim = [
  "Runtime selection uses the source-order matvec prototype by default.",
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-138"
status = "merged"
branch = "codex/slm-cpu-138-source-order-qproj-receipt-pairs"
pr = 798
head_sha = "8702eea700b9e04b022f2e359a3ea3127d6aa5ea"
merge_sha = "a37191a21077be6badac6ab8366d0953bb63d58c"
merged_at = "2026-05-26T18:37:50Z"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-137"]
acceptance = "Capture or precisely block Qwen3 Q8_0 and Qwen2.5 Q8_0 before/after strict CPU receipt pairs that include the SLM-CPU-137 source-order Q8_0 q_proj candidate identity surface. A valid slice must preserve identical model SHA, GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, source-order q_proj candidate path/kernel/receipt identity, q_proj numeric evidence, and fallback_used=false before any source-order selector use. If capture is unsafe or unavailable, emit a machine-checkable blocker naming the exact missing model cache, CLI flag, receipt field, trace identity field, q_proj numeric evidence, or runtime input binding. It must not claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-137 added a default-disabled source-order q_proj candidate identity surface.",
  "SLM-CPU-138 may capture before/after receipt-pair evidence for that identity surface or keep it fail-closed with an exact blocker.",
]
must_not_claim = [
  "Runtime selection uses the source-order matvec prototype by default.",
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-139"
status = "merged"
pr = 800
head_sha = "6ca914079eca8355cce1b6e073c8dede60635b94"
merge_sha = "3c15e338ef9713024a3f7c6e1133ab49dd5431b6"
merged_at = "2026-05-26T19:53:56Z"
branch = "codex/slm-cpu-139-source-order-qproj-exact-model-receipts"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-138"]
acceptance = "Restore or precisely block the exact Qwen2.5 Q8_0 model cache, then capture Qwen3 Q8_0 and Qwen2.5 Q8_0 before/after strict CPU receipts on current main with the SLM-CPU-137 source-order q_proj candidate identity fields. A valid slice must preserve identical model SHA, GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, source-order q_proj candidate path/kernel/dimensions/receipt identity, q_proj numeric evidence, and fallback_used=false. If capture is unavailable, emit a machine-checkable blocker naming the exact missing model cache, receipt field, trace identity field, q_proj numeric evidence, or runtime input binding. It must not claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-138 recorded the current blocker for source-order receipt-pair capture.",
  "SLM-CPU-139 may restore or precisely block the missing exact-model evidence needed for fresh source-order identity receipt pairs.",
]
must_not_claim = [
  "Runtime selection uses the source-order matvec prototype by default.",
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-140"
status = "merged"
pr = 802
head_sha = "c78e38f780d3740d6ea6c26976bd335f8ca1c5e3"
merge_sha = "59917c0d1a46fa6f3876dc56cf5f6aa128882faf"
merged_at = "2026-05-26T20:39:55Z"
branch = "codex/slm-cpu-140-qwen25-cache-restore-contract"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-139"]
acceptance = "Define the exact Qwen2.5 Q8_0 cache restoration contract needed before source-order q_proj receipt capture can resume. The slice must name the required model path, SHA256, non-committed cache handling, receipt capture commands, source-order q_proj identity fields, and validation gates for Qwen3 Q8_0 plus Qwen2.5 Q8_0 fresh before/after strict CPU receipts. It must not commit models/** or target/** artifacts, claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-139 precisely blocked source-order receipt capture on the missing exact Qwen2.5 Q8_0 cache and stale receipt identity fields.",
  "SLM-CPU-140 may define the non-committed cache restoration and receipt-capture contract for the next evidence slice.",
]
must_not_claim = [
  "The exact Qwen2.5 Q8_0 model binary is committed.",
  "Fresh source-order Qwen3/Qwen2.5 receipt pairs have been captured.",
  "Runtime selection uses the source-order matvec prototype by default.",
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-141"
status = "merged"
branch = "codex/slm-cpu-141-source-order-receipt-capture"
pr = 804
head_sha = "0e54b136d5eab0404f59171463d9e7c6e4891bbc"
merge_sha = "96e339a1f5945935eab6e25b9ee325ff5a728778"
merged_at = "2026-05-26T21:54:03Z"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-140"]
acceptance = "Restore the exact Qwen2.5 Q8_0 GGUF into the non-committed cache named by SLM-CPU-140, verify its SHA256, then capture Qwen3 Q8_0 and Qwen2.5 Q8_0 before/after strict CPU receipts with the SLM-CPU-137 source-order q_proj identity fields and q_proj numeric evidence. A valid slice must preserve model SHA, GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, source-order q_proj candidate path/kernel/dimensions/receipt identity, q_proj numeric evidence, and fallback_used=false. If the exact model is unavailable or receipts cannot include required fields, emit a machine-checkable blocker naming the missing cache, receipt field, trace identity field, q_proj numeric evidence, or runtime input binding. It must not commit models/** or target/** artifacts, claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-140 defined the exact non-committed Qwen2.5 Q8_0 cache restoration and receipt capture contract.",
  "SLM-CPU-141 may restore the exact cache locally and capture Qwen3/Qwen2.5 source-order q_proj receipt-pair evidence or precisely block on missing evidence fields.",
]
must_not_claim = [
  "The exact Qwen2.5 Q8_0 model binary is committed.",
  "Runtime selection uses the source-order matvec prototype by default.",
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-142"
status = "merged"
pr = 806
head_sha = "c81cf827e50320fc894beebaca71f81497d15493"
merge_sha = "c666fe63897231162d9ac88d68e32c4689482451"
merged_at = "2026-05-26T22:42:50Z"
branch = "codex/slm-cpu-142-source-order-qproj-evidence-fields"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-141"]
acceptance = "Resolve the SLM-CPU-141 blocker by adding or precisely blocking the missing q_proj numeric evidence and cross-model source-order q_proj candidate identity fields needed for source-order selector gating. A valid slice may teach the strict CPU receipt path to carry q_proj numeric evidence and source-order q_proj identity for the exact Qwen3 Q8_0 and Qwen2.5 Q8_0 receipt pair, or it may emit a machine-checkable blocker naming the exact missing trace identity, receipt field, runtime input binding, shape contract, or model-specific selector split. It must preserve model SHA, GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, fallback_used=false, and default eager_f32_candle runtime. It must not commit models/** or target/** artifacts, claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-141 restored and verified the exact Qwen2.5 Q8_0 cache locally and captured fresh Qwen3/Qwen2.5 before/after strict CPU receipts.",
  "SLM-CPU-142 may add or precisely block the missing q_proj numeric evidence and cross-model source-order candidate identity fields required before source-order selector use is considered.",
]
must_not_claim = [
  "The exact Qwen2.5 Q8_0 model binary is committed.",
  "Runtime selection uses the source-order matvec prototype by default.",
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-143"
status = "merged"
pr = 808
merge_sha = "d6639ff79645738e4cfe6065355e9e4c71a64d83"
merged_at = "2026-05-27T00:00:54Z"
branch = "codex/slm-cpu-143-source-order-evidence-receipts"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-142"]
acceptance = "Regenerate or precisely block the exact Qwen3 Q8_0 and Qwen2.5 Q8_0 before/after strict CPU warm-session receipts on current main with the SLM-CPU-142 source-order q_proj candidate identity and q_proj numeric evidence fields present. A valid slice must preserve model SHA, GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, source_order_qproj_candidate_identity, q_proj_numeric_evidence, fallback_used=false, and default eager_f32_candle runtime. If capture is unavailable, emit a machine-checkable blocker naming the exact missing model cache, trace identity field, q_proj numeric evidence boundary, runtime input binding, or receipt capture command. It must not commit models/** or target/** artifacts, claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-142 added explicit fail-closed source-order q_proj candidate identity and q_proj numeric evidence receipt fields.",
  "SLM-CPU-143 may refresh exact Qwen3/Qwen2.5 strict CPU receipt evidence or keep source-order selector use blocked with the exact missing evidence.",
]
must_not_claim = [
  "The exact Qwen2.5 Q8_0 model binary is committed.",
  "Runtime selection uses the source-order matvec prototype by default.",
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-144"
status = "merged"
pr = 810
merged_at = "2026-05-27T00:43:53Z"
merge_sha = "e14899edcfcb433ebedcf82fe5c2c8e597bf74dc"
branch = "codex/slm-cpu-144-source-order-qproj-identity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-143"]
acceptance = "Expose or precisely block a real payload-bearing source-order q_proj boundary identity for the exact Qwen3 Q8_0 and Qwen2.5 Q8_0 warm-session receipt surface. A valid slice must explain why SLM-CPU-143 records source_order_qproj_candidate_identity.status=payload_boundary_present_without_source_order_identity for both models, then either emit a candidate path/kernel/receipt identity at the correct q_proj boundary or produce a machine-checkable blocker naming the missing hook boundary, sidecar payload binding, source-order shape classification, or runtime input binding. It must preserve model SHA, GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, q_proj_numeric_evidence fail-closed status, fallback_used=false, and default eager_f32_candle runtime. It must not commit models/** or target/** artifacts, claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-143 refreshed exact Qwen3/Qwen2.5 strict CPU receipt evidence and kept source-order selector use fail-closed.",
  "SLM-CPU-144 may expose a usable source-order q_proj boundary identity or precisely block why the identity is still unavailable.",
]
must_not_claim = [
  "The exact Qwen2.5 Q8_0 model binary is committed.",
  "Runtime selection uses the source-order matvec prototype by default.",
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-145"
status = "merged"
branch = "codex/slm-cpu-145-payload-runtime-qproj-capture"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-144"]
acceptance = "Regenerate or precisely block the exact Qwen3 Q8_0 and Qwen2.5 Q8_0 strict CPU warm-session after-run receipts with both BITNET_DENSE_Q8_PAYLOAD_* and BITNET_DENSE_Q8_RUNTIME_* gates set for blk.0.attn_q.weight, then compare them against the existing before-run receipt oracles. A valid slice must preserve model SHA, GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, fallback_used=false, and default eager_f32_candle runtime. It must expose the payload-bearing q_proj boundary identity if available, keep selector use blocked unless q_proj numeric evidence is attached and behavior is preserved, and precisely name any remaining payload binding, runtime input binding, trace identity, or q_proj numeric evidence blocker. It must not commit models/** or target/** artifacts, claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-144 fixed null payload-boundary receipt classification and precisely blocked source-order q_proj identity capture on missing payload gates.",
  "SLM-CPU-145 may capture exact-model q_proj payload/runtime-gated receipts or precisely block the remaining boundary.",
]
must_not_claim = [
  "The exact Qwen2.5 Q8_0 model binary is committed.",
  "Runtime selection uses the source-order matvec prototype by default.",
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-146"
status = "ready"
branch = "codex/slm-cpu-146-qproj-numeric-evidence-gate"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-145"]
acceptance = "Attach or precisely block bounded q_proj numeric evidence for the exact layer-0 blk.0.attn_q.weight payload/runtime path using the SLM-CPU-145 Qwen3 Q8_0 and Qwen2.5 Q8_0 receipt oracles. A valid slice must preserve model SHA, GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, fallback_used=false, and the default eager_f32_candle runtime outside explicit gates. It must report max_abs_diff, mean_abs_diff, rms_abs_diff, first differing index, accepted tolerance, payload/runtime selector identity, and behavior preservation when numeric evidence is available; otherwise it must precisely name why q_proj numeric evidence cannot yet be captured. It must not commit models/** or target/** artifacts, claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server/accelerator execution, Qwen3.5, or BitNet QK256 behavior."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-models/**",
  "crates/bitnet-transformer/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "models/**",
  "target/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
]
may_claim = [
  "SLM-CPU-145 captured exact Qwen3/Qwen2.5 q_proj payload/runtime-gated receipts.",
  "Qwen3 remains fail-closed on payload-order mismatch while Qwen2.5 preserves generated IDs/text with the opt-in packed q_proj path selected.",
  "SLM-CPU-146 may attach bounded q_proj numeric evidence or precisely block why it is still unavailable.",
]
must_not_claim = [
  "The exact Qwen2.5 Q8_0 model binary is committed.",
  "Runtime selection uses the source-order matvec prototype by default.",
  "Allocation reduction or speedup is proven.",
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-103"
status = "merged"
branch = "codex/slm-cpu-103-typed-qnorm-rope-consumer-gate"
pr = 666
head_sha = "4aef8fca97f13605c12f4bcf4ae0179bb876630d"
merge_sha = "26f4f8527882cb062112a0d7fe0743c7508dba57"
merged_at = "2026-05-25T00:30:52Z"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-102"]
acceptance = "Define or precisely block the next typed q_norm/RoPE consumer boundary after SLM-CPU-102 for the exact Qwen3 Q8_0 `layers.0.attention.q_proj.weight` packed-Q8 sidecar path. A valid slice may add a behavior-preserving typed q_norm/RoPE consumer or a single-materialization boundary that carries the logical Q-head view through q_norm, RoPE, trace/workspace identity, and attention-score handoff without promoting packed_q8_sidecar to default runtime; otherwise it must emit a machine-checkable blocker naming the exact Tensor API, lifetime, layout, accumulator-order, trace identity, score-handoff, or receipt-safety gap. Any runtime-adjacent change must preserve the Qwen3 Q8_0 and Qwen2.5 Q8_0 strict CPU behavior oracles with before/after receipts proving identical model SHA, tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, and fallback_used=false before claiming allocation or timing improvement. The slice must not claim sustained 8250U throughput, broaden Q4/Q5 runtime support, or touch server/GPU/NPU/OpenVINO/UHD 620 execution, Qwen3.5 support, or BitNet QK256 paths."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-102 classified the typed attention-head consumer boundary and identified q_norm/RoPE/trace/score handoff as the next blockers.",
  "SLM-CPU-103 may define a typed q_norm/RoPE consumer or record the concrete blocker that still prevents safe fused consumer execution.",
  "Any allocation or timing improvement remains gated by behavior-preserving before/after Qwen3 Q8_0 and Qwen2.5 Q8_0 receipts.",
]
must_not_claim = [
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]

[[work_item]]
id = "SLM-CPU-104"
status = "merged"
branch = "codex/slm-cpu-104-qnorm-materialization-boundary"
pr = 671
head_sha = "b75fad244d501374b45302297e5999988a746939"
merge_sha = "0d08ed0f42ba63865ff5dcc560327a52e99e8301"
merged_at = "2026-05-25T01:25:49Z"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["SLM-CPU-103"]
acceptance = "Resolve the post-SLM-CPU-103 typed q_norm/RoPE blocker into one precise next boundary for the exact Qwen3 Q8_0 `layers.0.attention.q_proj.weight` packed-Q8 sidecar path: either define a behavior-preserving typed q_norm kernel contract, define a behavior-preserving typed RoPE kernel contract, or select exactly one Candle materialization boundary (`q_norm_input`, `after_q_norm_before_rope`, or `after_q_rope_before_attention_scores`) with the required Qwen3 Q8_0 and Qwen2.5 Q8_0 before/after receipt gates. The slice must name the accepted boundary or remaining blocker, preserve eager F32 default runtime, and make no allocation, timing, sustained-throughput, Q4/Q5, server/GPU/NPU/OpenVINO/UHD 620, Qwen3.5, or BitNet QK256 claim."
commands = [
  "cargo run -p xtask --no-default-features -- campaign generate",
  "cargo run -p xtask --no-default-features -- campaign doctor",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-transformer/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/**",
  "ci/slm-cpu/**",
  "docs/slm/**",
  "docs/tracking/campaigns/slm-cpu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-quantization/**",
  "crates/bitnet-kernels/src/gpu/**",
  "crates/bitnet-server/**",
  "crates/bitnet-npu/**",
  "packed_q8_sidecar default-runtime promotion",
  "Q4/Q5 runtime implementation",
  "server inference",
  "GPU, NPU, OpenVINO, or UHD 620 execution",
  "Qwen3.5 or hybrid architecture support",
  "BitNet QK256/I2_S kernels",
]
may_claim = [
  "SLM-CPU-104 may choose the next typed q_norm/RoPE kernel contract or one explicit materialization boundary after SLM-CPU-103.",
  "SLM-CPU-104 may define the before/after Qwen3 Q8_0 and Qwen2.5 Q8_0 receipt gates required before any later allocation or timing claim.",
]
must_not_claim = [
  "The packed-Q8 sidecar is promoted to default runtime.",
  "Allocation reduction or speedup is proven without before/after strict CPU receipts.",
  "Sustained 8250U throughput is proven.",
  "Broad answer quality is proven.",
  "Q4_K_M or Q4_K_S runtime support exists.",
  "Server, GPU, NPU, OpenVINO, or UHD 620 execution is involved.",
  "Qwen3.5 or hybrid architecture support is in scope.",
  "BitNet QK256/I2_S kernels are modified.",
]
