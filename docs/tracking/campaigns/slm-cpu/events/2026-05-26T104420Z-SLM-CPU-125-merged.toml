timestamp = "2026-05-26T10:44:20Z"
campaign = "slm-cpu"
item = "SLM-CPU-125"
event = "merged"
actor = "codex"
previous_status = "pr_open"
new_status = "merged"
pr = 772
branch = "codex/slm-cpu-125-qproj-numeric-tolerance-gate"
head_sha = "dae4625ff2740461140dc63f726657bb889ab0ff"
merge_sha = "03ac5609751404d9948220cb45f667140b346d06"
summary = "Merged PR #772 accepting the exact-boundary q_proj-output numeric tolerance gate."
notes = [
  "SLM-CPU-125 accepts an absolute 1e-4 f32 gate for the exact layer-0 attention.q_proj_output_pre_optional_qnorm diagnostic boundary.",
  "The accepted gate uses Qwen3 exact-match evidence and Qwen2.5 full-vector bounded-drift evidence while preserving prompt IDs, generated IDs, decoded text, CPU backend identity, and fallback_used=false.",
  "The merge does not claim allocation reduction, timing improvement, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server, accelerator, Qwen3.5, or BitNet QK256 changes.",
]

[[next_items]]
id = "SLM-CPU-126"
status = "ready"
branch = "codex/slm-cpu-126-qproj-allocation-timing-experiment"
summary = "Use the accepted exact-boundary gate as one prerequisite for the next before/after allocation or timing experiment, with fresh strict receipts required before any improvement claim."
