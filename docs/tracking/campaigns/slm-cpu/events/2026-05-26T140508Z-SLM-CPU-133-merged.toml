timestamp = "2026-05-26T14:05:08Z"
campaign = "slm-cpu"
item = "SLM-CPU-133"
event = "merged"
actor = "codex"
previous_status = "pr_open"
new_status = "merged"
pr = 788
branch = "codex/slm-cpu-133-qwen3-source-order-qproj-kernel-contract"
head_sha = "f6a6d5527495d72daa81d1121ade529f7e03e7de"
merge_sha = "593342a2e3e8b700b5078a9eeff4c82d09462cf5"
summary = "Merged PR #788 defining the SLM-CPU-133 Qwen3 source-order q_proj kernel contract."
notes = [
  "The slice defines a runtime-disabled source-order Q8_0 q_proj kernel contract for the exact Qwen3 layer-0 attention.q_proj tensor.",
  "Runtime selection remains disabled until a proof-gated source-order matvec preserves accumulator order, block-scale decode, generated IDs, and receipt identity.",
  "SLM-CPU-134 is ready to add or precisely block the source-order q_proj matvec prototype without changing the default eager runtime path.",
  "The merge does not claim allocation reduction, timing improvement, speedup, sustained throughput, default-runtime promotion, Q4/Q5 runtime support, server, accelerator, Qwen3.5, or BitNet QK256 behavior.",
]
