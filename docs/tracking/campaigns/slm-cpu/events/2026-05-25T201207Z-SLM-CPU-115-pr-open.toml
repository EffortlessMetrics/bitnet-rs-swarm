timestamp = "2026-05-25T20:12:07Z"
campaign = "slm-cpu"
item = "SLM-CPU-115"
event = "pr_open"
previous_status = "ready"
new_status = "pr_open"
pr = 740
head_sha = "0567dee4c44ac967f126bb1217c207d8d7db2e8e"
branch = "codex/slm-cpu-115-qnorm-proof-next-boundary"
actor = "codex"
summary = "Opened PR #740 to resolve the SLM-CPU-114 q_norm-input proof blocker into a shared Qwen-family next boundary."
notes = [
  "The artifact keeps the Qwen3 q_norm_input fingerprint evidence but rejects it as a cross-family proof boundary because Qwen2.5 has no q_norm-input stage on the accepted artifact.",
  "The selected next boundary is attention.q_proj_output_pre_optional_qnorm, which exists before optional q_norm/k_norm on Qwen3 and also exists on Qwen2.5 Q8_0.",
  "The slice preserves eager_f32_candle as the default runtime and makes no allocation, timing, sustained-throughput, Q4/Q5, server, accelerator, Qwen3.5, or BitNet QK256 claim.",
]
