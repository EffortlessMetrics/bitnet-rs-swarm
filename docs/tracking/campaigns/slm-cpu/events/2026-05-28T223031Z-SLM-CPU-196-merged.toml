timestamp = "2026-05-28T22:30:31Z"
campaign = "slm-cpu"
item = "SLM-CPU-196"
event = "merged"
actor = "codex"
previous_status = "ready"
new_status = "merged"
pr = 918
merge_sha = "a7aefaced611dd591a43511c7fddd87a1f700eba"
branch = "codex/slm-cpu-196-no-bias-candidate-preservation-receipts"
summary = "Merged no-bias candidate preservation receipt overlay."
notes = [
  "SLM-CPU-196 binds the runtime-disabled SLM-CPU-195 no-bias down-projection candidate to strict Qwen3/Qwen2.5 warm-session identity evidence.",
  "The overlay records candidate_compute_implemented=true while runtime_selection_enabled=false and selected_path=eager_f32_candle.",
  "The merged slice does not execute or select the candidate in normal inference and makes no allocation, timing, speedup, sustained throughput, Q4/Q5, server, accelerator, Qwen3.5, or BitNet QK256 claim.",
]
