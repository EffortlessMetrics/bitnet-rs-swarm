timestamp = "2026-05-21T12:59:00Z"
campaign = "slm-cpu"
item = "SLM-CPU-073"
event = "in_progress"
previous_status = "none"
new_status = "ready"
actor = "codex"
branch = "codex/slm-cpu-073-packed-q8-regression-root-cause"
summary = "Queue the real six-prompt packed Q8_0 sidecar regression root-cause slice after SLM-CPU-072."
notes = [
  "SLM-CPU-072 proved behavior equivalence but classified the opt-in exact-tensor packed_q8_sidecar path as slower than eager_f32_candle on the bounded i5-8250U six-prompt artifact.",
  "The next slice must identify whether the remaining regression belongs in payload decode, packed-block matvec locality, scratch allocation, selector/dispatch overhead, tensor layout, or timing instrumentation.",
  "The Qwen3 Q8_0 appliance behavior oracle remains prompt IDs, generated IDs, decoded text, strict tokenizer authority, selected CPU backend/kernel identity, model SHA, dense hook identity, and fallback=false.",
  "No default packed-Q8 enablement, speedup, sustained-throughput, broad quality, Q4/Q5, accelerator, server, Qwen3.5, or BitNet QK256 claim is added.",
]
