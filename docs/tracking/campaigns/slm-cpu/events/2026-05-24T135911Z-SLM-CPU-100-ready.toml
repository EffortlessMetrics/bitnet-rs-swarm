timestamp = "2026-05-24T13:59:11Z"
campaign = "slm-cpu"
item = "SLM-CPU-100"
event = "in_progress"
previous_status = "none"
new_status = "ready"
branch = "codex/slm-cpu-100-typed-fused-q-consumer-implementation-gate"
actor = "codex"
summary = "Queued the typed fused Q projection consumer implementation-or-blocker gate after SLM-CPU-099."
notes = [
  "SLM-CPU-099 defined the design-only typed fused Q projection consumer contract for the exact Qwen3 Q8_0 `layers.0.attention.q_proj.weight` packed-Q8 sidecar boundary.",
  "SLM-CPU-100 is the next performance-lane gate: implement that exact fused Q consumer or emit a concrete blocker naming the API, lifetime, tensor-layout, accumulator-order, or receipt-safety gap.",
  "Any runtime-adjacent change remains gated by before/after Qwen3 Q8_0 receipts preserving model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook identity, and fallback_used=false.",
  "The item makes no allocation reduction, speedup, sustained throughput, Q4/Q5, server, GPU, NPU, OpenVINO, UHD 620, Qwen3.5, or BitNet QK256 claim.",
]
