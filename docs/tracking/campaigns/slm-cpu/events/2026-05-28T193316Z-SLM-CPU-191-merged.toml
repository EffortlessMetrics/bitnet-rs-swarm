timestamp = "2026-05-28T19:33:16Z"
campaign = "slm-cpu"
item = "SLM-CPU-191"
event = "merged"
actor = "codex"
previous_status = "ready"
new_status = "merged"
pr = 908
merge_sha = "b8442ca63"
branch = "codex/slm-cpu-191-no-bias-selector-dry-run-receipts"
summary = "Merged no-bias selector dry-run receipt artifact."
notes = [
  "SLM-CPU-191 emits 366 policy decisions from the SLM-CPU-189 role_records manifest and SLM-CPU-190 fail-closed policy gate.",
  "The artifact records 294 eligible future no-bias candidates and 72 fail-closed biased Qwen2.5 attention q/k/v roles.",
  "The merged slice does not implement, enable, or select a no-bias fast path and makes no allocation, timing, speedup, Q4/Q5, server, accelerator, Qwen3.5, or BitNet QK256 claim.",
]
