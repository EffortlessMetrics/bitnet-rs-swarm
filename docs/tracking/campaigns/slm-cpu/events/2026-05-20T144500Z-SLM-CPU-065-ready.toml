timestamp = "2026-05-20T14:45:00Z"
campaign = "slm-cpu"
item = "SLM-CPU-065"
event = "in_progress"
previous_status = "proposed"
new_status = "ready"
actor = "codex"
summary = "Queue the first BitNet-rs runtime-promotion gate after accepting a bitnet-rs-swarm Kaby SLM package."
notes = [
  "The item is blocked by the SLM-CPU-064 release-surface intake review.",
  "The scope is limited to the exact layers.0.attention.q_proj.weight packed Q8_0 sidecar candidate returned from bitnet-rs-swarm.",
  "Runtime promotion remains disabled by default unless before/after strict CPU receipts preserve model SHA, tokenizer authority, generated IDs, decoded text, backend/kernel identity, dense hook-selection identity, and fallback_used=false.",
  "No speedup, sustained throughput, broad answer-quality, Q4/Q5 runtime support, server, GPU/NPU/OpenVINO/UHD 620, Qwen3.5, or BitNet QK256 claim is allowed.",
]
