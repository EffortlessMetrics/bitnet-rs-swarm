timestamp = "2026-05-20T09:15:00Z"
campaign = "slm-cpu"
item = "SLM-CPU-062"
event = "in_progress"
actor = "codex"
previous_status = "proposed"
new_status = "ready"
summary = "Queued the first swarm-to-release Qwen3 Q8_0 packed-sidecar artifact export."
notes = [
  "SLM-CPU-062 follows the SLM-CPU-061 real sidecar payload candidate and packages its behavior-preserving evidence for BitNet-rs release-surface artifact intake.",
  "The package must include candidate summary, before receipt, after receipt, equivalence report, timing report or timing-not-claimed report, and source swarm commit identity.",
  "The export must preserve model SHA, strict GGUF tokenizer authority, prompt IDs, generated IDs, decoded text, selected CPU backend/kernel identity, dense hook-selection identity, and fallback_used=false.",
  "The item must not claim release-surface acceptance, speedup, sustained throughput, broad answer quality, Q4/Q5 runtime, server, GPU/NPU/OpenVINO/UHD 620, Qwen3.5, or BitNet QK256 changes.",
]
