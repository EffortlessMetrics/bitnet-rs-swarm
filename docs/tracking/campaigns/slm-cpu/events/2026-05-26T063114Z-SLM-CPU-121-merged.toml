timestamp = "2026-05-26T06:30:35Z"
campaign = "slm-cpu"
item = "SLM-CPU-121"
event = "merged"
actor = "github"
previous_status = "pr_open"
new_status = "merged"
branch = "codex/slm-cpu-121-qwen3-preboundary-allocation"
pr = 764
head_sha = "51274c5040a87c1598d28ae91190b3f2e41942ff"
merge_sha = "9d7393b2e31b92ae904c7d6eca1226b1e4bdf6d4"
summary = "Merged PR #764 with prompt-plus-generation bounded KV cache capacity for the Qwen3 pre-boundary allocation failure."
notes = [
  "The SLM-CPU-121 Qwen3 Q8_0 run emits a strict receipt and reaches the layer-0 attention.q_proj_output_pre_optional_qnorm fingerprint after the bounded KV allocation.",
  "The slice preserves the SLM-CPU-120 packed-Q8 payload-order guard and keeps eager_f32_candle as the default runtime.",
  "The slice does not claim q_proj sidecar behavior proof, packed-Q8 default-runtime promotion, allocation-performance improvement, timing improvement, sustained throughput, Q4/Q5 runtime support, server, accelerator, Qwen3.5, or BitNet QK256 changes.",
]

[[next_items]]
id = "SLM-CPU-122"
status = "ready"
branch = "codex/slm-cpu-122-qproj-output-proof-refresh"
summary = "Resume the shared q_proj-output behavior proof from the now-reachable Qwen3 boundary and the existing Qwen2.5 proof gate."
