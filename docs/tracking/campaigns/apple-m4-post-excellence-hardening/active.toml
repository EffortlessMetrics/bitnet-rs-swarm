id = "apple-m4-post-excellence-hardening"
title = "Apple M4 post excellence hardening"
status = "active"

objective = "Keep the completed M4 Apple Silicon inference surface moving forward by hardening real user paths: dense local server behavior, timeout/cancellation, response conformance, BitNet repaired-quality analysis, benchmark variance, operator classes, and release-envelope refresh without reopening apple-m4-inference-excellence."

end_state = [
  "Dense local-server timeout and cancellation behavior is enforced, tested, and receipt-backed without enabling BitNet serve.",
  "Dense local-server non-streaming response, streaming event, and receipt export shapes are locked for supported dense SLM routes and fail closed for unsupported routes.",
  "The repaired BitNet 250-case regression is explained before any corpus expansion.",
  "A repaired subset rerun either proves improvement for the failing families or records non-improvement with unchanged model, tokenizer, prompt, and backend identity.",
  "Benchmark variance reports include repeat counts, percentile summaries, memory drift, timing variance, outlier policy, and advisory/failure thresholds.",
  "The operator envelope maps current evidence into interactive, advisory, batch, diagnostic, and unsupported classes without broad Apple Silicon, quality, or speed claims.",
]

hard_constraints = [
  "Use ci/hardware/apple-m4-mac-mini/2026-05-22/m4-inference-excellence-completion-audit.json as the completed baseline evidence.",
  "Do not reopen apple-m4-inference-excellence unless a real regression proves the baseline wrong.",
  "Do not rerun the whole M4 excellence proof set for this lane.",
  "Do not use dense SLM server evidence as BitNet chat or BitNet serve enablement evidence.",
  "Do not enable BitNet chat or BitNet serve in the dense server items.",
  "Do not expand the repaired BitNet 250-case corpus to 500 before the regression analysis item completes.",
  "Do not mix dense SLM and BitNet evidence, receipts, or claims.",
  "Do not claim full apple-m4-metal inference.",
  "Do not claim QK256-on-Apple support.",
  "Do not claim Neural Engine or MPSGraph model inference.",
  "Do not use MacBook evidence as M4 Mac mini runtime proof.",
  "Do not claim broad Apple Silicon support, broad model quality, or speedup.",
  "Do not commit model binaries.",
]

[[work_item]]
id = "M4-HARDEN-001"
status = "merged"
branch = "codex/apple-m4/M4-HARDEN-001-seed-post-excellence-hardening"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
pr = 1087
merge_sha = "0f979e07f6ef0a4025d388975e6526f4e7a5b29a"
acceptance = "Seed the apple-m4-post-excellence-hardening campaign and docs/tracking queue only, linking to the completed BitNet-rs apple-m4-inference-excellence completion audit, recording that the old lane is complete, and defining the dense SLM server hardening, BitNet repair, benchmark variance, and operator-envelope queues without runtime changes."
commands = [
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-post-excellence-hardening",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/tracking/campaigns/apple-m4-post-excellence-hardening/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "ci/hardware/**",
  "ci/quality/**",
  "crates/**",
  "models/**",
]
may_claim = [
  "The post-excellence hardening campaign and queue are seeded from the completed M4 inference excellence baseline.",
]
must_not_claim = [
  "Any new runtime, quality, speed, server, BitNet chat, BitNet serve, Metal, QK256, Neural Engine, MPSGraph, MacBook, or broad Apple Silicon proof was produced.",
]

[[work_item]]
id = "M4-HARDEN-002"
status = "merged"
branch = "codex/apple-m4/M4-HARDEN-002-dense-server-timeout-cancel"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-HARDEN-001"]
pr = 1091
head_sha = "16a79b384ffb35a797a4532aa2788342bafdc6e6"
merge_sha = "793a7a2fae6f686254332f08bc309c7201d74241"
acceptance = "Enforce dense local-server request timeout, prove streaming cancellation, emit partial-generation receipts, record stop reason, prove later requests still work, keep health/ready cheap, preserve fallback_used=false, and do not enable BitNet serve."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-server timeout -- --nocapture",
  "cargo test --locked -p bitnet-server cancel -- --nocapture",
  "cargo test --locked -p bitnet-cli --test cli_arg_tests mac_serve -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-post-excellence-hardening",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-server/**",
  "crates/bitnet-cli/src/mac.rs",
  "crates/bitnet-cli/tests/**",
  "ci/hardware/apple-m4-mac-mini/**/slm-serve/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-post-excellence-hardening/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-kernels/src/metal/**",
  "models/**",
]
may_claim = [
  "Dense M4 local-server timeout and cancellation behavior is enforced for the scoped supported dense SLM route.",
]
must_not_claim = [
  "BitNet serve is enabled.",
  "Dense server evidence proves BitNet behavior.",
  "The local server is production hosting.",
]

[[work_item]]
id = "M4-HARDEN-003"
status = "merged"
branch = "codex/apple-m4/M4-HARDEN-003-dense-server-response-conformance"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-HARDEN-002"]
pr = 1580
head_sha = "7b70b12bd7d8680be4e326fcbdcf173e56a583c6"
merge_sha = "8c0ffff61385e551fcd243be5cfa83be99a3737c"
acceptance = "Lock dense local-server non-streaming response shape, streaming event shape, and receipt export path; ensure bad model IDs fail cleanly, unsupported BitNet serve fails cleanly, and model/backend/fallback fields are visible in receipts."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-server response -- --nocapture",
  "cargo test --locked -p bitnet-server receipt -- --nocapture",
  "cargo test --locked -p bitnet-server model -- --nocapture",
  "cargo test --locked -p bitnet-server bitnet -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-post-excellence-hardening",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-server/**",
  "crates/bitnet-cli/src/mac.rs",
  "crates/bitnet-cli/tests/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-post-excellence-hardening/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-kernels/src/metal/**",
  "models/**",
]
may_claim = [
  "Dense M4 local-server response, streaming event, and receipt export shapes are locked for the scoped supported dense SLM route.",
]
must_not_claim = [
  "BitNet serve is enabled.",
  "The server is OpenAI-compatible beyond the tested response profile.",
  "The local server is production hosting.",
]

[[work_item]]
id = "M4-HARDEN-004"
status = "merged"
branch = "codex/apple-m4/M4-HARDEN-004-bitnet-repaired-250-analysis"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-HARDEN-003"]
pr = 1587
head_sha = "78e091d0810af079e88704b1e86cc55c986f1cb7"
merge_sha = "1d5ec72e84d23ebbbfed8b26992974186e6271fe"
acceptance = "Classify the repaired 250-case BitNet regressions by task family, isolate scorer/template/runtime/model causes, compare against baseline, recommend a repair path, do not expand to 500, and do not enable chat or serve."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli bitnet_eval -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet -- mac regression ci/hardware/apple-m4-mac-mini/2026-05-20T0133Z/bitnet-eval-250-repaired/answer-corpus.json --baseline ci/hardware/apple-m4-mac-mini/2026-05-18T1806Z/bitnet-eval-250-repaired/answer-corpus.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-post-excellence-hardening",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/bitnet-eval-250*/**",
  "ci/quality/apple-m4-bitnet-eval-seeded-corpus-250.yaml",
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-post-excellence-hardening/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-server/**",
  "models/**",
]
may_claim = [
  "The repaired 250-case BitNet regression is classified and has a recommended repair path.",
]
must_not_claim = [
  "The corpus expanded to 500.",
  "BitNet chat is enabled.",
  "BitNet serve is enabled.",
  "BitNet quality broadly improved.",
]

[[work_item]]
id = "M4-HARDEN-005"
status = "merged"
branch = "codex/apple-m4/M4-HARDEN-005-bitnet-repaired-subset-rerun"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-HARDEN-004"]
pr = 1603
head_sha = "a4bd936535a1ce6fad0976ae83bd16320e3dfb8d"
merge_sha = "35f957e8765656f7d21a98fb44b9ece9f23816b2"
acceptance = "Rerun only the failing or repaired BitNet task families, prove improvement or document non-improvement, preserve model/tokenizer/prompt/backend identity, and keep dense and BitNet evidence separate."
commands = [
  "cargo build --release --locked -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet",
  "target/release/bitnet answer-corpus --device apple-m4-cpu-neon --model models/BitNet-b1.58-2B-4T/ggml-model-i2_s.gguf --tokenizer models/microsoft-bitnet-b1.58-2B-4T/tokenizer.json --corpus ci/quality/apple-m4-bitnet-eval-repaired-subset.yaml --json-out ci/hardware/apple-m4-mac-mini/2026-06-05T112555Z/bitnet-eval-repaired-subset/answer-corpus.json --per-prompt-timeout-seconds 300",
  "target/release/bitnet mac receipts-check ci/hardware/apple-m4-mac-mini/2026-06-05T112555Z/bitnet-eval-repaired-subset/answer-corpus.json --json",
  "target/release/bitnet mac regression ci/hardware/apple-m4-mac-mini/2026-06-05T112555Z/bitnet-eval-repaired-subset/answer-corpus.json --baseline ci/hardware/apple-m4-mac-mini/2026-06-05T112555Z/bitnet-eval-repaired-subset/baseline-2026-05-18T1806Z-answer-corpus.json --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-post-excellence-hardening",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/bitnet-eval-repaired-subset/**",
  "ci/quality/apple-m4-bitnet-eval-repaired-subset.yaml",
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-post-excellence-hardening/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-kernels/src/metal/**",
  "crates/bitnet-server/**",
  "models/**",
]
may_claim = [
  "The scoped BitNet repaired subset rerun improved, or did not improve, under unchanged recorded identity.",
]
must_not_claim = [
  "Dense SLM evidence supports BitNet quality.",
  "BitNet chat is enabled.",
  "BitNet serve is enabled.",
  "Broad BitNet quality is proven.",
]

[[work_item]]
id = "M4-HARDEN-006"
status = "pr_open"
branch = "codex/apple-m4/M4-HARDEN-006-benchmark-operator-envelope"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M4-HARDEN-005"]
pr = 1739
head_sha = "59ae1bcaa"
acceptance = "Record benchmark repeat count, p50/p90/p99/min/max, memory drift, timing variance, outlier handling, and advisory-vs-failure thresholds; then classify operator envelopes as interactive, advisory, batch, diagnostic, or unsupported."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli benchmark -- --nocapture",
  "cargo test --locked -p bitnet-cli variance -- --nocapture",
  "cargo test --locked -p bitnet-cli operator -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli --bin bitnet -- mac regression-dashboard --json-out target/apple-m4-post-excellence-hardening/regression-dashboard.json --markdown-out target/apple-m4-post-excellence-hardening/regression-dashboard.md --json",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m4-post-excellence-hardening",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-m4-mac-mini/**/benchmark-variance/**",
  "ci/hardware/apple-m4-mac-mini/**/bitnet-benchmark-variance/**",
  "crates/bitnet-cli/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m4-post-excellence-hardening/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-kernels/src/metal/**",
  "models/**",
]
may_claim = [
  "M4 benchmark variance and operator envelope classes are refreshed for the exact evidence families recorded by the item.",
]
must_not_claim = [
  "The evidence proves broad Apple Silicon performance.",
  "The evidence proves broad model quality.",
  "The evidence proves speedup.",
  "The evidence enables unsupported BitNet server routes.",
]
