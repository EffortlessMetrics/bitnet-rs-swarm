id = "intel-npu"
title = "Intel NPU validation"
status = "active"

objective = "Validate Intel Lunar Lake NPU through OpenVINO static-shape detection, smoke, parity, and receipts without conflating NPU, GPU, or CPU work."

end_state = [
  "Intel NPU backend identity is distinct from Intel GPU, OpenCL, CPU, and generic accelerator labels.",
  "Probe records device-node, driver, OpenVINO runtime, available devices, NPU visibility, and environment facts.",
  "Tiny OpenVINO NPU graph smoke executes with fallback_used=false.",
  "Static-shape BitNet subgraph parity is receipt-backed before inference claims.",
]

hard_constraints = [
  "Device-node detection is not inference.",
  "OpenVINO NPU smoke is not full BitNet inference.",
  "CPU fallback cannot count as NPU execution.",
  "Do not assume WSL can see the NPU unless OpenVINO reports NPU inside WSL.",
]

[[work_item]]
id = "NPU-002"
status = "merged"
branch = "codex/intel-npu/NPU-002-lite-backend-identity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Preserve Intel NPU requested and selected backend identity without mapping it to Metal, CUDA, generic GPU, or CPU fallback."
commands = [
  "rustfmt --check crates/bitnet-common/src/backend_selection.rs crates/bitnet-device-config-core/src/lib.rs crates/bitnet-cli-config-core/src/lib.rs",
  "cargo test --locked -p bitnet-common --lib intel_npu --no-default-features -- --nocapture",
  "cargo test --locked -p bitnet-device-config-core --no-default-features",
  "cargo test --locked -p bitnet-cli-config-core --no-default-features",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-common/src/backend_selection.rs",
  "crates/bitnet-device-config-core/src/lib.rs",
  "crates/bitnet-cli-config-core/src/lib.rs",
  "docs/tracking/campaigns/intel-npu/**",
]
forbidden_paths = [
  "crates/bitnet-inference/**",
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/src/commands/inference.rs",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Intel NPU backend identity is preserved through config and help surfaces.",
]
must_not_claim = [
  "Intel NPU runtime detection works.",
  "OpenVINO NPU graph execution works.",
  "BitNet inference works on Intel NPU.",
]

[[work_item]]
id = "NPU-003"
status = "merged"
branch = "codex/intel-npu/NPU-003-openvino-runtime-probe"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["NPU-002"]
acceptance = "Add Intel NPU runtime detection fields that keep OS accelerator evidence separate from OpenVINO NPU visibility and record OpenVINO NPU full name, driver/compiler/memory properties, runtime device, proof_stage=runtime_detected, and fallback_used=false without graph execution claims."
commands = [
  "rustfmt --check crates/bitnet-device-probe/src/runtimes/openvino.rs crates/bitnet-device-probe/src/intel/lunar_lake/npu.rs crates/bitnet-device-probe/src/intel/lunar_lake/mod.rs crates/bitnet-device-probe/src/runtimes/mod.rs crates/bitnet-device-probe/tests/lunar_lake_platform_probe.rs",
  "cargo test --locked -p bitnet-device-probe --no-default-features",
  "cargo test --locked -p bitnet-device-probe --no-default-features --features cpu",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-device-probe/src/intel/lunar_lake/npu.rs",
  "crates/bitnet-device-probe/src/intel/lunar_lake/mod.rs",
  "crates/bitnet-device-probe/src/runtimes/openvino.rs",
  "crates/bitnet-device-probe/src/runtimes/mod.rs",
  "crates/bitnet-device-probe/tests/**",
  "docs/hardware/intel-258v-validation.md",
  "docs/specs/intel-lunar-lake-npu-roadmap.md",
  "docs/tracking/campaigns/intel-npu/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Intel NPU runtime visibility fields can be recorded after merge.",
]
must_not_claim = [
  "OpenVINO NPU graph execution works.",
  "BitNet inference works on Intel NPU.",
  "Intel NPU accelerates BitNet.",
]

[[work_item]]
id = "NPU-004"
status = "merged"
branch = "codex/intel-npu/NPU-004-smoke-probe-command"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["NPU-003"]
acceptance = "Add an Intel NPU smoke probe command that writes a machine-readable OpenVINO NPU runtime visibility receipt with requested/selected backend identity, runtime API/device, strict mode, proof_stage=runtime_detected, fallback_used=false, and no graph, kernel, or BitNet inference claims."
commands = [
  "rustfmt --check --config skip_children=true,newline_style=Unix crates/bitnet-cli/src/main.rs crates/bitnet-cli/src/intel_npu.rs --edition 2024",
  "cargo test --locked -p bitnet-cli --bin bitnet intel_npu::tests::intel_npu_probe_receipt_is_visibility_only -- --exact --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet intel_npu::tests::strict_intel_npu_probe_records_error_without_fallback -- --exact --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu -- intel-npu-probe --json-out target/intel-npu-runtime-probe.json",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/src/intel_npu.rs",
  "docs/specs/intel-lunar-lake-npu-roadmap.md",
  "docs/tracking/campaigns/intel-npu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The CLI can emit an Intel NPU OpenVINO runtime visibility receipt after merge.",
]
must_not_claim = [
  "OpenVINO NPU graph execution works.",
  "Intel NPU accelerates BitNet.",
  "BitNet inference works on Intel NPU.",
]

[[work_item]]
id = "NPU-005"
status = "merged"
branch = "codex/intel-npu/NPU-005-openvino-graph-smoke"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["NPU-004"]
acceptance = "Add a tiny static OpenVINO NPU graph smoke command that compiles and runs a fixed F16 matmul-add graph on runtime device NPU when available, writes a machine-readable receipt with shape_mode=static, graph identity, timing, requested/selected backend identity, fallback_used=false, cpu_fallback_allowed=false, and never claims BitNet inference or NPU acceleration."
commands = [
  "rustfmt --check --config skip_children=true,newline_style=Unix crates/bitnet-cli/src/main.rs crates/bitnet-cli/src/intel_npu.rs crates/bitnet-device-probe/src/runtimes/openvino.rs crates/bitnet-device-probe/src/runtimes/mod.rs --edition 2024",
  "cargo test --locked -p bitnet-device-probe --no-default-features openvino_npu_tiny_graph -- --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet intel_npu::tests::intel_npu_smoke_receipt_records_static_graph_execution_only -- --exact --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet intel_npu::tests::strict_intel_npu_smoke_records_error_without_fallback -- --exact --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu -- intel-npu-smoke --json-out target/intel-npu-tiny-graph-smoke.json",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/src/intel_npu.rs",
  "crates/bitnet-device-probe/src/runtimes/openvino.rs",
  "crates/bitnet-device-probe/src/runtimes/mod.rs",
  "docs/specs/intel-lunar-lake-npu-roadmap.md",
  "docs/tracking/campaigns/intel-npu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The CLI can attempt an Intel NPU tiny static OpenVINO graph smoke and emits kernel_smoke_tested only when the graph executes on NPU and matches CPU expected output.",
]
must_not_claim = [
  "BitNet inference works on Intel NPU.",
  "Intel NPU accelerates BitNet.",
  "Packed BitNet QK256 decode works on Intel NPU.",
  "CPU fallback satisfies NPU proof.",
]

[[work_item]]
id = "NPU-006"
status = "merged"
pr = 3860
merge_sha = "e0152fee429ee28eba97eeda2ec23ba642c77e04"
branch = "codex/intel-npu/NPU-006-receipt-fields"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["NPU-005"]
acceptance = "Harden Intel NPU probe and tiny graph smoke receipts with structured backend_runtime, shape_contract, fallback_policy, graph identity, timing, and kernels_or_graphs fields while preserving fallback_used=false and no BitNet inference claims."
commands = [
  "rustfmt --check --config skip_children=true,newline_style=Unix crates/bitnet-cli/src/main.rs crates/bitnet-cli/src/intel_npu.rs --edition 2024",
  "cargo test --locked -p bitnet-cli --bin bitnet intel_npu::tests::intel_npu_probe_receipt_is_visibility_only -- --exact --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet intel_npu::tests::intel_npu_smoke_receipt_records_static_graph_execution_only -- --exact --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet intel_npu::tests::strict_intel_npu_smoke_records_error_without_fallback -- --exact --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/src/intel_npu.rs",
  "docs/specs/intel-lunar-lake-npu-roadmap.md",
  "docs/tracking/campaigns/intel-npu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "Intel NPU probe and tiny graph smoke receipts carry structured backend runtime, static-shape, graph, timing, and fallback policy fields after merge.",
]
must_not_claim = [
  "BitNet inference works on Intel NPU.",
  "Intel NPU accelerates BitNet.",
  "Packed BitNet QK256 decode works on Intel NPU.",
  "CPU fallback satisfies NPU proof.",
]

[[work_item]]
id = "NPU-007"
status = "merged"
pr = 3873
merge_sha = "edc4a29dd41bd83584a6f97ff017532f402be227"
branch = "codex/intel-npu/NPU-007-bitnet-subgraph-parity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["NPU-006"]
acceptance = "Add selected static BitNet RMSNorm subgraph parity through OpenVINO NPU with CPU reference comparison, selected backend/runtime identity, timing, fallback_used=false, and no full BitNet inference or QK256 decode claims."
commands = [
  "rustfmt --check --config skip_children=true,newline_style=Unix crates/bitnet-cli/src/main.rs crates/bitnet-cli/src/intel_npu.rs crates/bitnet-device-probe/src/runtimes/openvino.rs --edition 2024",
  "cargo test --locked -p bitnet-device-probe --no-default-features runtimes::openvino::tests::parses_openvino_npu_bitnet_subgraph_parity_pass -- --exact --nocapture",
  "cargo test --locked -p bitnet-device-probe --no-default-features runtimes::openvino::tests::parses_openvino_npu_bitnet_subgraph_missing_npu_as_runtime_only -- --exact --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet intel_npu::tests::intel_npu_bitnet_subgraph_receipt_records_parity_only -- --exact --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet intel_npu::tests::strict_intel_npu_bitnet_subgraph_records_error_without_fallback -- --exact --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu -- intel-npu-bitnet-subgraph --json-out target/intel-npu-bitnet-subgraph-parity.json",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/src/intel_npu.rs",
  "crates/bitnet-device-probe/src/runtimes/openvino.rs",
  "crates/bitnet-device-probe/src/runtimes/mod.rs",
  "docs/specs/intel-lunar-lake-npu-roadmap.md",
  "docs/tracking/campaigns/intel-npu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The CLI can attempt selected static BitNet RMSNorm subgraph parity on OpenVINO NPU and emits parity_tested only when NPU execution matches the CPU reference.",
]
must_not_claim = [
  "Full BitNet inference works on Intel NPU.",
  "Intel NPU accelerates BitNet.",
  "Packed BitNet QK256 decode works on Intel NPU.",
  "CPU fallback satisfies NPU proof.",
]

[[work_item]]
id = "NPU-008"
status = "merged"
pr = 3963
branch = "codex/intel-npu/NPU-008-linear-subgraph-parity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["NPU-007"]
acceptance = "Add selected static BitNet linear-projection subgraph parity through OpenVINO NPU with CPU reference comparison, selected backend/runtime identity, timing, fallback_used=false, and no full BitNet inference, acceleration, or QK256 decode claims."
commands = [
  "rustfmt --check --config skip_children=true,newline_style=Unix crates/bitnet-cli/src/main.rs crates/bitnet-cli/src/intel_npu.rs crates/bitnet-device-probe/src/runtimes/openvino.rs crates/bitnet-device-probe/src/runtimes/mod.rs --edition 2024",
  "cargo test --locked -p bitnet-device-probe --no-default-features runtimes::openvino::tests::parses_openvino_npu_bitnet_linear_projection_parity_pass -- --exact --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet intel_npu::tests::intel_npu_bitnet_linear_subgraph_receipt_records_parity_only -- --exact --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu -- intel-npu-bitnet-linear-subgraph --json-out target/intel-npu-bitnet-linear-subgraph-parity.json",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/src/intel_npu.rs",
  "crates/bitnet-device-probe/src/runtimes/openvino.rs",
  "crates/bitnet-device-probe/src/runtimes/mod.rs",
  "docs/specs/intel-lunar-lake-npu-roadmap.md",
  "docs/tracking/campaigns/intel-npu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The CLI can attempt selected static BitNet linear-projection subgraph parity on OpenVINO NPU and emits parity_tested only when NPU execution matches the CPU reference.",
]
must_not_claim = [
  "Full BitNet inference works on Intel NPU.",
  "Intel NPU accelerates BitNet.",
  "Packed BitNet QK256 decode works on Intel NPU.",
  "CPU fallback satisfies NPU proof.",
]

[[work_item]]
id = "NPU-009"
status = "merged"
pr = 4068
merge_sha = "d90daacbc927f00a1cc355208ab63139a1b5aa08"
branch = "codex/intel-npu/NPU-009-openvino-llamacpp-gguf-reference"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["NPU-008"]
acceptance = "Evaluate OpenVINO llama.cpp GGUF as an external Intel NPU reference lane by recording a Lunar Lake GGUF reference receipt or explicit blocked receipt with model, runtime, selected device, shape/context, fallback status, and claim boundary. This does not prove native bitnet-rs NPU inference, packed QK256 decode, or acceleration."
commands = [
  "Parse ci/hardware/intel-258v/2026-05-08/npu-openvino-llamacpp-gguf-reference.json",
  "Parse docs/tracking/campaigns/intel-npu/active.toml",
  "Parse docs/tracking/campaigns/intel-npu/generated/status.md",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/intel-258v/**",
  "docs/hardware/intel-258v-validation.md",
  "docs/specs/intel-lunar-lake-npu-roadmap.md",
  "docs/tracking/campaigns/intel-npu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "tests/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
]
may_claim = [
  "OpenVINO llama.cpp GGUF is tracked as an external Intel NPU reference lane with either evidence or explicit blocker status.",
]
must_not_claim = [
  "Full BitNet inference works on Intel NPU.",
  "Native bitnet-rs NPU inference works.",
  "Intel NPU accelerates BitNet.",
  "Packed BitNet QK256 GGUF decode works on Intel NPU.",
  "CPU fallback satisfies NPU proof.",
]

[[work_item]]
id = "NPU-010"
status = "merged"
pr = 4080
merge_sha = "7303c0d174bb32df525ea5af30e2441769937be0"
branch = "codex/intel-npu/NPU-010-live-openvino-receipts"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["NPU-009"]
acceptance = "Record live 258V OpenVINO 2026.1 Intel NPU runtime visibility, tiny static graph smoke, and selected BitNet RMSNorm and linear-projection static subgraph parity receipts with selected_backend=intel-npu-openvino, runtime_api=openvino, runtime_device=NPU, fallback_used=false, and no full BitNet inference, acceleration, QK256 decode, or CPU fallback proof claims."
commands = [
  "Parse ci/hardware/intel-258v/2026-05-08/npu-openvino-runtime-probe.json",
  "Parse ci/hardware/intel-258v/2026-05-08/npu-openvino-tiny-graph-smoke.json",
  "Parse ci/hardware/intel-258v/2026-05-08/npu-bitnet-rmsnorm-subgraph-parity.json",
  "Parse ci/hardware/intel-258v/2026-05-08/npu-bitnet-linear-projection-subgraph-parity.json",
  "Parse docs/tracking/campaigns/intel-npu/active.toml",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/intel-258v/**",
  "docs/hardware/intel-258v-validation.md",
  "docs/specs/intel-lunar-lake-npu-roadmap.md",
  "docs/tracking/campaigns/intel-npu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "tests/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
]
may_claim = [
  "OpenVINO 2026.1 sees Intel AI Boost NPU on the 258V and selects intel-npu-openvino.",
  "The recorded tiny static OpenVINO graph executed on NPU with fallback_used=false.",
  "The recorded RMSNorm and linear-projection static BitNet-shaped subgraphs executed on NPU and matched CPU NumPy references within tolerance.",
]
must_not_claim = [
  "Full BitNet inference works on Intel NPU.",
  "Native bitnet-rs NPU inference works.",
  "Intel NPU accelerates BitNet.",
  "Packed BitNet QK256 decode works on Intel NPU.",
  "CPU fallback satisfies NPU proof.",
]

[[work_item]]
id = "NPU-011"
status = "merged"
pr = 4097
merge_sha = "a53b3e8fee2e68beec28904b15f23db65438b8f7"
branch = "codex/intel-npu/NPU-011-ffn-subgraph-parity"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["NPU-010"]
acceptance = "Add a selected static BitNet-shaped OpenVINO NPU FFN/ReLU2 subgraph parity experiment with CPU reference comparison, static shape metadata, tolerance, timing, selected backend/runtime identity, fallback_used=false, and no full inference, acceleration, QK256, or CPU fallback proof claims."
commands = [
  "rustfmt --check --config skip_children=true,newline_style=Unix crates/bitnet-cli/src/main.rs crates/bitnet-cli/src/intel_npu.rs crates/bitnet-device-probe/src/runtimes/openvino.rs crates/bitnet-device-probe/src/runtimes/mod.rs --edition 2024",
  "cargo test --locked -p bitnet-device-probe --no-default-features runtimes::openvino::tests -- --nocapture",
  "cargo test --locked -p bitnet-cli --bin bitnet intel_npu::tests -- --nocapture",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu -- intel-npu-bitnet-ffn-subgraph --cpu-reference ci/hardware/intel-258v/2026-05-08/cpu-reference-bundle-post-mechanics.json --json-out ci/hardware/intel-258v/2026-05-08/npu-bitnet-ffn-subgraph-parity.json",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/src/main.rs",
  "crates/bitnet-cli/src/intel_npu.rs",
  "crates/bitnet-device-probe/src/runtimes/openvino.rs",
  "crates/bitnet-device-probe/src/runtimes/mod.rs",
  "ci/hardware/intel-258v/**",
  "docs/hardware/intel-258v-validation.md",
  "docs/specs/intel-lunar-lake-npu-roadmap.md",
  "docs/tracking/campaigns/intel-npu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-models/**",
  "crates/bitnet-tokenizers/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The CLI can attempt the selected static BitNet-shaped FFN/ReLU2 or post-linear subgraph parity on OpenVINO NPU and emits parity_tested only when NPU execution matches the CPU reference.",
]
must_not_claim = [
  "Full BitNet inference works on Intel NPU.",
  "Native bitnet-rs NPU inference works.",
  "Intel NPU accelerates BitNet.",
  "Packed BitNet QK256 decode works on Intel NPU.",
  "CPU fallback satisfies NPU proof.",
]

[[work_item]]
id = "NPU-012"
status = "merged"
pr = 5634
merge_sha = "b5f70d28a18bf7d5e62adad5d04fb2f4609e3aa4"
branch = "codex/add-npu-productization-proposal"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["NPU-011"]
acceptance = "Add the NPU source-of-truth map and implementation plan that make current NPU-002 through NPU-011 evidence, claim boundaries, Intel Lunar Lake/OpenVINO target scope, and future Apple/Qualcomm/AMD NPU family separation visible without runtime claims or route promotion."
commands = [
  "cargo run --locked -p xtask --no-default-features -- campaign check intel-npu",
  "cargo run --locked -p xtask --no-default-features -- campaign check intel-258v-platform",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/npu/**",
  "plans/npu/**",
  "docs/specs/INDEX.md",
  "docs/tracking/campaigns/intel-npu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "tests/**",
  "ci/hardware/**",
  "docs/hardware/**",
  "crates/bitnet-quantization/**",
  "crates/bitnet-qk256-layout-core/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-inference/**",
  "crates/bitnet-server/**",
]
may_claim = [
  "The repository has a docs-only NPU source-of-truth map and rollout plan for Intel Lunar Lake/OpenVINO NPU productization.",
  "NPU-002 through NPU-011 prove runtime visibility, tiny static graph execution, and selected static BitNet-shaped subgraph parity only.",
]
must_not_claim = [
  "Full BitNet inference works on Intel NPU.",
  "Native bitnet-rs NPU inference works.",
  "Intel NPU accelerates BitNet.",
  "Packed BitNet QK256 decode works on Intel NPU.",
  "CPU fallback satisfies NPU proof.",
  "OpenVINO GPU or Arc 140V OpenCL proof is NPU proof.",
  "Intel NPU proof applies to Apple Neural Engine, Qualcomm Hexagon, or AMD Ryzen AI.",
  "NPU warm/resident dense-SLM routes are promoted.",
]

[[work_item]]
id = "NPU-013"
status = "merged"
branch = "codex/npu-013-productization-specs"
pr = 5903
head_sha = "6f1a73fef54eab9e199cede86e6a0816a6ebb35a"
merge_sha = "1576cf1d8f4f68d58561d2cdff39f9045d2a0a46"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["NPU-012"]
acceptance = "Add docs-only NPU productization proposal and spec contracts for route identity, proof ladder, cold/cache/warm timing, dense SLM, BitNet subgraph, quality, performance, residency, and status surfaces without runtime claims or route promotion."
commands = [
  "cargo run --locked -p xtask --no-default-features -- campaign check intel-npu",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/proposals/BITNET-PROP-0007-npu-productization.md",
  "docs/specs/BITNET-SPEC-NPU-*.md",
  "docs/specs/INDEX.md",
  "docs/tracking/campaigns/intel-npu/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "tests/**",
  "ci/hardware/**",
  "models/**",
  "docs/npu/**",
  "plans/npu/**",
  "docs/hardware/**",
]
may_claim = [
  "The NPU productization proposal and spec contracts are defined for follow-on work.",
]
must_not_claim = [
  "Full BitNet inference works on Intel NPU.",
  "Native bitnet-rs NPU inference works.",
  "Intel NPU accelerates BitNet.",
  "Packed BitNet QK256 decode works on Intel NPU.",
  "CPU fallback satisfies NPU proof.",
  "OpenVINO GPU or Arc 140V OpenCL proof is NPU proof.",
  "Intel NPU proof applies to Apple Neural Engine, Qualcomm Hexagon, or AMD Ryzen AI.",
  "NPU warm/resident dense-SLM routes are promoted.",
]
