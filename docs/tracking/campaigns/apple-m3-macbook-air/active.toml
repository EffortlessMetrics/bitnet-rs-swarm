id = "apple-m3-macbook-air"
title = "Apple M3 MacBook Air"
status = "active"

objective = "Turn the available M3 MacBook Air into a disciplined Apple Silicon lane for machine-profile evidence, dense SLM cross-checks, large BitNet artifact qualification, and M4 strict-proof handoff planning without converting MacBook receipts into M4 Mac mini performance or BitNet local-answer claims."

end_state = [
  "A real M3 MacBook Air machine-profile receipt is committed with inference_run=false.",
  "M3 Air dense Qwen receipts use an explicit MacBook backend label and record power, thermal, storage, model, tokenizer, and fallback context.",
  "Larger BitNet candidate downloads are accepted, rejected, or blocked with source, revision, size, SHA256, tokenizer authority, prompt output, and cleanup status.",
  "Accepted artifacts feed separate M4 Mac mini strict Apple CPU/NEON proof items; M3 evidence does not become M4 evidence by wording.",
  "The MacBook lane remains storage-aware and never commits model binaries.",
]

hard_constraints = [
  "This is the Apple M3 MacBook Air lane, not the M4 Mac mini product, performance, or strict-proof lane.",
  "Do not claim BitNet local-answer quality from dense Qwen SLM receipts.",
  "Do not claim M4 Mac mini performance, broad Apple Silicon performance, QK256 support, full Apple Metal inference, Neural Engine execution, or MPSGraph model inference from this lane.",
  "Do not weaken existing M4 receipt checks to make M3 receipts fit; add the smallest MacBook-specific label or validation path instead.",
  "Do not add live model downloads, large artifact sweeps, or hardware timing runs to generic required CI.",
  "Do not use near-completion timeouts as M3 lane cost control; route the job away, choose a smaller profile, or cap healthy selected runs with cushion.",
  "Never commit model binaries.",
]

[[work_item]]
id = "M3MBA-001"
status = "ready"
branch = "codex/apple-m3-macbook-air/roadmap-campaign"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Add the M3 MacBook Air campaign control plane, roadmap linkage, generated status, and first lifecycle event without running models or changing runtime behavior."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/apple-silicon/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/hardware/**",
  "models/**",
]
may_claim = [
  "The M3 MacBook Air lane has a campaign-local roadmap and tracker.",
]
must_not_claim = [
  "Any model was run.",
  "Any BitNet artifact was accepted.",
  "M3 Air evidence proves M4 Mac mini performance.",
]

[[work_item]]
id = "M3MBA-002"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-002-machine-profile"
pr = 4592
merge_sha = "52c37076de1c7e716e8335be60069df255ecb230"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-001"]
acceptance = "Commit a real M3 MacBook Air machine-profile receipt with model identifier, chip, core split, memory, macOS version, free disk, cache root, power source, thermal state when available, CPU/NEON visibility, Metal visibility, MPSGraph visibility when available, and inference_run=false."
commands = [
  "sw_vers",
  "sysctl -n machdep.cpu.brand_string hw.memsize hw.physicalcpu hw.perflevel0.physicalcpu hw.perflevel1.physicalcpu",
  "system_profiler SPHardwareDataType SPDisplaysDataType SPPowerDataType",
  "df -k . \"$HOME/Library/Caches\"",
  "jq -e '.artifact_kind == \"apple_silicon_macbook_machine_profile\" and .claim_boundary.inference_run == false and .model_cache.model_binaries_committed == false and .storage.large_artifact_sweep_allowed == true' ci/hardware/apple-silicon-macbook/2026-05-12/m3-air/machine-profile.json",
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-silicon-macbook/**",
  "docs/apple-silicon/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "models/**",
]
may_claim = [
  "The M3 Air machine facts and storage budget are recorded.",
]
must_not_claim = [
  "Model inference was run.",
  "Dense SLM or BitNet answer quality is proven.",
]

[[work_item]]
id = "M3MBA-003"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-003-receipt-label"
pr = 4596
head_sha = "23d5f06f87b706beb5e625552b532ca4f82b3ad5"
merge_sha = "8f9fbf8497db650bddb0f9eed9e634684c7a582c"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-002"]
acceptance = "Add or confirm the smallest receipt validation path for apple-m3-air-cpu-neon, preserving existing apple-m4-cpu-neon checks and making MacBook timing impossible to label as M4 evidence."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli-config-core --no-default-features",
  "cargo test --locked -p bitnet-common --no-default-features backend_selection",
  "cargo test --locked -p bitnet-device-config-core --no-default-features",
  "cargo test --locked -p bitnet-receipts-core --no-default-features strict_cpu_proof",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli-config-core/**",
  "crates/bitnet-common/**",
  "crates/bitnet-device-config-core/**",
  "crates/bitnet-receipts-core/**",
  "ci/hardware/apple-silicon-macbook/**",
  "docs/apple-silicon/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "models/**",
]
may_claim = [
  "M3 Air receipts can use an explicit MacBook Apple CPU/NEON label.",
]
must_not_claim = [
  "M3 Air receipts are M4 Mac mini receipts.",
  "BitNet local-answer quality is proven.",
]

[[work_item]]
id = "M3MBA-012"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-012-dense-harness-contract"
pr = 4658
head_sha = "b35ec82b8765a4cb5529ed53d7fb653e5e6490bf"
merge_sha = "cad5693291ef80152d75dffaa25824ba08fe09c9"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-003"]
acceptance = "Define the M3 Air dense SLM harness contract before live smoke runs, including how mac validate and mac receipts-check accept apple-m3-air-cpu-neon without reusing M4 labels, the synthetic no-model receipt fixture needed for CI, and the local-only boundary for smoke/operator timing receipts."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/apple-silicon/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/hardware/**",
  "models/**",
]
may_claim = [
  "The M3 Air dense SLM harness contract is specified before live smoke evidence starts.",
]
must_not_claim = [
  "M3 Air dense smoke ran.",
  "Runtime support for M3 dense receipts has been implemented.",
  "M3 Air evidence proves BitNet behavior or M4 performance.",
]

[[work_item]]
id = "M3MBA-013"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-013-ci-completion-workflow"
pr = 4665
head_sha = "6da90069dee38cdbc7f87ecc111eab57b8c1e086"
merge_sha = "7389b271a9cf508bc13b9d81aed9e15be925eb17"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-012"]
acceptance = "Encode the M3 Air CI completion policy in the campaign and shared CI docs, and add a staged manual M3 Air dense SLM workflow so selected long hardware/model jobs are routed, preflighted, artifact-retaining, and capped from healthy completed runtimes with cushion instead of being ended just before receipt emission."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- lint-workflows",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  ".github/workflows/apple-m3-air-dense-slm.yml",
  "docs/apple-silicon/**",
  "docs/ci/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
  "policy/ci-budget.toml",
  "policy/ci-routed-rollout.toml",
]
forbidden_paths = [
  "crates/**",
  "ci/hardware/**",
  "models/**",
]
may_claim = [
  "The M3 Air lane has an explicit CI completion policy and staged manual workflow for selected long jobs.",
]
must_not_claim = [
  "Live M3 model CI has been enabled.",
  "Any M3 Air model inference was run.",
  "Long job caps prove performance.",
]

[[work_item]]
id = "M3MBA-014"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-014-tracker-readiness"
pr = 4766
head_sha = "f740999c680e62316ed74f0057f2285ec5d5d973"
merge_sha = "3e8fea9ebefcd06ddfbcdd192271410e3b638377"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-013"]
acceptance = "Implement the no-live-model Mac validate and receipts-check bridge for apple-m3-air-cpu-neon so M3 Air dense SLM smoke can emit M3 machine identity, selected backend identity, allocation-audit eligibility, and M3-specific receipt artifact kinds without reusing M4 Mac mini labels."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-device-config-core --no-default-features",
  "cargo test --locked -p bitnet-receipts-core --no-default-features strict_cpu_proof",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple -- --nocapture",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "docs/apple-silicon/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  ".github/workflows/**",
  "ci/hardware/**",
  "models/**",
]
may_claim = [
  "The Mac validate and receipt-check path can preserve apple-m3-air-cpu-neon identity before live M3 dense smoke.",
]
must_not_claim = [
  "M3 Air dense smoke ran.",
  "Any live model inference was performed.",
  "M3 Air receipts are M4 Mac mini evidence.",
  "M3 Air evidence proves BitNet behavior, Metal inference, MPSGraph model inference, Neural Engine execution, or QK256 support.",
]

[[work_item]]
id = "M3MBA-015"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-015-device-labels"
pr = 4929
head_sha = "4bba8ea9f7d7d10474b0f6ffcf4e8b5982e4b384"
merge_sha = "7df11e9bde314a2a0aa61de86a8e0a1664a4a6da"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-014"]
acceptance = "Extend the shared device/backend model with explicit apple-m3-air-metal and apple-m3-air-mpsgraph identities so M3 Air Metal and graph/reference requests cannot be mislabeled as generic Metal, MPSGraph, or M4 proof evidence; keep execution strict/unavailable until a later receipt-backed runtime item lands."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-common --no-default-features backend_selection",
  "cargo test --locked -p bitnet-device-config-core --no-default-features",
  "cargo test --locked -p bitnet-cli-config-core --no-default-features",
  "cargo test --locked -p bitnet-device-probe --no-default-features apple_metal",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m3",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-common/**",
  "crates/bitnet-device-config-core/**",
  "crates/bitnet-cli-config-core/**",
  "crates/bitnet-device-probe/**",
  "crates/bitnet-cli/**",
  "docs/apple-silicon/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
  "ci/hardware/**",
  "models/**",
]
may_claim = [
  "The repo device model preserves M3 Air Metal, M3 Air MPSGraph, and M3 Air CPU/NEON identities as distinct labels.",
  "M3 Air Metal and MPSGraph labels are strict request identities and do not silently become CPU, generic Metal, or M4 evidence.",
]
must_not_claim = [
  "M3 Air Metal inference works.",
  "M3 Air MPSGraph model inference works.",
  "M3 Air labels are M4 Mac mini labels.",
  "Any model inference was run.",
]

[[work_item]]
id = "M3MBA-016"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-016-backend-visibility-preflight"
pr = 5043
head_sha = "c8001743decb744739d830efe754e00e0abdc6f6"
merge_sha = "d3566a240b8ab761049fa8aab332985ec7dde26e"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-015"]
acceptance = "Add M3 Air Metal and MPSGraph backend visibility receipts plus a bounded Apple preflight contract that records host/runtime visibility, requested backend identity, machine identity, fallback status, and claim boundaries without loading models, downloading artifacts, or claiming full Metal/MPSGraph inference."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-device-probe --no-default-features apple",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m3",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-device-probe/**",
  "crates/bitnet-cli/**",
  "docs/apple-silicon/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "ci/hardware/**",
  "models/**",
]
may_claim = [
  "M3 Air Metal and MPSGraph backend visibility can be recorded with explicit M3 Air request identity.",
  "The preflight is bounded to host/runtime visibility and does not execute model inference.",
]
must_not_claim = [
  "M3 Air Metal inference works.",
  "M3 Air MPSGraph model inference works.",
  "Neural Engine execution works.",
  "Any model artifact was downloaded or loaded.",
  "Any speedup or performance result was measured.",
]

[[work_item]]
id = "M3MBA-017"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-017-device-help-alignment"
pr = 5148
head_sha = "8e058b725bacbf3d09e23a20175f86a157d68dd8"
merge_sha = "cd03ab9aba7bc57c4f5d155fd5c696a8993bcf97"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-016"]
acceptance = "Align M3 Air device identity help and rejection surfaces so apple-m3-air-metal, apple-m3-air-mpsgraph, and apple-m3-air-cpu-neon are consistently discoverable as distinct proof-lane labels while mac validate still rejects unsupported Metal/MPSGraph model inference and hidden CPU fallback."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli-config-core --no-default-features validates_apple_m3_air_label_without_aliasing",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli mac_validate_rejects_m3_metal_identity_without_cpu_fallback_claim",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli cli_snapshot_tests__mac_validate_help",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli-config-core/**",
  "crates/bitnet-cli/**",
  "docs/apple-silicon/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
  "models/**",
]
may_claim = [
  "M3 Air device identity help and rejection text consistently expose all three M3 Air labels.",
]
must_not_claim = [
  "M3 Air Metal model inference works.",
  "M3 Air MPSGraph model inference works.",
  "M3 Air CPU fallback can count as Metal or MPSGraph execution.",
  "Any live M3 model run occurred.",
]

[[work_item]]
id = "M3MBA-018"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-018-roadmap-state-refresh"
pr = 5225
head_sha = "3cb6ac1aee7cf41404de3054726c03adcfd08b63"
merge_sha = "fd6bd1fd7c606a21d8a25d5432597c2eb8015b07"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-017"]
acceptance = "Refresh the M3 Air roadmap and campaign prose after M3MBA-017 so the current stack no longer points at merged work, the remaining M3MBA-007/M3MBA-008 sequence is explicit, and the selected-long-job CI completion rule remains encoded through comparison-profile workflow inputs and phase evidence uploads before expensive M3 runs."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  ".github/workflows/apple-m3-air-dense-slm.yml",
  "docs/apple-silicon/**",
  "docs/ci/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/hardware/**",
  "models/**",
]
may_claim = [
  "The M3 Air roadmap and campaign state reflect the post-M3MBA-017 stack.",
  "The selected M3 long-job CI completion rule remains a lane design constraint.",
]
must_not_claim = [
  "Any new M3 Air model inference was run.",
  "Any new BitNet artifact was downloaded or accepted.",
  "M3 Air evidence proves M4 Mac mini performance.",
]

[[work_item]]
id = "M3MBA-004A"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-004A-dense-qwen-smoke"
pr = 4775
head_sha = "0538db9ab59a076d9a36c4cb47593d2e95e9879c"
merge_sha = "3881e754e8b1ffe4bdd0d5808fcc836a058ae006"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
acceptance = "Run the dense Qwen2.5 0.5B Mac smoke path on M3 Air with the known model hash, tokenizer metadata, deterministic settings, exact requested/selected backend label identity, fallback status, power and thermal context, storage before/after, receipts-check output, and dense-only claim boundary."
commands = [
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac validate --profile-set smoke --device apple-m3-air-cpu-neon --corpus ci/quality/apple-m4-slm-quality-corpus.yaml --corpus-repeat-runs 2 --max-new-tokens 32 --cache-dir ~/Library/Caches/bitnet-rs/models --json-out ci/hardware/apple-silicon-macbook/2026-05-12/m3-air/qwen-mirror-smoke.json --quiet",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac receipts-check ci/hardware/apple-silicon-macbook/2026-05-12/m3-air/qwen-mirror-smoke.json --json",
  "jq -e '.backend.requested_backend == \"apple-m3-air-cpu-neon\" and .backend.selected_backend == \"apple-m3-air-cpu-neon\" and .backend.fallback_used == false and .mac_claim_boundary.requested_backend == \"apple-m3-air-cpu-neon\"' ci/hardware/apple-silicon-macbook/2026-05-12/m3-air/qwen-mirror-smoke.json",
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/hardware/apple-silicon-macbook/**",
  "docs/apple-silicon/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "models/**",
]
may_claim = [
  "The dense Qwen SLM smoke path passed or failed on M3 Air for the recorded context.",
]
must_not_claim = [
  "Dense Qwen evidence proves BitNet behavior.",
  "M3 Air timing replaces the M4 Mac mini envelope.",
]

[[work_item]]
id = "M3MBA-004B"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-004B-dense-qwen-operator"
pr = 4785
head_sha = "3df6b435908e3cb8fb98a86367732a9b3ca891f1"
merge_sha = "2ddc425a5ff2f4914b6c077f75cc6c8f80528188"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-004A"]
acceptance = "Run the dense Qwen2.5 0.5B bounded operator profile only after smoke passes, recording allocation-audit context, power and thermal context, repeat count, token budget, storage before/after, receipts-check output, exact requested/selected backend label identity, fallback status, and whether the result is comparison-grade or diagnostic-only."
commands = [
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac validate --profile-set operator --device apple-m3-air-cpu-neon --model-id qwen2.5-0.5b-instruct-q8_0 --cache-dir ~/Library/Caches/bitnet-rs/models --allocation-audit --json-out ci/hardware/apple-silicon-macbook/2026-05-12/m3-air/qwen-mirror-operator.json --quiet",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac receipts-check ci/hardware/apple-silicon-macbook/2026-05-12/m3-air/qwen-mirror-operator.json --json",
  "jq -e '.artifact_kind == \"apple_m3_air_slm_operator_profiles\" and .requested_backend == \"apple-m3-air-cpu-neon\" and .selected_backend == \"apple-m3-air-cpu-neon\" and .fallback_used == false and .profile_set == \"operator\" and .allocation_audit.enabled == true and .allocation_audit.scope == \"selected Apple M3 Air CPU/NEON SLM warm-session profile set\" and (.profiles | length) == 3 and ([.profiles[].profile_id] | sort) == [\"warm_16\",\"warm_32\",\"warm_64\"] and all(.profiles[]; .allocation_audit.scope == \"selected Apple M3 Air CPU/NEON SLM warm-session prompt hot path\")' ci/hardware/apple-silicon-macbook/2026-05-12/m3-air/qwen-mirror-operator.json",
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "ci/hardware/apple-silicon-macbook/**",
  "docs/apple-silicon/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
  "models/**",
]
may_claim = [
  "The dense Qwen SLM operator profile passed, failed, or remained diagnostic-only on M3 Air for the recorded context.",
]
must_not_claim = [
  "Dense Qwen evidence proves BitNet behavior.",
  "M3 Air timing replaces the M4 Mac mini envelope.",
]

[[work_item]]
id = "M3MBA-005A"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-005A-microsoft-2b-identity"
pr = 4795
head_sha = "98e19583b66292535051bc6ea3d3b17e81180a56"
merge_sha = "6e3fa444e48a38daa91504c6988587b09cda23f9"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-004B"]
acceptance = "Identify and hash the official Microsoft BitNet 2B I2_S GGUF on M3 Air without accepting model behavior yet, recording source repository, revision, filename, size, SHA256, local cache root, free space before/after, and the shared model-artifact gate and compatibility ledger entries consulted."
commands = [
  "curl -L --fail --continue-at - --output ~/Library/Caches/bitnet-rs/models/microsoft-bitnet-2b-i2s/ggml-model-i2_s.gguf https://huggingface.co/microsoft/bitnet-b1.58-2B-4T-gguf/resolve/a1f2f1c765812aa8af3f6eda4a313707064bba15/ggml-model-i2_s.gguf",
  "shasum -a 256 ~/Library/Caches/bitnet-rs/models/microsoft-bitnet-2b-i2s/ggml-model-i2_s.gguf",
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-silicon-macbook/**",
  "ci/model-artifacts/**",
  "docs/apple-silicon/**",
  "docs/model-artifacts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/campaigns/apple-bitnet-artifact-sweep/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
  "models/**",
]
may_claim = [
  "The official Microsoft 2B I2_S artifact identity and hash are recorded for the M3 Air context.",
]
must_not_claim = [
  "The Microsoft 2B artifact is accepted.",
  "Rust M4 BitNet local answers work.",
  "Apple Metal BitNet inference works.",
  "QK256 works on Apple Silicon.",
]

[[work_item]]
id = "M3MBA-005B"
status = "merged"
pr = 4802
branch = "codex/apple-m3-macbook-air/M3MBA-005B-microsoft-2b-tokenizer-authority"
head_sha = "af307d470044435fb10913660eeba5b8471762cc"
merge_sha = "686ea7e8301f71e4127744b37d46e8decf37ca9d"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-005A"]
acceptance = "Record tokenizer and external pre-tokenizer authority for the official Microsoft BitNet 2B I2_S GGUF, including required override keys, bad/no-authority rejection evidence when applicable, and links to the shared answer artifact gate and compatibility ledger."
commands = [
  "cargo run --release --locked -p xtask --no-default-features -- fetch-cpp --backend cpu",
  "${BITNET_CPP_DIR:-$HOME/.cache/bitnet_cpp}/build/bin/llama-cli -m <cache-root>/microsoft-bitnet-2b-i2s/ggml-model-i2_s.gguf -p <diagnostic-prompt> -n 8 --no-display-prompt --temp 0 --top-k 1 --override-kv tokenizer.ggml.pre=str:llama-bpe --no-mmap",
  "${BITNET_CPP_DIR:-$HOME/.cache/bitnet_cpp}/build/bin/llama-cli -m <cache-root>/microsoft-bitnet-2b-i2s/ggml-model-i2_s.gguf -p <diagnostic-prompt> -n 8 --no-display-prompt --temp 0 --top-k 1 --no-mmap",
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-silicon-macbook/**",
  "ci/model-artifacts/**",
  "docs/apple-silicon/**",
  "docs/model-artifacts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/campaigns/apple-bitnet-artifact-sweep/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
  "models/**",
]
may_claim = [
  "The tokenizer authority path for the official Microsoft 2B I2_S artifact is recorded or blocked.",
]
must_not_claim = [
  "The Microsoft 2B artifact is accepted.",
  "Rust M4 BitNet local answers work.",
  "Apple Metal BitNet inference works.",
]

[[work_item]]
id = "M3MBA-005C"
status = "merged"
pr = 4811
branch = "codex/apple-m3-macbook-air/M3MBA-005C-microsoft-2b-reference-output"
head_sha = "3590143516c7d101404c5a28597d0db2de196edb"
merge_sha = "5f3088d7c17ba97206ae9c8e7abdc4d01c78fb48"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-005B"]
acceptance = "Decide whether the official Microsoft BitNet 2B I2_S artifact is accepted, rejected, or blocked for the M3 Air reference context by recording the reference-runner command, prompt-suite outputs, answer-gate result or failing prompt IDs, and cleanup/retention status."
commands = [
  "${BITNET_CPP_DIR:-$HOME/.cache/bitnet_cpp}/build/bin/llama-cli -m <cache-root>/microsoft-bitnet-2b-i2s/ggml-model-i2_s.gguf -f ci/quality/bitnet-answer-corpus.yaml -n 32 --no-display-prompt --temp 0 --top-k 1 --override-kv tokenizer.ggml.pre=str:llama-bpe --no-mmap",
  "rg -n \"answer-gate|failing prompt|accepted|rejected|blocked\" docs/reports/apple-silicon-macbook-m3-air-microsoft-2b-i2s.md",
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-silicon-macbook/**",
  "ci/model-artifacts/**",
  "docs/apple-silicon/**",
  "docs/model-artifacts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/campaigns/apple-bitnet-artifact-sweep/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
  "models/**",
]
may_claim = [
  "The official Microsoft 2B I2_S artifact is accepted, rejected, or blocked for the recorded M3 Air reference context.",
]
must_not_claim = [
  "Rust M4 BitNet local answers work.",
  "Apple Metal BitNet inference works.",
  "QK256 works on Apple Silicon.",
]

[[work_item]]
id = "M3MBA-011"
status = "merged"
branch = "codex/apple-m3-macbook-air/roadmap-depth"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-001"]
acceptance = "Deepen the M3 Air roadmap into operating horizons, stop/go decisions, parallel lane policy, local resource budgets, and explicit SLM vs BitNet sequencing without running models or changing runtime behavior."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-silicon-macbook/**",
  "docs/apple-silicon/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/campaigns/apple-silicon-macbook/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "models/**",
]
may_claim = [
  "The M3 Air lane roadmap now names operating horizons, stop/go decisions, parallel work boundaries, and local resource budgets.",
]
must_not_claim = [
  "Any M3 Air model inference was run.",
  "Any BitNet artifact was downloaded or accepted.",
  "M3 Air evidence proves M4 Mac mini performance.",
]

[[work_item]]
id = "M3MBA-006"
status = "blocked"
branch = "codex/apple-m3-macbook-air/M3MBA-006-1bitllm-07b-preflight"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-005C", "M3MBA-010"]
acceptance = "Evaluate 1bitLLM/bitnet_b1_58-large as the smaller M3 Air control candidate only after the Microsoft 2B path records acceptance, rejection, or a clear blocker and the M3MBA-010 storage audit permits another large candidate download."
commands = [
  "huggingface-cli download 1bitLLM/bitnet_b1_58-large <gguf-file> --revision <revision> --local-dir <cache-root>/1bitllm-07b",
  "shasum -a 256 <cache-root>/1bitllm-07b/<gguf-file>",
  "${BITNET_CPP_DIR:-$HOME/.cache/bitnet_cpp}/build/bin/llama-cli -m <cache-root>/1bitllm-07b/<gguf-file> -p <prompt-from-ci/quality/bitnet-answer-corpus.yaml> -n 32 --no-display-prompt --temp 0 --top-k 1 --no-mmap",
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-silicon-macbook/**",
  "ci/model-artifacts/**",
  "docs/apple-silicon/**",
  "docs/model-artifacts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
  "models/**",
]
may_claim = [
  "The 0.7B candidate is blocked as a smaller M3 Air control artifact until an official GGUF, reproducible conversion path, or explicitly approved third-party artifact path exists.",
]
must_not_claim = [
  "The official 1bitLLM 0.7B repository contains a GGUF artifact.",
  "A third-party 1bitLLM 0.7B GGUF is accepted for M3 Air evidence.",
  "The 0.7B candidate proves official Microsoft 2B behavior.",
  "Rust M4 BitNet local answers work.",
]

[[work_item]]
id = "M3MBA-007"
status = "blocked"
pr = 5235
merge_sha = "a987d7c5607cd9026da006e292a617b287a49ac0"
head_sha = "13c783bcec22406f42b8306e4042fbdd87683b12"
branch = "codex/apple-m3-macbook-air/M3MBA-007-3b-diagnostic"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-005C", "M3MBA-010"]
acceptance = "Evaluate the 3B candidate only on supported TL1/TL2 diagnostic routes after M3MBA-010 permits another large candidate download, recording why I2_S remains unsupported and why the result is diagnostic rather than proof."
commands = [
  "huggingface-cli download 1bitLLM/bitnet_b1_58-3B <tl1-or-tl2-gguf-file> --revision <revision> --local-dir <cache-root>/1bitllm-3b-diagnostic",
  "shasum -a 256 <cache-root>/1bitllm-3b-diagnostic/<tl1-or-tl2-gguf-file>",
  "${BITNET_CPP_DIR:-$HOME/.cache/bitnet_cpp}/build/bin/llama-cli -m <cache-root>/1bitllm-3b-diagnostic/<tl1-or-tl2-gguf-file> -p <diagnostic-prompt> -n 32 --no-display-prompt --temp 0 --top-k 1 --no-mmap",
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-silicon-macbook/**",
  "ci/model-artifacts/**",
  "docs/apple-silicon/**",
  "docs/model-artifacts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
  "models/**",
]
may_claim = [
  "The 3B candidate is blocked for M3 Air TL diagnostic evidence until an official GGUF, reproducible conversion path, or explicitly approved third-party TL diagnostic artifact exists and local storage can absorb the large-candidate work safely.",
]
must_not_claim = [
  "The official 1bitLLM 3B repository contains a GGUF artifact.",
  "A third-party 1bitLLM 3B GGUF is accepted for M3 Air evidence.",
  "3B TL1/TL2 diagnostic inference was run.",
  "3B I2_S is supported.",
  "The 3B diagnostic route proves Apple local-answer quality.",
]

[[work_item]]
id = "M3MBA-008"
status = "merged"
pr = 5372
head_sha = "f274700750fda9d413880feb7e3df2bb773428d9"
merge_sha = "843da973eb609ddcb5943be6b99b93d6afec0669"
branch = "codex/apple-m3-macbook-air/M3MBA-008-m4-proof-handoff"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-005C", "M3MBA-006", "M3MBA-007"]
acceptance = "Promote accepted M3 Air BitNet artifact evidence into a separate M4 Mac mini strict Apple CPU/NEON proof item, preserving source, hash, tokenizer authority, route, and claim boundary without running or claiming the proof in this handoff item; if the M3 Air candidate sweep rejects or blocks every candidate, close this handoff with an explicit no-accepted-artifact report instead."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/apple-silicon/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/campaigns/apple-bitnet-artifact-sweep/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "models/**",
]
may_claim = [
  "The official Microsoft 2B I2_S artifact is ready for a separate M4 strict-proof item because M3 Air BitNet.cpp reference-runner evidence accepted it for the recorded M3 context.",
  "The 0.7B and 3B secondary candidates remain blocked and are not proof handoff targets.",
]
must_not_claim = [
  "The M4 strict proof already passed.",
  "M3 Air evidence is M4 Mac mini evidence.",
  "The repository Rust Apple backend accepted the artifact.",
  "The blocked secondary candidates are accepted artifacts.",
]

[[work_item]]
id = "M3MBA-009"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-009-slm-synthesis"
pr = 4902
head_sha = "9483c0597711becb4c1fe93a02906648a379ff61"
merge_sha = "238295105a28a014d3e2ecbe6980efea76c3f325"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-004B"]
acceptance = "Synthesize M3 Air dense SLM behavior against comparable M4 Mac mini and SLM CPU receipts, naming exact models, hashes, tokenizers, prompt sets, backend labels, fallback status, power/thermal context, and gaps that make evidence non-comparable."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/apple-silicon/**",
  "docs/reports/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "models/**",
]
may_claim = [
  "M3 Air dense SLM evidence has been compared against named M4 and SLM CPU receipts where fields match.",
]
must_not_claim = [
  "M3 Air timing replaces M4 Mac mini performance.",
  "Dense SLM evidence proves BitNet behavior.",
  "Non-comparable receipts are equivalent.",
]

[[work_item]]
id = "M3MBA-010"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-010-storage-audit"
pr = 4839
head_sha = "8f8916cd5d2d32058457ae86fa68178637b131cd"
merge_sha = "3c28779a4409baa4b92b5039ef6df3c22efee962"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-005A"]
acceptance = "Audit the M3 Air model cache after the first large BitNet download, recording retained artifacts, deleted artifacts, SHA256 evidence, cleanup status, free-space before/after, and whether additional candidate downloads are allowed under the lane storage policy."
commands = [
  "df -k . \"$HOME/Library/Caches\"",
  "shasum -a 256 <retained-artifacts>",
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-silicon-macbook/**",
  "docs/apple-silicon/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "models/**",
]
may_claim = [
  "The M3 Air artifact cache retention and cleanup state is recorded for the lane.",
]
must_not_claim = [
  "Deleted artifacts remain available locally.",
  "Cache retention proves model quality.",
  "Large model binaries were committed.",
]

[[work_item]]
id = "M3MBA-019"
status = "merged"
pr = 5388
head_sha = "0d19e3ada5c95a8cdf0451a662c4b7c48fed871a"
merge_sha = "2592ba36eac3dafb18d4f84e4116e95e6f1c2da0"
branch = "codex/apple-m3-macbook-air/M3MBA-019-post-handoff-roadmap"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-008"]
acceptance = "Refresh the M3 Air roadmap after the Microsoft 2B handoff stack closes so the next lane work is not a vague backlog: define post-handoff tracks for device-model hardening, M3 accuracy comparison profiles, bounded performance evidence from completed runs, secondary artifact unblock preflights, and M4 strict-proof handoff alignment while preserving M3/M4 claim boundaries."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/apple-silicon/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/hardware/**",
  "models/**",
]
may_claim = [
  "The M3 Air lane has a post-handoff roadmap for device-model, accuracy, performance, artifact-unblock, and M4 handoff alignment work.",
]
must_not_claim = [
  "Any new M3 Air model inference was run.",
  "Any new BitNet artifact was downloaded or accepted.",
  "M3 Air evidence proves M4 Mac mini performance or local-answer quality.",
]

[[work_item]]
id = "M3MBA-020"
status = "merged"
pr = 5937
head_sha = "aba72a51099e4a5e83343c21421c3ea97aeca573"
merge_sha = "4eb60320b46cd59e183525839587b38a656a4cbe"
branch = "codex/apple-m3-macbook-air/M3MBA-020-execution-roadmap"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Encode the post-handoff M3 Air execution queue as concrete follow-on work items so device-model hardening, accuracy comparison, bounded performance, secondary artifact unblock, and M4 strict-proof alignment can proceed PR-by-PR without turning the roadmap into a vague backlog."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/apple-silicon/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  ".github/workflows/**",
  "ci/hardware/**",
  "crates/**",
  "models/**",
]
may_claim = [
  "The M3 Air lane has concrete post-handoff execution items for device modeling, accuracy, performance, artifact unblock, and M4 handoff alignment.",
]
must_not_claim = [
  "Any new M3 Air model inference was run.",
  "Any new BitNet artifact was downloaded or accepted.",
  "Any runtime behavior changed.",
  "M3 Air evidence proves M4 Mac mini performance or local-answer quality.",
]

[[work_item]]
id = "M3MBA-021"
status = "merged"
pr = 5952
head_sha = "256c46491b1cffa69c2992fee8833f7cb5e2eda9"
merge_sha = "16de92563ceffcbe2b9e13beb2d5dd5144330849"
branch = "codex/apple-m3-macbook-air/M3MBA-021-device-profile-contract"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Extend the shared device/profile model with a structured M3 Air host profile contract that records SoC family, fanless thermal policy, core split, memory tier, storage budget, cache root, and supported proof-lane labels while preserving strict rejection for unsupported Metal, MPSGraph, Neural Engine, and hidden CPU fallback claims."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-device-config-core --no-default-features",
  "cargo test --locked -p bitnet-device-probe --no-default-features apple",
  "cargo test --locked -p bitnet-cli-config-core --no-default-features apple_m3",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli-config-core/**",
  "crates/bitnet-device-config-core/**",
  "crates/bitnet-device-probe/**",
  "docs/apple-silicon/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
  "ci/hardware/**",
  "models/**",
]
may_claim = [
  "M3 Air device/profile metadata is represented as a structured proof-lane contract.",
]
must_not_claim = [
  "M3 Air Metal model inference works.",
  "M3 Air MPSGraph model inference works.",
  "Neural Engine execution works.",
  "Any live model inference was run.",
]

[[work_item]]
id = "M3MBA-022"
status = "merged"
pr = 6043
head_sha = "d48dc056beb693947f20686337ad0e4b0bccd91a"
merge_sha = "822325ba6acaab744aa2bc4762cca23ce3c60946"
branch = "codex/apple-m3-macbook-air/M3MBA-022-accuracy-comparison-profile"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-021"]
acceptance = "Add an M3 Air dense SLM accuracy comparison profile that reuses the established corpus and receipt-check contract, records prompt IDs and scoring policy, and makes non-comparable M3/M4/SLM-CPU evidence explicit before any new comparison-grade claim."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m3",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  ".github/workflows/apple-m3-air-dense-slm.yml",
  "crates/bitnet-cli/**",
  "ci/quality/**",
  "docs/apple-silicon/**",
  "docs/slm/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "models/**",
]
may_claim = [
  "The M3 Air dense SLM accuracy comparison profile is wired and receipt-checked for the named corpus.",
]
must_not_claim = [
  "Any new M3 Air accuracy run passed unless a committed receipt proves it.",
  "Dense SLM accuracy proves BitNet behavior.",
  "M3 Air accuracy replaces M4 Mac mini evidence.",
]

[[work_item]]
id = "M3MBA-023"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-023-bounded-performance-profile-port"
pr = 1526
head_sha = "2f99883602aaeaaed8aa46dd673679023982423a"
merge_sha = "d91dbe8f627a1d778b306e7bbf29e522760942f2"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-022"]
acceptance = "Add a bounded M3 Air performance profile that records cold load, warm prompt timing, token budget, power source, thermal availability, storage state, and timeout cap provenance from completed healthy runs plus cushion instead of near-completion cancellation."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m3",
  "cargo run --locked -p xtask --no-default-features -- lint-workflows",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  ".github/workflows/apple-m3-air-dense-slm.yml",
  ".github/workflows/pr-plan.yml",
  "crates/bitnet-receipts-core/src/lib.rs",
  "docs/apple-silicon/**",
  "docs/ci/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
  "policy/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "models/**",
]
may_claim = [
  "M3 Air selected long jobs use completed-run cap provenance and phase artifact retention.",
]
must_not_claim = [
  "A performance speedup exists without committed timing receipts.",
  "Timeout caps are performance evidence.",
  "M3 Air timing is broad Apple Silicon or M4 Mac mini performance.",
]

[[work_item]]
id = "M3MBA-024"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-024-artifact-unblock-preflight-20260604"
pr = 1534
head_sha = "978bc94535b6d33775786fadacfdeb31ed20c292"
merge_sha = "d791c5b53e1b2612f72636d961fe8e79b0bded28"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-021"]
acceptance = "Add secondary BitNet artifact unblock preflight evidence for the blocked 0.7B and 3B candidates by resolving official artifact availability, tokenizer authority, expected local storage footprint, conversion or third-party approval needs, and cleanup plan before any new large download."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/model-artifacts/**",
  "docs/apple-silicon/**",
  "docs/model-artifacts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/campaigns/apple-bitnet-artifact-sweep/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/hardware/**",
  "models/**",
]
may_claim = [
  "Secondary BitNet candidate unblock requirements are recorded before any new M3 Air large download.",
]
must_not_claim = [
  "The blocked 0.7B or 3B candidates are accepted.",
  "A new BitNet artifact was downloaded or retained.",
  "Third-party artifacts are approved without an explicit policy decision.",
]

[[work_item]]
id = "M3MBA-025"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-025-m4-proof-alignment-20260604"
pr = 1537
head_sha = "ec2257274fa1158414a4211cb01e8dd3853215f7"
merge_sha = "5130a862cdde19fcf1d4f0e76f47b689856c0e84"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-021", "M3MBA-024"]
acceptance = "Align M3 Air accepted-artifact metadata with the separate M4 strict Apple CPU/NEON proof campaign by producing a handoff checklist that names source revision, hash, tokenizer authority, runner route, required fresh M4 receipts, and claims that remain explicitly unsupported."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/apple-silicon/**",
  "docs/model-artifacts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/campaigns/apple-bitnet-artifact-sweep/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/**",
  "ci/hardware/**",
  "models/**",
]
may_claim = [
  "M3 Air accepted-artifact metadata is aligned with a separate M4 strict-proof checklist.",
]
must_not_claim = [
  "The M4 strict proof already passed.",
  "M3 Air evidence is M4 Mac mini evidence.",
  "Rust Apple backend accepted the artifact.",
  "QK256 works on Apple Silicon.",
]

[[work_item]]
id = "M3MBA-026"
status = "merged"
pr = 1540
head_sha = "a2acc0e34f64c86f42ee6a9fd09a7156881b7919"
merge_sha = "46260204933e38421ac2f1274f22885cdeb576c4"
branch = "codex/apple-m3-macbook-air/M3MBA-026-successor-roadmap"
stackable = true
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Seed the next M3 Air excellence execution queue after M3MBA-025, turning the leading-device goal into concrete PR-sized work items for device-model SRP cleanup, compact Linux synthetic CI, dense SLM accuracy, bounded performance actuals, M3 BitNet proof prep, M3 BitNet receipting, exact-profile M3 ask/benchmark readiness, measured CPU/NEON bottleneck review, and first measured CPU/NEON follow-up without changing runtime behavior in this roadmap PR."
commands = [
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/apple-silicon/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  ".github/workflows/**",
  "ci/hardware/**",
  "crates/**",
  "models/**",
]
may_claim = [
  "The M3 Air lane has a concrete successor execution queue for leading-device support.",
  "The next M3 work is split into reviewable device, CI, SLM, BitNet, user-facing exact-profile, performance, and CPU/NEON items.",
]
must_not_claim = [
  "Any runtime behavior changed.",
  "Any new M3 Air model inference was run.",
  "Any BitNet artifact was downloaded, accepted, or retained.",
  "M3 Air evidence proves M4 Mac mini performance, full Apple Metal inference, MPSGraph model inference, Neural Engine execution, QK256 support, or broad Apple Silicon performance.",
]

[[work_item]]
id = "M3MBA-027"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-027-device-model-srp"
pr = 1545
head_sha = "81c7a62577c5574cef7180ff97465dc01c3b33ec"
merge_sha = "26c0cadcf4f05dcdcce7a4ea6e1ac3ae6ba5ca9d"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-026"]
acceptance = "Move the M3 Air host/profile/backend-label contract into a small single-responsibility device-model module or equivalent local table so CLI config, device config, device probe, backend selection, and Mac receipt code share the same M3 Air identity source while preserving existing M4 behavior, documenting or rejecting any historical M3 alias drift, and keeping strict unsupported-backend rejection."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-common --no-default-features backend_selection",
  "cargo test --locked -p bitnet-cli-config-core --no-default-features apple_m3",
  "cargo test --locked -p bitnet-device-config-core --no-default-features apple_m3",
  "cargo test --locked -p bitnet-device-probe --no-default-features apple",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m3",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-common/**",
  "crates/bitnet-cli-config-core/**",
  "crates/bitnet-device-config-core/**",
  "crates/bitnet-device-probe/**",
  "crates/bitnet-cli/**",
  "docs/apple-silicon/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
  "ci/hardware/**",
  "models/**",
]
may_claim = [
  "M3 Air device identity is centralized in a single-responsibility shared surface.",
  "Host-independent backend-selection tests cover the positive M3 Air CPU/NEON path and strict unsupported accelerator rejection.",
  "Existing M4 labels and unsupported M3 Metal/MPSGraph rejection behavior remain intact.",
]
must_not_claim = [
  "M3 Air Metal model inference works.",
  "M3 Air MPSGraph model inference works.",
  "Neural Engine execution works.",
  "Any live model inference was run.",
]

[[work_item]]
id = "M3MBA-028"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-028-compact-linux-synthetic-ci"
pr = 1564
head_sha = "379d1ae1e29c2e3a7b0750f11e4014dba88b6afc"
merge_sha = "bc45026d8e962eac774d75234f5e083b0c90c142"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-027"]
acceptance = "Add compact Linux-friendly synthetic guards for M3 Air receipt/profile shape so generic CI verifies M3 identity, fallback, proof-family, and timeout-cap invariants without live Mac hardware, model downloads, or broad Rust fallback routing."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-receipts-core --no-default-features apple_m3",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m3",
  "cargo run --locked -p xtask --no-default-features -- lint-workflows",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  ".github/workflows/**",
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "docs/apple-silicon/**",
  "docs/ci/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
  "policy/**",
]
forbidden_paths = [
  "ci/hardware/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "models/**",
]
may_claim = [
  "Linux CI checks synthetic M3 Air receipt/profile invariants without live Mac model work.",
]
must_not_claim = [
  "Linux CI proves live M3 model performance.",
  "Hosted or generic CI downloaded M3 model artifacts.",
  "Canceling near-complete selected M3 jobs is valid cost control.",
]

[[work_item]]
id = "M3MBA-029"
status = "blocked"
branch = "codex/apple-m3-macbook-air/M3MBA-029-dense-slm-accuracy-receipt"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-028"]
acceptance = "Run or record a blocker for the bounded M3 Air dense SLM accuracy profile with exact model, tokenizer, prompt IDs, generated token IDs, decoded text, requested/selected backend, fallback status, power/thermal/storage context, and comparable/non-comparable decisions."
commands = [
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac validate --profile-set accuracy --device apple-m3-air-cpu-neon --corpus ci/quality/apple-m4-slm-quality-corpus.yaml --cache-dir ~/Library/Caches/bitnet-rs/models --json-out ci/hardware/apple-silicon-macbook/2026-05-12/m3-air/qwen-mirror-accuracy.json --quiet",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac receipts-check ci/hardware/apple-silicon-macbook/2026-05-12/m3-air/qwen-mirror-accuracy.json --json",
  "jq -e '.backend.requested_backend == \"apple-m3-air-cpu-neon\" and .backend.selected_backend == \"apple-m3-air-cpu-neon\" and .backend.fallback_used == false and .accuracy_comparison_profile.comparison_grade_claim_made == false' ci/hardware/apple-silicon-macbook/2026-05-12/m3-air/qwen-mirror-accuracy.json",
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-silicon-macbook/**",
  "docs/apple-silicon/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  ".github/workflows/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "models/**",
]
may_claim = [
  "The dense SLM accuracy profile passed, failed, or produced a named blocker on the exact M3 Air context.",
]
must_not_claim = [
  "Dense SLM accuracy proves BitNet behavior.",
  "M3 Air accuracy replaces M4 Mac mini or SLM CPU evidence.",
  "Broad answer quality is proven.",
]

[[work_item]]
id = "M3MBA-030"
status = "ready"
branch = "codex/apple-m3-macbook-air/M3MBA-030-dense-slm-performance-actuals"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-029"]
acceptance = "Commit completed-run M3 Air dense SLM performance actuals for the bounded performance profile, including cold load, warm_16/warm_32/warm_64/warm_128 TTFT, decode throughput, token budget, thread count, power, thermal, storage, timeout-cap provenance, phase artifacts, and explicit exclusion of timeouts/cancellations from healthy samples."
commands = [
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac validate --profile-set performance --device apple-m3-air-cpu-neon --cache-dir ~/Library/Caches/bitnet-rs/models --json-out ci/hardware/apple-silicon-macbook/2026-05-12/m3-air/qwen-mirror-performance.json --quiet",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac receipts-check ci/hardware/apple-silicon-macbook/2026-05-12/m3-air/qwen-mirror-performance.json --json",
  "jq -e '.requested_backend == \"apple-m3-air-cpu-neon\" and .selected_backend == \"apple-m3-air-cpu-neon\" and .fallback_used == false and .profile_set == \"performance\" and .performance_profile.timeout_cap_source == \"completed_healthy_runs_plus_cushion\"' ci/hardware/apple-silicon-macbook/2026-05-12/m3-air/qwen-mirror-performance.json",
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-silicon-macbook/**",
  "docs/apple-silicon/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  ".github/workflows/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "models/**",
]
may_claim = [
  "A bounded M3 Air dense SLM performance receipt records exact completed-run TTFT and throughput for the selected context.",
]
must_not_claim = [
  "M3 Air timing is broad Apple Silicon or M4 Mac mini performance.",
  "Timeout caps are performance evidence.",
  "BitNet answer quality or BitNet performance is proven.",
]

[[work_item]]
id = "M3MBA-031"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-031-bitnet-proof-prep"
pr = 1573
merge_sha = "0301ef304e0d6fd3cb7f2ef14480c702088aff88"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-028"]
acceptance = "Prepare the M3 Air BitNet CPU/NEON local-answer proof surface for the accepted Microsoft 2B I2_S artifact by wiring strict synthetic receipt checks, artifact/tokenizer identity requirements, prompt authority, generated text/token ID fields, fallback=false semantics, and disabled chat/serve/Metal boundaries before any new live BitNet claim."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m3",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli bitnet",
  "cargo test --locked -p bitnet-receipts-core --no-default-features apple_m3",
  "cargo test --locked -p bitnet-receipts-core --no-default-features bitnet",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "docs/apple-silicon/**",
  "docs/model-artifacts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  ".github/workflows/**",
  "ci/hardware/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "models/**",
]
may_claim = [
  "The M3 Air BitNet CPU/NEON local-answer proof harness is ready for a strict receipt attempt.",
]
must_not_claim = [
  "The M3 Air BitNet local-answer proof passed.",
  "Chat, serve, Metal, MPSGraph, Neural Engine, QK256, or broad Apple Silicon BitNet support is enabled.",
]

[[work_item]]
id = "M3MBA-032"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-032-bitnet-local-answer-receipt"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Run or block the accepted Microsoft 2B I2_S artifact on the exact M3 Air CPU/NEON proof surface, committing strict receipt evidence with generated text, generated token IDs, tokenizer authority, backend identity, truthful runtime_api=cpu-neon, fallback=false, timing fields, cleanup/retention state, and answer-gate result or failing prompt IDs."
commands = [
  "cargo run --release --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- answer-corpus --device apple-m3-air-cpu-neon --model ~/Library/Caches/bitnet-rs/models/microsoft-bitnet-2b-i2s/ggml-model-i2_s.gguf --tokenizer ~/Library/Caches/bitnet-rs/tokenizers/microsoft-bitnet-2b/04c3b9ad9361b824064a1f25ea60a8be9599b127/tokenizer.json --fail-on-quality --json-out ci/hardware/apple-silicon-macbook/2026-05-12/m3-air/microsoft-2b-bitnet-local-answer.json",
  "cargo run --locked -p bitnet-cli --no-default-features --features cpu,full-cli -- mac receipts-check ci/hardware/apple-silicon-macbook/2026-05-12/m3-air/microsoft-2b-bitnet-local-answer.json --json",
  "rg -n \"answer-gate|fallback_used|apple-m3-air-cpu-neon|generated token\" docs/reports/apple-silicon-macbook-m3-air-microsoft-2b-bitnet-local-answer.md",
  "cargo fmt --all -- --check",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "ci/hardware/apple-silicon-macbook/**",
  "docs/apple-silicon/**",
  "docs/model-artifacts/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  ".github/workflows/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "models/**",
]
may_claim = [
  "The M3 Air BitNet local-answer attempt passed, failed, or produced a named blocker for the exact accepted artifact and CPU/NEON context.",
]
must_not_claim = [
  "M3 Air BitNet evidence proves M4 Mac mini proof.",
  "QK256, Metal, MPSGraph, Neural Engine, chat, serve, or broad BitNet support is enabled.",
  "The blocked 0.7B or 3B candidates are accepted.",
]

[[work_item]]
id = "M3MBA-033"
status = "ready"
branch = "codex/apple-m3-macbook-air/M3MBA-033-cpu-neon-bottleneck-review"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-030", "M3MBA-032"]
acceptance = "Turn completed M3 Air dense SLM and BitNet receipts into a measured CPU/NEON bottleneck review that identifies the smallest safe optimization PR, names exact comparators, preserves greedy output and answer gates, and explicitly separates M3 improvements from M4 or broad Apple Silicon claims."
commands = [
  "cargo fmt --all -- --check",
  "rg -n \"TTFT|tok/s|decode|NEON|fallback_used|apple-m3-air-cpu-neon\" docs/reports ci/hardware/apple-silicon-macbook/2026-05-12/m3-air",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "docs/apple-silicon/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  ".github/workflows/**",
  "crates/**",
  "ci/hardware/**",
  "models/**",
]
may_claim = [
  "Completed M3 Air receipts identify the next smallest measured CPU/NEON optimization target.",
]
must_not_claim = [
  "CPU/NEON performance improved before an optimization PR with before/after receipts lands.",
  "M3 Air timing is broad Apple Silicon or M4 Mac mini performance.",
  "BitNet and dense SLM performance are interchangeable proof families.",
]

[[work_item]]
id = "M3MBA-034"
status = "blocked"
branch = "codex/apple-m3-macbook-air/M3MBA-034-exact-profile-ask-benchmark"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-030", "M3MBA-032"]
acceptance = "Enable or explicitly block exact-profile M3 Air `mac ask` and `mac benchmark` surfaces for the proven dense SLM and BitNet CPU/NEON contexts, preserving requested/selected backend, model/tokenizer identity, generated text/token IDs, timing fields, fallback=false semantics, and disabled chat/serve/Metal/MPSGraph/Neural Engine/QK256 boundaries."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m3",
  "cargo test --locked -p bitnet-receipts-core --no-default-features apple_m3",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "docs/apple-silicon/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  ".github/workflows/**",
  "ci/hardware/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
  "models/**",
]
may_claim = [
  "Exact-profile M3 Air ask and benchmark surfaces are enabled or blocked with explicit receipt evidence for the proven dense SLM and BitNet CPU/NEON contexts.",
]
must_not_claim = [
  "M3 chat or serve readiness exists.",
  "M3 ask/benchmark evidence proves M4, Metal, MPSGraph, Neural Engine, QK256, or broad Apple Silicon support.",
  "Unproven artifacts or secondary candidates are answer-ready.",
]

[[work_item]]
id = "M3MBA-035"
status = "ready"
branch = "codex/apple-m3-macbook-air/M3MBA-035-first-measured-cpu-neon-optimization"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = ["M3MBA-033"]
acceptance = "Implement only the single M3-relevant CPU/NEON optimization target named by M3MBA-033, with before/after exact-profile receipts or parity fixtures preserving greedy token IDs, answer gates, fallback=false, and proof-family boundaries for dense SLM and BitNet as applicable."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-kernels --no-default-features",
  "cargo test --locked -p bitnet-neon-quantize --no-default-features",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m3",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-kernels/**",
  "crates/bitnet-neon-quantize/**",
  "crates/bitnet-cli/**",
  "docs/apple-silicon/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  ".github/workflows/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
  "models/**",
]
may_claim = [
  "The first measured M3 Air CPU/NEON optimization landed for the exact named bottleneck with before/after or parity evidence.",
]
must_not_claim = [
  "Unmeasured CPU/NEON performance improved.",
  "Dense SLM and BitNet optimization evidence are interchangeable.",
  "M3 Air timing is broad Apple Silicon or M4 Mac mini performance.",
  "QK256, Metal, MPSGraph, Neural Engine, chat, or serve support is enabled.",
]

[[work_item]]
id = "M3MBA-036"
status = "merged"
branch = "codex/apple-m3-macbook-air/M3MBA-036-receipt-command-repair"
stackable = false
review_mode = "codex_premerge"
merge_policy = "automerge_when_green"
human_gate = "on_blocker_only"
blocked_by = []
acceptance = "Repair the strict M3 Air BitNet local-answer command path so an explicit M3-compatible command emits bitnet_apple_m3_air_local_answer_corpus evidence with truthful CPU/NEON runtime identity, generated text/token IDs, timing, strict tokenizer authority, fallback=false, and receipt-check compatibility."
commands = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m3",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli bitnet",
  "cargo test --locked -p bitnet-receipts-core --no-default-features apple_m3",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
allowed_paths = [
  "crates/bitnet-cli/**",
  "crates/bitnet-receipts-core/**",
  "docs/apple-silicon/**",
  "docs/reports/**",
  "docs/tracking/campaigns/apple-m3-macbook-air/**",
  "docs/tracking/generated/**",
]
forbidden_paths = [
  ".github/workflows/**",
  "ci/hardware/**",
  "crates/bitnet-kernels/**",
  "crates/bitnet-qk256-dispatch/**",
  "crates/bitnet-quantization/src/qk256/**",
  "crates/bitnet-server/**",
  "models/**",
]
may_claim = [
  "The M3 strict local-answer command and receipt contract are ready for a renewed local receipt attempt.",
]
must_not_claim = [
  "The M3 Air BitNet local-answer proof passed before M3MBA-032 is rerun.",
  "M3 Air evidence proves M4 proof, chat, serve, Metal, MPSGraph, Neural Engine, QK256, or broad Apple Silicon support.",
]
