schema_version = "1.0"
campaign = "apple-m3-macbook-air"
item = "M3MBA-026"
event = "pr_open"
timestamp = "2026-05-20T08:32:00Z"
actor = "codex"
pr = 30
head_sha = "5373abbfacf1a90501e5570f554839f5dad7fd71"
branch = "codex/apple-m3-macbook-air/M3MBA-020-server-device-labels"

summary = "Opened #30 for M3 Air server device-label CUDA-active receipt hardening."

artifacts = [
  "https://github.com/EffortlessMetrics/bitnet-rs-swarm/pull/30",
  "crates/bitnet-server/src/lib.rs",
  "docs/apple-silicon/m3-macbook-air-roadmap.md",
  "docs/tracking/campaigns/apple-m3-macbook-air/active.toml",
  "docs/tracking/campaigns/apple-m3-macbook-air/CAMPAIGN.md",
]

notes = [
  "The PR preserves all three configured M3 Air backend labels in server shared-engine receipts when active-model metadata reports a CUDA device.",
  "The server test asserts M3 Air labels do not collapse into generic active-model device text, CUDA smoke claims, or M4 proof labels.",
  "No M3 Air Metal/MPSGraph model inference, live M3 model run, M4 proof, Neural Engine, QK256, or broad Apple Silicon performance claim is made.",
]

validation = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-server --no-default-features server_shared_engine_receipt_preserves_m3_air_configured_backend_labels",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
