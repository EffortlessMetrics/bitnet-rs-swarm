schema_version = "1.0"
campaign = "apple-m3-macbook-air"
item = "M3MBA-028"
event = "in_progress"
timestamp = "2026-06-05T02:40:59Z"
actor = "codex"
branch = "codex/apple-m3-macbook-air/M3MBA-028-compact-linux-synthetic-ci"

summary = "Started compact Linux synthetic CI coverage for M3 Air receipt/profile invariants."

artifacts = [
  ".github/workflows/apple-m3-air-synthetic-tier0.yml",
  ".github/workflows/apple-m3-air-dense-slm.yml",
  "crates/bitnet-cli/src/mac.rs",
  "crates/bitnet-receipts-core/src/lib.rs",
  "docs/tracking/campaigns/apple-m3-macbook-air/active.toml",
]

notes = [
  "The tier-0 workflow runs only synthetic M3 receipt/profile tests on Linux; it does not use live Mac hardware, fetch models, or run live mac validate.",
  "Receipt-core coverage keeps the M3 Air CPU/NEON strict proof under the apple_m3 filter and rejects generic CPU fallback, unsupported accelerator labels, hidden fallback, and missing NEON evidence.",
  "CLI receipt checks validate exact M3 Air prompt backend identity, no fallback, accuracy claim boundaries, and M3 performance timeout-cap provenance that excludes timed-out and cancelled runs from healthy samples.",
  "Started synthetic proof jobs use cancel-in-progress=false so useful bounded CI work reaches a real result instead of being cancelled near completion.",
]

validation = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-receipts-core --no-default-features apple_m3",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m3",
  "cargo run --locked -p xtask --no-default-features -- lint-workflows",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
]
