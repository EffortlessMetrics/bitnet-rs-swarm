schema_version = "1.0"
campaign = "apple-m3-macbook-air"
item = "M3MBA-028"
event = "merged"
timestamp = "2026-06-05T04:04:47Z"
actor = "codex"
pr = 1564
head_sha = "379d1ae1e29c2e3a7b0750f11e4014dba88b6afc"
merge_sha = "bc45026d8e962eac774d75234f5e083b0c90c142"
branch = "codex/apple-m3-macbook-air/M3MBA-028-compact-linux-synthetic-ci"

summary = "Merged #1564 with compact Linux synthetic M3 Air receipt/profile guards."

artifacts = [
  "https://github.com/EffortlessMetrics/bitnet-rs-swarm/pull/1564",
  ".github/workflows/apple-m3-air-synthetic-tier0.yml",
  ".github/workflows/em-ci-routed-rust.yml",
  ".github/workflows/rocm-smoke.yml",
  "crates/bitnet-cli/src/mac.rs",
  "crates/bitnet-receipts-core/src/lib.rs",
  "docs/tracking/campaigns/apple-m3-macbook-air/active.toml",
]

notes = [
  "The merged PR adds compact Linux synthetic coverage for M3 Air receipt/profile invariants without live Mac hardware, model downloads, live mac validate, or broad Rust fallback routing.",
  "Receipt-core and CLI tests validate exact M3 Air CPU/NEON backend identity, hidden-fallback rejection, unsupported accelerator rejection, proof-family boundaries, accuracy claim boundaries, and timeout-cap provenance that excludes timed-out and cancelled runs from healthy samples.",
  "The required routed Rust proof selected the M3 synthetic tier-0 target and completed on the self-hosted rust-small pool with runner labels self-hosted, linux, x64, em-ci, rust-small, trusted-pr.",
  "No live M3 model performance, BitNet/QK256 Apple Silicon proof, Metal inference, MPSGraph inference, Neural Engine execution, or broad Apple Silicon claim is made.",
  "M3MBA-029 is the next ready item for a bounded dense SLM accuracy receipt or named blocker on the exact M3 Air CPU/NEON context.",
]

validation = [
  "cargo fmt --all -- --check",
  "cargo test --locked -p bitnet-receipts-core --no-default-features apple_m3",
  "cargo test --locked -p bitnet-cli --no-default-features --features cpu,full-cli apple_m3",
  "actionlint -ignore 'label \".+\" is unknown' .github/workflows/apple-m3-air-synthetic-tier0.yml .github/workflows/em-ci-routed-rust.yml .github/workflows/rocm-smoke.yml",
  "cargo run --locked -p xtask --no-default-features -- lint-workflows",
  "cargo run --locked -p xtask --no-default-features -- campaign check apple-m3-macbook-air",
  "cargo run --locked -p xtask --no-default-features -- campaign doctor",
  "cargo run --locked -p xtask --no-default-features -- campaign generate --check",
  "git diff --check",
  "GitHub Actions: EM CI Routed Rust / Apple M3 Air Synthetic Tier 0 passed on run 26994494512",
]
