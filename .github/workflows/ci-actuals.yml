name: CI Actuals

on:
  workflow_dispatch:
    inputs:
      run_id:
        description: "GitHub Actions run id to collect"
        required: true
  schedule:
    - cron: '0 4 * * 1'

permissions:
  contents: read
  actions: read

concurrency:
  group: ci-actuals-${{ github.run_id }}-${{ github.run_attempt }}
  cancel-in-progress: false

jobs:
  record:
    name: Record CI actuals
    runs-on:
      group: em-ci-tiny
      labels:
        - self-hosted
        - linux
        - x64
        - em-ci
        - rust-tiny
        - trusted-pr
    timeout-minutes: 10
    if: ${{ github.event_name == 'workflow_dispatch' || github.event_name == 'schedule' }}
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@5d458579430fc14a04a08a1e7d3694f545e91ce6  # stable
        with:
          toolchain: "1.95.0"

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@98c8021b550208e191a6a3145459bfc9fb29c4c0  # v2
        with:
          shared-key: ci-actuals
          save-if: ${{ github.ref == 'refs/heads/main' }}

      - name: Fetch job timing data
        env:
          GH_TOKEN: ${{ github.token }}
          RUN_ID: ${{ github.event.inputs.run_id || github.run_id }}
        run: |
          set -euo pipefail
          mkdir -p target/ci
          gh api --paginate -H "Accept: application/vnd.github+json" \
            "/repos/${GITHUB_REPOSITORY}/actions/runs/${RUN_ID}/jobs?per_page=100" \
            | jq -s '{ jobs: map(.jobs // []) | add }' \
            > target/ci/jobs-raw.json

      - name: Emit ci-actuals
        env:
          RUN_ID: ${{ github.event.inputs.run_id || github.run_id }}
          WF_NAME: ${{ github.workflow }}
          WF_HEAD_SHA: ${{ github.sha }}
          WF_HEAD_BRANCH: ${{ github.ref_name }}
          WF_EVENT: ${{ github.event_name }}
        run: |
          set -euo pipefail
          args=(
            --repo "${GITHUB_REPOSITORY}"
            --sha "${WF_HEAD_SHA}"
            --workflow "${WF_NAME}"
            --workflow-run-id "${RUN_ID}"
            --event "${WF_EVENT}"
            --head-branch "${WF_HEAD_BRANCH}"
            --github-jobs-json target/ci/jobs-raw.json
            --json-out target/ci/ci-actuals.json
            --summary-out "${GITHUB_STEP_SUMMARY}"
          )
          cargo run --locked -p xtask --no-default-features -- ci actuals "${args[@]}"

      - name: Upload ci-actuals
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1
        with:
          name: ci-actuals-${{ github.event.inputs.run_id || github.run_id }}
          path: target/ci/ci-actuals.json
          retention-days: 7
          if-no-files-found: warn
